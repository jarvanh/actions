__pycache__/
.codebuddy/
# 项目级 skill 需随仓库共享（.workbuddy-ai/skills/），其余本地数据（memory 等）仍忽略。
# 注意：必须用 .workbuddy-ai/* + 否定的写法——直接写 /.workbuddy-ai/ 时 git 不会进入
# 该目录，子级的否定规则不生效。
.workbuddy-ai/*
!.workbuddy-ai/skills/
