# emby.yml 方案评估 与 302 起播延迟复核报告

- 日期：2026-09-13 22:5x（+08）
- 被评估对象：`.github/workflows/emby.yml`（2556 行）+ `.github/scripts/emby302/`
- 代码基线：`HEAD = 5c01d32`（emby.yml 的有效版本来自 `216a9b8`「修恢复链/ELAPSED/取链自检，补齐起播测量与归档」）
- 证据来源：**本次现场拉取的三轮 Actions 日志 + 线上备份产物（rclone 直读）+ 当前代码**。不引用注释作为事实、不引用记忆；代码注释只在能由日志交叉验证时作为旁证。
- 与仓库根 `emby-302-startup-latency-ds4.1-report-2026-09-13.md` 的关系：那一份是同主题的 earlier 报告。本报告独立重做了一遍取证，§6 列出了结论不一致的地方。

---

## 0. 三个问题的直接回答

### Q1：目前这个方案怎么样？

**架构是对的，302 该拿的收益拿到了；但现在有两个"结构性"问题比 10 秒起播严重得多。**

1. **每轮轮换有 25–48 分钟的服务中断**（实测两次：25.5 min、48 min），一天 4 次 → **每天约 1.7–3.2 小时 Emby 不可用（7%–13%）**。这是当前最大的问题，量级比 10 秒大两个数量级。
2. **观测链路断在最后一环**：唯一能测到"客户端侧几秒出画"的探针（`[起播]`，`emby.yml:1166`）写进 `playlog.log`，而 `playlog.log` 只在 `自续触发（下轮接力）` 这一步归档，该步骤在 HEAD 上**必然崩溃**（exit 127，`run_elapsed_seconds: command not found`）。结果：**测量写进去了也读不出来，且自续接力失效**。

服务端本身压得不错：点播放这条链路上服务器自己要干的活合计 **0.9 秒（热路径）/ 最坏 2.0 秒**；冷路径（未在 dir_cache 里的条目）约 **2.3 秒**。

### Q2：302 模式点播放后约 10 秒才起播，正常吗？

- **服务器不是原因**：可测的三段加起来 0.9 秒（热）/ 2.3 秒（冷）。302 的语义就是"302 之后视频流不经过服务器"，剩下的 7–9 秒发生在服务器完全看不到的地方。
- **10 秒在这个拓扑下属于"可以出现的量级"，但不等于"正常"**——因为**全库至今没有一个客户端侧读数**。`[起播]` 探针 0 个数据点：HEAD 的两轮（34743346060 / 34743972517）**一次播放都没有**（`playback_reporting.db` 最后一条停在 09-13 14:16 +08，在旧代码那轮）。
- **今天就能做的零成本对照**：通知里那行 `▶ 打开直链` 是同一个 OneDrive URL。用同一个客户端、同一个网络直接打开它——秒开说明 10 秒里没有 Emby/ge2o 的事（问题在播放器缓冲/解码）；也慢说明问题在客户端到微软 CDN 这一段（本仓库完全碰不到）；直链秒开而 Emby 里 10 秒，说明多出来的是**额外的往返**（字幕、二次请求、HLS 协商）。**这个实验比改任何代码都值。**

### Q3：可以改善吗？

**能，但天花板有限，而且必须先修测量。**

| 手段 | 预期 | 代价 | 建议 |
|---|---|---|---|
| 修 `自续触发` 崩溃（补一行 source） | 0（是修复观测+接力） | 一行 | **第一优先** |
| 对照组实验（直链直开 vs Emby 起播） | 0（是测量） | 零 | **第一优先** |
| dir_cache 跨 run 持久化 / 提高回填覆盖 | **省 ~1.5 s**（首次播放冷解析 1.75s → 0.29s） | 需改 odlink.py + 落盘 | 值得做 |
| 字幕预热扩到 `recent` 条目 | **省 0.5–1.0 s**（首次抽字幕） | 每轮几次挂载读 | 值得做 |
| 缩短轮换空档（25–48 min/次） | **每天多 1.7–3.2 h 可用** | 需要调度改造 | 比 10 秒更值钱 |
| 增量备份排除 metadata imagecache | install 从 25–47 min → ~10 min | 缩略图需重建 | 值得做 |
| 加大 `WU_PI_ITEMS`（起播准备预热） | **≈0**（冷 30 ms / 热 9 ms） | 白打请求 | **不要做** |
| 加大头尾预热条数 | **≈0**（302 下视频不过挂载） | 真实流量 | **不要做** |

---

## 1. 证据来源与口径

### 1.1 本次实际拉取的日志

| run id | 时间（UTC） | 结论 | 代码 | 有播放？ |
|---|---|---|---|---|
| 34720938292 | 09-12 22:03 → 09-13 03:04 | success | 9548261 | 否 |
| 34734584799 | 09-13 03:04 → 06:41 | cancelled（3h36m） | 9548261 | **是（5 次）** |
| 34743346060 | 09-13 06:41 → 12:00 | **failure**（5h19m） | **216a9b8（HEAD）** | 否 |
| 34743972517 | 09-13 12:00 起在跑 | in_progress | HEAD | 未知（`playback_reporting.db` 无新记录） |

> 注意 `gh run list` 显示 34743972517 创建于 06:56 UTC，但 job 的 `started_at` 是 **12:00:15Z** —— 排了 5 小时队。判断"为什么不动了"看 job 的 `started_at`，不要看 run 创建时间。

日志处理：GitHub 日志里 `##[group]Run …` 到 `##[endgroup]` 之间是**脚本回显**，会污染关键词检索。本报告全部统计都在剥掉回显后进行（下文凡引用日志行，均为真实执行输出）。

### 1.2 线上产物（rclone 直读，不经 Actions）

- `onedrive:backup/emby/live/data/playback_reporting.db`（本次实拉，本地 sqlite3 查询）
- `onedrive:backup/emby/live/data/library.db`（162 MB，本次实拉）→ 媒体库真实规模
- `onedrive:backup/emby/live/warm-state.json`（mtime 09-13 14:28Z，当前轮在写）

### 1.3 客户端是谁（`playback_reporting.db` 最近 30 条，实测）

- **最近 30 条全部是 `Emby Web / Google Chrome macOS`，全部 `DirectStream`**（没有 DirectPlay、没有转码）。
- 出口 IP 每次都不同且都在海外：`50.7.250.50`、`45.149.92.22`、`94.177.131.137`、`81.28.13.x`、`58.249.142.81`、`143.198.80.220` 等。
- **即：观测到的"客户端"始终是一个海外代理出口，不是家宽直连。** 客户端到微软 CDN 那一段走的是代理的路由质量，本仓库无论如何改都碰不到它。

---

## 2. 方案现状：实测数字

### 2.1 一轮长什么样（步骤计时，来自 jobs API）

| 步骤 | 34720938292 | 34734584799 | 34743346060（HEAD） |
|---|---|---|---|
| 前置（rclone/cloudflared/openlist/odlink/ge2o） | 22:03:03→22:03:57 | 03:04:36→03:05:21 | 06:41:06→06:41:46 |
| **`install emby`（恢复 + 启动）** | **1659 s（27.6 min）** | **1471 s（24.5 min）** | **2833 s（47.2 min）** |
| `run emby` → 隧道就绪 | 22:31:36→22:31:52 | 03:29:52→03:29:58 | 07:28:59→07:29:06 |
| `sleep`（保留 270 min） | → 03:01:52 | → 06:38:22（被取消） | → 11:59:06 |
| `archive diagnostics` | 03:01:52 | 06:38:22 | 11:59:06 |
| `backup emby data` | 152 s | 155 s | 63 s |
| `send telegram notification` | success | **failure** | **failure** |
| `自续触发（下轮接力）` | success | success（旧代码） | **failure（exit 127）** |

### 2.2 「install emby」的时间都去哪了（HEAD 轮逐段实测）

```
06:41:46  步骤开始
06:46:35  ✅ 发现增量备份目录（1038MB），优先恢复关键数据...
07:13:14  🎉 增量备份恢复并校验成功（不含图片缓存）      ← 26 分 39 秒
07:13:21  ✅ 关键数据已由增量恢复，全量包只补图片缓存...
07:28:59  🎉 图片缓存已从全量包补齐（关键数据未被覆盖）  ← 15 分 38 秒
```

对照旧代码轮（34734584799）：live 恢复 18.5 min + 整包恢复 1.4 min。

**结论：大头是两个"2823 个文件 / 1057 MB"的小文件批量拉取。**
增量备份脚本（`emby.yml:2032-2038`）的 rsync 只排除了 `cache/`、`logs/`、`transcoding-temp/` 与 `data/*.db`，**`metadata/library/**/imagecache/*.jpg`（缩略图）全在包里**。恢复日志里满屏都是这类文件被逐个 `Copied`。2823 个文件 / 1599 s ≈ **1.8 文件/秒**——瓶颈是 OneDrive 的逐文件请求延迟，不是带宽。

### 2.3 预热各段（warmup.log 实测）

| 段 | 34720938292 | 34734584799 | 34743346060（HEAD） |
|---|---|---|---|
| 直链预热 `fs/get` | — | 35 条 / 均值 760 ms | 37 条 / 均值 567 ms |
| 直链 TTFB（runner → OneDrive，1 字节 Range） | 0.23 s / 最大 1.06 s | **0.20 s / 最大 0.89 s** | **0.33 s / 最大 3.30 s** |
| 目录回填 dir_cache | — | 400 目录 / **529 s** | — |
| 隧道边缘机房 | — | iad03 | lax05 |
| 隧道往返 TTFB（runner → 公网域名） | 0.179 s | 0.216 s | 0.437 s |
| 海报墙 / 首屏预热 | 0 次（档位"无"） | 0 次（档位"无"） | **280 次 + 3423 张（档位 `9.0 3.0 1.5 4.0 6.0 13.0 7.0`）** |
| 头尾预热（挂载冷读） | — | 3 条 / 192 MB / 均值 1609 ms | 3 条 / 192 MB / 均值 1370 ms |
| 起播准备 `PlaybackInfo` 冷 / 热 | 冷 23 ms / 热 7 ms | 冷 29 ms / 热 8 ms | **冷 30 ms / 热 9 ms** |

**「起播准备预热」实测接近空转。** 三轮一致：冷 23–30 ms、热 7–9 ms。如果这一步真触发了 ffprobe 或内封字幕提取（都要读挂载），冷调用应该是几百毫秒到秒级（同轮挂载冷读 64 MB 实测 1.2–3.4 s）。冷 = 30 ms 说明媒体信息在库扫描时已入库，`PlaybackInfo` 只是拼装响应。**"连打两次逼 Emby 提前做完探测/抽字幕"的预期不成立。** 真正在关键路径上的字幕是**另一个请求**（见 §3）。

### 2.4 媒体库真实规模 vs 预热覆盖率（library.db 实拉实测）

```
MediaItems 总数              85,709
/onedrive/ 路径的媒体文件     83,464
去重目录数                    1,837
顶层分布   /onedrive/1/ 53,862   /2/ 19,589   /5/ 4,913
          /4/ 2,844   /0/ 1,366   /3/   890
```

而目录回填上限 `EMBY_PREFETCH_MAX_DIRS = 400`（`emby.yml:1191`），本轮 `odlink /stats` 的 `dirs_cached = 26360`。

> **覆盖率 ≈ 26360 / 83464 ≈ 32%**（目录口径 400/1837 ≈ 22%）。
> **约三分之二的媒体文件不在 dir_cache 里，首次播放要走冷解析。**

代码注释给出的冷解析实测是 **1.75 s/条（段数 5、跨盘 1 次）**，命中 dir_cache 后只需重新签发直链（1 次 Graph）。实测热路径 292 ms —— 两者相差 **约 1.5 秒**，这是服务端侧可压的最大一块。

---

## 3. 起播 10 秒的分段账

按"点下播放 → 出画面"拆开，标出哪些有实测、哪些测不到：

| # | 段 | 谁在等 | 实测 | 说明 |
|---|---|---|---|---|
| ① | 客户端 → Cloudflare → ge2o → Emby | 你 | **测不到** | runner 侧自测 0.18–0.44 s，那不是你的往返 |
| ② | Emby 组装 `PlaybackInfo` | Emby | **9 ms**（n=21，最大 99 ms） | 冷/热都只有几十毫秒 |
| ③ | ge2o + odlink 取直链（热 / 冷） | 服务器 | **292 ms / 最大 845 ms**（n=7）<br>冷 ≈ **1.75 s**（代码注释实测值，本次无冷样本） | 见 §2.4：约 2/3 条目是冷路径 |
| ④ | Emby 抽内封字幕 | Emby | **542 ms / 最大 1038 ms**（n=2） | 独立请求，不在 `PlaybackInfo` 里 |
| ⑤ | 客户端 → OneDrive：DNS + TCP + TLS + 首字节 | 你 | **测不到** | runner 侧基线 0.20–0.33 s 均值 / 最大 3.30 s，与你的路径无关 |
| ⑥ | 播放器缓冲 + 解码出画 | 你 | **测不到** | 302 之后服务器永远看不到 |

**可测部分：热路径 ②③④ ≈ 0.84 s（最坏 1.97 s）；冷路径 ≈ 2.3 s。**
**①⑤⑥ 完全在服务器视野之外，且至今 0 个读数。**

结论：**10 秒里至少 7 秒发生在 ①⑤⑥；服务器侧没有任何一段能解释它。**

### 3.1 排除一个"看起来像证据"的假线索

`ge2o 请求耗时统计` 里有一行 `其它: n=663 均值 123ms 最大 35683ms`，容易被当成"起播卡了 35 秒"。**本次在日志里定位到了它**：

```
[ge2o:v2.8.2] 2026-09-13 14:28:37 | 200 | 35.683393437s | 45.149.92.22 | 8095 (?i)^/.*(socket|embywebsocket) | GET /emby
```

**是 `/embywebsocket` 长连接的连接时长，不是卡住的请求。** 同一批里还有 `1m0.228s`、`1m29.426s`、`1m54.595s`、`2m5.011s`，全是 websocket 与 `users/*/items` 长轮询。这类"耗时"= 连接生命周期，把它们算进起播等待是误读。

### 3.2 为什么说"不能就此认定正常"

- 当前**没有任何一条 ①⑤⑥ 的读数**。HEAD 新增的 `probe_client_start`（轮询 Emby `Sessions` 的 `PositionTicks`，首次 > 0 即"客户端出画"，减去 `/videos/` 到达时刻）是为这个设计的，但 HEAD 两轮都没有播放 → **至今 0 个数据点**。
- 即使它跑起来，**归档也拿不到**（§5.1）。
- 行为旁证（不是证据）：09-13 那 5 次播放时长 17 s / 94 s / 34 s / 173 s / 5 s，14:12–14:16 四分钟内同一部片起了三次、中间还换了一部——"起了就停、换个再试"的形态与"起播体验不好"一致，但这只是旁证。

---

## 4. 判断：能不能改善、改善多少

### 4.1 服务端还能压的（有账可算）

| 项 | 现状 | 可压到 | 省 | 前提 |
|---|---|---|---|---|
| 冷路径取直链 | 1.75 s（约 2/3 条目） | ~0.29 s | **~1.5 s** | dir_cache 覆盖提上去 |
| 首次抽字幕 | 0.54 s（最大 1.04 s） | ~0 | **~0.5–1.0 s** | 预热覆盖 `recent` 条目 |
| `PlaybackInfo` | 9 ms | 9 ms | 0 | 已到地板 |
| 热路径取直链 | 292 ms | ~250 ms | <0.05 s | 不值得做 |

**服务端天花板 ≈ 省 1.5–2.0 秒（仅首次播放某条目时）。** 把 10 秒变成 3 秒，服务端做不到。

### 4.2 真正能动的是"换路"或"测量"

- **先量**：修 §5.1 让 `[起播]` 探针的产出能落到日志里，然后播一次同一部片，读出 ①⑤⑥ 的合计。
- **再 A/B**：`workflow_dispatch` + `playback_mode=direct`，同一部片各播一次比 `[起播]` 的秒数。direct 下视频流经 runner（Azure 出口）中转，runner → OneDrive 首字节实测 0.20–0.33 s，如果客户端到微软 CDN 那段很烂，direct 反而更快。**注意避开每轮开头约 110 秒的数据源 B 盲区**（`emby.yml` 的 Sessions 轮询，旧两轮实测连续 11 次 `http=000`，即 ~110 s；HEAD 轮未见该现象，但 direct 模式下它是唯一播放来源）。
- **零成本对照（推荐先做这个）**：用同一个客户端、同一个网络直接打开通知里的 `▶ 打开直链`，与 Emby 内起播对比。

### 4.3 比 10 秒更值钱的两件事

**① 轮换空档。** 实测两次交接：

```
34720938292 job 结束 03:04:30  →  34734584799 隧道就绪 03:29:58   = 25.5 min 中断
34734584799 job 结束 06:41:00  →  34743346060 隧道就绪 07:29:06   = 48.1 min 中断
```

一天 4 次轮换 → **每天 1.7–3.2 小时 Emby 连不上（7%–13%）**。而且自续接力（本该消除这个空档）现在也坏了（§5.1），只剩 `cron 0 2,8,14,20`（UTC）兜底——某轮中途失败时空档最长可达 6 小时。

**② install emby 25–47 分钟。** 其中大头是 2823 个 metadata 缩略图小文件的拉取（§2.2）。把 imagecache 从增量备份里排除（或单独成包、按需恢复）能把它压到 10 分钟级，等效于同时缩短轮换空档。

---

## 5. 本次排查确认的缺陷（按严重度）

### 5.1 【P0】`自续触发（下轮接力）` 必然崩溃 → 接力失效 + 诊断归档全丢

**证据**

- 34743346060 该步骤 exit 127，日志原文：
  ```
  /home/runner/work/_temp/b7fb9ba2-….sh: line 18: run_elapsed_seconds: command not found
  ##[error]Process completed with exit code 127.
  ```
- 代码：该步骤（`emby.yml:2449-2455`）只 `source "$EMBY302_DIR/lib.sh"`，而 `run_elapsed_seconds` 定义在 `.github/scripts/lib/run_elapsed.sh`。对照 `backup emby data` 步骤 —— `emby.yml:2244` 有 `source "$GITHUB_WORKSPACE/.github/scripts/lib/run_elapsed.sh"`，**接力步骤漏了这一行**。崩溃点是脚本第 18 行 `ELAPSED=$(run_elapsed_seconds)`（`emby.yml:2467`），而 `gh workflow run emby.yml`（`emby.yml:2482`）在它之后。

**影响**

1. `gh workflow run` 永远不执行 → **自续接力失效**（只剩 cron 兜底）。
2. 崩溃点在归档块之前 → 该步骤承载的 `playlog.log`、`ge2o 分段统计`、`seek.log` 全量、`odlink.log`、`rclone-mount.log`、`emby-console.log`、`warmup.log` 汇总行、`wallwarm.log` **全部丢失**。
3. 直接命中本次主题：**`[起播]` 是唯一写进 `playlog.log` 的客户端侧读数，而 `playlog.log` 恰好只在这一步归档**（`archive diagnostics` 步骤的清单里没有它）。

**改法**：`emby.yml:2455` 之后补一行，与 `:2244` 同款：

```bash
source "$GITHUB_WORKSPACE/.github/scripts/lib/run_elapsed.sh"
```

### 5.2 【P0】`send telegram notification` 在 `set -e` 下静默中止 → 收尾通知发不出

**证据**

- 34734584799 与 34743346060 该步骤均 **exit 1**，且**零执行输出**（脚本回显结束后直接 `##[error]`；第一个 `echo` 在脚本第 ~68 行，从未打印）。
- 代码：`emby.yml:2385-2386` 两个赋值没有兜底：
  ```bash
  WW_N=$(grep -oE '共 [0-9]+ 次图片请求' /opt/logs/wallwarm.log 2>/dev/null | tail -1 | grep -oE '[0-9]+')
  [ -n "$WW_N" ] || WW_N=$(grep -oE '已预热 [0-9]+ 请求' /opt/logs/wallwarm.log 2>/dev/null | tail -1 | grep -oE '[0-9]+')
  ```
  末端 `grep` 无匹配 → 管道退出 1 → 赋值返回 1 → `bash -e` 立即终止整步。本次本机复现：
  ```
  $ bash -e -c 'X=$(printf "abc" | grep -oE "[0-9]+"); echo after'; echo EXIT=$?
  EXIT=1        # after 没打印
  ```
- **而且这不是偶发**：wallwarmer 的收尾行 `=== wallwarmer 结束: 共 $DONE 次图片请求 ...`（`emby.yml:1995`）只在循环跑完时打印，而 `WW_MAX_MIN=300` 分钟（`emby.yml:1864`）**大于本轮保留时长 270 分钟** —— 只要 wallwarmer 真的在干活（HEAD 轮：280 次请求 + 3423 张），它在 job 结束时一定还没跑到那一行 → 2385 必然无匹配 → 2386 兜底也必然无匹配 → **必然 exit 1**。
  反过来说，旧代码那两轮里 wallwarmer 档位为空、秒退、打印了"共 0 次"因而没死——这解释了为什么 34720938292 成功而另两轮失败。

**影响**：收尾汇总通知（状态 / 模式 / 本轮直链 / 海报预热 / 数据备份 / 播放客户端）整条丢失。

**改法**：两处都补 `|| true`；并把 `WW_MAX_MIN` 调到 ≤ 保留时长，或让 wallwarmer 定期打印可被 2386 捕获的进度行。

### 5.3 【P1】dir_cache 覆盖率只有约 1/3 → 多数条目首次播放付 1.75 s 冷解析

见 §2.4。回填上限 400 目录用了 529 s（8.8 min），覆盖 26,360 / 83,464 个文件。

**改法（按性价比）**：
1. **让 dir_cache 跨 run 持久化**（odlink 加 dump/load，随增量备份一起走）。它是无 TTL 的纯映射表，进程内存在每轮冷启动是纯浪费；持久化后几轮就能接近全量覆盖，且**不增加单轮耗时**。
2. 退而求其次：提高 `EMBY_PREFETCH_MAX_DIRS` + 并发回填（串行 400 目录 529 s，16 并发可到 ~2 min，全量 1837 目录约 10 min）。
3. 至少把直链预热的条目来源从"最近添加 30 条"扩到"最近播放 + 用户最常访问"。

### 5.4 【P1】增量备份把 metadata 缩略图一起打包 → install 慢 16–27 分钟

见 §2.2。`emby.yml:2032-2038` 的 rsync 只排除 `cache/`、`logs/`、`transcoding-temp/`、`data/*.db*`。

**改法**：给 rsync 加 `--exclude 'metadata/library/*/imagecache/'`，缩略图按需重建或随全量包单独走；把恢复的 `--transfers` 从 8 提到 16（与 `sync_up` 一致）。

### 5.5 【P1】`warm-state.json` 字段顺序错位 → 每轮开头海报预热用错宽度档

**证据链（代码 + 日志 + 线上文件三方对齐）**

1. 线上文件是 `[宽度, 得分]`：`{"scores":[[342,6.5],[630,4.5],[338,3.5],...]}`（本次 rclone 实拉）。
2. 读侧 `emby.yml:1876` 按 `[0] [1]` 取 → `"342 6.5"`（宽度 得分）。
3. `emby.yml:1880` 的 `DECAYED` 保持同序；`:1893` 的 `FILL` 也输出"宽度 得分"。
4. 但 `persist_state`（`emby.yml:1919`）的契约是 `# $1="得分 宽度"多行`，内部 `read -r s w` 后写 `[$w,$s]`。
5. `emby.yml:1935` 启动时 `persist_state "$BASE"` —— **把"宽度 得分"喂给了期望"得分 宽度"的函数**，写回变成 `[得分, 宽度]`。
6. 日志佐证：HEAD 轮打印 `海报墙/首屏预热宽度档: 9.0 3.0 1.5 4.0 6.0 13.0 7.0`（40 条 × 7 档 = 280 次请求）—— 这 7 个"宽度"全是小数，是**得分**。对照旧轮打印的是 `342 630 338 406 490 624 494 620`（真宽度）。

**自愈性**：运行中 `merged_sizes` 的输出是"得分 宽度"，经 `persist_state "$MERGED"` 写回后文件被修正回 `[宽度, 得分]`（这也解释了线上文件现在是对的）。但**每轮开头那次预热一定用错档**，且中途被取消的轮次会把错位留到下一轮。

**改法**：`emby.yml:1935` 换序：
```bash
persist_state "$(printf '%s\n' "$BASE" | awk 'NF>=2 {print $2, $1}')"
```

### 5.6 【P2】`起播准备预热` 与 302 起播无关

实测冷 23–30 ms / 热 7–9 ms（三轮一致），说明 `PlaybackInfo` 不做 ffprobe、不抽字幕。**不要再加大 `WU_PI_ITEMS`**；要压字幕应针对 `warm-state.json` 的 `recent`（现有 7 条）做针对性预热。

### 5.7 【P2】`ge2o.log` 归档用纯 `tail`，会被 watchdog 探针占满

HEAD 轮（无客户端访问时）`--- ge2o.log (tail 80) ---` 的 80 行全是：
```
[ge2o:v2.8.2] … | 200 | 704µs | 127.0.0.1 | 8095 .* | GET /emby/System/Info/Public
```
（watchdog 每 60 s 一次，80 行 ≈ 最近 80 分钟。）同一问题在 `odlink.log` 上已经修过（改 grep 关键行），**`ge2o.log` 漏了**，而它恰恰是 302 链路的证据源。

**改法**：照 odlink 的做法改成按关键行 grep 并排除 `127.0.0.1` 的自身探针。

### 5.8 【P2】数据源 B 的 ~110 秒盲区

`Sessions 查询失败 http=000（第 1 次）…（第 11 次）` 在 34720938292 与 34734584799 两轮出现（计数器每 10 次打印一次 → 连续 11 次 × 10 s ≈ 110 s），HEAD 轮未见。紧邻的认证矩阵四种方式全 200，说明不是认证问题，是 Emby 启动期忙 + `-m 8` 无重试。302 模式下只是兜底源，**但 direct 模式下它是唯一播放来源**，做 A/B 要避开这个窗口。

### 5.9 【已修复确认】恢复链的 `RESTORED_FROM` 被无条件清空 — HEAD 已修好

旧代码轮（34734584799）的脚本回显里还有 `RESTORED_FROM=` 那一行；HEAD 轮（34743346060）已无此行，实测行为正确：
```
07:13:14  🎉 增量备份恢复并校验成功（不含图片缓存）
07:13:21  ✅ 关键数据已由增量恢复，全量包只补图片缓存...
07:28:59  🎉 图片缓存已从全量包补齐（关键数据未被覆盖）
```
即：live 增量恢复成功后，全量包**不再覆盖**关键数据，只补图片缓存。**这条不用再改。**

### 5.10 【已澄清】"POSTERS 预热 0 请求"不是漏改

HEAD 轮 0 请求是因为档位表被 5.5 的错位污染、衰减后全被淘汰（`if(s>=1)` 过滤）；修好 5.5 之后会恢复。

---

## 6. 与仓库内既有报告（`emby-302-startup-latency-ds4.1-report-2026-09-13.md`）的差异

逐条复核后，三处结论**不一致**，以本报告为准：

1. **「35.7 秒请求无法定位」→ 已定位。** 它是 `/embywebsocket` 长连接的连接时长（`emby.yml` 归档日志中可查到该行），不是起播卡顿（§3.1）。把它算作"用户最可能感知为点了没反应"的证据不成立。
2. **「install emby 的 2833 s 是否回归，需对照」→ 不是回归，是新增的图片缓存恢复。** HEAD 轮 live 恢复 26.6 min **+ 图片缓存恢复 15.6 min** = 42 min，其中图片缓存那 15.6 min 是旧代码"整包恢复 84 秒"时根本没真正拿到的东西。**恢复链修复本身是对的，代价是 install 变慢**（§2.2、§5.9）。
3. **`send telegram notification` 的根因是"wallwarm.log 缺行"→ 更精确：是必然事件。** `WW_MAX_MIN=300 min > EMBY_RUN_MINUTES=270 min`，只要 wallwarmer 真在干活就永远跑不到收尾行（§5.2）。所以不是"偶发定时炸弹"，而是"海报预热一生效，收尾通知就必死"。

其余（P0-1 接力崩溃、P1-1 字段顺序、P1-2 ge2o 归档、数据源 B 盲区、起播准备预时空转）复核后**结论一致**，本报告补充了线上 `library.db` 的规模数据与 dir_cache 覆盖率这一项（§2.4），它是既有报告没有的服务端侧最大一块。

---

## 7. 建议的下一步（按顺序）

1. **修 §5.1**（补一行 source）。不修则接力断、诊断归档丢、`[起播]` 永远读不到。
2. **修 §5.2**（`|| true` + `WW_MAX_MIN` 对齐保留时长）。
3. **立刻做零成本对照**：同一客户端同一网络，直接打开通知里的 `▶ 打开直链` vs Emby 内起播。这一步决定后面所有优化的方向，**不需要改任何代码**。
4. **等下一次真实播放**（目前最近一次是 09-13 14:16 +08，之后没有），读出 `playlog.log` 里的 `[起播] item=N 客户端出画 +Xs`。这是整份报告里唯一缺的那个数字。
5. **做 §5.3**（dir_cache 持久化），服务端侧唯一能省 1.5 秒的改动。
6. **修 §5.5**（字段换序）+ **§5.7**（ge2o 归档改 grep）。
7. **§5.4**（排除 imagecache）→ 顺带把轮换空档从 25–47 min 压下来。
8. 若第 4 步读出的客户端侧数字明显大于 5 秒，再做 direct A/B（避开每轮开头 ~110 s 盲区）。

---

## 8. 附录：数据出处与未取得的数据

| 数据 | 出处 |
|---|---|
| 步骤计时 / 步骤结论 | `gh api repos/jarvanh/actions/actions/runs/<id>/jobs` |
| install emby 内部里程碑、warmup 各段、ge2o 分段统计、odlink `/stats`、seek、ge2o.log 尾部 | 三轮 `0_build.txt`（剔除 `##[group]Run … ##[endgroup]` 回显后统计） |
| 播放记录（方式/时长/客户端/出口 IP） | `onedrive:backup/emby/live/data/playback_reporting.db` → `PlaybackActivity`（本次实拉，sqlite3 查询） |
| 媒体库规模（85,709 / 83,464 / 1,837 目录） | `onedrive:backup/emby/live/data/library.db` → `MediaItems`（本次实拉） |
| 海报档位与最近播放路径 | `onedrive:backup/emby/live/warm-state.json` |
| 代码行号 | `HEAD = 5c01d32`（emby.yml 同 `216a9b8`），`.github/workflows/emby.yml` |
| `bash -e` 下 `VAR=$(… | grep …)` 的行为 | 本机 bash 5.3.15 复现 |

**未取得 / 无法取得（诚实标注）**

- 客户端 → Cloudflare → 服务器的往返、客户端 → OneDrive 的建连与首字节、播放器缓冲与解码：**302 之后服务器看不到，且 `[起播]` 探针 0 个数据点**。
- 冷路径取直链的本次实测值：唯一有播放的那轮（34734584799）播放的都是 `recent` 里的热条目，`1.75 s` 是代码注释给出的历史实测值，本次没有冷样本可复核。
- run 34743972517（正在跑）的日志：GitHub 对进行中的 job 拉不到日志，zip 为空。
- 2026-09-12 之前的 run 日志：GitHub 已过期（HTTP 410）。
