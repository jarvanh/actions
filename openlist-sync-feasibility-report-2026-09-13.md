# OpenList 同步方案可行性评估 · 2026-09-13

> 评估对象：`.github/workflows/openlist.yml` + `.github/scripts/openlist/*.sh`
> 依据：本机 `rclone` 实测源端清单、`gh` 拉取的最近 40 轮运行记录、run `34728107625` 完整日志（24,128 行）、
> `onedrive:/logs/sync_state/trend.jsonl`（20 轮）、`onedrive:/logs/sync_state/task_rotation.json`、当前源码。
> **所有结论以代码与日志为准，不采信注释与既往记忆；注释与日志矛盾处一律以日志为准并显式标注。**

---

## 一、结论速览

| 问题 | 结论 |
|---|---|
| **当前配置下能否完成同步** | **不能。** 不是"慢"，是主后端 `wopan176Crypt` 处于确定性拒写状态，而系统把可恢复的失败误判成不可恢复，主动放弃治疗 |
| **"已传输"是否可信** | **不可信。** 存在大规模"假成功"：rclone 报 `Copied` 但后端不落盘，跨轮重复率达 **32.3%**，同一个文件 24 小时内被"成功复制" 9 次仍判缺失（§4.3.1） |
| **真实进度** | 近 5 天 20 轮，**净传 13.42 GB**，"剩余量"指标同期却显示减少 2.49 TB —— 该减少是指标失真，不是真实进展 |
| **时间利用率** | 以 run `34728107625` 为例：同步 297 分钟，**仅约 42 分钟（14%）有文件报 Copied，且这 42 分钟的成果未经落盘验证** |
| **当前状态** | **已停摆。** 僵尸 job 占住 `concurrency: openlist-singleton` 锁约 4 小时，后续运行全部挂起 |
| **修复后能否完成** | 能。前提是三项必修（§六 P0），理论工期约 1.5–2 个月；不做 P0 则永远收敛不了 |

**一句话**：架构和容错设计本身是认真且成熟的（轮转、预算闸、truth-check、marker 还原链路都到位），
但**根因定位错了层级** —— 把"文件名长度超限"这一内容性问题，判定成了"后端整体故障"这一基础设施问题，
于是所有本可生效的兜底（短哈希目录、改名、分卷）被熔断一起短路掉，剩余时间全部空转。

---

## 二、规模基线（本机实测，非估算）

源端 `onedrive:` 实际清点结果：

| 源端 | 大小 | 文件数 |
|---|---|---|
| `onedrive:0` | 0.27 TiB | 2,636 |
| `onedrive:1` | 0.76 TiB | 60,214 |
| `onedrive:2` | 0.90 TiB | 20,844 |
| `onedrive:3` | 0.73 TiB | 1,759 |
| `onedrive:4` | 0.70 TiB | 2,953 |
| `onedrive:5` | 0.68 TiB | 5,616 |
| `onedrive:backup` | 0.06 TiB | 4,241 |
| **合计（去重）** | **4.10 TiB（4.51 TB）** | **98,263** |

`task_engine.sh` 的 `SYNC_TASK_REGISTRY` 定义了 16 个活跃同步对（`gd:` 的 4 个已注释）。按同源端多目标展开：

- `backup` → 2 个目标（aliyundriveCrypt / wopan176Crypt）
- `0`、`1` → 各 3 个目标（wopan176Crypt / baidupanCrypt / wopan175）
- `2`~`5` → 各 2 个目标（wopan176Crypt / wopan175）

**同步对合计工作量 ≈ 9.2 TiB（约 10.1 TB），是去重源端的 2.25 倍。**
`trend.jsonl` 里那个 ≈9.90 TB 的"剩余未传"，就是这 16 个同步对的合计口径，**其中约 56% 是重复副本**。

⚠️ 这意味着：即使一切正常，也要传 10.1 TB 才能填满所有目标；而只要保住一份灾备副本，实际只需 4.51 TB。

---

## 三、近 5 天实际产出（`trend.jsonl` 全量 20 轮）

时间跨度 2026-09-09 07:33 UTC → 2026-09-13 10:53 UTC（约 4.1 天）：

| 指标 | 实测值 |
|---|---|
| 轮数 | 20 |
| 每轮 `duration_s` 中位数 | **330 分钟**（即几乎每轮都跑满预算上限） |
| `transferred_bytes` 合计 | **13.42 GB** |
| 零产出轮数 | **8 / 20**（40%） |
| 单轮最高净传 | 5.15 GB（run 34540284750） |
| `remaining_bytes` | 9.90 TB → 7.40 TB |

**按 2.68 GB/天的实测速率，传完 10.1 TB 需要约 3,770 天（10.3 年）；只填去重源端 4.51 TB 也需约 4.6 年。**

### 3.1 这个"剩余量下降 2.49 TB"是假的

同口径对照即可证伪：同期 `transferred_bytes` 合计只有 13.42 GB，而"剩余"少了 2.49 TB —— 相差 185 倍。

代码层面的成因在 `task_preview.sh:134-141`：

```bash
src_json=$(_get_listing_json "$source_path" "${FILTER_ARGS[@]}")
if [ -z "$src_json" ]; then
  echo "⚠️ add_preview_pair: 源端清单获取失败 (${source_path})" >&2
  src_json="[]"          # ← 清单失败 → 该同步对按"0 待传"计入
fi
```

`sync_trend.sh:50-58` 又把 `PREVIEW_PENDING_MAP` 各对 bytes 直接求和得到 `remaining_bytes`。
于是：**任何一个大同步对的源端/目标端清单列举失败，都会让"剩余量"瞬间跳动 TB 级。**
目标端失败会走 `dst_fail=1` 并打提示，源端失败则只有一行 stderr 静默跳过（`:139`）——极难察觉。

### 3.2 `transferred_bytes` 也不可信

出现多轮完全相同的值，说明记账有问题：

- `171257627` 连续出现在 run `34723502766` 与 `34728107625`
- `18282971` 出现在 run `34550419201`、`34622300065`、`34660149180`

记账点只有两处（`task_engine.sh:316`、`:869`），都依赖 `SYNC_TRANSFERRED_BYTES`，
且**只在 `_sync_task_finalize` 正常返回时执行** —— 被 330min 强杀的轮次，在途传输全部不计。
run `34728107625` 被杀时 rclone 已传 1.43 GiB，trend 记的却是 0.163 GB，就是这个原因。

> **推论**：`trend.jsonl` 的两个核心数字（`remaining_bytes` / `transferred_bytes`）都不能作为进度依据。
> 唯一可信的进度证据是**目标端实际清单**，而它只能靠下一轮的 truth-check 拿到。

---

## 四、根因：不是"后端死了"，是"名字太长"

### 4.1 日志自己给出了精确诊断

run `34728107625` 日志中，系统在真正动手前就已经测出了正确原因：

```
2026-09-13T07:52:51Z 名长诊断已启用: crypt=openlist:wopan176/Crypt, 后端已接受最长密文名 100 字节
2026-09-13T09:41:47Z   📏 名长: 原名 72B → 密文名 107B 🔴 >后端已接受最长(100B)
2026-09-13T09:41:47Z   ⏭ 名长注定失败 → 跳过 修复方法1·copyto 原名 / 修复方法3·zip 分卷，直接试短哈希名方法
2026-09-13T09:40:37Z ⚠️ 批次 1 巩固: 1037 个顽固缺失（后端内容性拒收，如密文名超长），转修复管线换方法落盘...
```

**后端 `wopan176` 只接受最长 100 字节的 crypt 密文名。** 超名的写请求被 OpenList 包成 `405 Method Not Allowed` 返回给 rclone。

### 4.2 405 的实测分布（run `34728107625`）

- `405 Method Not Allowed` 相关行 **2,394** 条，`Failed to copy` **2,103** 次
- 全部集中在 `2/1024j/动漫本子/路人女主/已解压/(C88) [バス停シャワー (桂井よしあき)] EGOISTIC LOVERLILY (冴えない彼女の育てかた) [中国翻訳]/` 一类**超长目录名**下

按密文名长度统计 528 次修复样本：

| 密文名长 | 数量 | 说明 |
|---|---|---|
| 22 B（p50） | 391 | **远低于 100B 上限，本不该失败** |
| 100–109 B | 61 | 超限 |
| 120–239 B | 67 | 超限 |
| **>100B 合计** | **128（24.2%）** | 文件级超名，改名/分卷可解 |

**关键交叉验证**：391 个文件自身密文名只有 22B 却全部失败 —— 它们失败的原因是**父目录名超长**，
不是文件超长。也就是说：

- **文件级超名**（24%）→ 内容性拒收，短哈希改名/分卷本可绕过
- **目录级超名**（74%）→ 该目录下所有写入失败，短哈希目录折叠本可绕过

### 4.3 但"路径长度"不是全部 —— 存在假成功

同一轮里还有一组决定性反例（`:6756`、`:6787`）：

```
2026-09-13T07:55:56Z 2026/09/13 07:55:56 INFO  : 44.jpg: Copied (new)
2026-09-13T07:55:56Z 27.339 MiB / 27.339 MiB, 100%, 552.657 KiB/s, ETA 0s
2026-09-13T07:55:56Z   ❌ 批量折叠零落盘（rc=0），该目录退回逐文件修复
```

rclone 退出码 0、报 `Copied (new)` 27.34 MiB，**重启容器从后端取真值后，文件并不存在**。
同样的事在 07:58:38 又发生一次（39.jpg / 21.76 MiB）。

所以 `wopan176Crypt` 上有两类失败同时存在：

1. **硬失败**：`405 Method Not Allowed`（名长超限）
2. **软失败（假成功）**：HTTP 200 + rclone 报 Copied，但后端零落盘，只有重启后 diff 才暴露

第 2 类更危险 —— 它让所有基于 rclone 退出码和 `Copied` 行数的判据全部失效。

#### 4.3.1 假成功的规模（跨轮追踪，已量化）

追踪同一个文件在连续三轮中的命运，例如 `(๑\`･ᴗ･´๑) (3421948) - 46132973_p0 - 舐めますー？？.jpg`
（该文件在 run 34728107625 被诊断 `密文名 107B 🔴 >100B`）：

| 轮次 (UTC) | 该文件的记录 |
|---|---|
| 34709806073 / 09-12 23:16 | `Copied`（报成功） |
| 34723502766 / 09-13 04:05、04:16、04:22、04:31 | `Copied` ×4（报成功） |
| 34723502766 / 09-13 04:35 | **修复失败**（仍在缺失清单里） |
| 34728107625 / 09-13 09:16、09:25、09:28、09:38 | `Copied` ×4（报成功） |
| 34728107625 / 09-13 09:41 | **修复失败**（仍在缺失清单里） |

**同一个文件在 24 小时内被"成功复制" 9 次，每一次之后仍被判为缺失。**

全量统计三轮（34709806073 / 34723502766 / 34728107625）：

| 指标 | 值 |
|---|---|
| 三轮 `Copied` 事件合计 | 11,236 次 |
| 去重后涉及文件 | 6,319 个 |
| **跨轮重复（≥2 轮都报 Copied）** | **2,042 个 = 32.3%** |
| 三轮全部重复 | 801 个（12.7%） |
| 单轮内重复 | 每轮约 **1,037** 次 |

最后一行尤其说明问题：run 34723502766 有 3,879 次 `Copied` 事件但只涉及 2,842 个文件，
run 34728107625 有 3,114 次但只涉及 2,077 个 —— **两轮的差值都恰为 1,037**，
与日志中 `⚠️ 批次 1 巩固: 1037 个顽固缺失` 完全对应：同一批 1,037 个文件在同一轮内
被批量 sync 传一遍、又被修复管线重试一遍，**两次都没落盘**。

> **因此 §三 那个"净传 13.42 GB"是 rclone 上报量，不是落盘量。真实落盘量只能用
> "跨轮去重后仅出现一次的文件"来上界估计，约为 67.7% × 13.42 GB ≈ 9 GB 的上界，
> 且这 9 GB 里可能还含有未落盘的部分。**

#### 4.3.2 附带后果：另外 15 个同步对在这四轮里一次都没跑过

统计四轮日志（34694126684 / 34709806073 / 34723502766 / 34728107625）中的目标端归属：

| 目标后端 | 上报 Copied | Failed to copy |
|---|---|---|
| `wopan176Crypt` | 11,236 | 10,527 |
| `baidupanCrypt` | **0** | **0** |
| `wopan175` | **0** | **0** |
| `aliyundriveCrypt` | **0** | **0** |

后三者在日志里只出现在预览/注册阶段（`注册进度: 12/16 onedrive:0 → openlist:baidupanCrypt/0`），
**执行 pass 从未到达它们** —— 轮转游标从第 9 个同步对（`task2` → `wopan176Crypt/2`）开始，
每轮预算都在这个同步对上耗光（§六-10）。

两个推论：

1. **`baidupanCrypt` / `wopan175` / `aliyundriveCrypt` 的当前可写性完全未知**，
   没有任何近期证据表明它们是好是坏；不能想当然地当作"健康备份目标"。
2. 这也解释了为什么 `remaining_bytes` 长期在 9.9 TB 附近横盘 ——
   16 个同步对里只有 `wopan176Crypt` 那几个在被处理，而且处理失败。

### 4.4 熔断把正确诊断覆盖掉了

系统对上述现象的处理（`file_fix.sh:831-833`）：

```
2026-09-13T08:00:03Z 🚫 后端 openlist:wopan176Crypt 连续 3 个目录不可写（阈值 3）→ 判定后端级故障，
                        本轮该后端剩余目录一律直接判不可写（不再逐个探测/重启）
```

**熔断粒度是"挂载根"，而故障粒度是"名字长度"。** 后果：

- 短哈希目录兜底被熔断直接短路，从未真正实测过可写性：
  ```
  08:00:11Z    🔀 目录级兜底: 整条目录折叠为短哈希目录 218b0d8e
  08:00:11Z    ✅ 短哈希目录已确认存在
  08:00:11Z    ❌ 短哈希目录不可写（后端熔断），兜底终止     ← 不是实测，是熔断推断
  ```
  注意：8 字符哈希目录的密文名极短，**大概率本可写入**，但熔断让它连试都没试。
- 熔断后每个文件仍要跑完 mkdir → 探测 → 折叠判定全流程才判失败（约 11–12 秒/个），
  400 个文件 = 约 80 分钟纯空转。

代码层面确认，逐文件循环（`file_fix_pipeline.sh:755`）**唯一的终止条件是时间预算**：

```bash
while IFS= read -r failed_line; do
  ...
  if [ -n "${OPENLIST_SYNC_DEADLINE_EPOCH:-}" ] \
     && [ $(( $(date +%s) + _fix_slice )) -ge "$OPENLIST_SYNC_DEADLINE_EPOCH" ]; then
    echo "⏳ 修复管线预算将尽，优雅收摊..."
    SYNC_TIME_EXHAUSTED=1
    break
  fi
  # ← 此处没有 _BACKEND_DEAD 短路
```

`_BACKEND_DEAD` 的读取点全库只有三处：`file_fix.sh:753`、`file_fix_pipeline.sh:385`、`sync_engine.sh:312`。
**逐文件循环不在其中。**

---

## 五、一轮 5.5 小时的时间都去哪了（run `34728107625` 逐段还原）

run 创建 00:29 UTC → job 实际启动 05:18 UTC（**等锁 4 小时 49 分**）→ 10:51 UTC 被 330min 超时强杀。

| 时段 (UTC) | 时长 | 行为 | 产出 |
|---|---|---|---|
| 05:18–05:54 | 36 min | 预览 pass（16 个同步对逐一列举源端+目标端） | 0 |
| 05:57–07:52 | **115 min** | `动漫本子` 批量 sync，**2,103 次 405** | **0** |
| 07:53–08:00 | 7 min | 3 次目录探测 + 2 次批量折叠 | 27MB+22MB 报成功但**零落盘** |
| 08:00–09:00 | **60 min** | 熔断后逐文件修复，**528 次全败**（每个空转 ~12s） | **0** |
| 09:00–09:39 | 39 min | 切换子目录 `yande.re`，列举 + 传输 | ~2,000 Copied（**未验证**） |
| 09:39–10:34 | **55 min** | `pixiv` 目录 1,037 顽固缺失 → 400 次修复全败 | **0** |
| 10:34–10:51 | 17 min | 继续传输 | ~1,100 Copied（**未验证**） |
| 10:51 | — | 330min 超时强杀，收尾截断 | — |

**有 Copied 的分钟数：42 / 297 = 14%。** 也就是说 **86% 的同步时间零产出**。

### 5.1 那 3,114 个 Copied 是真的吗？—— 已可判定：大概率不是

run 在 10:51:58 被强杀，`_sync_persist_verify_and_retry` 没机会跑，本轮确实没有直接验证。
但**跨轮追踪已经间接判定**（见 §4.3.1）：同一批文件在 09-12 至 09-13 的四轮里反复被报 `Copied`，
却始终留在缺失清单中。最近四轮全部耗在同一个同步对上：

| 轮次 | Failed to copy | Copied（上报） | trend 记的净传 |
|---|---|---|---|
| 34694126684 | 4,208 | **0** | 0 |
| 34709806073 | 2,107 | 4,243 | 2.27 GB |
| 34723502766 | 2,106 | 3,879 | 0.17 GB |
| 34728107625 | 2,103 | 3,114 | 0.17 GB |

**四轮约 22 小时、约 1.1 万次"成功复制"，实际落盘接近于零。**
其中 34694126684 一轮是 4,208 次失败、0 次成功——整轮空转。

---

## 六、改进建议

### P0 · 不做就永远收敛不了

**1. 熔断粒度从"挂载根"下沉到"名字长度"**

现有判据（`file_fix.sh:831`）用"连续 3 个目录不可写"推断整个后端故障，粒度太粗。
系统已经能测出 `后端已接受最长密文名 100 字节`、`名长: 原名 72B → 密文名 107B`，
应直接据此**预判**：密文名 >100B 的条目 → 跳过所有"保留原名"的方法，直接走短哈希改名/分卷，
**不要计入后端熔断计数**。熔断计数只应统计"短名也写不进"的情形。

**2. 逐文件循环加熔断短路**

在 `file_fix_pipeline.sh:755` 的 `while` 循环内、进入单个文件处理前，增加
`_BACKEND_DEAD` 判断（命中则整批判失败并退出循环）。
按本轮实测可立即回收约 80 分钟/轮（占预算 24%）。

**3. 熔断后不再启动注定失败的新批次**

熔断置位后，本同步对的剩余子目录/批次应直接跳过并把预算交给健康后端；
熔断出口要走 `sync_engine.sh` 尾部 → 置 `SYNC_BACKEND_DEAD` → `run_all_tasks` 后移轮转游标。
注意**不要置 `SYNC_TIME_EXHAUSTED`**（那是"预算到点"语义，会把后端故障误报成时间不够）。

### P1 · 显著提升有效吞吐

**4. 削减副本冗余（最大杠杆）**

16 个同步对里有 2.25× 冗余。`wopan176Crypt` 承载 7 对（backup + task0~5）却是当前故障后端。

**前提**：`baidupanCrypt` / `wopan175` / `aliyundriveCrypt` 的可写性目前**零证据**（§4.3.2 ——
它们近四轮一次都没被执行过）。所以第一步不是切换，而是**先用调试模式对这三个后端各跑一次
单任务写入自检**，拿到可写性证据后再决定把预算给谁。

若其中任一个健康：集中填满**一份**目标，把 `wopan176Crypt` 的 7 对降级或暂停，
工作量从 10.1 TB 降到 4.51 TB。若三个都不健康，则问题不在 openlist 脚本层，
而在网盘账号/配额层，需先解决后端。

**5. 把 `_refresh_openlist_cache` 的无条件 `sleep 60` 改成条件等待**

每子目录前后各一次，一轮 85 次 ≈ 85 分钟纯 sleep（占预算 26%），是第二大时间黑洞。
改为"仅在驱动未就绪时等待"或按目录数摊薄。

**6. 405 快速失败（fail-fast）**

批量 sync 阶段对同一目录连续 N 个 405 后立即停手，而不是把 2,103 个文件逐个试完（本轮烧掉 115 分钟）。
当前 `OPENLIST_8005_RETRY_ATTEMPTS=1` 只控制了单文件重试次数，控制不了"整批继续"。

### P2 · 可观测性（让问题不再靠翻 2.4 万行日志）

**7. 修掉三个失真指标**

- `remaining_bytes`：源端清单失败时（`task_preview.sh:139`）应置 `unknown` 并**显式告警**，
  而不是静默记 0 —— 现在一次源端列举失败就能让"剩余"假降 TB 级。
- `transferred_bytes`：被强杀的批次需要有兜底记账（可在批次开始时预登记"计划量"，结束时按实际修正）。
- 收尾的"本轮修复成效"：`ROUND_FIXED_OK` 目前含"沿用上轮修复"（本轮 0 成功却会显示成功数），需拆分口径。

**8. 每轮必须打印"落盘校验结论"**

现在"报 Copied 但零落盘"只在批量折叠分支里打了一行，主 sync 路径没有。
建议每轮收尾强制输出：`本轮 rclone 报 Copied N 字节 / 重启校验确认落盘 M 字节`。

### P3 · 运维

**9. 处理僵尸 job（当前阻塞项）**

run `34752801560` 自 09-13 10:48 UTC 起 `in_progress`，10:53 UTC 后无任何日志更新，
已挂起约 4 小时，持续占用 `concurrency: openlist-singleton`。
后果：run `34763535089` 自 14:44 UTC 起一直 `pending`，整个同步链路停摆。

建议：给同步 step 加 `timeout-minutes` 之外的**心跳检测**（如 30 分钟无日志输出则自毁），
或在自续触发 step 里检测"存在超过 N 分钟无更新的 in_progress run"时主动 cancel。

**10. 轮转游标已被钉住**

`task_rotation.json` 当前为 `{"cursor": 8, "attempts": 6, "updated": "2026-09-13T11:33:19Z"}`
—— 即第 9 个同步对（`task2` → `wopan176Crypt/2`）连续尝试 6 次未完成。
原因是被 330min 强杀时 `_sync_task_impl` 不返回，`task_engine.sh:203-219` 的轮转逻辑根本没执行到，
游标只能靠 `ROTATION_MAX_CONSECUTIVE_ATTEMPTS=8` 阀门解救 —— **一个故障同步对要吃掉 8 × 5.5h ≈ 44 小时。**

---

## 七、证据索引

| 结论 | 证据位置 |
|---|---|
| 净传 13.42 GB / 20 轮 | `onedrive:/logs/sync_state/trend.jsonl` |
| 8/20 轮零产出、中位 330min | 同上 |
| `remaining` 口径失真 | `task_preview.sh:134-141`、`sync_trend.sh:50-58` |
| `transferred` 记账点 | `task_engine.sh:316`、`:869` |
| 100B 密文名上限 | run `34728107625` 日志 `07:52:51Z`、`09:41:47Z` |
| 2,103 次 405 | 同日志 `05:59:13Z` 起，集中在长名目录 |
| 假成功（Copied 但零落盘） | 同日志 `07:55:56Z`、`07:58:38Z` |
| 熔断短路兜底 | 同日志 `08:00:11Z`、`08:00:25Z`…（400 次） |
| 逐文件循环无熔断短路 | `file_fix_pipeline.sh:755-767` |
| `_BACKEND_DEAD` 读取点仅三处 | `file_fix.sh:753`、`file_fix_pipeline.sh:385`、`sync_engine.sh:312` |
| 轮转游标 attempts=6 | `onedrive:/logs/sync_state/task_rotation.json` |
| 假成功（同一文件 24h 内 9 次 Copied 仍缺失） | run 34709806073 / 34723502766 / 34728107625 跨轮比对 |
| 跨轮重复率 32.3%、单轮内重复 1,037 | 同上三轮 `Copied` 事件去重统计 |
| 整轮 4,208 失败 / 0 成功 | run 34694126684（09-12 12:36，同同步对） |
| 僵尸 job 占锁 | `gh run list --workflow openlist.yml`（run 34752801560 / 34763535089） |
| 源端规模 | 本机 `rclone size onedrive:{0..5,backup}` |
| 16 个同步对 | `task_engine.sh:33-58` `SYNC_TASK_REGISTRY` |

---

## 八、待验证项（本报告未能闭环）

1. ~~**09:00 后 3,114 个 Copied 是否真落盘**~~ —— **已通过跨轮追踪间接判定为假成功**（§4.3.1），
   但**仍建议**用一次调试模式（`调试 · 修复管线测试` + `force_sync=true`）在健康后端上做单次 truth-check 正面确认。
2. **短哈希目录到底可不可写** —— 本轮因熔断从未实测。建议用调试模式（`调试 · 修复管线测试`，
   `force_sync=true`）在健康后端上单任务验证一次。
3. **`baidupanCrypt` / `wopan175` / `aliyundriveCrypt` 的当前可写性** —— 已确认**近四轮零执行**，
   属于完全未知（§4.3.2），不是"待验证"而是"从未测过"。决定 P1-4 该把预算给谁，优先级很高。
4. **100B 上限是文件名还是完整路径** —— 影响"目录级超名（74%）"能否只靠折叠解决。

---

*报告生成时间：2026-09-13（本地）。本报告只做评估，未修改任何代码或配置。*
