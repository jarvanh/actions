name: proxy-speedtest-gistnodes

# 从 gist.github.com 抓公开节点订阅 → 临时起一个 Sub-Store 去重并产出 mihomo YAML →
# 发布到本工作流专属 Gist → 把该 Gist 的 raw URL 当订阅源，交给选定的测速工作流测速。
#
# 与三套测速工作流的分工：本工作流自己**不测速**，只做「换一个节点来源」这一件事。
# 测速、达标筛选、结果订阅上传各自 Gist、Telegram 通知，全部由被复用的那套完成——
# 所以这里不发通知，否则同一轮会出现两条通知（而通知版式有规范约束，见
# docs/telegram-notify.md）。抓取情况走 job 摘要与 progress 日志，不进通知。
#
# 引擎选择（环境变量，两者取其一，dispatch 优先）：
#   仓库 Variable PROXY_SPEEDTEST_ENGINE = taier|gitee|cdn   定时运行的默认值
#   workflow_dispatch 的 engine 入参                          单次运行的覆盖值
#   两处都没设 = taier
#
# 为什么用 workflow_call 复用而不是 gh workflow run 另起一轮：单轮内串起来跑，
# 日志/耗时/失败原因都在同一个 run 里；另起一轮还要 PAT 具备 Actions 写权限，
# 且抓取结果与测速轮次之间会脱钩。
on:
  schedule:
    # 每天 2 次：UTC 00/08 = 北京 08:00 / 16:00（原先为 4 次，2026-09-17 减半）。
    # 为什么减：单轮要跑 20–55 分钟，4 次/天时后一轮常与前一轮尾巴重叠、又频繁被手动
    # 触发的新轮顶掉（实测 2026-09-17 连续 7 轮 cancelled/failure），产出反而更不稳；
    # 减到 2 次让每轮有充足窗口跑完。
    # 为什么是这 2 个点：刻意避开三套测速的定时轮（gitee 京 05/09/13/17、
    # cdn 06/10/14/18、taier 07/11/15/19）——本工作流抓完节点会**直接调用**其中一套来
    # 测速，撞点会互相抢节点带宽、读数彼此污染。08/16 各在 taier（07/15）之后 1 小时，
    # 与所有测速点至少错开 1 小时。
    # 仍只排在北京 05:00–20:00：夜间 runner 排队 + 出口拥塞会让测速读数失真。
    - cron: "0 0,8 * * *"
  workflow_dispatch:
    inputs:
      engine:
        description: '用哪套测速：taier（泰尔三网）/ gitee（Gitee 上行下行）/ cdn（国内镜像站）'
        required: false
        type: choice
        default: '(用仓库 Variable)'
        options:
          - '(用仓库 Variable)'
          - 'taier'
          - 'gitee'
          - 'cdn'
      queries:
        description: '搜索关键词（逗号分隔；留空用脚本默认：ss://,vless://,vmess://,trojan://,hysteria2://,tuic://）'
        required: false
        type: string
      max_nodes:
        description: '最终订阅保留多少节点（留空 = 0 = 不限。下游 mihomo 按 provider 全量做健康检查，节点多会拖长启动）'
        required: false
        type: string
      alive_filter:
        description: '发布前先做一轮健康检查、只发布活节点（留空 = 1 = 开）。关掉则原样发布全部节点'
        required: false
        type: choice
        default: '1'
        options:
          - '1'
          - '0'
      trial_load:
        description: '发布前把订阅交给本机 mihomo 试装一遍，摘掉装不上的节点（留空 = 1 = 开）。关掉则不做排雷'
        required: false
        type: choice
        default: '1'
        options:
          - '1'
          - '0'
      test_nodes:
        description: '交给测速引擎实测多少个节点（留空 = 0 = 不限）'
        required: false
        type: string
      target_subs:
        description: '目标：凑够多少个「像订阅」的文件（留空 = 100；0 = 不限，翻到没有 24h 内更新的为止）'
        required: false
        type: string
      max_age_hours:
        description: '只要最近多少小时内更新过的 Gist（留空 = 24；0 = 不限）'
        required: false
        type: string

concurrency:
  # 只管住「编排自己」不并发；被复用的测速工作流沿用它们各自的 job 级 singleton。
  # 不要在这里再加被调工作流的同名 group：官方文档明确警告 caller 与 called 用同一个
  # group 值会互相影响（cancel-in-progress: true 时会把 caller 取消；false 时 caller
  # 持着 group 等被调 job、被调 job 又在等同一个 group，互等到超时）。
  group: proxy-speedtest-gistnodes-singleton
  cancel-in-progress: false

jobs:
  fetch-nodes:
    runs-on: ubuntu-latest
    # 70 分钟：搜索页会 429，重试带退避（5s/10s × 3 次）会把单页拖到十几秒，
    # 十几页下来搜索阶段本身就可能吃掉好几分钟，再叠加取文、Sub-Store 解析、健康检查
    # 与试装排雷。
    #
    # ⚠️ 这个数字是**最后兜底**，不是预算。真正的收口在脚本的**五个**阶段预算里，它们都
    # 显著小于这里：抓取 GIST_NODES_BUDGET_SECONDS（默认 900 秒）+ Sub-Store 阶段
    # SUB_STORE_BUDGET_SECONDS（默认 300 秒）+ 健康检查 GIST_NODES_ALIVE_BUDGET_SECONDS
    # （默认 600 秒）+ 试装 GIST_NODES_TRIAL_BUDGET_SECONDS（默认 1500 秒）
    # ⇒ 最坏 0.5 + 15 + 5 + 10 + 25 ≈ 55.5 分钟 < 70。
    # 改任何一个都要回头看其余的（40→70 是 2026-09-15 为试装段抬的）。
    #
    # 为什么必须这样分工：超时是 GitHub **硬取消**，被取消时整个 job 的工作全废——抓到的
    # 订阅文件、Sub-Store 已投喂的订阅、下游三个测速 job（全 skipped）。2026-09-14 三次运行
    # 就是这么废掉的（34800369606 / 34808992495 / 34827324756，都是抓取阶段空转到 30 分钟被
    # 取消）；2026-09-15 run 34989917789 又因为试装层没有预算、逐区间重起 mihomo 而重演了
    # 一次（7 块只跑完 4 块）。阶段预算则优先降级、其次才失败。
    timeout-minutes: 70
    outputs:
      engine: ${{ steps.engine.outputs.engine }}
      # 注意：**没有** sub_url / gist_id / gist_html_url。脚本写出的这三个值里含 gist id，
      # 而 `PROXY_SPEEDTEST_GISTNODES_GIST_ID` 是个注册过的 secret（值就是这个 id），
      # GitHub 见到 output 里出现已注册 secret 的完整字符串就把整个 output 丢掉
      # （日志留 `Skip output 'X' since it may contain secret.`）。实测 run 34956069334
      # 三个全中被丢，下游拿空值 → fallback 到仓库 secret → 订阅源退回用户自己的机场、
      # 结果写进另一个泰尔测速的 Gist（整轮 gist 抓节点名存实亡，且零报错）。
      # 需要那三个值的下游改用 secret 直传，见下面各 speedtest job 的 gist_id 参数。
      count: ${{ steps.fetch.outputs.count }}
      parsed_count: ${{ steps.fetch.outputs.parsed_count }}
      gists_scanned: ${{ steps.fetch.outputs.gists_scanned }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Resolve engine
        id: engine
        env:
          # 经 env 传入而不是拼进脚本：Variable 是仓库可写值，拼字符串会被引号破坏语法
          INPUT_ENGINE: ${{ inputs.engine }}
          VAR_ENGINE: ${{ vars.PROXY_SPEEDTEST_ENGINE }}
        run: |
          set -euo pipefail
          ENGINE="$INPUT_ENGINE"
          case "$ENGINE" in
            ''|'(用仓库 Variable)') ENGINE="$VAR_ENGINE" ;;
          esac
          ENGINE="${ENGINE:-taier}"
          case "$ENGINE" in
            taier|gitee|cdn) ;;
            *) echo "::error::未知测速引擎 '$ENGINE'（只支持 taier / gitee / cdn）"; exit 1 ;;
          esac
          echo "engine=$ENGINE" >> "$GITHUB_OUTPUT"
          echo "本次测速引擎：**$ENGINE**" >> "$GITHUB_STEP_SUMMARY"

      - name: Start Sub-Store backend
        # 只跑在本机回环上（-p 127.0.0.1），容器用完即删：不需要隧道、不需要 CORS 白名单、
        # 也不碰仓库里那套常驻 sub-store（那是给网页端用的，单宿主，会互相抢隧道）。
        #
        # **只暴露后端 API 的 3000**。镜像 `xream/sub-store:http-meta` 的默认布局是
        # 「后端 3000 / 前端 http-meta 3001」，容器启动日志写得很清楚：
        #   [BACKEND] listening on :::3000
        #   [FRONTEND] :::3001
        # 我们只用后端，前端不必发布到宿主上。
        #
        # **不要设 SUB_STORE_BACKEND_API_PORT / _HOST**：后端默认已是 3000 且绑在 `::` 上，
        # 不需要覆盖；而设成 3001 会让它去抢前端已经占着的 3001 —— 日志是
        # 「HTTP-META is running...」之后紧接 `listen EADDRINUSE: address already in use
        # 0.0.0.0:3001`，后端起来就死（本工作流首次运行就栽在这；当时以为「镜像没默认端口
        # env」，其实默认有，只是把号记反了）。
        #
        # **另外两个 env 必须设，理由都在 sub-store 源码里**：
        #
        #   1) `SUB_STORE_BODY_JSON_LIMIT`（默认 `'1mb'`）。出处 `backend/src/vendor/express.js`：
        #      `const limit = eval('process.env.SUB_STORE_BODY_JSON_LIMIT') || '1mb';`
        #      投喂是「整份订阅正文塞进 JSON」POST 到 `/api/subs`，累积那份已 1.07MB，
        #      转义后必超 1mb ⇒ `413 PayloadTooLargeError`。实测 run 34840068571：
        #      `gist-nodes-000`（累积）+ 3 个较大抓取文件被拒，`subs_failed=4`。抬到 16mb，
        #      给 `GIST_NODES_CARRYOVER_MAX_MB`（8）与 `GIST_NODES_MAX_FILE_MB`（2）都留足余量。
        #      ⚠️ **这两个数字成对**：谁把 CARRYOVER_MAX_MB 抬过 ~14 就要回头抬这里。
        #
        #   2) `SUB_STORE_FRONTEND_BACKEND_PATH`。出处 `backend/src/core/proxy-utils/index.js`
        #      的 `loadScriptItem`：`isNode && !process.env.SUB_STORE_FRONTEND_BACKEND_PATH
        #      ?.startsWith('/') && !process.env.SUB_STORE_BACKEND_CUSTOM_NAME` 就直接 throw。
        #      不设 ⇒ 组合里只要带 Script Operator（限量算子），取回就 HTTP 500。
        #      而 `GIST_NODES_MAX_NODES=0` 时脚本**根本不生成** Script Operator，所以这条
        #      路径此前每轮都被绕过、一直没暴露（run 34840068571 带 `max_nodes=100` 才炸出来）。
        #      只在本机回环上跑、不对外暴露，按官方提示直接给 `/` 即可。
        run: |
          set -euo pipefail
          # 先拉镜像再起容器，且**拉取带重试**。
          #
          # 为什么不能直接 `docker run`：它把「拉镜像」和「起容器」合成一步，而拉取失败
          # 会以 exit 125 把整个 step 打死、连重试的机会都没有。Docker Hub 侧
          # `auth.docker.io` / `registry-1.docker.io` 的瞬时重置很常见——实测 2026-09-16
          # run 35046469047（schedule 轮）：
          #   Head ".../manifests/http-meta": read tcp ...->104.18.43.178:443:
          #   read: connection reset by peer   ⇒ exit 125
          # 同一镜像在 33 分钟前的 run 35044338569 拉得好好的，仓库侧没有任何改动。
          # 一次瞬时重置就报废整轮（含已抓到的订阅），与 `fetch_text` 那次是同一类
          # 「单次网络抖动 ⇒ 静默/剧烈失败」形态，所以同样用指数退避重试兜住。
          for attempt in $(seq 1 4); do
            if docker pull xream/sub-store:http-meta; then
              echo "镜像拉取成功（第 $attempt 次尝试）"
              break
            fi
            if [ "$attempt" = "4" ]; then
              echo "::error::xream/sub-store:http-meta 拉取失败且重试已用尽（4 次，Docker Hub 侧不可达？）"
              exit 1
            fi
            wait_s=$((5 * 2 ** (attempt - 1)))
            echo "::warning::镜像拉取失败（第 $attempt 次），${wait_s} 秒后重试"
            sleep "$wait_s"
          done
          docker run -d --name sub-store \
            -p 127.0.0.1:3000:3000 \
            -e SUB_STORE_BODY_JSON_LIMIT=16mb \
            -e SUB_STORE_FRONTEND_BACKEND_PATH=/ \
            xream/sub-store:http-meta
          # 探测判据必须**与消费者一致**：脚本的就绪探活要求恰好 200（gist_nodes.py 里
          # `GET /api/subs` 只要 status != 200 就直接失败）。早先这里用 `curl -fsS`，而 `-f`
          # 只对 ≥400 报错、302 一律算「就绪」⇒ 探测绿了、脚本红了，白烧一轮
          # （2026-09-14 run 34831792493：探测第 2 次通过，5 分钟后脚本拿到 404）。
          #
          # 另一个坑（2026-09-17 修）：**200 与容器日志之间有竞态**。/api/subs 一开始响应
          # 200，body JSON limit 那行可能还没被刷进 `docker logs` —— 实测 run 35203699226：
          # 镜像拉取完 09:11:02.7，第 **1** 次探测 09:11:04.97 就拿到 200，紧跟着 grep
          # 落空、job 判失败；而成功轮（35199572284）在第 2 次探测才 200，多出约 2 秒就躲过了。
          # 所以这里**不能一探到 200 就立刻断言**：把「日志里有没有这行」也当成就绪条件之一，
          # 未见到就继续下一轮探测（同一个 30 次 × 2 秒的上限），只有逐轮都见不到才算真失败。
          code=000
          limit_ok=0
          for i in $(seq 1 30); do
            code=$(curl -s -o /dev/null -w '%{http_code}' \
                     http://127.0.0.1:3000/api/subs 2>/dev/null || true)
            if [ "$code" = "200" ]; then
              # 就绪之后还要确认「抬上限」**真的生效**：env 名来自第三方镜像，被上游改名或
              # 忽略时**不会报错**，只会静默退回 1mb —— 那时症状是若干较大订阅 413，而 job
              # 依然「成功」（413 只记进 subs_failed 计数）。后端启动时会打这一行，且与
              # `[BACKEND] listening on` 走同一个 logger，所以一定在容器日志里。
              # 另一条 env（SUB_STORE_FRONTEND_BACKEND_PATH）不设的后果是 HTTP 500 + job 失败，
              # 是**响亮**的失败，不需要守卫。
              if docker logs sub-store 2>&1 | grep -q 'body JSON limit: 16mb'; then
                limit_ok=1
                echo "Sub-Store 后端就绪（第 $i 次探测，HTTP ${code}）；body JSON limit 已确认 16mb"
                exit 0
              fi
              echo "第 $i 次探测 HTTP 200，但容器日志还没出现 body JSON limit 行，继续等（日志刷写滞后于端口就绪）"
            fi
            sleep 2
          done
          if [ "$limit_ok" = "0" ] && [ "$code" = "200" ]; then
            echo "::error::Sub-Store 后端响应 200 但始终没报告 body JSON limit: 16mb（SUB_STORE_BODY_JSON_LIMIT 可能已被上游改名或忽略）。实际启动日志："
            docker logs --tail 40 sub-store || true
            exit 1
          fi
          echo "::error::Sub-Store 后端 60 秒内未就绪（最后一次 GET /api/subs → HTTP ${code}）"
          # 失败时把容器状态与端口映射一并打出来：只看日志分不清「没起来」和「起来了但
          # 端口不对」（首次运行就吃了这个亏 —— 日志里有 EADDRINUSE，却看不出谁占了端口）
          docker ps -a --filter name=sub-store || true
          docker port sub-store || true
          docker logs --tail 200 sub-store || true
          exit 1

      - name: Fetch nodes and dedupe via Sub-Store
        id: fetch
        env:
          SUB_STORE_BACKEND_URL: http://127.0.0.1:3000
          # 抓取范围与产出（dispatch 可覆盖；这里的默认值与脚本默认值一致）
          GIST_NODES_QUERIES: ${{ inputs.queries }}
          GIST_NODES_TARGET_SUBS: ${{ inputs.target_subs || '100' }}
          GIST_NODES_MAX_AGE_HOURS: ${{ inputs.max_age_hours || '24' }}
          GIST_NODES_MAX_NODES: ${{ inputs.max_nodes || '0' }}
          # 发布前的健康检查：只把活节点交出去。为什么必须有——下游三套测速会拿这份订阅
          # 各自起 mihomo 做**非惰性**全量健康检查，上万节点时跑不完（实测发布 12635 个、
          # 下游只认到 14 个，见 docs/proxy-speedtest-gistnodes.md）。这里先筛，下游就是
          # 几百个活节点，秒级探完。失败/超时一律降级为「原样发布全部」，不会零产出。
          #
          # 注意 `GIST_NODES_WORKDIR` **不能**直接当过滤层的 provider 目录：mihomo 拒绝
          # 加载 home 之外的 provider 文件（`SAFE_PATHS`，level=fatal 直接退出）。过滤层
          # 自己会把越界路径改落回 home 内（`_safe_home_dir`），这里不必特殊处理。
          GIST_NODES_ALIVE_FILTER: ${{ inputs.alive_filter || '1' }}
          # 试装排雷：把「mihomo 装不上」的节点二分定位并摘掉。与健康检查是两件事——
          # mihomo 对 provider 是「全有或全无」，一个非法节点会让整份订阅在下游归零
          # （实测 2026-09-15：13210 个节点 → 下游 total: 0，见 docs 的「为什么还要试装」）。
          # 排雷层自己故障同样只降级为「原样发布」，绝不零节点。
          GIST_NODES_TRIAL_LOAD: ${{ inputs.trial_load || '1' }}
          # 试装阶段的墙钟预算。**必须有**：单次试装冷启动 ~3 秒（其余走热重载 ~0.03 秒），
          # 坏节点多时二分要跑很多层；没有它就会撞上面的 timeout-minutes 被硬取消，连已抓到的
          # 订阅一起报废（2026-09-15 run 34989917789 就是这么废的）。到点降级为「已摘的
          # 照摘、未测块原样放行」，不是失败、也不跳过整层。
          GIST_NODES_TRIAL_BUDGET_SECONDS: '1500'
          GIST_NODES_WORKDIR: ${{ github.workspace }}/gist-nodes
          # GitHub API 认证：匿名 60 次/h，一轮要拉上百个 Gist（每个至少一次 API 调用），
          # 不带 token 会被限流截断
          GH_TOKEN: ${{ secrets.PAT }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # 订阅导出：本工作流专属 Gist（与三套测速结果 Gist 分开，互不覆盖）。
          # 留空则首次运行自动新建，job 摘要会给链接；拿到 id 后回填此 secret 避免每轮新建
          PROXY_SPEEDTEST_GIST_ID: ${{ secrets.PROXY_SPEEDTEST_GISTNODES_GIST_ID }}
          # 新建 gist 后自动回填到哪个 secret（治「每轮新建 ⇒ 旧链接 404」的自续循环，
          # 见 speedtest_common._backfill_gist_secret）。本域 Gist 恒归自己，无条件回填。
          PROXY_SPEEDTEST_GIST_SECRET_NAME: PROXY_SPEEDTEST_GISTNODES_GIST_ID
          PROXY_SPEEDTEST_GIST_FILENAME: proxy_speedtest_gistnodes_providers.yaml
          PROXY_SPEEDTEST_GIST_DESCRIPTION: 'proxy speedtest source nodes (gist 搜索 + Sub-Store 去重)'
        run: python .github/scripts/proxy-speedtest/gist_nodes.py

      - name: Dump Sub-Store log and remove container
        # always：抓取失败时最需要看的就是 Sub-Store 侧的解析/去重日志
        if: ${{ always() }}
        run: |
          mkdir -p gist-nodes
          docker logs --tail 200 sub-store > gist-nodes/sub-store.log 2>&1 || true
          docker rm -f sub-store || true

      # 这里**刻意不上传 artifact**。原本传 `gist-nodes/`（含 providers.yaml：12783 个
      # 节点的 server/uuid/password），而本仓库是 **public** —— artifact 对任何登录 GitHub 的
      # 人都可下载，等于把节点凭据公开发布一份。排查需要的东西（Sub-Store 日志、计数、
      # 各阶段耗时）都已经在 job 摘要与 progress 日志里，不必再单独留一份含凭据的快照。

  # 三个引擎各一个条件 job：GitHub 不支持动态的 uses:，只能写三条并按 engine 选一条。
  # secrets: inherit 把仓库 secrets 透传给被调工作流（它们内部照旧读自己的 secret）。
  #
  # ⚠️ 这里**不能**再传 `sub_url` / `gist_id`：gist id 是注册过的 secret
  # （`PROXY_SPEEDTEST_GISTNODES_GIST_ID`），GitHub 见到 job output 里出现与已注册 secret
  # 相同的字符串就把整个 output 丢掉（`Skip output 'X' since it may contain secret.`）。
  # 实测 2026-09-15 run 34956069334：sub_url / gist_html_url / gist_id 三个全被丢，下游拿到
  # 空值就 fallback 到各自 secret ⇒ 订阅源退回用户自己的机场订阅、结果写进另一个泰尔 Gist，
  # 整轮「gist 抓节点」名存实亡且零报错。
  #
  # 也不能在 `with:` 里写 `${{ secrets.X }}`——`secrets` 上下文在 job 级 `with:` 里不可用
  # （会报 `Unrecognized named-value: 'secrets'`）。所以只传一个**布尔开关**，叫被调工作流
  # 从它自己的 secrets 里取 `PROXY_SPEEDTEST_GISTNODES_GIST_ID`，并自行按 id 解析订阅
  # raw URL（见各工作流的「Resolve source subscription」步骤）。
  speedtest-taier:
    needs: fetch-nodes
    if: needs.fetch-nodes.outputs.engine == 'taier'
    uses: ./.github/workflows/proxy-speedtest-taier.yml
    with:
      use_gistnodes_source: '1'
      result_gist_id_from_gistnodes: '1'
      # ⚠️ 不再传 max_nodes（该入参已于 2026-09-24 从 taier 删除）：它是「按 provider
      # 原序砍尾巴」，会砍掉还没测过的节点，与「到点收摊」重复且更易误伤。
      # 池子大小只由下面的 include_regex 决定，跑不完交给 TAIER_BUDGET_SECONDS 收摊。
      # 节点名 include 过滤（命中保留、未命中丢弃）——编排轮必须过滤，理由（2026-09-22
      # run 35684842162 实测）：上游交接 15793 个节点，5 小时预算（18000 秒）只测完 1905 个
      # （单节点 ≈9.5 秒，全测完 ≈41 小时），轮轮撞 TAIER_BUDGET_SECONDS 到点收摊。
      # 池子大小只由这一层决定：优先级正则与 max_nodes 已于 2026-09-24 删除（前者只排序、
      # 后者按原序砍尾巴），都不减量，只有 include 过滤能真正把池子压进预算。
      #
      # 词表依据（**每一轮都要重算**，池子随跨轮累积持续变大：本层发布 16012→19369 节点、
      # 累积文件 5.46MB→6.57MB，kept 也跟着 1647→2123）：拉当轮真实发布池
      # （`proxy_speedtest_gistnodes_providers.yaml` 的 raw URL）逐词统计**独占命中**
      # （去掉该词后少掉多少命中），按独占命中从小到大砍，目标 kept ≤ 1800
      # （1800 × 11.4s ≈ 5.7h … 收紧到 ~1540 × 11.4s ≈ 4.9h，给收尾留余量）。
      #
      # 现词表 = 原优先正则去掉「专线 / TW / 台 / Taiwan / 新加坡」：
      #   - 「专线」独占 757 个（首轮统计）**几乎全是游戏加速线**（无畏契约/王者荣耀/PUBG→
      #     日/英/法/澳/瑞士等非优质地区），既非优质地区又最占窗口；
      #   - 台湾（TW/台/Taiwan 共 353）与「新加坡」（448，已被 `SG` 覆盖英文侧）是本轮为
      #     压进预算让出的部分，香港（1093，占比最大）与 `SG` 保留。
      # 剩余词（HK/Hong/港/SG + IPLC/IPEL/IEPL 容错）本轮实测 kept=1544。
      # ⚠️ 2026-09-24 起 target 收紧到 ~800：duration 由 5 上调到 13（单节点 ≈31 秒），
      # 同等 5 小时预算能测的节点数约为原来的一半，词表必须跟着收，否则仍会轮轮到点收摊。
      # **只对 taier 引擎生效**：gitee / cdn 未实现 include 过滤，切引擎时仍是不限量全测。
      include_regex: 'IPLC|IPEL|IEPL|HK|Hong|港|SG'
      # 测活开关显式传 on：被调工作流测活**默认开**，编排不覆盖，这里写明让它不随 dispatch 漂移
      alive_probe: 'on'
      # 测速结果写进**本工作流专属 Gist**（与源节点订阅同库、不同文件名），
      # 而不是三套测速各自的 Gist —— 这样一轮 gist 抓取的产物集中在一处
      gist_filename: proxy_speedtest_gistnodes_result_taier.yaml
      gist_description: 'proxy speedtest result (gist 抓节点 · taier 三网)'
      label: 'gist 节点'
    secrets: inherit

  speedtest-gitee:
    needs: fetch-nodes
    if: needs.fetch-nodes.outputs.engine == 'gitee'
    uses: ./.github/workflows/proxy-speedtest-gitee.yml
    with:
      use_gistnodes_source: '1'
      result_gist_id_from_gistnodes: '1'
      # 不限节点数：抓来多少就测多少（用户要求；节点多会让 mihomo 启动健康检查变慢）
      max_nodes: ${{ inputs.test_nodes || '0' }}
      # 测速结果写进**本工作流专属 Gist**（与源节点订阅同库、不同文件名），
      # 而不是三套测速各自的 Gist —— 这样一轮 gist 抓取的产物集中在一处
      gist_filename: proxy_speedtest_gistnodes_result_gitee.yaml
      gist_description: 'proxy speedtest result (gist 抓节点 · gitee 上下行)'
      label: 'gist 节点'
    secrets: inherit

  speedtest-cdn:
    needs: fetch-nodes
    if: needs.fetch-nodes.outputs.engine == 'cdn'
    uses: ./.github/workflows/proxy-speedtest-cdn.yml
    with:
      use_gistnodes_source: '1'
      result_gist_id_from_gistnodes: '1'
      # 不限节点数：抓来多少就测多少（用户要求；节点多会让 mihomo 启动健康检查变慢）
      max_nodes: ${{ inputs.test_nodes || '0' }}
      # 测速结果写进**本工作流专属 Gist**（与源节点订阅同库、不同文件名），
      # 而不是三套测速各自的 Gist —— 这样一轮 gist 抓取的产物集中在一处
      gist_filename: proxy_speedtest_gistnodes_result_cdn.yaml
      gist_description: 'proxy speedtest result (gist 抓节点 · cdn 镜像站)'
      label: 'gist 节点'
    secrets: inherit
