name: openclaw

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  build:
    concurrency: openclaw-singleton
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 'current' # 始终使用官方发布的最新版
          
      - name: Force system Node to latest version
        run: |
          sudo ln -sf $(which node) /usr/local/bin/node
          node -v
          /usr/local/bin/node -v

      - name: Install Rclone
        run: |
          curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip
          unzip rclone-*-amd64.zip
          cd rclone-*-linux-amd64
          sudo mv rclone /usr/bin
          sudo chmod +x /usr/bin/rclone

      - name: Configure Rclone
        env:
          RCLONE_CONFIG: ${{ secrets.RCLONE }}
        run: |
          mkdir -p ~/.config/rclone/
          cat << EOF > ~/.config/rclone/rclone.conf
          $RCLONE_CONFIG
          EOF

      - name: Mount Dropbox
        run: |
          sudo apt-get install fuse3
          sudo mkdir -p /dropbox

          # 挂载 Dropbox
          sudo nohup rclone mount dropbox: /dropbox \
            --allow-non-empty \
            --no-gzip-encoding \
            --umask 000 \
            --allow-other \
            --attr-timeout 10m \
            --vfs-cache-mode full \
            --vfs-cache-max-age 5m \
            --vfs-read-chunk-size-limit 10G \
            --buffer-size 100M \
            --vfs-cache-max-size 10G &

          echo "Waiting for mount..."
          for i in {1..30}; do
            if mount | grep -q " on /dropbox " && ls /dropbox >/dev/null 2>&1; then
              echo "✅ Dropbox mounted"
              break
            fi
            sleep 2
          done

      - name: Install Cloudflared
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb
          sudo mkdir -p ~/.cloudflared/
          sudo cp -r /dropbox/self-hosted/.cloudflared/* ~/.cloudflared/ 2>/dev/null || true

      - name: Run Cloudflared Tunnel
        run: |
          # 提前在 Dropbox 挂载目录中创建 logs 文件夹并赋予权限
          sudo mkdir -p /dropbox/self-hosted/openclaw/logs

          cloudflared tunnel route dns oc oc.'${{ secrets.VD }}'.eu.org || true

          # 启动隧道并记录 PID
          # 使用 nohup 配合重定向，确保进程在后台稳健运行，日志直接写入 Dropbox
          nohup cloudflared tunnel run --url http://127.0.0.1:18789 oc \
            > /dropbox/self-hosted/openclaw/logs/cloudflared_oc.log 2>&1 &

          # 等待一小段时间确保进程启动成功并记录 PID
          sleep 10
          echo $! > /dropbox/self-hosted/openclaw/logs/cloudflared_oc.pid

      - name: Run rss-to-telegram container
        run: |
          echo "Starting rss-to-telegram before CLIProxyAPI..."
          mkdir -p /dropbox/self-hosted/rsstt
          rm -rf /tmp/local_rsstt
          mkdir -p /tmp/local_rsstt

          if [ -f /dropbox/self-hosted/openclaw/.env ]; then
            if rclone ls dropbox:self-hosted/rsstt.tar.gz > /dev/null 2>&1; then
              echo "📦 Found rsstt.tar.gz! Downloading and extracting..."
              rclone copyto dropbox:self-hosted/rsstt.tar.gz /tmp/rsstt.tar.gz
              MIN_SIZE=$((1024 * 1024))
              actual_size=$(stat -c%s /tmp/rsstt.tar.gz 2>/dev/null || echo 0)
              if [ "$actual_size" -ge "$MIN_SIZE" ] && tar -tzf /tmp/rsstt.tar.gz >/dev/null 2>&1; then
                tar -xzf /tmp/rsstt.tar.gz -C /tmp/local_rsstt
              else
                echo "⚠️ rsstt.tar.gz validation failed, skipping archive restore."
              fi
              rm -f /tmp/rsstt.tar.gz
            else
              echo "⚠️ No rsstt archive found. Falling back to sync from raw directory..."
              rclone copy dropbox:self-hosted/rsstt /tmp/local_rsstt \
                --retries 10 \
                --low-level-retries 20 \
                --timeout 1m \
                --contimeout 15s \
                --exclude "docker-compose.yml" \
                -P || true
            fi

            mkdir -p /tmp/local_rsstt/config
            set -a
            source /dropbox/self-hosted/openclaw/.env
            set +a

            docker rm -f rss-to-telegram 2>/dev/null || true
            docker run -d \
              --name rss-to-telegram \
              --restart unless-stopped \
              -v /tmp/local_rsstt/config:/app/config \
              -e TOKEN="$TELEGRAM_BOT_TOKEN_RSS_SB_BOT" \
              -e MANAGER="83279194" \
              -e MULTIUSER="0" \
              rongronggg9/rss-to-telegram:latest

            docker ps --filter name=rss-to-telegram || true
          else
            echo "⚠️ openclaw .env not found, skipping rss-to-telegram startup."
          fi

      - name: Run CLIProxyAPI container
        run: |
          echo "Configuring local high-speed CLIProxyAPI directory..."
          mkdir -p /dropbox/self-hosted/CLIProxyAPI
          rm -rf /tmp/local_CLIProxyAPI
          mkdir -p /tmp/local_CLIProxyAPI

          # 优先从压缩包恢复；没有压缩包时再回退为目录同步
          if rclone ls dropbox:self-hosted/CLIProxyAPI.tar.gz > /dev/null 2>&1; then
            echo "📦 Found CLIProxyAPI.tar.gz! Downloading and extracting..."
            rclone copyto dropbox:self-hosted/CLIProxyAPI.tar.gz /tmp/CLIProxyAPI.tar.gz
            MIN_SIZE=$((1024 * 1024))
            actual_size=$(stat -c%s /tmp/CLIProxyAPI.tar.gz 2>/dev/null || echo 0)
            if [ "$actual_size" -ge "$MIN_SIZE" ] && tar -tzf /tmp/CLIProxyAPI.tar.gz >/dev/null 2>&1; then
              tar -xzf /tmp/CLIProxyAPI.tar.gz -C /tmp/local_CLIProxyAPI
            else
              echo "⚠️ CLIProxyAPI.tar.gz validation failed, skipping archive restore."
            fi
            rm -f /tmp/CLIProxyAPI.tar.gz
          else
            echo "⚠️ No CLIProxyAPI archive found. Falling back to sync from raw directory..."
            rclone copy dropbox:self-hosted/CLIProxyAPI /tmp/local_CLIProxyAPI
          fi

          # 【防崩溃机制】在宿主机提前创建好文件和目录，防止 Docker 误建文件夹
          mkdir -p /tmp/local_CLIProxyAPI/auth-dir
          touch /tmp/local_CLIProxyAPI/config.yaml

          echo "=== restored CLIProxyAPI tree ==="
          find /tmp/local_CLIProxyAPI -maxdepth 3 | sort || true

          # 先清理同名旧容器，避免名称冲突
          docker rm -f cliproxyapi 2>/dev/null || true

          # 使用重启策略守护 CLIProxyAPI；直接挂载本地 /tmp/local_CLIProxyAPI 目录
          docker run -d \
            --name cliproxyapi \
            --restart unless-stopped \
            -p 8317:8317 \
            -v /tmp/local_CLIProxyAPI/config.yaml:/CLIProxyAPI/config.yaml \
            -v /tmp/local_CLIProxyAPI/auth-dir:/root/.cli-proxy-api \
            eceasy/cli-proxy-api:latest

          echo "等待 CLIProxyAPI API 就绪..."
          READY=false
          for i in {1..120}; do
            if curl -fsS http://127.0.0.1:8317/ >/dev/null 2>&1; then
              READY=true
              echo "✅ CLIProxyAPI 已就绪"
              break
            fi
            echo "⏳ 等待中... ${i}/120"
            sleep 2
          done
          docker ps -a

          if [ "$READY" != true ]; then
            echo "❌ CLIProxyAPI 未在预期时间内就绪"
            docker logs cliproxyapi || true
            exit 1
          fi

          echo "📥 开始执行 CLIProxyAPI 历史统计数据导入（前台立即执行）..."

          # 从本地临时目录读取 stats.json
          if [ -s /tmp/local_CLIProxyAPI/stats.json ] && jq empty /tmp/local_CLIProxyAPI/stats.json >/dev/null 2>&1; then
              SUCCESS=false
              for i in {1..60}; do
                  echo "🔁 CLIProxyAPI 历史统计导入重试次数：${i}/60"
                  if curl -fsS -X POST "http://127.0.0.1:8317/v0/management/usage/import" \
                      -H "Content-Type: application/json" \
                      -H "X-Management-Key: ${{ secrets.CLIPROXYAPIKEY }}" \
                      -d @/tmp/local_CLIProxyAPI/stats.json > /dev/null 2>&1; then
                      echo -e "\n✅ CLIProxyAPI 历史统计导入执行完毕。"
                      SUCCESS=true
                      break
                  else
                      echo "⚠️ CLIProxyAPI 历史统计导入失败，5 秒后重试。"
                      sleep 5
                  fi
              done

              if [ "$SUCCESS" = false ]; then
                  echo "❌ CLIProxyAPI 历史统计导入彻底失败！正在提取 CLIProxyAPI 容器的运行日志："
                  docker logs cliproxyapi || true
              fi
          else
              echo "ℹ️ 云端未找到有效 stats.json 存档文件，本次为全新统计。"
          fi

          # 启动前清理可能残留的 ai-api 隧道进程
          pkill -f "cloudflared tunnel run --protocol http2 --url http://127.0.0.1:8317 ai-api" || true

          # 运行 8317 端口的命名隧道，并将输出保存到 Dropbox
          nohup cloudflared tunnel run --protocol http2 --url http://127.0.0.1:8317 ai-api \
            > /dropbox/self-hosted/openclaw/logs/cloudflared_CLIProxyAPI.log 2>&1 &

          sleep 5
          echo $! > /dropbox/self-hosted/openclaw/logs/cloudflared_CLIProxyAPI.pid

          echo "CLIProxyAPI 命名隧道已启动：ai-api（固定地址模式）"

      - name: Install OpenClaw
        run: |
          echo "Installing OpenClaw base..."
          curl -fsSL https://openclaw.ai/install.sh | bash -s -- --no-onboard

          echo "/home/runner/.npm-global/bin" >> $GITHUB_PATH
          export PATH="/home/runner/.npm-global/bin:$PATH"

          echo "Configuring local ~/.openclaw with Dropbox-linked config..."
          mkdir -p /dropbox/self-hosted/openclaw
          mkdir -p /home/runner/.openclaw
          mkdir -p /home/runner/.openclaw/extensions

          TMP_OPENCLAW_TAR="/tmp/openclaw.tar.gz"
          RESTORE_DIR="/tmp/openclaw-restore"

          rm -rf "$RESTORE_DIR"
          mkdir -p "$RESTORE_DIR"

          echo "📦 Restoring ~/.openclaw: prefer archive, fallback to raw directory sync..."
          if rclone ls dropbox:self-hosted/openclaw.tar.gz > /dev/null 2>&1; then
            echo "📦 Found openclaw.tar.gz! Downloading and extracting..."
            rclone copyto dropbox:self-hosted/openclaw.tar.gz "$TMP_OPENCLAW_TAR"
            if [ -s "$TMP_OPENCLAW_TAR" ] && tar -tzf "$TMP_OPENCLAW_TAR" >/dev/null 2>&1; then
              tar -xzf "$TMP_OPENCLAW_TAR" -C "$RESTORE_DIR"
              if [ -d "$RESTORE_DIR/.openclaw" ]; then
                find /home/runner/.openclaw -mindepth 1 -maxdepth 1 ! -name openclaw.json ! -name .env -exec rm -rf {} +
                cp -a "$RESTORE_DIR/.openclaw/." /home/runner/.openclaw/
                echo "✅ Restored ~/.openclaw from archive"
              else
                echo "⚠️ Archive does not contain .openclaw root, skipping archive restore."
              fi
            else
              echo "⚠️ openclaw.tar.gz validation failed, skipping archive restore."
            fi
            rm -f "$TMP_OPENCLAW_TAR"
          else
            echo "⚠️ No openclaw archive found. Falling back to sync from raw directory..."
            rclone copy dropbox:self-hosted/openclaw /home/runner/.openclaw \
              --retries 10 \
              --low-level-retries 20 \
              --timeout 1m \
              --contimeout 15s \
              --exclude "logs/**" \
              -P || true
          fi

          rm -f /home/runner/.openclaw/openclaw.json
          ln -s /dropbox/self-hosted/openclaw/openclaw.json /home/runner/.openclaw/openclaw.json

          rm -f /home/runner/.openclaw/.env
          ln -s /dropbox/self-hosted/openclaw/.env /home/runner/.openclaw/.env

          npm i -g @google/gemini-cli
          npm i -g clawhub

      - name: Run OpenClaw
        id: run_openclaw
        run: |
          # npm i -g "openclaw@2026.4.5"
          check_openclaw_health() {
            if sudo lsof -Pi :18789 -sTCP:LISTEN -t >/dev/null && [ "$(curl -fsS -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:18789/health || true)" = "200" ]; then
              return 0
            fi
            return 1
          }
          fail_startup() {
            local msg="$1"
            echo "❌ $msg"
            exit 1
          }


          echo "🔧 Fixing file permissions to pass security checks..."
          sudo chmod -R o-w $(npm root -g)
          sudo chmod 700 ~/.openclaw
          if [ -f ~/.openclaw/openclaw.json ]; then
            sudo chmod 600 ~/.openclaw/openclaw.json
          fi

          echo "Restarting OpenClaw gateway..."
          openclaw gateway install --force
          openclaw gateway restart

          echo "Waiting for OpenClaw ..."
          sleep 10

          if check_openclaw_health; then
              echo "✅ OpenClaw Gateway is healthy!"
          else
              echo "⚠️ Gateway not healthy yet. Checking logs:"
              tail -n 20 openclaw.log || echo "No log file found."

              echo "🛠️ Attempting to fix with 'openclaw doctor --fix'..."
              openclaw doctor --fix
              openclaw gateway restart

              echo "Waiting for OpenClaw after doctor fix..."
              sleep 15

              if check_openclaw_health; then
                  echo "✅ OpenClaw Gateway is healthy after doctor fix!"
              else
                  echo "🔄 Doctor fix failed. Attempting to fallback and reinstall recorded OpenClaw version..."

                  CONFIG_FILE="/dropbox/self-hosted/openclaw/openclaw.json"
                  TARGET_VERSION=""

                  if [ -s "$CONFIG_FILE" ]; then
                    READ_VERSION=$(sudo jq -r '.wizard.lastRunVersion // empty' "$CONFIG_FILE" 2>/dev/null)
                    if [ -n "$READ_VERSION" ] && [ "$READ_VERSION" != "null" ]; then
                      TARGET_VERSION="$READ_VERSION"
                      echo "✅ Found recorded version in config: $TARGET_VERSION"
                    else
                      fail_startup ".wizard.lastRunVersion not found in $CONFIG_FILE"
                    fi
                  else
                    fail_startup "Config file missing or empty at $CONFIG_FILE"
                  fi

                  echo "Installing openclaw@$TARGET_VERSION ..."
                  npm i -g "openclaw@$TARGET_VERSION"

                  sudo chmod -R o-w $(npm root -g)
                  sudo chmod 700 ~/.openclaw
                  if [ -f ~/.openclaw/openclaw.json ]; then
                    sudo chmod 600 ~/.openclaw/openclaw.json
                  fi

                  echo "Restarting OpenClaw..."
                  openclaw gateway install --force
                  openclaw gateway restart

                  echo "Waiting for OpenClaw after fallback reinstall..."
                  sleep 15

                  if check_openclaw_health; then
                    echo "✅ OpenClaw Gateway is healthy after fallback reinstall!"
                  else
                    err="回退到最近记录版本后，OpenClaw 依然无法启动（健康检查失败）"
                    fail_startup "$err"
                  fi
              fi
          fi


      - name: Configure tmate (Enable Mouse Mode)
        if: always()
        run: echo "set -g mouse on" > ~/.tmate.conf

      - name: Run tmate
        if: always()
        uses: mxschmitt/action-tmate@v3
        with:
          detached: true
          install-dependencies: true
          limit-access-to-actor: true

      - name: Notify OpenClaw startup failure
        if: ${{ steps.run_openclaw.outcome == 'failure' }}
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            run_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            tmate_log="$RUNNER_TEMP/tmate-connection.log"

            error_summary=$(perl -pe 's/\x1b\[[0-9;]*[A-Za-z]//g; s/\r//g' openclaw.log 2>/dev/null | grep -Ei 'error|fail|exception' | tail -n 20 || true)
            if [ -z "$error_summary" ]; then
              error_summary=$(tail -n 20 openclaw.log 2>/dev/null || echo "openclaw.log not found")
            fi
            error_summary=$(printf '%s' "$error_summary" | tail -c 2500)

            tmate_url=""
            if [ -f "$tmate_log" ]; then
              tmate_url=$(grep -Eo 'https://tmate\.io/t/[A-Za-z0-9_-]+' "$tmate_log" | tail -n 1 || true)
            fi
            if [ -z "$tmate_url" ]; then
              tmate_url="（tmate 链接暂不可用，稍后查看 Actions 日志）"
            fi

            msg=$(printf '%s\n' \
              '⚠️ OpenClaw 启动失败报告' \
              '' \
              '步骤: Run OpenClaw' \
              'Run ID: ${{ github.run_id }}' \
              "链接: ${run_url}" \
              "TMate: ${tmate_url}" \
              '' \
              '关键日志：' \
              "$error_summary")
            curl -fsS --retry 3 --retry-delay 2 --max-time 20 \
              -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
              --data-urlencode text="$msg"
          else
            echo "⚠️ Telegram alert skipped: TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID is empty"
          fi

      - name: Start background archive loop
        if: always()
        run: |
          echo "Starting background archive tasks: export CLIProxyAPI stats + archive ~/.openclaw every 20 minutes..."
          # 这里拆成两个临时脚本，不是重复：
          # 1) /tmp/create-openclaw-archive.sh 负责“单次归档”——筛选 ~/.openclaw、打包、校验 tar。
          # 2) /tmp/openclaw-archive-loop.sh 负责“循环调度”——每 20 分钟调用一次上面的单次归档脚本并上传 Dropbox。
          # 这样拆开后，后面的“最终收尾归档”也能直接复用 create-openclaw-archive.sh，避免重复维护两份打包逻辑。
          cat > /tmp/create-openclaw-archive.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          OUTPUT_TAR="${1:-}"
          if [ -z "$OUTPUT_TAR" ]; then
            echo "Usage: $0 /path/to/output.tar.gz" >&2
            exit 2
          fi

          MIN_SIZE=$((1024 * 1024))
          rm -f "$OUTPUT_TAR"

          build_openclaw_manifest() {
            cd /home/runner
            find .openclaw \
              \( \
                -path '.openclaw/logs' -o \
                -path '.openclaw/cache' -o \
                -path '.openclaw/tmp' -o \
                -path '.openclaw/browser/*/Crashpad' -o \
                -path '.openclaw/browser/*/GPUCache' -o \
                -path '.openclaw/browser/*/Code Cache' -o \
                -path '.openclaw/browser/*/GrShaderCache' -o \
                -path '.openclaw/browser/*/GraphiteDawnCache' \
              \) -prune -o \
              \( \
                -path '.openclaw/openclaw.json' -o \
                -path '.openclaw/.env' -o \
                -name 'SingletonLock' -o \
                -name 'SingletonSocket' -o \
                -name 'SingletonCookie' -o \
                -name '*.lock' -o \
                -name '*.pid' -o \
                -name '*.jsonl.lock' \
              \) -prune -o \
              -print0
          }

          build_openclaw_manifest | tar \
            --null \
            --no-recursion \
            -C /home/runner \
            -czf "$OUTPUT_TAR" \
            -T -

          if [ -s "$OUTPUT_TAR" ] \
            && [ "$(stat -c%s "$OUTPUT_TAR" 2>/dev/null || echo 0)" -ge "$MIN_SIZE" ] \
            && tar -tzf "$OUTPUT_TAR" >/dev/null 2>&1; then
            echo "archive_ready"
          else
            echo "archive_invalid"
            exit 1
          fi
          EOF
          chmod +x /tmp/create-openclaw-archive.sh

          cat > /tmp/openclaw-archive-loop.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          TMP_OPENCLAW_TAR="/tmp/openclaw.tar.gz"
          LOCK_FILE="/tmp/openclaw-archive.lock"
          PID_FILE="/tmp/openclaw-archive.pid"

          echo $$ > "$PID_FILE"
          trap 'rm -f "$PID_FILE"' EXIT

          while true; do
            if flock -E 200 -n "$LOCK_FILE" /tmp/create-openclaw-archive.sh "$TMP_OPENCLAW_TAR" >/dev/null; then
              echo "[$(date '+%F %T')] archive ok, uploading..."
              rclone copyto "$TMP_OPENCLAW_TAR" dropbox:self-hosted/openclaw.tar.gz
              echo "[$(date '+%F %T')] upload ok"
            else
              rc=$?
              if [ "$rc" -eq 200 ]; then
                echo "[$(date '+%F %T')] ⏭️ previous archive still running, skip this round"
              else
                echo "[$(date '+%F %T')] ⚠️ openclaw.tar.gz validation failed in periodic archive (rc=$rc)"
              fi
            fi
            sleep 1200
          done
          EOF
          chmod +x /tmp/openclaw-archive-loop.sh
          nohup /tmp/openclaw-archive-loop.sh > /tmp/openclaw-archive-loop.log 2>&1 &
          echo $! > /tmp/openclaw-archive-loop.launcher.pid

      - name: Keep alive
        if: always()
        run: sleep 330m

      - name: Stop OpenClaw and Final Archive
        if: always()
        run: |
          echo "1. Stopping OpenClaw gateway..."
          openclaw gateway stop || echo "OpenClaw already stopped or command failed."
          sleep 3m

          echo "2. Killing background archive process..."
          if [ -f /tmp/openclaw-archive-loop.launcher.pid ]; then
            kill "$(cat /tmp/openclaw-archive-loop.launcher.pid)" 2>/dev/null || true
          fi
          if [ -f /tmp/openclaw-archive.pid ]; then
            kill "$(cat /tmp/openclaw-archive.pid)" 2>/dev/null || true
          fi

          echo "3. Stopping rsstt and CLIProxyAPI before final archive..."
          docker stop rss-to-telegram 2>/dev/null || echo "rss-to-telegram already stopped or missing."
          docker stop cliproxyapi 2>/dev/null || echo "cliproxyapi already stopped or missing."

          echo "4. Performing final archive to Dropbox..."
          TMP_OPENCLAW_TAR="/tmp/openclaw-final.tar.gz"

          if /tmp/create-openclaw-archive.sh "$TMP_OPENCLAW_TAR" >/dev/null; then
            rclone copyto "$TMP_OPENCLAW_TAR" dropbox:self-hosted/openclaw.tar.gz -v
          else
            echo "⚠️ final openclaw.tar.gz validation failed, skip upload"
          fi

          MIN_SIZE=$((1024 * 1024))

          echo "5. Archiving CLIProxyAPI..."
          sudo tar -czf /tmp/CLIProxyAPI.tar.gz -C /tmp/local_CLIProxyAPI .
          if sudo tar -tzf /tmp/CLIProxyAPI.tar.gz >/dev/null 2>&1; then
            actual_size=$(stat -c%s /tmp/CLIProxyAPI.tar.gz 2>/dev/null || echo 0)
            if [ "$actual_size" -ge "$MIN_SIZE" ]; then
              rclone copyto /tmp/CLIProxyAPI.tar.gz dropbox:self-hosted/CLIProxyAPI.tar.gz -v
            else
              echo "⚠️ CLIProxyAPI 压缩包小于 1MB，放弃覆盖云端数据"
            fi
          fi

          echo "6. Archiving rss-to-telegram data..."
          if [ -d /tmp/local_rsstt ]; then
            sudo tar -czf /tmp/rsstt.tar.gz -C /tmp/local_rsstt --exclude='docker-compose.yml' .
            if sudo tar -tzf /tmp/rsstt.tar.gz >/dev/null 2>&1; then
              actual_size=$(stat -c%s /tmp/rsstt.tar.gz 2>/dev/null || echo 0)
              if [ "$actual_size" -ge "$MIN_SIZE" ]; then
                rclone copyto /tmp/rsstt.tar.gz dropbox:self-hosted/rsstt.tar.gz -v
              else
                echo "⚠️ rsstt 压缩包小于 1MB，放弃覆盖云端数据"
              fi
            fi
          fi

      - name: Sync ~/.openclaw to Dropbox
        if: always()
        run: |
          echo "Syncing ~/.openclaw to Dropbox..."
          rclone sync /home/runner/.openclaw dropbox:self-hosted/openclaw \
            --exclude "logs/**" \
            --exclude "cache/**" \
            --exclude "tmp/**" \
            --exclude "browser/**/Crashpad/**" \
            --exclude "browser/**/GPUCache/**" \
            --exclude "browser/**/Code Cache/**" \
            --exclude "browser/**/GrShaderCache/**" \
            --exclude "browser/**/GraphiteDawnCache/**" \
            --exclude "**/*.lock" \
            --exclude "**/*.pid" \
            --exclude "**/*.jsonl.lock" \
            --exclude "openclaw.json" \
            --exclude ".env" \
            -v
