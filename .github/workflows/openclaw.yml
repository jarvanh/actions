name: openclaw

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    concurrency: openclaw-singleton
    runs-on: ubuntu-latest
    env:
      TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
      TS_OAUTH_CLIENT_SECRET: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 'current' # 始终使用官方发布的最新版
          
      - uses: actions/checkout@v7
      - name: rclone-install
        run: |
          set -e
          # 安装 rclone：GitHub Releases 直下 .deb，失败回退 rclone.org 安装脚本
          bash "$GITHUB_WORKSPACE/.github/scripts/rclone/setup_rclone.sh" install
      - name: rclone-config
        env:
          RCLONE_CONFIG: ${{ secrets.RCLONE }}
        run: |
          set -e
          # 写配置 + 持久化：secret 起步 → 掩码 → Dropbox 恢复优先 → 有效性检查 → 同步回 Dropbox
          bash "$GITHUB_WORKSPACE/.github/scripts/rclone/setup_rclone.sh" config
      - name: Mount Dropbox
        run: |
          sudo apt-get update
          sudo apt-get install -y fuse3
          sudo mkdir -p /dropbox /mnt/vfs/dropbox /opt/logs
          sudo chown runner:runner /dropbox /mnt/vfs/dropbox /opt/logs

          # 挂载改用 runner 用户（与 emby.yml 一致）：conf 全程以 runner 身份读写。
          # 原来用 sudo 挂载，token 刷新时 rclone 以 root 回写 rclone.conf（属主 root、权限 600），
          # 之后非 root 的归档循环再读它就变成
          # "Failed to load config file ... permission denied" —— 即本次 CLIProxyAPI.tar.gz 上传失败的根因。
          # --allow-other 需要 fuse 允许非 root 共享挂载，挂载点也交给 runner 才能免 sudo 挂载。
          grep -q '^user_allow_other' /etc/fuse.conf 2>/dev/null || echo user_allow_other | sudo tee -a /etc/fuse.conf >/dev/null

          # 挂载 Dropbox
          # 参数按"配置 / Docker 数据卷"画像——小文件随机读写，不需要大块顺序预读：
          #   --cache-dir        默认 ~/.cache/rclone 会落进根分区（≈14GB），必须指向 /mnt
          #   --attr-timeout 1m  原 10m：容器内文件频繁增删改，属性缓存 10 分钟有陈旧风险
          #   --poll-interval 0  run 期间远端目录不会自己变，关轮询省 API 配额
          #   --buffer-size 16M  原 100M：每个打开的文件常驻一份，小文件场景用不到
          #   删除 --vfs-read-chunk-size-limit 10G：会让单个 Range 请求一路翻倍到 10GB
          nohup rclone mount dropbox: /dropbox \
            --config /home/runner/.config/rclone/rclone.conf \
            --cache-dir /mnt/vfs/dropbox \
            --allow-other --umask 000 --no-gzip-encoding \
            --attr-timeout 1m \
            --dir-cache-time 72h --poll-interval 0 \
            --vfs-cache-mode full \
            --vfs-cache-max-size 2G --vfs-cache-max-age 1h \
            --vfs-cache-min-free-space 2G \
            --vfs-cache-poll-interval 30s \
            --buffer-size 16M \
            --log-file /opt/logs/rclone-dropbox.log --log-level INFO &

          echo "Waiting for mount..."
          for i in {1..30}; do
            if mount | grep -q " on /dropbox " && ls /dropbox >/dev/null 2>&1; then
              echo "✅ Dropbox mounted"
              break
            fi
            sleep 2
          done

      - name: Install OpenClaw
        run: |
          echo "Installing OpenClaw base..."
          curl -fsSL https://openclaw.ai/install.sh | bash -s -- --no-onboard

          echo "Configuring local ~/.openclaw with Dropbox-linked config..."
          mkdir -p /dropbox/self-hosted/openclaw
          mkdir -p ~/.openclaw
          mkdir -p ~/.openclaw/extensions

          TMP_OPENCLAW_TAR="/tmp/openclaw.tar.gz"
          RESTORE_DIR="/tmp/openclaw-restore"

          cleanup_archive_workdir() {
            rm -f "$TMP_OPENCLAW_TAR" 2>/dev/null || true
            rm -rf "$RESTORE_DIR" 2>/dev/null || true
          }
          free_kb() { df -Pk "${1:-/tmp}" | awk 'NR==2 {print $4}'; }
          require_free_kb() {
            local path="$1" min_kb="$2" label="$3"
            local avail
            avail=$(free_kb "$path")
            if [ "${avail:-0}" -lt "$min_kb" ]; then
              echo "❌ ${label}: insufficient free space at ${path}. available=${avail}KB required=${min_kb}KB" >&2
              exit 1
            fi
            echo "✅ ${label}: free space ok at ${path}. available=${avail}KB required=${min_kb}KB"
          }
          require_free_kb /tmp $((2 * 1024 * 1024)) "openclaw restore preflight"

          cleanup_archive_workdir
          mkdir -p "$RESTORE_DIR"

          echo "📦 Restoring ~/.openclaw: prefer archive, fallback to raw directory sync..."
          if rclone ls dropbox:self-hosted/openclaw.tar.gz > /dev/null 2>&1; then
            echo "📦 Found openclaw.tar.gz! Downloading and extracting..."
            rclone copyto dropbox:self-hosted/openclaw.tar.gz "$TMP_OPENCLAW_TAR"
            archive_kb=$(du -sk "$TMP_OPENCLAW_TAR" | awk '{print $1}')
            if [ -z "${archive_kb:-}" ] || [ "$archive_kb" -le 0 ]; then
              echo "❌ openclaw extract preflight: unable to calculate archive size" >&2
              exit 1
            fi
            echo "openclaw.tar.gz 大小: ${archive_kb}KB"
            require_free_kb /tmp $((archive_kb * 3 + 1024 * 1024)) "openclaw extract preflight"
            if [ -s "$TMP_OPENCLAW_TAR" ] && tar -tzf "$TMP_OPENCLAW_TAR" >/dev/null 2>&1; then
              tar -xzf "$TMP_OPENCLAW_TAR" -C "$RESTORE_DIR"
              if [ -d "$RESTORE_DIR/.openclaw" ]; then
                find ~/.openclaw -mindepth 1 -maxdepth 1 -exec rm -rf {} +
                cp -a "$RESTORE_DIR/.openclaw/." ~/.openclaw/
                echo "✅ Restored ~/.openclaw from archive"
              else
                echo "⚠️ Archive does not contain .openclaw root, skipping archive restore."
              fi
            else
              echo "⚠️ openclaw.tar.gz validation failed, skipping archive restore."
            fi
            cleanup_archive_workdir
          else
            echo "⚠️ No openclaw archive found. Falling back to sync from raw directory..."
            rclone copy dropbox:self-hosted/openclaw ~/.openclaw \
              --retries 10 \
              --low-level-retries 20 \
              --timeout 1m \
              --contimeout 15s \
              --exclude "logs/**" \
              -P || true
          fi

          # 恢复后诊断：主包里到底恢复了什么（判断是否缺关键文件/状态库）
          echo "=== restored ~/.openclaw ==="
          ls -A ~/.openclaw | sort || true
          du -sh ~/.openclaw 2>/dev/null || true
          stat -c '%s bytes  %n' ~/.openclaw/openclaw.json 2>/dev/null || echo "⚠️ openclaw.json 不存在"
          ls -l ~/.openclaw/state 2>/dev/null || echo "⚠️ state/ 不存在"

          # ZCode 数据恢复：~/.zcode/cli（会话库/rollout/记忆）在 runner 重置后会丢，从 Dropbox 主包还原。
          # 只覆盖 cli/ 子树：server/ 等运行时由桌面客户端自动重新部署，不入包也不被还原触碰
          TMP_ZCODE_TAR="/tmp/zcode-restore.tar.gz"
          ZCODE_RESTORE_DIR="/tmp/zcode-restore"
          rm -rf "$ZCODE_RESTORE_DIR" 2>/dev/null || true
          if rclone ls dropbox:self-hosted/zcode.tar.gz > /dev/null 2>&1; then
            echo "📦 Found zcode.tar.gz! Downloading and extracting..."
            rclone copyto dropbox:self-hosted/zcode.tar.gz "$TMP_ZCODE_TAR"
            if [ -s "$TMP_ZCODE_TAR" ] && tar -tzf "$TMP_ZCODE_TAR" >/dev/null 2>&1; then
              mkdir -p "$ZCODE_RESTORE_DIR"
              tar -xzf "$TMP_ZCODE_TAR" -C "$ZCODE_RESTORE_DIR"
              if [ -d "$ZCODE_RESTORE_DIR/.zcode/cli" ]; then
                mkdir -p ~/.zcode
                rm -rf ~/.zcode/cli
                cp -a "$ZCODE_RESTORE_DIR/.zcode/cli" ~/.zcode/cli
                echo "✅ Restored ~/.zcode/cli from archive"
              else
                echo "⚠️ Archive does not contain .zcode/cli, skipping ZCode restore."
              fi
              # 还原 server 设备身份（v2/certs CA + deviceMid）。每次重新部署 server 都会重新
              # 生成身份，客户端把归档等本地 UI 状态 keyed 到该身份上；身份一变，客户端里
              # 已归档会话全部复活（2026-09-12 排查结论：归档动作在 runner 侧零落盘）。复用
              # 身份后客户端跨重置认得同一个 server。tasks-index.sqlite 是可再生索引，不备份。
              if [ -d "$ZCODE_RESTORE_DIR/.zcode/v2/certs" ]; then
                mkdir -p ~/.zcode/v2/certs
                cp -a "$ZCODE_RESTORE_DIR/.zcode/v2/certs/." ~/.zcode/v2/certs/
                if [ -f "$ZCODE_RESTORE_DIR/.zcode/v2/telemetry-state.json" ]; then
                  cp -a "$ZCODE_RESTORE_DIR/.zcode/v2/telemetry-state.json" ~/.zcode/v2/
                fi
                echo "✅ Restored ~/.zcode/v2 server identity (certs + deviceMid)"
              else
                echo "ℹ️ Archive has no .zcode/v2 identity (old package), server will generate a fresh one."
              fi
            else
              echo "⚠️ zcode.tar.gz validation failed, skipping ZCode restore."
            fi
            rm -f "$TMP_ZCODE_TAR" 2>/dev/null || true
            rm -rf "$ZCODE_RESTORE_DIR" 2>/dev/null || true
            echo "=== restored ~/.zcode/cli ==="
            du -sh ~/.zcode/cli 2>/dev/null || true
            ls ~/.zcode/cli/db/db.sqlite ~/.zcode/cli/rollout 2>/dev/null | head -5 || echo "⚠️ 还原后未见 db.sqlite/rollout"
          else
            echo "ℹ️ No zcode.tar.gz found on Dropbox (first run), skipping ZCode restore."
          fi

          npm i -g @google/gemini-cli
          npm i -g clawhub


      - name: Restore skill symlink for proactivity
        run: |
          set -euo pipefail
          # Persist real Proactivity state under ~/.openclaw, keep ~/proactivity as compatibility symlink.
          mkdir -p "$HOME/.openclaw/workspace/skills-data/proactivity"
          if [ -e "$HOME/proactivity" ] && [ ! -L "$HOME/proactivity" ]; then
            cp -a "$HOME/proactivity/." "$HOME/.openclaw/workspace/skills-data/proactivity/" 2>/dev/null || true
            mv "$HOME/proactivity" "$HOME/proactivity.migrated.$(date -u +%Y%m%dT%H%M%SZ)"
          elif [ -L "$HOME/proactivity" ]; then
            rm "$HOME/proactivity"
          fi
          ln -sfn "$HOME/.openclaw/workspace/skills-data/proactivity" "$HOME/proactivity"
          chmod 700 "$HOME/.openclaw/workspace/skills-data/proactivity" || true
          find "$HOME/.openclaw/workspace/skills-data/proactivity" -type d -exec chmod 700 {} + 2>/dev/null || true
          find "$HOME/.openclaw/workspace/skills-data/proactivity" -type f -exec chmod 600 {} + 2>/dev/null || true
          ls -ld "$HOME/proactivity" "$HOME/.openclaw/workspace/skills-data/proactivity"

      - name: Setup Tailscale (SSH 调试入口)
        id: ts
        if: ${{ always() && env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_CLIENT_SECRET != '' }}
        uses: tailscale/github-action@306e68a486fd2350f2bfc3b19fcd143891a4a2d8 # v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
          tags: tag:ci

      - name: Enable Tailscale SSH
        id: ts_ssh
        if: ${{ always() && steps.ts.outcome == 'success' }}
        run: |
          set -euo pipefail
          # 开启 Exit Node 前置条件：启用内核转发
          echo 'net.ipv4.ip_forward = 1' | sudo tee /etc/sysctl.d/99-tailscale-forward.conf
          echo 'net.ipv6.conf.all.forwarding = 1' | sudo tee -a /etc/sysctl.d/99-tailscale-forward.conf
          sudo sysctl -p /etc/sysctl.d/99-tailscale-forward.conf

          # 固定主机名 openclaw（singleton 并发下不会撞名）；节点为 ephemeral，job 结束自动移除
          # 开启 Exit Node：tailnet 内设备可将本 runner 设为出口节点
          # 批准方式（任选其一）：
          #   ① 管理页手动批准: https://login.tailscale.com/admin/machines → 找到 openclaw 节点
          #      → Edit route settings → 勾选 0.0.0.0/0, ::/0（ephemeral 节点每次运行都需重新批准）
          #   ② ACL autoApprovers（推荐，免每次批准）: 在 Tailscale ACL rules 前加：
          #        "autoApprovers": {
          #          "routes": { "0.0.0.0/0": ["tag:ci"], "::/0": ["tag:ci"] }
          #        },
          #   ③ Tailscale API 程序化批准:
          #      GET  https://api.tailscale.com/api/v2/tailnet/{tailnet}/devices  查到节点 id
          #      POST https://api.tailscale.com/api/v2/device/{device_id}/routes  body: {"routes": ["0.0.0.0/0", "::/0"]}
          #      （需 TS_API_KEY，ephemeral 节点 id 每次变化，需按主机名动态查询）
          sudo tailscale set --ssh --hostname=openclaw --advertise-exit-node
          # 回读实际注册的 DNS 名。--hostname 的重命名有传播延迟，若 set 后立刻读取
          # 可能拿到机器原始主机名（如 github-runnervmgx7h7），导致 ts.env 里的 SSH/
          # RustDesk 直连地址每轮漂移。这里轮询等待名字收敛为 openclaw。
          TS_FQDN=""
          for i in $(seq 1 15); do
            TS_FQDN="$(tailscale status --json | jq -r '.Self.DNSName // empty' | sed 's/\.$//')"
            case "$TS_FQDN" in
              openclaw.*) break ;;
            esac
            sleep 2
          done
          TS_FQDN="${TS_FQDN:-$(tailscale status --json | jq -r '.Self.DNSName // empty' | sed 's/\.$//')}"
          TS_HOST="$(printf '%s' "$TS_FQDN" | cut -d. -f1)"
          {
            printf 'TS_HOST=%s\n' "$TS_HOST"
            printf 'TS_FQDN=%s\n' "$TS_FQDN"
            printf 'TS_IP=%s\n' "$(tailscale ip -4)"
          } > "$HOME/ts.env"
          echo "SSH 入口: ssh runner@${TS_HOST}（RustDesk 直连: ${TS_FQDN}:21118）"

      - name: Tailscale secrets missing
        if: ${{ always() && env.TS_OAUTH_CLIENT_ID == '' && env.TS_OAUTH_CLIENT_SECRET == '' }}
        run: echo "⚠️ 未配置 TS_OAUTH_CLIENT_ID / TS_OAUTH_CLIENT_SECRET，跳过 Tailscale SSH 调试入口"

      - name: Prepare runtime env for later steps
        run: |
          set -euo pipefail

          NODE_PATH_ACTUAL="$(command -v node)"
          NODE_BIN_DIR="$(dirname "$NODE_PATH_ACTUAL")"
          NPM_PREFIX="$(npm config get prefix)"
          NPM_GLOBAL_BIN="$NPM_PREFIX/bin"

          echo "$NODE_BIN_DIR" >> "$GITHUB_PATH"
          if [ -d "$NPM_GLOBAL_BIN" ]; then
            echo "$NPM_GLOBAL_BIN" >> "$GITHUB_PATH"
          fi

          echo "node path: $NODE_PATH_ACTUAL"
          echo "node bin dir: $NODE_BIN_DIR"
          echo "npm prefix: $NPM_PREFIX"
          echo "npm global bin: $NPM_GLOBAL_BIN"

          cat > "$HOME/runtime-env.sh" <<'EOF'
          #!/usr/bin/env bash

          # Load OpenClaw env vars
          if [ -f "$HOME/.openclaw/.env" ]; then
            set -a
            . "$HOME/.openclaw/.env"
            set +a
          fi

          # Import GitHub Actions add_path entries so shells inherit setup-node/toolcache PATH
          if [ -d "$HOME/work/_temp/_runner_file_commands" ]; then
            _openclaw_add_path_file=""
            _openclaw_added_path=""
            for _openclaw_add_path_file in "$HOME"/work/_temp/_runner_file_commands/add_path_*; do
              [ -f "$_openclaw_add_path_file" ] || continue
              while IFS= read -r _openclaw_added_path || [ -n "$_openclaw_added_path" ]; do
                [ -n "$_openclaw_added_path" ] || continue
                case ":$PATH:" in
                  *":$_openclaw_added_path:"*) ;;
                  *) PATH="$_openclaw_added_path:$PATH" ;;
                esac
              done < "$_openclaw_add_path_file"
            done
            : "${_openclaw_add_path_file:=}"
            : "${_openclaw_added_path:=}"
          fi

          # Persist GitHub CLI auth locally from GH_TOKEN, so shells without the env can still use gh.
          # Also connect Git HTTPS auth to gh, so plain `git push` can reuse the authenticated flow.
          if command -v gh >/dev/null 2>&1; then
            GH_TOKEN_VALUE="${GH_TOKEN:-}"
            GH_CONFIG_HOME_LOCAL="$HOME/.config/gh"
            GH_HOSTS_FILE_LOCAL="$GH_CONFIG_HOME_LOCAL/hosts.yml"

            if [ -n "$GH_TOKEN_VALUE" ]; then
              mkdir -p "$GH_CONFIG_HOME_LOCAL"
              chmod 700 "$HOME/.config" "$GH_CONFIG_HOME_LOCAL" 2>/dev/null || true

              if ! env -u GH_TOKEN -u GITHUB_TOKEN -u GH_ENTERPRISE_TOKEN -u GITHUB_ENTERPRISE_TOKEN -u BASH_ENV gh auth status >/dev/null 2>&1; then
                GH_USER_LOGIN="$(gh api user --jq .login 2>/dev/null || true)"
                if [ -n "$GH_USER_LOGIN" ]; then
                  GH_TMP_DIR="$(mktemp -d)"
                  GH_TMP_HOSTS_FILE="$GH_TMP_DIR/hosts.yml"
                  # 不用 heredoc：heredoc 结束符必须顶格，但本脚本是嵌在 YAML 块里生成的、
                  # 每行都带缩进，结束符永远匹配不上 —— 会把 runtime-env.sh 剩余部分整个吞进 hosts.yml
                  {
                    printf 'github.com:\n'
                    printf '    user: %s\n' "${GH_USER_LOGIN}"
                    printf '    oauth_token: %s\n' "${GH_TOKEN_VALUE}"
                    printf '    git_protocol: https\n'
                  } > "$GH_TMP_HOSTS_FILE"
                  chmod 600 "$GH_TMP_HOSTS_FILE" 2>/dev/null || true

                  if GH_CONFIG_DIR="$GH_TMP_DIR" env -u GH_TOKEN -u GITHUB_TOKEN -u GH_ENTERPRISE_TOKEN -u GITHUB_ENTERPRISE_TOKEN -u BASH_ENV gh auth status >/dev/null 2>&1; then
                    mv "$GH_TMP_HOSTS_FILE" "$GH_HOSTS_FILE_LOCAL"
                    chmod 600 "$GH_HOSTS_FILE_LOCAL" 2>/dev/null || true
                  fi

                  rm -rf "$GH_TMP_DIR" 2>/dev/null || true
                fi
              fi

              [ -f "$GH_HOSTS_FILE_LOCAL" ] && chmod 600 "$GH_HOSTS_FILE_LOCAL" 2>/dev/null || true
            fi

            # Wire Git HTTPS auth through gh so `git push` can reuse gh auth without embedding tokens in remotes.
            gh auth setup-git >/dev/null 2>&1 || true
          fi
          EOF

          chmod +x "$HOME/runtime-env.sh"
          echo "BASH_ENV=$HOME/runtime-env.sh" >> "$GITHUB_ENV"
          
      - name: Install Cloudflared
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb
          sudo mkdir -p ~/.cloudflared/
          sudo cp -r /dropbox/self-hosted/.cloudflared/* ~/.cloudflared/ 2>/dev/null || true
          # 家目录里的状态必须是 runner 属主：sudo 复制会让凭据变成 root 属主，
          # 而 cloudflared 是 runner 身份启动的（读 ~/.cloudflared 会 EACCES）
          sudo chown -R runner:runner ~/.cloudflared

      - name: Run Cloudflared Tunnel
        run: |
          mkdir -p ~/.openclaw/logs

          cloudflared tunnel route dns oc oc.'${{ secrets.VD }}'.eu.org || true

          nohup cloudflared tunnel run --url http://127.0.0.1:18789 oc \
            > ~/.openclaw/logs/cloudflared_oc.log 2>&1 &
          echo $! > ~/.openclaw/logs/cloudflared_oc.pid

          cloudflared tunnel route dns sub-store sub-store.'${{ secrets.VD }}'.eu.org || true

          nohup cloudflared tunnel run --url http://127.0.0.1:3001 sub-store \
            > ~/.openclaw/logs/cloudflared_sub_store.log 2>&1 &
          echo $! > ~/.openclaw/logs/cloudflared_sub_store.pid

          sleep 10
          echo "OpenClaw and Sub-Store tunnels started."
          
      - name: Run sub-store container
        env:
          SUB_STORE_FRONTEND_BACKEND_PATH: ${{ secrets.SUB_STORE_FRONTEND_BACKEND_PATH }}
        run: |
          set -euo pipefail

          echo "Starting Sub-Store container from Dropbox persistent data..."
          mkdir -p /dropbox/self-hosted/sub-store ~/.openclaw/logs

          docker rm -f sub-store 2>/dev/null || true
          docker run -d \
            --restart=always \
            -e "SUB_STORE_BACKEND_SYNC_CRON=50 * * * *" \
            -e SUB_STORE_FRONTEND_BACKEND_PATH="$SUB_STORE_FRONTEND_BACKEND_PATH" \
            -p 9876:9876 \
            -p 127.0.0.1:3001:3001 \
            -v /dropbox/self-hosted/sub-store:/opt/app/data \
            --name sub-store \
            xream/sub-store:http-meta

          echo "Waiting for Sub-Store backend/frontend to become reachable..."
          ready=false
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:3001/ >/dev/null 2>&1 || curl -fsS http://127.0.0.1:9876/ >/dev/null 2>&1; then
              ready=true
              echo "✅ Sub-Store is reachable"
              break
            fi
            echo "⏳ Waiting for Sub-Store... ${i}/60"
            sleep 2
          done

          docker ps -a --filter name=sub-store
          if [ "$ready" != true ]; then
            echo "❌ Sub-Store did not become reachable in time"
            docker logs sub-store || true
            exit 1
          fi

      - name: Run rss-to-telegram container
        env:
          # rsstt 服务使用独立 bot，token 经同名 secret 注入（此前未接线导致容器 TOKEN 恒空）
          TELEGRAM_BOT_TOKEN_RSS_SB_BOT: ${{ secrets.TELEGRAM_BOT_TOKEN_RSS_SB_BOT }}
        run: |
          set -euo pipefail
          echo "Starting rss-to-telegram before CLIProxyAPI..."
          mkdir -p /dropbox/self-hosted/rsstt
          cleanup_archive_workdir() {
            rm -f /tmp/rsstt.tar.gz 2>/dev/null || true
            rm -rf /tmp/rsstt-restore 2>/dev/null || true
          }
          free_kb() { df -Pk "${1:-/tmp}" | awk 'NR==2 {print $4}'; }
          require_free_kb() {
            local path="$1" min_kb="$2" label="$3"
            local avail
            avail=$(free_kb "$path")
            if [ "${avail:-0}" -lt "$min_kb" ]; then
              echo "❌ ${label}: insufficient free space at ${path}. available=${avail}KB required=${min_kb}KB" >&2
              exit 1
            fi
            echo "✅ ${label}: free space ok at ${path}. available=${avail}KB required=${min_kb}KB"
          }
          require_free_kb /tmp $((1024 * 1024)) "rsstt restore preflight"
          rm -rf /tmp/local_rsstt
          cleanup_archive_workdir
          mkdir -p /tmp/local_rsstt /tmp/rsstt-restore

          # 门禁只看 rsstt 真正依赖的凭据（同名 secret 注入）：
          # ~/.openclaw/.env 由 OpenClaw 自身维护、与 rsstt 无关，不能作为启动条件
          if [ -n "${TELEGRAM_BOT_TOKEN_RSS_SB_BOT:-}" ]; then
            if rclone ls dropbox:self-hosted/rsstt.tar.gz > /dev/null 2>&1; then
              echo "📦 Found rsstt.tar.gz! Downloading and extracting..."
              rclone copyto dropbox:self-hosted/rsstt.tar.gz /tmp/rsstt.tar.gz
              archive_kb=$(du -sk /tmp/rsstt.tar.gz | awk '{print $1}')
              if [ -z "${archive_kb:-}" ] || [ "$archive_kb" -le 0 ]; then
                echo "❌ rsstt extract preflight: unable to calculate archive size" >&2
                exit 1
              fi
              echo "rsstt.tar.gz 大小: ${archive_kb}KB"
              require_free_kb /tmp $((archive_kb * 3 + 512 * 1024)) "rsstt extract preflight"
              if [ -s /tmp/rsstt.tar.gz ] && tar -tzf /tmp/rsstt.tar.gz >/dev/null 2>&1; then
                tar -xzf /tmp/rsstt.tar.gz -C /tmp/rsstt-restore
                cp -a /tmp/rsstt-restore/. /tmp/local_rsstt/ || true
              else
                echo "⚠️ rsstt.tar.gz validation failed, skipping archive restore."
              fi
              cleanup_archive_workdir
            else
              echo "⚠️ No rsstt archive found. Falling back to sync from raw directory..."
              rclone copy dropbox:self-hosted/rsstt /tmp/local_rsstt \
                --retries 10 \
                --low-level-retries 20 \
                --timeout 1m \
                --contimeout 15s \
                --exclude "docker-compose.yml" \
                -P || true
            fi

            mkdir -p /tmp/local_rsstt/config /tmp/local_rsstt/data
            touch /tmp/local_rsstt/config/.keep /tmp/local_rsstt/data/.keep

            echo "=== restored rss-to-telegram tree ==="
            find /tmp/local_rsstt -maxdepth 3 | sort || true

            start_rsstt_container() {
              docker rm -f rss-to-telegram 2>/dev/null || true
              docker run -d \
                --name rss-to-telegram \
                --restart unless-stopped \
                -v /tmp/local_rsstt/config:/app/config \
                -v /tmp/local_rsstt/data:/app/data \
                -e TOKEN="$TELEGRAM_BOT_TOKEN_RSS_SB_BOT" \
                -e MANAGER="83279194" \
                -e MULTIUSER="0" \
                rongronggg9/rss-to-telegram:latest
            }

            print_rsstt_status() {
              echo "=== rss-to-telegram status ==="
              docker ps -a --filter name=rss-to-telegram
              container_status="$(docker inspect --format '{{.State.Status}}' rss-to-telegram)"
              health_status="$(docker inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}' rss-to-telegram)"
              restart_count="$(docker inspect --format '{{.RestartCount}}' rss-to-telegram)"
              echo "rss-to-telegram status=${container_status} health=${health_status} restarts=${restart_count}"
            }

            validate_rsstt_container() {
              print_rsstt_status

              if [ "$container_status" != "running" ] || [ "$health_status" = "unhealthy" ]; then
                echo "❌ rss-to-telegram failed to become healthy"
                docker logs --tail 200 rss-to-telegram || true
                return 1
              fi

              if docker logs --tail 200 rss-to-telegram 2>&1 | grep -Eqi 'LOGIN FAILED|Unknown error occurred during login'; then
                echo "❌ rss-to-telegram encountered a Telegram login failure"
                docker logs --tail 200 rss-to-telegram || true
                return 1
              fi
            }

            archive_rsstt_state() {
              local min_size=$((5 * 1024))
              local actual_size

              echo "📦 Archiving repaired rss-to-telegram state immediately..."
              rm -f /tmp/rsstt.tar.gz
              tar -czf /tmp/rsstt.tar.gz -C /tmp/local_rsstt .
              tar -tzf /tmp/rsstt.tar.gz >/dev/null
              actual_size="$(stat -c%s /tmp/rsstt.tar.gz 2>/dev/null || echo 0)"
              if [ "$actual_size" -lt "$min_size" ]; then
                echo "❌ repaired rsstt archive is too small: size=${actual_size} min=${min_size}"
                exit 1
              fi
              rclone copyto /tmp/rsstt.tar.gz dropbox:self-hosted/rsstt.tar.gz -v
              rm -f /tmp/rsstt.tar.gz
              echo "✅ Repaired rss-to-telegram state uploaded"
            }

            echo "Waiting for rss-to-telegram container to stabilize..."
            start_rsstt_container
            sleep 12

            if ! validate_rsstt_container; then
              if docker logs --tail 200 rss-to-telegram 2>&1 | grep -qi 'AuthKeyDuplicatedError'; then
                echo "⚠️ rss-to-telegram session was invalidated by AuthKeyDuplicatedError; deleting session and retrying once."
                docker rm -f rss-to-telegram 2>/dev/null || true
                rm -f \
                  /tmp/local_rsstt/config/bot.session \
                  /tmp/local_rsstt/config/bot.session-journal \
                  /tmp/local_rsstt/config/bot.session-shm \
                  /tmp/local_rsstt/config/bot.session-wal
                start_rsstt_container
                sleep 12
                validate_rsstt_container
                archive_rsstt_state
              else
                exit 1
              fi
            fi

            echo "=== rss-to-telegram /app tree (for persistence verification) ==="
            docker exec rss-to-telegram sh -lc 'find /app -maxdepth 3 \( -type f -o -type d \) | sort | sed -n "1,200p"'
            echo "=== rss-to-telegram local bind tree after startup ==="
            find /tmp/local_rsstt -maxdepth 3 | sort || true
          else
            echo "❌ TELEGRAM_BOT_TOKEN_RSS_SB_BOT 未配置，跳过 rss-to-telegram 启动；本轮无 rsstt 数据，最终归档会跳过上传"
          fi

      - name: Run AI API gateway (CliRelay first, fallback CLIProxyAPI)
        run: |
          set -euo pipefail

          echo "Configuring AI API gateway: CliRelay (compose) first, CLIProxyAPI as fallback..."
          mkdir -p /dropbox/self-hosted/CLIProxyAPI ~/.openclaw/logs

          BACKEND_STATE=/tmp/active-ai-backend.env
          : > "$BACKEND_STATE"

          CLIRELAY_DIR=/tmp/local_CliRelay
          CLIRELAY_TAR=/tmp/CliRelay.tar.gz
          CLIRELAY_ARCHIVE="dropbox:self-hosted/CliRelay.tar.gz"
          COMPOSE="docker compose -p clirelay -f $CLIRELAY_DIR/docker-compose.yml --project-directory $CLIRELAY_DIR"

          cleanup_archive_workdir() {
            rm -f /tmp/CLIProxyAPI.tar.gz /tmp/CliRelay.tar.gz 2>/dev/null || true
            rm -rf /tmp/clirelay-restore 2>/dev/null || true
          }
          free_kb() { df -Pk "${1:-/tmp}" | awk 'NR==2 {print $4}'; }
          require_free_kb() {
            local path="$1" min_kb="$2" label="$3"
            local avail
            avail=$(free_kb "$path")
            if [ "${avail:-0}" -lt "$min_kb" ]; then
              echo "❌ ${label}: insufficient free space at ${path}. available=${avail}KB required=${min_kb}KB" >&2
              exit 1
            fi
            echo "✅ ${label}: free space ok at ${path}. available=${avail}KB required=${min_kb}KB"
          }

          wait_api_ready() {
            local label="$1"
            local tries="${2:-120}"
            local code
            for i in $(seq 1 "$tries"); do
              code=$(curl -s --connect-timeout 2 -o /dev/null -w "%{http_code}" http://127.0.0.1:8317/ || true)
              if [ -n "$code" ] && [ "$code" -ge 100 ] && [ "$code" -lt 600 ]; then
                echo "✅ [$label] 8317 已就绪 (HTTP ${code})"
                return 0
              fi
              echo "⏳ [$label] 等待中... ${i}/${tries}"
              sleep 2
            done
            return 1
          }

          # ---------- CLIProxyAPI 数据恢复（现有逻辑函数化，幂等；回退与 bootstrap 共用） ----------
          restore_cliproxyapi_data() {
            if [ -f /tmp/local_CLIPProxyAPI/config.yaml ]; then
              echo "ℹ️ CLIProxyAPI 数据目录已就绪，跳过恢复"
              return 0
            fi
            echo "Configuring local CLIProxyAPI directory with persistent data..."
            cleanup_archive_workdir
            require_free_kb /tmp $((2 * 1024 * 1024)) "CLIProxyAPI restore preflight"
            rm -rf /tmp/local_CLIPProxyAPI
            mkdir -p /tmp/local_CLIPProxyAPI
            if rclone ls dropbox:self-hosted/CLIProxyAPI.tar.gz > /dev/null 2>&1; then
              echo "📦 Found CLIProxyAPI.tar.gz! Downloading and extracting..."
              rclone copyto dropbox:self-hosted/CLIProxyAPI.tar.gz /tmp/CLIProxyAPI.tar.gz
              archive_kb=$(du -sk /tmp/CLIProxyAPI.tar.gz | awk '{print $1}')
              if [ -z "${archive_kb:-}" ] || [ "$archive_kb" -le 0 ]; then
                echo "❌ CLIProxyAPI extract preflight: unable to calculate archive size" >&2
                exit 1
              fi
              echo "CLIProxyAPI.tar.gz 大小: ${archive_kb}KB"
              require_free_kb /tmp $((archive_kb * 3 + 512 * 1024)) "CLIProxyAPI extract preflight"
              MIN_SIZE=$((5 * 1024))
              actual_size=$(stat -c%s /tmp/CLIProxyAPI.tar.gz 2>/dev/null || echo 0)
              if [ "$actual_size" -ge "$MIN_SIZE" ] && tar -tzf /tmp/CLIProxyAPI.tar.gz >/dev/null 2>&1; then
                tar -xzf /tmp/CLIProxyAPI.tar.gz -C /tmp/local_CLIPProxyAPI
              else
                echo "⚠️ CLIProxyAPI.tar.gz validation failed, skipping archive restore."
              fi
              cleanup_archive_workdir
            else
              echo "⚠️ No CLIProxyAPI archive found. Falling back to sync from raw directory..."
              rclone copy dropbox:self-hosted/CLIProxyAPI /tmp/local_CLIPProxyAPI \
                --retries 10 \
                --low-level-retries 20 \
                --timeout 1m \
                --contimeout 15s \
                -P || true
            fi
            mkdir -p /tmp/local_CLIPProxyAPI/auth-dir /tmp/local_CLIPProxyAPI/logs
            touch /tmp/local_CLIPProxyAPI/config.yaml
            touch /tmp/local_CLIPProxyAPI/stats.json
            echo "=== restored CLIProxyAPI tree ==="
            find /tmp/local_CLIPProxyAPI -maxdepth 3 | sort || true
          }

          # ---------- CLIProxyAPI 数据 → CliRelay 部署目录（bootstrap 迁移） ----------
          bootstrap_clirelay_from_cliproxyapi() {
            echo "🔄 Bootstrapping CliRelay deployment from CLIProxyAPI data..."
            rm -rf "$CLIRELAY_DIR"
            mkdir -p "$CLIRELAY_DIR"/auths "$CLIRELAY_DIR"/sql "$CLIRELAY_DIR"/postgres-data "$CLIRELAY_DIR"/redis-data "$CLIRELAY_DIR"/logs "$CLIRELAY_DIR"/data
            if [ -f /tmp/local_CLIPProxyAPI/config.yaml ]; then
              cp -a /tmp/local_CLIPProxyAPI/config.yaml "$CLIRELAY_DIR/config.yaml"
            else
              touch "$CLIRELAY_DIR/config.yaml"
            fi
            if [ -d /tmp/local_CLIPProxyAPI/auth-dir ]; then
              cp -a /tmp/local_CLIPProxyAPI/auth-dir/. "$CLIRELAY_DIR/auths/" 2>/dev/null || true
            fi
            find "$CLIRELAY_DIR/auths" -name '._*' -delete 2>/dev/null || true
            curl -fsSL --retry 3 https://raw.githubusercontent.com/kittors/CliRelay/main/docker-compose.yml \
              -o "$CLIRELAY_DIR/docker-compose.yml" || { echo "❌ 无法下载 CliRelay docker-compose.yml"; return 1; }
            local pgpass
            pgpass=$(openssl rand -hex 16)
            {
              printf 'CLIRELAY_PROJECT_DIR=%s\n' "$CLIRELAY_DIR"
              printf 'AUTH_PATH=/root/.cli-proxy-api\n'
              printf 'CLIRELAY_POSTGRES_DB=clirelay\n'
              printf 'CLIRELAY_POSTGRES_USER=clirelay\n'
              printf 'CLIRELAY_POSTGRES_PASSWORD=%s\n' "$pgpass"
            } > "$CLIRELAY_DIR/.env"
            chmod 600 "$CLIRELAY_DIR/.env"
            echo "✅ CliRelay 部署目录就绪: $CLIRELAY_DIR"
          }

          # ---------- CliRelay 数据恢复：Dropbox 归档优先，无效则 bootstrap 迁移 ----------
          restore_clirelay_data() {
            rm -rf "$CLIRELAY_DIR"
            local restored=0
            if rclone ls "$CLIRELAY_ARCHIVE" > /dev/null 2>&1; then
              echo "📦 Found CliRelay.tar.gz! Downloading and validating..."
              require_free_kb /tmp $((2 * 1024 * 1024)) "clirelay restore preflight"
              if rclone copyto "$CLIRELAY_ARCHIVE" "$CLIRELAY_TAR"; then
                if [ -s "$CLIRELAY_TAR" ] && tar -tzf "$CLIRELAY_TAR" >/dev/null 2>&1; then
                  mkdir -p /tmp/clirelay-restore
                  tar -xzf "$CLIRELAY_TAR" -C /tmp/clirelay-restore
                  if [ -f /tmp/clirelay-restore/docker-compose.yml ] && [ -f /tmp/clirelay-restore/.env ]; then
                    mkdir -p "$CLIRELAY_DIR"
                    cp -a /tmp/clirelay-restore/. "$CLIRELAY_DIR/"
                    mkdir -p "$CLIRELAY_DIR"/auths "$CLIRELAY_DIR"/sql "$CLIRELAY_DIR"/postgres-data "$CLIRELAY_DIR"/redis-data "$CLIRELAY_DIR"/logs "$CLIRELAY_DIR"/data
                    echo "✅ Restored CliRelay deployment from archive"
                    restored=1
                  else
                    echo "⚠️ CliRelay.tar.gz 缺少 docker-compose.yml/.env，转 bootstrap 迁移。"
                  fi
                else
                  echo "⚠️ CliRelay.tar.gz validation failed, 转 bootstrap 迁移。"
                fi
              else
                echo "⚠️ CliRelay.tar.gz 下载失败，转 bootstrap 迁移。"
              fi
            else
              echo "ℹ️ 未找到 CliRelay 归档，从 CLIProxyAPI 数据 bootstrap 迁移。"
            fi
            cleanup_archive_workdir
            if [ "$restored" != 1 ]; then
              restore_cliproxyapi_data
              bootstrap_clirelay_from_cliproxyapi
            fi
          }

          # ---------- CliRelay 全栈启动：postgres 先行 → SQL 导入 → 全栈 up ----------
          start_clirelay() {
            echo "🚀 Starting CliRelay compose stack..."
            docker rm -f cli-proxy-api clirelay 2>/dev/null || true
            $COMPOSE down --remove-orphans 2>/dev/null || true
            $COMPOSE up -d postgres || return 1
            local pg_ok=0
            for i in $(seq 1 30); do
              if $COMPOSE exec -T postgres sh -c '. /clirelay-deploy/.env; pg_isready -U "$CLIRELAY_POSTGRES_USER" -d "$CLIRELAY_POSTGRES_DB"' >/dev/null 2>&1; then
                pg_ok=1
                break
              fi
              sleep 2
            done
            if [ "$pg_ok" != 1 ]; then
              echo "❌ CliRelay postgres 未在预期时间内就绪"
              return 1
            fi
            # 恢复脚本：先导 SQL 再起全栈（PG 数据卷不入归档，每轮均为全新库，直接导入无冲突）
            shopt -s nullglob
            local sql_files=( "$CLIRELAY_DIR"/sql/*.sql )
            shopt -u nullglob
            if [ "${#sql_files[@]}" -gt 0 ]; then
              local latest_sql
              latest_sql=$(ls -1t "${sql_files[@]}" | head -n 1)
              echo "📥 Importing PostgreSQL snapshot: $latest_sql"
              $COMPOSE exec -T postgres sh -c '. /clirelay-deploy/.env; exec psql -U "$CLIRELAY_POSTGRES_USER" -d "$CLIRELAY_POSTGRES_DB" -v ON_ERROR_STOP=1' < "$latest_sql" || return 1
              echo "✅ PostgreSQL 数据导入完成"
            fi
            $COMPOSE up -d || return 1
          }

          # ---------- CLIProxyAPI 单容器启动（回退后端，参数与原步骤一致） ----------
          start_cliproxyapi() {
            echo "🚀 Starting CLIProxyAPI container (fallback backend)..."
            docker rm -f cliproxyapi cli-proxy-api clirelay 2>/dev/null || true
            mkdir -p /tmp/local_CLIPProxyAPI/auth-dir /tmp/local_CLIPProxyAPI/logs
            touch /tmp/local_CLIPProxyAPI/config.yaml
            touch /tmp/local_CLIPProxyAPI/stats.json
            docker run -d \
              --name cliproxyapi \
              --restart unless-stopped \
              -p 8317:8317 \
              -e TZ=Asia/Shanghai \
              -v /tmp/local_CLIPProxyAPI/config.yaml:/CLIProxyAPI/config.yaml \
              -v /tmp/local_CLIPProxyAPI/auth-dir:/root/.cli-proxy-api \
              -v /tmp/local_CLIPProxyAPI/logs:/CLIProxyAPI/logs \
              -v /tmp/local_CLIPProxyAPI/stats.json:/CLIProxyAPI/stats.json \
              eceasy/cli-proxy-api:latest || return 1
          }

          cleanup_archive_workdir
          # 数据准备失败不终止步骤（条件上下文抑制 errexit），记录原因后仍走回退路径
          if ! restore_clirelay_data; then
            FALLBACK_REASON="CliRelay 数据准备失败 · 归档缺失/校验失败或 bootstrap 迁移失败"
            echo "⚠️ $FALLBACK_REASON，将直接走 CLIProxyAPI 回退路径"
          fi

          # ---------- 主流程：CliRelay 优先，启动失败回退 CLIProxyAPI ----------
          ACTIVE_BACKEND=""

          if start_clirelay && wait_api_ready "clirelay" 120; then
            ACTIVE_BACKEND=clirelay
            docker ps -a
            echo "✅ 当前生效 AI 网关: CliRelay（全栈）"
          else
            if [ -z "${FALLBACK_REASON:-}" ]; then
              FALLBACK_REASON="CliRelay 未能在预期时间内就绪 · compose 启动或 8317 健康检查失败"
            fi
            echo "⚠️ $FALLBACK_REASON，打印日志并回退 CLIProxyAPI..."
            echo "--- clirelay compose logs (tail 80) ---"
            $COMPOSE logs --tail 80 2>/dev/null || true
            $COMPOSE down -v --remove-orphans 2>/dev/null || true
            docker rm -f cli-proxy-api clirelay 2>/dev/null || true

            restore_cliproxyapi_data
            if start_cliproxyapi && wait_api_ready "cliproxyapi" 120; then
              ACTIVE_BACKEND=cliproxyapi
              docker ps -a
              echo "✅ 已回退，当前生效 AI 网关: CLIProxyAPI"
            else
              echo "❌ CLIProxyAPI 未在预期时间内就绪"
              docker logs cliproxyapi || true
              exit 1
            fi
          fi
          {
            printf 'ACTIVE_BACKEND=%s\n' "$ACTIVE_BACKEND"
            if [ -n "${FALLBACK_REASON:-}" ]; then printf 'FALLBACK_REASON=%s\n' "$FALLBACK_REASON"; fi
          } > "$BACKEND_STATE"

          pkill -f "cloudflared tunnel run --protocol http2 --url http://127.0.0.1:8317 ai-api" || true

          nohup cloudflared tunnel run --protocol http2 --url http://127.0.0.1:8317 ai-api \
            > ~/.openclaw/logs/cloudflared_CLIPProxyAPI.log 2>&1 &

          sleep 5
          echo $! > ~/.openclaw/logs/cloudflared_CLIPProxyAPI.pid

          echo "ai-api 命名隧道已启动（固定地址模式），当前后端: ${ACTIVE_BACKEND}"

      - name: Notify Tailscale SSH entry
        if: ${{ always() && steps.ts_ssh.outcome == 'success' }}
        continue-on-error: true
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # 收尾区接线由 env 注入（此前是写进 ts.env 再由脚本前缀传参）
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
        run: |
          set -euo pipefail
          # runner 就绪后推送 SSH 入口 + 当前生效 AI 网关；通知失败不影响主流程
          # 注：本步骤位于 AI 网关启动步骤之后，以便读取 /tmp/active-ai-backend.env
          . "$HOME/ts.env"

          # 获取 runner 出口 IP 信息（IP/ISP/ASN/ASN 组织/位置）；失败不影响主流程
          IP_INFO="$(curl -fsS --max-time 10 'https://ipwho.is/' 2>/dev/null || true)"
          [ -z "${IP_INFO}" ] && IP_INFO='{}'
          PUB_IP="$(printf '%s' "${IP_INFO}" | jq -r '.ip // ""')"
          IP_ISP="$(printf '%s' "${IP_INFO}" | jq -r '.connection.isp // ""')"
          IP_ASN="$(printf '%s' "${IP_INFO}" | jq -r '.connection.asn // ""')"
          IP_ORG="$(printf '%s' "${IP_INFO}" | jq -r '.connection.org // ""')"
          IP_LOC="$(printf '%s' "${IP_INFO}" | jq -r '[.city, .region, (.country_code // .country)] | map(select(. != null and . != "")) | join(", ")')"
          PUB_IP="${PUB_IP:-未知}"; IP_ISP="${IP_ISP:-未知}"; IP_ASN="${IP_ASN:-}"; IP_ORG="${IP_ORG:-未知}"; IP_LOC="${IP_LOC:-未知}"

          # 当前生效 AI 网关 + 回退原因（状态文件由 AI 网关启动步骤写入）
          AI_BACKEND="clirelay"
          AI_FALLBACK_REASON=""
          if [ -f /tmp/active-ai-backend.env ]; then
            AI_BACKEND=$(grep '^ACTIVE_BACKEND=' /tmp/active-ai-backend.env | tail -n 1 | cut -d= -f2- || true)
            AI_FALLBACK_REASON=$(grep '^FALLBACK_REASON=' /tmp/active-ai-backend.env | tail -n 1 | cut -d= -f2- || true)
          fi
          AI_BACKEND="${AI_BACKEND:-clirelay}"

          # 统一 HTML 通知 + 统一收尾区（tg_notify.sh，checkout 后可用）。
          # TG_RUN_URL / TG_RUN_STARTED_AT 由本 step 的 env 注入，tg_add_footer 直接读取。
          # 版式与 tailscale-windows.yml "🟢 Windows runner 已就绪" 同款：
          # 按套分节（规范 · 凭据与入口：多套同类信息各占一分节）+ 组内树形条目，凭据/网络/出口/AI 网关
          # 各归其节，避免一级平铺 10 行 kv 混排
          source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"
          msg=""
          tg_add_title msg "🟢 OpenClaw Runner 已就绪"

          _entries="命令：<code>ssh runner@$(escape_html "${TS_HOST}")</code>"
          _entries+=$'\n'"备用 IP：<code>$(escape_html "${TS_IP}")</code>"
          _entries+=$'\n'"文件管理：<code>sftp://runner@$(escape_html "${TS_HOST}")/</code>"
          # 列表分节带计数（与 tailscale-windows / AI 网关同款；此前四节形态不一）
          tg_add_section msg "🔐 SSH · $(printf '%s' "$_entries" | grep -c .)"
          tg_add_block msg "$(tree_lines "$_entries")"

          if [ -n "${TS_FQDN:-}" ]; then
            tg_add_section msg "🖥️ RustDesk · 1"
            tg_add_block msg "$(tree_lines "直连：<code>$(escape_html "${TS_FQDN}"):21118</code>")"
          fi

          # 取值行口径（规范 · 取值行口径）：一次 ipwho.is 返回的 IP/ISP/ASN/位置 都是机器返回值，
          # 整节同为 <code>；按「值不值得复制」逐个判断会产出等宽/正体交错的斑马纹
          _entries="出口 IP：<code>$(escape_html "${PUB_IP}")</code>"
          _entries+=$'\n'"ISP：<code>$(escape_html "${IP_ISP}")</code>"
          _entries+=$'\n'"ASN：<code>$(escape_html "AS${IP_ASN} · ${IP_ORG}")</code>"
          _entries+=$'\n'"位置：<code>$(escape_html "${IP_LOC}")</code>"
          tg_add_section msg "🌐 出口网络 · $(printf '%s' "$_entries" | grep -c .)"
          tg_add_block msg "$(tree_lines "$_entries")"

          _entries="管理页批准：<code>admin/machines → openclaw → Edit routes → 勾选 Exit Node</code>"
          _entries+=$'\n'"ACL 免批准（推荐）：<code>routes 0.0.0.0/0,::/0 → tag:ci</code> · 免每次批准"
          _entries+=$'\n'"API：<code>POST /api/v2/device/{device_id}/routes</code>"
          tg_add_section msg "🛰️ 出口节点 · $(printf '%s' "$_entries" | grep -c .)"
          tg_add_block msg "$(tree_lines "$_entries")"
          # 「状态」是自然语言结论 → 说明段，留在取值行里会变成 code/裸交错（规范 · 取值行口径）
          tg_add_note msg "状态：已广播 · 可作为出口节点"

          if [ "$AI_BACKEND" = "cliproxyapi" ]; then _ai_backend="CLIProxyAPI"; _ai_state="回退"; else _ai_backend="CliRelay"; _ai_state="全栈"; fi
          # 后端**标识**是机器值 → <code>；「回退/全栈」是自然语言结论 → 裸文本。
          # 此前二者拼成一个串整段套 <code>，等于让自然语言冒充机器值（规范 · 取值行口径）
          tg_add_section msg "🤖 AI 网关 · 1"
          tg_add_block msg "$(tree_lines "后端：<code>$(escape_html "${_ai_backend}")</code> · $(escape_html "${_ai_state}")")"
          if [ -n "$AI_FALLBACK_REASON" ]; then
            tg_add_note msg "回退原因：${AI_FALLBACK_REASON}"
          fi
          # footer 读 TG_RUN_URL/TG_RUN_STARTED_AT（由本 step 的 env 注入）
          tg_add_footer msg
          # 走发送层而非 curl 直发：429 重试已内建，HTML 解析失败直接报错（规范 · 发送层），
          # 429 限流按 retry_after 等待重试，disable_web_page_preview 内置
          send_tg "$msg" || true
          echo "已推送 SSH 入口通知: ssh runner@${TS_HOST}（AI 网关: ${AI_BACKEND}）"

      - name: Run OpenClaw
        id: run_openclaw
        run: |
          set -euo pipefail

          RUN_OPENCLAW_STEP_LOG="/tmp/run-openclaw-step.log"
          RUN_OPENCLAW_META="/tmp/run-openclaw-meta.env"
          : > "$RUN_OPENCLAW_STEP_LOG"
          : > "$RUN_OPENCLAW_META"
          exec > >(tee -a "$RUN_OPENCLAW_STEP_LOG") 2>&1

          CURRENT_OPENCLAW_VERSION="$(openclaw --version 2>/dev/null | head -n 1 || true)"
          CURRENT_OPENCLAW_SEMVER="$(printf '%s\n' "$CURRENT_OPENCLAW_VERSION" | grep -oE '[0-9]{4}\.[0-9]+\.[0-9]+' | head -n 1 || true)"
          KNOWN_GOOD_VERSION_FILE="$HOME/.openclaw/openclaw-known-good-version"
          echo "CURRENT_OPENCLAW_VERSION=$CURRENT_OPENCLAW_VERSION" >> "$RUN_OPENCLAW_META"
          echo "CURRENT_OPENCLAW_SEMVER=$CURRENT_OPENCLAW_SEMVER" >> "$RUN_OPENCLAW_META"
          echo "FAIL_STAGE=preflight_doctor" >> "$RUN_OPENCLAW_META"
          PREFLIGHT_BEGIN_TS=$(date +%s)

          fail_startup() {
            local msg="$1"
            echo "❌ $msg"
            exit 1
          }

          normalize_openclaw_version() {
            printf '%s\n' "${1:-}" | tr -d '[:space:]' | grep -oE '^[0-9]{4}\.[0-9]+\.[0-9]+$' || true
          }

          read_openclaw_version_file() {
            local file="$1"
            if [ -s "$file" ]; then
              normalize_openclaw_version "$(head -n 1 "$file" 2>/dev/null || true)"
            fi
          }

          record_successful_openclaw_version() {
            local version="$1"
            version="$(normalize_openclaw_version "$version")"
            if [ -z "$version" ]; then
              echo "⚠️ Cannot record successful OpenClaw version: current version is empty or invalid."
              return 0
            fi
            mkdir -p "$HOME/.openclaw"
            printf '%s\n' "$version" > "$KNOWN_GOOD_VERSION_FILE"
            echo "RECORDED_SUCCESSFUL_OPENCLAW_VERSION=$version" >> "$RUN_OPENCLAW_META"
            echo "✅ Recorded successful OpenClaw version: $version"
          }

          resolve_fallback_openclaw_version() {
            local version=""

            version="$(read_openclaw_version_file "$KNOWN_GOOD_VERSION_FILE")"
            if [ -n "$version" ]; then
              echo "FALLBACK_VERSION_SOURCE=known_good_file" >> "$RUN_OPENCLAW_META"
              echo "$version"
              return 0
            fi

            if [ -s "$HOME/.openclaw/openclaw.json" ]; then
              version="$(jq -r '.meta.lastTouchedVersion // empty' "$HOME/.openclaw/openclaw.json" 2>/dev/null | head -n 1 || true)"
              version="$(normalize_openclaw_version "$version")"
              if [ -n "$version" ]; then
                echo "FALLBACK_VERSION_SOURCE=meta_lastTouchedVersion" >> "$RUN_OPENCLAW_META"
                echo "$version"
                return 0
              fi
            fi

            echo ""
            return 1
          }

          fix_openclaw_permissions() {
            echo "🔧 Fixing file permissions to pass security checks..."
            sudo chmod -R o-w "$(npm root -g)"
            chmod 700 ~/.openclaw
            if [ -f ~/.openclaw/openclaw.json ]; then
              chmod 600 ~/.openclaw/openclaw.json
            fi
          }

          openclaw_port_listening() {
            sudo lsof -Pi :18789 -sTCP:LISTEN -t >/dev/null
          }

          openclaw_log_ready() {
            journalctl --user -u openclaw-gateway.service --since "5 minutes ago" --no-pager 2>/dev/null \
              | grep -q "\[gateway\] ready"
          }

          openclaw_health_http_ok() {
            [ "$(curl -fsS -o /dev/null -w "%{http_code}" --connect-timeout 3 --max-time 15 http://127.0.0.1:18789/health || true)" = "200" ]
          }

          wait_for_openclaw_health() {
            local label="$1"
            local timeout_sec="${2:-180}"
            local interval_sec="${3:-5}"
            local start_ts now_ts elapsed
            start_ts=$(date +%s)

            echo "Waiting for OpenClaw health (${label}, timeout=${timeout_sec}s)..."
            while true; do
              now_ts=$(date +%s)
              elapsed=$((now_ts - start_ts))

              if openclaw_port_listening; then
                if openclaw_health_http_ok; then
                  echo "✅ OpenClaw Gateway health is OK after ${elapsed}s (${label})."
                  return 0
                fi

                if openclaw_log_ready; then
                  echo "⏳ Gateway is listening and logged ready, but /health is still slow after ${elapsed}s (${label}); continuing grace wait."
                else
                  echo "⏳ Gateway port is listening, waiting for ready/health... ${elapsed}/${timeout_sec}s (${label})"
                fi
              else
                echo "⏳ Gateway port is not listening yet... ${elapsed}/${timeout_sec}s (${label})"
              fi

              if [ "$elapsed" -ge "$timeout_sec" ]; then
                echo "❌ OpenClaw Gateway health did not become OK within ${timeout_sec}s (${label})."
                return 1
              fi
              sleep "$interval_sec"
            done
          }

          dump_openclaw_diagnostics() {
            echo "⚠️ Gateway not healthy yet. Diagnostics:"
            echo "--- systemd status ---"
            systemctl --user --no-pager --full status openclaw-gateway.service || true
            echo "--- recent journal ---"
            journalctl --user -u openclaw-gateway.service --since "10 minutes ago" --no-pager | tail -n 120 || true
            echo "--- /tmp/openclaw logs ---"
            find /tmp/openclaw -maxdepth 1 -type f -name 'openclaw-*.log' -printf '%T@ %p\n' 2>/dev/null \
              | sort -nr \
              | head -2 \
              | cut -d' ' -f2- \
              | while IFS= read -r log_file; do
                  echo "--- tail $log_file ---"
                  tail -n 80 "$log_file" || true
                done
          }

          fix_openclaw_permissions

          echo "FAIL_STAGE=config_validate" >> "$RUN_OPENCLAW_META"
          echo "🧪 Preflight config validation for $CURRENT_OPENCLAW_VERSION ..."
          CONFIG_VALID_FOR_CURRENT=0
          DOCTOR_RAN=0
          if openclaw config validate >/tmp/config-validate.log 2>&1; then
            CONFIG_VALID_FOR_CURRENT=1
            echo "✅ Config valid for current version."
          else
            echo "⚠️ Config invalid for current version. Trying 'openclaw doctor --fix' to repair, then re-validating. Details:"
            tail -n 15 /tmp/config-validate.log 2>/dev/null || true
            echo "🛠️ Running 'openclaw doctor --fix' to repair the config..."
            DOCTOR_RAN=1
            openclaw doctor --fix || echo "DOCTOR_EXIT=$? (non-fatal, continuing)"
            if openclaw config validate >/tmp/config-validate-2.log 2>&1; then
              CONFIG_VALID_FOR_CURRENT=1
              echo "✅ Config repaired by doctor; now valid for current version."
            else
              echo "❌ Config still invalid after doctor. Gateway start will be skipped; fallback reinstall, downgrade guard and snapshot rollback handle recovery. Details:"
              tail -n 10 /tmp/config-validate-2.log 2>/dev/null || true
            fi
          fi

          if [ "$CONFIG_VALID_FOR_CURRENT" = "1" ] && [ "$DOCTOR_RAN" != "1" ]; then
            echo "🛠️ Running preflight 'openclaw doctor --fix' before gateway restart..."
            openclaw doctor --fix || echo "DOCTOR_EXIT=$? (non-fatal, continuing)"
          fi
          PREFLIGHT_END_TS=$(date +%s)
          echo "PREFLIGHT_DOCTOR_DURATION_SEC=$((PREFLIGHT_END_TS - PREFLIGHT_BEGIN_TS))" >> "$RUN_OPENCLAW_META"

          echo "FAIL_STAGE=initial_start" >> "$RUN_OPENCLAW_META"
          INITIAL_START_BEGIN_TS=$(date +%s)
          echo "Restarting OpenClaw gateway..."
          INITIAL_START_RESTART_OK=1
          if [ "$CONFIG_VALID_FOR_CURRENT" != "1" ]; then
            echo "⚠️ Skipping gateway start: config still invalid after doctor. Going straight to fallback reinstall with the recorded compatible version."
            INITIAL_START_RESTART_OK=0
          elif ! openclaw gateway install --force; then
            echo "⚠️ Initial gateway install failed; will attempt fallback reinstall."
            INITIAL_START_RESTART_OK=0
          elif ! openclaw gateway restart; then
            echo "⚠️ Initial gateway restart failed; will attempt fallback reinstall."
            INITIAL_START_RESTART_OK=0
          fi

          if [ "$INITIAL_START_RESTART_OK" = "1" ] && wait_for_openclaw_health "initial_start" 180 5; then
              INITIAL_START_END_TS=$(date +%s)
              echo "INITIAL_START_DURATION_SEC=$((INITIAL_START_END_TS - INITIAL_START_BEGIN_TS))" >> "$RUN_OPENCLAW_META"
              record_successful_openclaw_version "$CURRENT_OPENCLAW_SEMVER"
              echo "✅ OpenClaw Gateway is healthy!"
          else
              INITIAL_START_END_TS=$(date +%s)
              echo "INITIAL_START_DURATION_SEC=$((INITIAL_START_END_TS - INITIAL_START_BEGIN_TS))" >> "$RUN_OPENCLAW_META"
              dump_openclaw_diagnostics

              echo "FAIL_STAGE=fallback_reinstall" >> "$RUN_OPENCLAW_META"
              echo "🔄 Initial start failed after preflight doctor. Attempting to fallback and reinstall recorded OpenClaw version..."

              TARGET_VERSION="$(resolve_fallback_openclaw_version || true)"
              if [ -z "$TARGET_VERSION" ]; then
                fail_startup "No fallback OpenClaw version found. Checked $KNOWN_GOOD_VERSION_FILE and .meta.lastTouchedVersion."
              fi
              echo "✅ Selected fallback OpenClaw version: $TARGET_VERSION"

              echo "Installing openclaw@$TARGET_VERSION ..."
              echo "FALLBACK_TARGET_VERSION=$TARGET_VERSION" >> "$RUN_OPENCLAW_META"
              FALLBACK_REINSTALL_BEGIN_TS=$(date +%s)
              npm i -g "openclaw@$TARGET_VERSION"

              fix_openclaw_permissions

              echo "🧪 Validating config against fallback openclaw@$TARGET_VERSION ..."
              if openclaw config validate >/tmp/config-validate-fallback.log 2>&1; then
                echo "✅ Config valid for fallback version."
              else
                echo "⚠️ Config invalid for fallback version. Trying 'openclaw doctor --fix' first. Details:"
                tail -n 10 /tmp/config-validate-fallback.log 2>/dev/null || true
                openclaw doctor --fix || echo "DOCTOR_EXIT=$? (non-fatal, continuing)"
                if openclaw config validate >/tmp/config-validate-fallback-2.log 2>&1; then
                  echo "✅ Config repaired by doctor for fallback version."
                else
                  DOWNGRADE_TS="$(date +%Y%m%d-%H%M%S)"
                  echo "❌ Config still invalid for fallback version after doctor. Moving the current state aside so the fallback version starts clean (kept as *.state-bak-$DOWNGRADE_TS; sessions will be lost, upgrade back to recover). Details:"
                  tail -n 10 /tmp/config-validate-fallback-2.log 2>/dev/null || true
                  for STATE_DB in "$HOME/.openclaw/state/openclaw.sqlite" "$HOME/.openclaw/state/openclaw.sqlite-wal" "$HOME/.openclaw/state/openclaw.sqlite-shm"; do
                    if [ -f "$STATE_DB" ]; then
                      mv "$STATE_DB" "$STATE_DB.state-bak-$DOWNGRADE_TS" && echo "➡️ Moved aside: $STATE_DB"
                    fi
                  done
                  echo "🛠️ Running 'openclaw doctor --fix' on the clean state..."
                  openclaw doctor --fix || echo "DOCTOR_EXIT=$? (non-fatal, continuing)"
                fi
              fi

              echo "Restarting OpenClaw after fallback reinstall..."
              FALLBACK_RESTART_OK=1
              if ! openclaw gateway install --force; then
                echo "⚠️ Fallback gateway install failed."
                FALLBACK_RESTART_OK=0
              elif ! openclaw gateway restart; then
                echo "⚠️ Fallback gateway restart failed."
                FALLBACK_RESTART_OK=0
              fi

              if [ "$FALLBACK_RESTART_OK" = "1" ] && wait_for_openclaw_health "fallback_reinstall" 180 5; then
                FALLBACK_REINSTALL_END_TS=$(date +%s)
                echo "FALLBACK_REINSTALL_DURATION_SEC=$((FALLBACK_REINSTALL_END_TS - FALLBACK_REINSTALL_BEGIN_TS))" >> "$RUN_OPENCLAW_META"
                record_successful_openclaw_version "$TARGET_VERSION"
                echo "✅ OpenClaw Gateway is healthy after fallback reinstall!"
              else
                FALLBACK_REINSTALL_END_TS=$(date +%s)
                echo "FALLBACK_REINSTALL_DURATION_SEC=$((FALLBACK_REINSTALL_END_TS - FALLBACK_REINSTALL_BEGIN_TS))" >> "$RUN_OPENCLAW_META"
                dump_openclaw_diagnostics

                echo "FAIL_STAGE=snapshot_restore" >> "$RUN_OPENCLAW_META"
                echo "🔁 Fallback reinstall did not become healthy. Trying automatic snapshot rollback (state + version pair from the healthy snapshot pool)..."
                SNAPSHOT_TGZ="$(rclone lsf dropbox:self-hosted/snapshots/ --files-only 2>/dev/null | grep -- "-v${TARGET_VERSION}.tar.gz" | sort | tail -n 1 || true)"
                if [ -n "$SNAPSHOT_TGZ" ]; then
                  echo "✅ Found version-matched snapshot: $SNAPSHOT_TGZ"
                else
                  echo "ℹ️ No snapshot for known-good version $TARGET_VERSION. Adopting the newest pool snapshot and its version instead (snapshot pool only contains verified-healthy pairs)..."
                  SNAPSHOT_TGZ="$(rclone lsf dropbox:self-hosted/snapshots/ --files-only 2>/dev/null | grep "openclaw-.*-v" | sort | tail -n 1 || true)"
                  if [ -n "$SNAPSHOT_TGZ" ]; then
                    ADOPTED_VERSION="$(printf '%s\n' "$SNAPSHOT_TGZ" | sed -n 's/.*-v\([0-9]\{4\}\.[0-9]*\.[0-9]*\)\.tar\.gz/\1/p')"
                    if [ -n "$ADOPTED_VERSION" ] && [ "$ADOPTED_VERSION" != "$TARGET_VERSION" ]; then
                      echo "🔄 Adopting snapshot version $ADOPTED_VERSION: installing openclaw@$ADOPTED_VERSION before restore..."
                      npm i -g "openclaw@$ADOPTED_VERSION"
                      fix_openclaw_permissions
                      TARGET_VERSION="$ADOPTED_VERSION"
                      echo "SNAPSHOT_ADOPTED_VERSION=$ADOPTED_VERSION" >> "$RUN_OPENCLAW_META"
                    fi
                  fi
                fi
                if [ -z "$SNAPSHOT_TGZ" ]; then
                  echo "ℹ️ Snapshot pool empty or unusable; snapshot rollback unavailable."
                else
                  echo "⬇️ Downloading snapshot: $SNAPSHOT_TGZ ..."
                  if rclone copyto "dropbox:self-hosted/snapshots/${SNAPSHOT_TGZ}" /tmp/snapshot-restore.tar.gz -v 2>&1 | tail -n 2; then
                    echo "🛑 Stopping gateway (cancels the systemd auto-restart loop)..."
                    systemctl --user stop openclaw-gateway.service 2>/dev/null || true
                    openclaw gateway stop 2>/dev/null || true
                    sleep 3
                    BAK_TS="$(date +%Y%m%d-%H%M%S)"
                    mv "$HOME/.openclaw" "$HOME/.openclaw.broken-bak-$BAK_TS"
                    if tar -xzf /tmp/snapshot-restore.tar.gz -C "$HOME" && [ -s "$HOME/.openclaw/openclaw.json" ]; then
                      echo "✅ State restored from snapshot (broken state kept at ~/.openclaw.broken-bak-$BAK_TS)."
                      fix_openclaw_permissions
                      if openclaw gateway install --force && openclaw gateway restart && wait_for_openclaw_health "snapshot_restore" 180 5; then
                        record_successful_openclaw_version "$TARGET_VERSION"
                        echo "✅ OpenClaw is healthy after automatic snapshot rollback!"
                      else
                        dump_openclaw_diagnostics
                        err="快照回滚（状态+版本成对恢复）后仍无法启动；损坏状态保留于 ~/.openclaw.broken-bak-$BAK_TS"
                        fail_startup "$err"
                      fi
                    else
                      echo "❌ Snapshot extraction failed; restoring the previous state and giving up."
                      rm -rf "$HOME/.openclaw"
                      mv "$HOME/.openclaw.broken-bak-$BAK_TS" "$HOME/.openclaw"
                      err="快照解包失败，已还原现场"
                      fail_startup "$err"
                    fi
                  else
                    echo "⚠️ Snapshot download failed; skipping automatic snapshot rollback."
                  fi
                fi
                err="回退到最近记录版本后，OpenClaw 依然无法启动（健康检查失败）"
                fail_startup "$err"
              fi
          fi

      - name: Notify OpenClaw startup failure
        if: ${{ always() && steps.run_openclaw.outcome == 'failure' }}
        env:
          # send_tg 发送闸 + 收尾区接线（TG_RUN_STARTED_AT 平台移除后为空值，
          # tg_add_footer 内有 runner 开机时刻兜底）
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            step_log="/tmp/run-openclaw-step.log"
            meta_file="/tmp/run-openclaw-meta.env"

            sanitize_log() {
              perl -pe 's/\x1b\[[0-9;]*[A-Za-z]//g; s/\r//g'
            }

            error_summary=""
            if [ -f openclaw.log ]; then
              error_summary=$(sanitize_log < openclaw.log | grep -Ei 'error|fail|exception|unhealthy|timed out|cannot|denied|missing|invalid' | tail -n 20 || true)
              if [ -z "$error_summary" ]; then
                error_summary=$(sanitize_log < openclaw.log | tail -n 40 || true)
              fi
            fi
            if [ -z "$error_summary" ] && [ -f "$step_log" ]; then
              error_summary=$(sanitize_log < "$step_log" | grep -Ei 'error|fail|exception|unhealthy|timed out|cannot|denied|missing|invalid|doctor|fallback|restart' | tail -n 30 || true)
              if [ -z "$error_summary" ]; then
                error_summary=$(sanitize_log < "$step_log" | tail -n 60 || true)
              fi
            fi
            if [ -z "$error_summary" ]; then
              error_summary="openclaw.log 与步骤日志均未找到可用摘要"
            fi
            # 与同文件 send_telegram_alert / send_archive_alert 同口径（尾部 1200 字节）：
            # 超长输出会让 <pre> 跨 4000 字符分片、标签断开即破版（规范 · 分节）
            error_summary=$(printf '%s' "$error_summary" | tail -c 1200)

            ts_host=""
            ts_ip=""
            if [ -f "$HOME/ts.env" ]; then
              ts_host=$(grep '^TS_HOST=' "$HOME/ts.env" | tail -n 1 | cut -d= -f2- || true)
              ts_ip=$(grep '^TS_IP=' "$HOME/ts.env" | tail -n 1 | cut -d= -f2- || true)
            fi
            ssh_entry="Tailscale SSH 暂不可用 · 稍后查看 Actions 日志"
            if [ -n "$ts_host" ] && [ -n "$ts_ip" ]; then
              ssh_entry="tailscale ssh runner@${ts_host} · 或 ssh runner@${ts_ip}"
            elif [ -n "$ts_host" ]; then
              ssh_entry="tailscale ssh runner@${ts_host}"
            fi

            current_version=""
            fallback_version=""
            fallback_version_source=""
            recorded_successful_version=""
            fail_stage=""
            initial_start_duration_sec=""
            preflight_doctor_duration_sec=""
            fallback_reinstall_duration_sec=""
            if [ -f "$meta_file" ]; then
              current_version=$(grep '^CURRENT_OPENCLAW_VERSION=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              fallback_version=$(grep '^FALLBACK_TARGET_VERSION=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              fallback_version_source=$(grep '^FALLBACK_VERSION_SOURCE=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              recorded_successful_version=$(grep '^RECORDED_SUCCESSFUL_OPENCLAW_VERSION=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              fail_stage=$(grep '^FAIL_STAGE=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              initial_start_duration_sec=$(grep '^INITIAL_START_DURATION_SEC=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              preflight_doctor_duration_sec=$(grep '^PREFLIGHT_DOCTOR_DURATION_SEC=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
              fallback_reinstall_duration_sec=$(grep '^FALLBACK_REINSTALL_DURATION_SEC=' "$meta_file" | tail -n 1 | cut -d= -f2- || true)
            fi

            export RUN_URL="${TG_RUN_URL}"
            export SSH_ENTRY="$ssh_entry"
            export ERROR_SUMMARY="$error_summary"
            export CURRENT_VERSION="$current_version"
            export FALLBACK_VERSION="$fallback_version"
            export FALLBACK_VERSION_SOURCE="$fallback_version_source"
            export RECORDED_SUCCESSFUL_VERSION="$recorded_successful_version"
            export FAIL_STAGE="$fail_stage"
            export INITIAL_START_DURATION_SEC="$initial_start_duration_sec"
            export PREFLIGHT_DOCTOR_DURATION_SEC="$preflight_doctor_duration_sec"
            export FALLBACK_REINSTALL_DURATION_SEC="$fallback_reinstall_duration_sec"
            # 统一 HTML 版式 + 统一收尾区（动态值一律转义；日志收敛 <pre> 等宽块；
            # HTML 解析失败直接报错不重发——修复"声明 parse_mode=HTML 但未转义"的隐患）
            # 失败阶段英文 token → 中文标签（规范 · 失败与异常：英文原因 token 不得直出通知）
            fail_stage_label=""
            if [ -n "$fail_stage" ]; then
              case "$fail_stage" in
                preflight_doctor) fail_stage_label="预检修复";;
                config_validate) fail_stage_label="配置校验";;
                initial_start) fail_stage_label="首次启动";;
                fallback_reinstall) fail_stage_label="回退重装";;
                snapshot_restore) fail_stage_label="快照回滚";;
                *) fail_stage_label="$fail_stage";;
              esac
            fi
            source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"
            # 耗时显示：≥1 秒用秒（两位小数），不足 1 秒用整数毫秒 —— 时长五层
            # （规范 · 时长写法第 3/4 层）同 emby.yml fmt_ms 口径，勿自创形态。
            # 上游写入的是整数秒，恒按 %.2f 输出时 0 会显示成 "0.00 秒"：既看不出
            # 快慢，也让人怀疑没测到（规范 · kv 行第 4 层就是为这个单列的）。
            fmt_ms() { # $1=秒（小数或空）→ "27 毫秒" / "1.40 秒"；空或非法返回空
              local v=$1
              case "$v" in ''|*[!0-9.]*) printf ''; return 0;; esac
              awk -v x="$v" 'BEGIN { if (x < 1) printf "%d 毫秒", x * 1000 + 0.5; else printf "%.2f 秒", x }'
            }
            msg=""
            tg_add_title msg "🚨 OpenClaw 自愈失败"
            # 英文步骤名不直出通知（规范 · 失败与异常）
            tg_add_kv msg "步骤" "运行 OpenClaw"
            tg_add_path msg "Run ID" "${{ github.run_id }}"
            [ -n "$fail_stage_label" ] && tg_add_kv msg "失败阶段" "$fail_stage_label"
            [ -n "$current_version" ] && tg_add_path msg "当前版本" "$current_version"
            [ -n "$fallback_version" ] && tg_add_path msg "回退版本" "$fallback_version"
            # 英文 token 不直出通知（规范 · 失败与异常）：known_good_file / meta_lastTouchedVersion → 中文
            if [ -n "$fallback_version_source" ]; then
              case "$fallback_version_source" in
                known_good_file) _fvs="known-good 版本文件" ;;
                meta_lastTouchedVersion) _fvs="meta 里的 lastTouchedVersion" ;;
                *) _fvs="$fallback_version_source" ;;
              esac
              tg_add_kv msg "回退来源" "$_fvs"
            fi
            [ -n "$recorded_successful_version" ] && tg_add_path msg "成功版本记录" "$recorded_successful_version"
            # 耗时走 fmt_ms（规范 · 时长写法第 3/4 层），不再一律按秒两位小数
            [ -n "$initial_start_duration_sec" ] && tg_add_kv msg "首次启动耗时" "$(fmt_ms "$initial_start_duration_sec")"
            [ -n "$preflight_doctor_duration_sec" ] && tg_add_kv msg "预检修复耗时" "$(fmt_ms "$preflight_doctor_duration_sec")"
            [ -n "$fallback_reinstall_duration_sec" ] && tg_add_kv msg "回退重装耗时" "$(fmt_ms "$fallback_reinstall_duration_sec")"
            [ -n "$ssh_entry" ] && tg_add_path msg "SSH 调试" "$ssh_entry"
            tg_add_section msg "🧾 关键日志"
            # 走真源 tg_add_pre（转义 + <pre> 包裹），与同文件归档告警同款；
            # 此前本处手拼 <pre>，是 tg_add_pre 的重复实现
            tg_add_pre msg "$error_summary"
            tg_add_footer msg
            # 统一走发送层：429 按 retry_after 重试已内置，HTML 解析失败直接报错。
            # 用 chunked 版：日志经转义后可能膨胀到单条上限，分片比整条被拒好
            send_tg_chunked "$msg" || true
          else
            echo "⚠️ Telegram alert skipped: TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID is empty"
          fi

      - name: Start background archive loop
        env:
          # send_telegram_alert 以这两个变量为发送闸，缺任一即静默跳过
          # （2026-09-05 复审发现：此前本 step 未注入，约 5 处归档告警从未发出）
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # 收尾区接线（规范 · 收尾区）：缺了只能走 runner 开机时刻兜底（同类 step 均已注入）
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
        if: ${{ always() }}
        run: |
          echo "Starting background archive tasks: archive ~/.openclaw every 20 minutes; CLIProxyAPI data is archived at final shutdown..."
          cat > /tmp/create-openclaw-archive.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          OUTPUT_TAR="${1:-}"
          if [ -z "$OUTPUT_TAR" ]; then
            echo "Usage: $0 /path/to/output.tar.gz" >&2
            exit 2
          fi

          MIN_SIZE=$((5 * 1024))
          SRC_DIR="/home/runner/.openclaw"
          STAGE_DIR=""

          cleanup() {
            if [ -n "${STAGE_DIR:-}" ] && [ -d "$STAGE_DIR" ]; then
              rm -rf "$STAGE_DIR"
            fi
          }
          trap cleanup EXIT

          rm -f "$OUTPUT_TAR"

          if [ ! -d "$SRC_DIR" ]; then
            echo "openclaw_dir_missing"
            exit 1
          fi

          STAGE_DIR=$(mktemp -d /tmp/openclaw-archive-stage.XXXXXX)
          mkdir -p "$STAGE_DIR/.openclaw"

          # Copy a stable snapshot first. rsync may return 24 when live files vanish
          # during copy; that is expected for active OpenClaw session/lock files, so
          # accept 24 and still validate the final tarball.
          set +e
          rsync -a --delete \
            --exclude 'logs/**' \
            --exclude 'cache/**' \
            --exclude 'browser/*/Crashpad/**' \
            --exclude 'browser/*/GPUCache/**' \
            --exclude 'browser/*/Code Cache/**' \
            --exclude 'browser/*/GrShaderCache/**' \
            --exclude 'browser/*/GraphiteDawnCache/**' \
            --exclude 'SingletonLock' \
            --exclude 'SingletonSocket' \
            --exclude 'SingletonCookie' \
            --exclude '*.lock' \
            --exclude '*.pid' \
            --exclude '*.jsonl.lock' \
            "$SRC_DIR"/ "$STAGE_DIR/.openclaw"/
          rsync_rc=$?
          set -e

          if [ "$rsync_rc" -ne 0 ] && [ "$rsync_rc" -ne 24 ]; then
            echo "openclaw_snapshot_failed rc=$rsync_rc" >&2
            exit "$rsync_rc"
          fi

          if [ "$rsync_rc" -eq 24 ]; then
            echo "openclaw_snapshot_partial_live_changes"
          fi

          # 完整性校验：关键文件必须存在且非空 —— 这才是「状态全不全」的判据。
          # 取代原先「新包不足云端现有包 60% 则拒绝覆盖」的体积基准：体积会被主动
          # 瘦身（排除缓存/调试转储）、日志轮转永久改变，拿上一次归档当基准会把
          # 正常瘦身判成状态丢失，且主包就此冻结、每轮重复告警（2026-09-12）。
          # 退出码 4 = 状态不全，调用方按归档失败处理，不覆盖云端。
          missing=""
          for f in openclaw.json; do
            if [ ! -s "$STAGE_DIR/.openclaw/$f" ]; then
              missing="$missing $f"
            fi
          done
          if [ -n "$missing" ]; then
            echo "openclaw_archive_incomplete missing=$missing" >&2
            exit 4
          fi

          tar -C "$STAGE_DIR" -czf "$OUTPUT_TAR" .openclaw

          actual_size=$(stat -c%s "$OUTPUT_TAR" 2>/dev/null || echo 0)
          if [ ! -s "$OUTPUT_TAR" ]; then
            echo "openclaw_archive_empty output=$OUTPUT_TAR"
            exit 1
          elif [ "$actual_size" -lt "$MIN_SIZE" ]; then
            echo "openclaw_archive_too_small size=${actual_size} min=${MIN_SIZE} output=$OUTPUT_TAR"
            exit 1
          elif ! tar -tzf "$OUTPUT_TAR" >/dev/null 2>&1; then
            echo "openclaw_archive_tar_validation_failed output=$OUTPUT_TAR"
            exit 1
          else
            echo "archive_ready size=${actual_size} output=$OUTPUT_TAR"
          fi
          EOF
          chmod +x /tmp/create-openclaw-archive.sh

          cat > /tmp/create-zcode-archive.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          OUTPUT_TAR="${1:-}"
          if [ -z "$OUTPUT_TAR" ]; then
            echo "Usage: $0 /path/to/output.tar.gz" >&2
            exit 2
          fi

          MIN_SIZE=$((5 * 1024))
          SRC_DIR="/home/runner/.zcode/cli"
          STAGE_DIR=""

          cleanup() {
            if [ -n "${STAGE_DIR:-}" ] && [ -d "$STAGE_DIR" ]; then
              rm -rf "$STAGE_DIR"
            fi
          }
          trap cleanup EXIT

          rm -f "$OUTPUT_TAR"

          if [ ! -d "$SRC_DIR" ]; then
            echo "zcode_dir_missing"
            exit 3
          fi

          STAGE_DIR=$(mktemp -d /tmp/zcode-archive-stage.XXXXXX)
          mkdir -p "$STAGE_DIR/.zcode"

          # 只备个人数据：db（会话库，连 -wal/-shm 一起打包保证一致性）、rollout（对话原始流）、
          # memories（持久记忆）、plugins/data + marketplaces（插件运行数据与市场配置）。
          # 排除 plugins/cache（13MB 插件缓存，可按需重新下载）与 log/exec（运行日志，可再生）。
          # 排除 rollout/model-io-*：模型请求/响应调试转储，单文件可膨胀到 64MB+，
          # 且会被 ZCode 随时轮转清零，进出归档会让包体积剧烈波动、误触缩水保护（2026-09-12 假警报）。
          # server/ 运行时由桌面客户端自动重新部署，不备份。
          # rsync 返回 24 表示活动会话文件复制中消失，属预期，仍校验最终包。
          set +e
          rsync -a --delete \
            --exclude 'log/**' \
            --exclude 'exec/**' \
            --exclude 'plugins/cache/**' \
            --exclude 'rollout/model-io-*.jsonl' \
            --exclude '*.lock' \
            --exclude '*.pid' \
            "$SRC_DIR"/ "$STAGE_DIR/.zcode/cli"/
          rsync_rc=$?
          set -e

          if [ "$rsync_rc" -ne 0 ] && [ "$rsync_rc" -ne 24 ]; then
            echo "zcode_snapshot_failed rc=$rsync_rc" >&2
            exit "$rsync_rc"
          fi

          if [ "$rsync_rc" -eq 24 ]; then
            echo "zcode_snapshot_partial_live_changes"
          fi

          # 一并备份 server 设备身份（v2/certs CA + telemetry-state.json 的 deviceMid），
          # 供下次还原后 server 复用，客户端跨重置保持同一实例认知（详见还原步骤注释）。
          if [ -d /home/runner/.zcode/v2/certs ]; then
            mkdir -p "$STAGE_DIR/.zcode/v2/certs"
            cp -a /home/runner/.zcode/v2/certs/. "$STAGE_DIR/.zcode/v2/certs"/
            [ -f /home/runner/.zcode/v2/telemetry-state.json ] && \
              cp -a /home/runner/.zcode/v2/telemetry-state.json "$STAGE_DIR/.zcode/v2"/
          fi

          # 完整性校验（口径同 openclaw 主包，取代体积基准）：db.sqlite 是会话真实
          # 状态，缺它说明本轮没有可恢复状态，宁可不覆盖云端。排除 model-io 之类的
          # 调试转储不影响此判据 —— 主动瘦身不再被误判为状态丢失。
          missing=""
          for f in db/db.sqlite; do
            if [ ! -s "$STAGE_DIR/.zcode/cli/$f" ]; then
              missing="$missing $f"
            fi
          done
          if [ -n "$missing" ]; then
            echo "zcode_archive_incomplete missing=$missing" >&2
            exit 4
          fi

          tar -C "$STAGE_DIR" -czf "$OUTPUT_TAR" .zcode

          actual_size=$(stat -c%s "$OUTPUT_TAR" 2>/dev/null || echo 0)
          if [ ! -s "$OUTPUT_TAR" ]; then
            echo "zcode_archive_empty output=$OUTPUT_TAR"
            exit 1
          elif [ "$actual_size" -lt "$MIN_SIZE" ]; then
            echo "zcode_archive_too_small size=${actual_size} min=${MIN_SIZE} output=$OUTPUT_TAR"
            exit 1
          elif ! tar -tzf "$OUTPUT_TAR" >/dev/null 2>&1; then
            echo "zcode_archive_tar_validation_failed output=$OUTPUT_TAR"
            exit 1
          else
            echo "archive_ready size=${actual_size} output=$OUTPUT_TAR"
          fi
          EOF
          chmod +x /tmp/create-zcode-archive.sh

          cat > /tmp/create-cliproxyapi-archive.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          OUTPUT_TAR="${1:-}"
          if [ -z "$OUTPUT_TAR" ]; then
            echo "Usage: $0 /path/to/output.tar.gz" >&2
            exit 2
          fi

          MIN_SIZE=$((5 * 1024))
          SRC_DIR="/tmp/local_CLIPProxyAPI"
          STAGE_DIR=""

          cleanup() {
            if [ -n "${STAGE_DIR:-}" ] && [ -d "$STAGE_DIR" ]; then
              sudo rm -rf "$STAGE_DIR"
            fi
          }
          trap cleanup EXIT

          sudo rm -f "$OUTPUT_TAR"

          if [ ! -d "$SRC_DIR" ]; then
            echo "cliproxyapi_dir_missing"
            exit 3
          fi

          STAGE_DIR=$(mktemp -d /tmp/cliproxyapi-archive-stage.XXXXXX)
          sudo mkdir -p "$STAGE_DIR"

          # Copy a stable snapshot first. Exclude volatile request logs/caches; persist
          # config, auth, and stats.
          sudo rsync -a --delete \
            --exclude 'logs/**' \
            --exclude 'cache/**' \
            --exclude 'tmp/**' \
            --exclude 'temp/**' \
            --exclude '*.lock' \
            --exclude '*.pid' \
            --exclude '*.jsonl.lock' \
            "$SRC_DIR"/ "$STAGE_DIR"/

          # 回退态 token 双保险：把 CliRelay 的 auths 一并打进包（恢复侧忽略未知目录，向后兼容）
          if [ -d /tmp/local_CliRelay/auths ]; then
            sudo mkdir -p "$STAGE_DIR/clirelay-auths"
            sudo cp -a /tmp/local_CliRelay/auths/. "$STAGE_DIR/clirelay-auths/" 2>/dev/null || true
            sudo find "$STAGE_DIR/clirelay-auths" -name '._*' -delete 2>/dev/null || true
          fi

          sudo tar -C "$STAGE_DIR" -czf "$OUTPUT_TAR" .
          # 压缩包是 root 产出、随后由 runner 身份的 rclone 上传，显式放开读权限，不依赖 umask
          sudo chmod 644 "$OUTPUT_TAR"

          actual_size=$(stat -c%s "$OUTPUT_TAR" 2>/dev/null || echo 0)
          if [ ! -s "$OUTPUT_TAR" ]; then
            echo "cliproxyapi_archive_empty output=$OUTPUT_TAR"
            exit 1
          elif [ "$actual_size" -lt "$MIN_SIZE" ]; then
            echo "cliproxyapi_archive_too_small size=${actual_size} min=${MIN_SIZE} output=$OUTPUT_TAR"
            exit 1
          elif ! sudo tar -tzf "$OUTPUT_TAR" >/dev/null 2>&1; then
            echo "cliproxyapi_archive_tar_validation_failed output=$OUTPUT_TAR"
            exit 1
          else
            echo "archive_ready size=${actual_size} output=$OUTPUT_TAR"
          fi
          EOF
          chmod +x /tmp/create-cliproxyapi-archive.sh

          cat > /tmp/create-clirelay-archive.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          OUTPUT_TAR="${1:-}"
          if [ -z "$OUTPUT_TAR" ]; then
            echo "Usage: $0 /path/to/output.tar.gz" >&2
            exit 2
          fi

          MIN_SIZE=$((5 * 1024))
          SRC_DIR="/tmp/local_CliRelay"
          COMPOSE="docker compose -p clirelay -f $SRC_DIR/docker-compose.yml --project-directory $SRC_DIR"
          STAGE_DIR=""

          cleanup() {
            if [ -n "${STAGE_DIR:-}" ] && [ -d "$STAGE_DIR" ]; then
              rm -rf "$STAGE_DIR"
            fi
          }
          trap cleanup EXIT

          rm -f "$OUTPUT_TAR"

          if [ ! -f "$SRC_DIR/docker-compose.yml" ] || [ ! -f "$SRC_DIR/.env" ]; then
            echo "clirelay_dir_missing"
            exit 3
          fi

          STAGE_DIR=$(mktemp -d /tmp/clirelay-archive-stage.XXXXXX)

          # 1. pg_dump 刷新 SQL（postgres 数据卷不入包，恢复时 psql 导入；容器仍在运行时执行）
          . "$SRC_DIR/.env"
          mkdir -p "$SRC_DIR/sql"
          if $COMPOSE exec -T postgres sh -c '. /clirelay-deploy/.env; exec pg_dump -U "$CLIRELAY_POSTGRES_USER" -d "$CLIRELAY_POSTGRES_DB" --no-owner' > "$SRC_DIR/sql/clirelay-latest.sql" 2>/tmp/clirelay-pgdump.err; then
            pg_size=$(stat -c%s "$SRC_DIR/sql/clirelay-latest.sql" 2>/dev/null || echo 0)
            echo "pg_dump ok size=${pg_size}"
            if [ "$pg_size" -lt 200 ]; then
              echo "⚠️ pg_dump 输出过小（${pg_size} bytes），可能为空库，仅告警不中断"
            fi
          else
            echo "⚠️ pg_dump 失败（非致命，保留旧 SQL 继续）: $(tail -n 3 /tmp/clirelay-pgdump.err 2>/dev/null || true)"
          fi
          rm -f /tmp/clirelay-pgdump.err

          # 2. 打包：auths + config.yaml + .env + compose 文件 + sql/（跳过 postgres-data/redis-data/logs/data 原始目录）
          mkdir -p "$STAGE_DIR"
          cp -a "$SRC_DIR/config.yaml" "$SRC_DIR/.env" "$SRC_DIR/docker-compose.yml" "$STAGE_DIR/"
          mkdir -p "$STAGE_DIR/auths" "$STAGE_DIR/sql"
          cp -a "$SRC_DIR/auths/." "$STAGE_DIR/auths/" 2>/dev/null || true
          find "$STAGE_DIR/auths" -name '._*' -delete 2>/dev/null || true
          cp -a "$SRC_DIR/sql/." "$STAGE_DIR/sql/" 2>/dev/null || true

          tar -C "$STAGE_DIR" -czf "$OUTPUT_TAR" .
          chmod 644 "$OUTPUT_TAR" 2>/dev/null || true

          actual_size=$(stat -c%s "$OUTPUT_TAR" 2>/dev/null || echo 0)
          if [ ! -s "$OUTPUT_TAR" ]; then
            echo "clirelay_archive_empty output=$OUTPUT_TAR"
            exit 1
          elif [ "$actual_size" -lt "$MIN_SIZE" ]; then
            echo "clirelay_archive_too_small size=${actual_size} min=${MIN_SIZE} output=$OUTPUT_TAR"
            exit 1
          elif ! tar -tzf "$OUTPUT_TAR" >/dev/null 2>&1; then
            echo "clirelay_archive_tar_validation_failed output=$OUTPUT_TAR"
            exit 1
          else
            echo "archive_ready size=${actual_size} output=$OUTPUT_TAR"
          fi
          EOF
          chmod +x /tmp/create-clirelay-archive.sh

          cat > /tmp/openclaw-archive-loop.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          TMP_OPENCLAW_TAR="/tmp/openclaw.tar.gz"
          TMP_CLIPROXYAPI_TAR="/tmp/CLIProxyAPI.tar.gz"
          TMP_CLIRELAY_TAR="/tmp/CliRelay.tar.gz"
          TMP_ZCODE_TAR="/tmp/zcode.tar.gz"
          LOCK_FILE="/tmp/openclaw-archive.lock"

          cleanup_archive_outputs() {
            rm -f "$TMP_OPENCLAW_TAR" 2>/dev/null || true
            sudo rm -f "$TMP_CLIPROXYAPI_TAR" 2>/dev/null || true
            rm -f "$TMP_CLIRELAY_TAR" 2>/dev/null || true
            rm -f "$TMP_ZCODE_TAR" 2>/dev/null || true
            rm -rf /tmp/openclaw-archive-stage.* 2>/dev/null || true
            sudo rm -rf /tmp/cliproxyapi-archive-stage.* 2>/dev/null || true
            rm -rf /tmp/clirelay-archive-stage.* 2>/dev/null || true
            rm -rf /tmp/zcode-archive-stage.* 2>/dev/null || true
          }
          cleanup_archive_outputs
          PID_FILE="/tmp/openclaw-archive.pid"

          # 用法: send_telegram_alert <对象> <结论> <原因> [原始输出]
          # 版式（docs/telegram-notify.md 2.3 / 4.3 / 规范 · 多行块）：对象是机器返回的包名 → <code>；
          # 结论与原因是人写的自然语言 → 裸文本 kv；rclone/tar 的原始输出 → <pre>。
          # 此前四者拼成一整串塞进单个 <pre>，自然语言被一并等宽化，与全库
          # 「kv 区 + 明细分节」形态不一致。
          # 对象 → 标题短名：五种归档曾共用「OpenClaw 归档告警」这一个标题，读者无法
          # 从标题判断是哪个包出问题（看到标题会以为是主包，实际可能是 ZCode）。
          obj_label() {
            case "$1" in
              openclaw.tar.gz)                  echo "OpenClaw 主包" ;;
              zcode.tar.gz)                     echo "ZCode" ;;
              CliRelay|CliRelay.tar.gz)         echo "CliRelay" ;;
              CLIProxyAPI|CLIProxyAPI.tar.gz)   echo "CLIProxyAPI" ;;
              rsstt|rsstt.tar.gz)               echo "rss-to-telegram" ;;
              *)                                echo "$1" ;;
            esac
          }
          send_telegram_alert() {
            local obj="$1" verdict="$2" reason="${3:-}" detail="${4:-}"
            if [ -n "${TELEGRAM_BOT_TOKEN:-}" ] && [ -n "${TELEGRAM_CHAT_ID:-}" ]; then
              local run_id="${GITHUB_RUN_ID:-manual}"
              # 统一 HTML 版式 + 统一收尾区（tg_add_* 内置转义，值原样传入即可；
              # 二次 escape_html 会把 & 变成 &amp;amp; 字面量直出）
              source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"
              local full_msg=""
              tg_add_title full_msg "⚠️ 归档告警 · $(obj_label "$obj")"
              tg_add_path full_msg "对象" "$obj"
              tg_add_kv full_msg "结论" "$verdict"
              if [ -n "$reason" ]; then
                tg_add_kv full_msg "原因" "$reason"
              fi
              # Run ID 属机器值 → <code>（规范 · 机器值）；排在 kv 区末行，
              # 让 <pre> 原始输出成为正文最后一块（其后只跟收尾区）
              tg_add_path full_msg "Run ID" "${run_id}"
              if [ -n "$detail" ]; then
                # 多行原始输出 → <pre>（规范 · 多行块）；取尾部 1200 字节（与 file_split.sh
                # 日志摘要同口径）：超长输出会让 <pre> 跨 4000 字符分片，标签断开即破版
                tg_add_section full_msg "🧾 原始输出"
                tg_add_pre full_msg "$(printf '%s' "$detail" | tail -c 1200)"
              fi
              TG_RUN_STARTED_AT="${TG_RUN_STARTED_AT:-}" tg_add_footer full_msg
              # 走发送层而非 curl 直发：429 重试已内建，HTML 解析失败直接报错（规范 · 发送层），
              # 429 限流按 retry_after 等待重试，disable_web_page_preview 内置
              send_tg "$full_msg" || true
            fi
          }

          echo $$ > "$PID_FILE"
          trap 'rm -f "$PID_FILE"' EXIT

          ARCHIVE_DEST="dropbox:self-hosted/openclaw.tar.gz"
          if [ -f /tmp/run-openclaw-meta.env ] && ! grep -q '^RECORDED_SUCCESSFUL_OPENCLAW_VERSION=' /tmp/run-openclaw-meta.env; then
            ARCHIVE_DEST="dropbox:self-hosted/failed/openclaw-failed-$(date -u +%Y%m%d-%H%M).tar.gz"
            echo "openclaw-failed-$(date -u +%Y%m%d-%H%M).tar.gz" > /tmp/failed-archive-name
            echo "ℹ️ Unhealthy run detected: background archive targets $ARCHIVE_DEST (main package keeps the last healthy state)."
          fi

          while true; do
            openclaw_archive_output=""
            if openclaw_archive_output=$(flock -E 200 -n "$LOCK_FILE" bash /tmp/create-openclaw-archive.sh "$TMP_OPENCLAW_TAR" 2>&1); then
              echo "[$(date '+%F %T')] openclaw archive ok, uploading... ${openclaw_archive_output}"
              new_size=$(stat -c%s "$TMP_OPENCLAW_TAR" 2>/dev/null || echo 0)
              remote_size=0
              if [ "$ARCHIVE_DEST" = "dropbox:self-hosted/openclaw.tar.gz" ]; then
                remote_size=$(rclone ls "$ARCHIVE_DEST" 2>/dev/null | awk '{s+=$1} END {print s+0}')
              fi
              # 体积骤降只记录、不拦截：完整性由归档脚本的关键文件校验把关（见
              # create-openclaw-archive.sh）。原先拿云端现有包当基准判「缩水」，会被
              # 主动瘦身（排除缓存/调试转储）永久卡死 —— 新包永远小于旧包，主包就此
              # 冻结且每轮重复告警；体积本也不是「状态全不全」的判据（2026-09-12）。
              if [ "$remote_size" -gt 0 ] && [ "$new_size" -lt "$((remote_size * 60 / 100))" ]; then
                echo "[$(date '+%F %T')] ℹ️ 体积较云端下降超 40%（new=${new_size}B remote=${remote_size}B），已通过完整性校验，正常覆盖"
              fi
              openclaw_upload_output=""
              if openclaw_upload_output=$(rclone copyto "$TMP_OPENCLAW_TAR" "$ARCHIVE_DEST" 2>&1); then
                echo "[$(date '+%F %T')] openclaw upload ok"
                rm -f "$TMP_OPENCLAW_TAR"
              else
                rc=$?
                echo "[$(date '+%F %T')] ⚠️ openclaw.tar.gz upload failed (rc=$rc): ${openclaw_upload_output}"
                send_telegram_alert "openclaw.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${openclaw_upload_output:-无输出}"
              fi
            else
              rc=$?
              if [ "$rc" -eq 200 ]; then
                echo "[$(date '+%F %T')] ⏭️ previous archive still running, skip this round"
                sleep 1200
                continue
              else
                echo "[$(date '+%F %T')] ⚠️ openclaw.tar.gz archive failed in periodic archive (rc=$rc): ${openclaw_archive_output}"
                send_telegram_alert "openclaw.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${openclaw_archive_output:-归档脚本无输出}"
              fi
            fi

            # AI 网关归档双轨：按当前生效后端选择归档脚本（clirelay → CliRelay.tar.gz；cliproxyapi → CLIProxyAPI.tar.gz，含 clirelay auths 附包）
            ACTIVE_BACKEND="clirelay"
            if [ -f /tmp/active-ai-backend.env ]; then
              ACTIVE_BACKEND=$(grep '^ACTIVE_BACKEND=' /tmp/active-ai-backend.env | tail -n 1 | cut -d= -f2- || true)
            fi
            ACTIVE_BACKEND="${ACTIVE_BACKEND:-clirelay}"

            if [ "$ACTIVE_BACKEND" = "cliproxyapi" ]; then
              AI_ARCHIVE_TAR="$TMP_CLIPROXYAPI_TAR"
              AI_ARCHIVE_DEST="dropbox:self-hosted/CLIProxyAPI.tar.gz"
              AI_ARCHIVE_SCRIPT="/tmp/create-cliproxyapi-archive.sh"
              AI_LABEL="CLIProxyAPI"
              AI_SUDO_RM="yes"
            else
              AI_ARCHIVE_TAR="$TMP_CLIRELAY_TAR"
              AI_ARCHIVE_DEST="dropbox:self-hosted/CliRelay.tar.gz"
              AI_ARCHIVE_SCRIPT="/tmp/create-clirelay-archive.sh"
              AI_LABEL="CliRelay"
              AI_SUDO_RM="no"
            fi

            ai_archive_output=""
            if ai_archive_output=$(flock -E 200 -n "$LOCK_FILE" bash "$AI_ARCHIVE_SCRIPT" "$AI_ARCHIVE_TAR" 2>&1); then
              echo "[$(date '+%F %T')] ${AI_LABEL} archive ok, uploading... ${ai_archive_output}"
              ai_upload_output=""
              if ai_upload_output=$(rclone copyto "$AI_ARCHIVE_TAR" "$AI_ARCHIVE_DEST" 2>&1); then
                echo "[$(date '+%F %T')] ${AI_LABEL} upload ok"
                if [ "$AI_SUDO_RM" = "yes" ]; then sudo rm -f "$AI_ARCHIVE_TAR"; else rm -f "$AI_ARCHIVE_TAR"; fi
              else
                rc=$?
                echo "[$(date '+%F %T')] ⚠️ ${AI_LABEL}.tar.gz upload failed (rc=$rc): ${ai_upload_output}"
                send_telegram_alert "${AI_LABEL}.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${ai_upload_output:-无输出}"
              fi
            else
              rc=$?
              if [ "$rc" -eq 200 ]; then
                echo "[$(date '+%F %T')] ⏭️ previous archive still running, skip ${AI_LABEL} archive"
              elif [ "$rc" -eq 3 ]; then
                echo "[$(date '+%F %T')] ⚠️ ${AI_LABEL} archive skipped because deployment/data directory is missing (rc=$rc): ${ai_archive_output}"
                send_telegram_alert "$AI_LABEL" "归档跳过 · 退出码 $rc" "本地部署/数据目录缺失 · 通常表示网关容器启动步骤被跳过或失败" "${ai_archive_output:-AI 本地部署/数据目录缺失}"
              else
                echo "[$(date '+%F %T')] ⚠️ ${AI_LABEL} archive failed in periodic archive (rc=$rc): ${ai_archive_output}"
                send_telegram_alert "${AI_LABEL}.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${ai_archive_output:-归档脚本无输出}"
              fi
            fi
            # ZCode 个人数据归档：~/.zcode/cli → dropbox:self-hosted/zcode.tar.gz（带完整性校验）
            if [ -d "$HOME/.zcode/cli" ]; then
              zcode_archive_output=""
              if zcode_archive_output=$(flock -E 200 -n "$LOCK_FILE" bash /tmp/create-zcode-archive.sh "$TMP_ZCODE_TAR" 2>&1); then
                echo "[$(date '+%F %T')] zcode archive ok, uploading... ${zcode_archive_output}"
                zcode_new_size=$(stat -c%s "$TMP_ZCODE_TAR" 2>/dev/null || echo 0)
                zcode_remote_size=$(rclone ls "dropbox:self-hosted/zcode.tar.gz" 2>/dev/null | awk '{s+=$1} END {print s+0}')
                # 体积骤降只记录、不拦截（口径同 openclaw 主包）：完整性由
                # create-zcode-archive.sh 的 db.sqlite 校验把关。原先的缩水保护
                # 在排除 model-io 调试转储后把正常瘦身判成状态丢失，主包冻结。
                if [ "$zcode_remote_size" -gt 0 ] && [ "$zcode_new_size" -lt "$((zcode_remote_size * 60 / 100))" ]; then
                  echo "[$(date '+%F %T')] ℹ️ zcode 体积较云端下降超 40%（new=${zcode_new_size}B remote=${zcode_remote_size}B），已通过完整性校验，正常覆盖"
                fi
                zcode_upload_output=""
                if zcode_upload_output=$(rclone copyto "$TMP_ZCODE_TAR" "dropbox:self-hosted/zcode.tar.gz" 2>&1); then
                  echo "[$(date '+%F %T')] zcode upload ok"
                  rm -f "$TMP_ZCODE_TAR"
                else
                  rc=$?
                  echo "[$(date '+%F %T')] ⚠️ zcode.tar.gz upload failed (rc=$rc): ${zcode_upload_output}"
                  send_telegram_alert "zcode.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${zcode_upload_output:-无输出}"
                fi
              else
                rc=$?
                if [ "$rc" -eq 200 ]; then
                  echo "[$(date '+%F %T')] ⏭️ previous archive still running, skip zcode archive"
                elif [ "$rc" -eq 3 ]; then
                  echo "[$(date '+%F %T')] ⏭️ zcode data dir missing, skip zcode archive (rc=3)"
                else
                  echo "[$(date '+%F %T')] ⚠️ zcode.tar.gz archive failed in periodic archive (rc=$rc): ${zcode_archive_output}"
                  send_telegram_alert "zcode.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${zcode_archive_output:-归档脚本无输出}"
                fi
              fi
            else
              echo "[$(date '+%F %T')] ℹ️ ~/.zcode/cli 不存在，跳过 zcode 归档"
            fi

            sleep 1200
          done
          EOF
          chmod +x /tmp/openclaw-archive-loop.sh
          nohup bash /tmp/openclaw-archive-loop.sh > /tmp/openclaw-archive-loop.log 2>&1 &
          echo $! > /tmp/openclaw-archive-loop.launcher.pid

      - name: Keep alive
        id: keepalive
        if: ${{ always() }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
        run: |
          set -euo pipefail
          KEEPALIVE_MINUTES=340
          FINAL_ARCHIVE_WARNING_BEFORE_MINUTES=15
          WARNING_DELAY_MINUTES=$((KEEPALIVE_MINUTES - FINAL_ARCHIVE_WARNING_BEFORE_MINUTES))

          send_final_archive_warning() {
            sleep "${WARNING_DELAY_MINUTES}m"
            if [ -n "${TELEGRAM_BOT_TOKEN:-}" ] && [ -n "${TELEGRAM_CHAT_ID:-}" ]; then
              # 统一 HTML 版式 + 统一收尾区
              source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"
              msg=""
              tg_add_title msg "⚠️ OpenClaw 即将进入最终归档"
              # 值只写时长，"后执行"由标签「预计」表达（值不重复标签语义）
              tg_add_kv msg "预计" "约 ${FINAL_ARCHIVE_WARNING_BEFORE_MINUTES} 分钟"
              # kv 区集中连排；英文 token 不直出通知（规范 · 失败与异常）：步骤名写中文
              tg_add_kv msg "随后执行" "停止 OpenClaw 并执行最终归档"
              # 说明段只装说明，且作末段（规范 · 说明段）：此前它插在两个 kv 之间
              tg_add_note msg "请及时保存状态或完成当前操作。"
              tg_add_footer msg
              # 统一走发送层（退化/429 重试内置）
              send_tg "$msg" || true
            else
              echo "⚠️ Telegram secrets missing, skip final archive warning."
            fi
          }

          send_final_archive_warning &
          echo $! > /tmp/openclaw-final-archive-warning.pid

          sleep "${KEEPALIVE_MINUTES}m"

      - name: Stop OpenClaw and Final Archive
        id: final_archive
        env:
          # send_archive_alert 同上：未注入时约 15 处最终归档告警静默不发
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
        if: ${{ always() }}
        run: |
          set -euo pipefail
          echo "1. Stopping OpenClaw gateway..."
          openclaw gateway stop || echo "OpenClaw already stopped or command failed."
          sleep 1m

          echo "2. Killing background archive process..."
          if [ -f /tmp/openclaw-archive-loop.launcher.pid ]; then
            kill "$(cat /tmp/openclaw-archive-loop.launcher.pid)" 2>/dev/null || true
          fi
          if [ -f /tmp/openclaw-archive.pid ]; then
            kill "$(cat /tmp/openclaw-archive.pid)" 2>/dev/null || true
          fi

          echo "3. Stopping service containers before final archive..."
          docker stop rss-to-telegram 2>/dev/null || echo "rss-to-telegram already stopped or missing."
          docker stop sub-store 2>/dev/null || echo "sub-store already stopped or missing."
          # CliRelay 主容器名为 cli-proxy-api（compose 固定 container_name）；postgres 保持运行，
          # 最终 pg_dump 在归档脚本内执行完成后才 compose down
          docker stop clirelay cli-proxy-api 2>/dev/null || true
          docker stop cliproxyapi 2>/dev/null || echo "cliproxyapi already stopped or missing."

          # 用法与版式同 send_telegram_alert（<对象> <结论> <原因> [原始输出]）；
          # 两个函数分处不同 step、无法共享定义，改版式时两处要一起改
          # （此前一个用 <i> 说明段、一个用 <b> kv，同一标题两种形态）
          # 对象 → 标题短名（与周期归档的 obj_label 同口径，两处脚本各自独立故各存一份）
          obj_label() {
            case "$1" in
              openclaw.tar.gz)                  echo "OpenClaw 主包" ;;
              zcode.tar.gz)                     echo "ZCode" ;;
              CliRelay|CliRelay.tar.gz)         echo "CliRelay" ;;
              CLIProxyAPI|CLIProxyAPI.tar.gz)   echo "CLIProxyAPI" ;;
              rsstt|rsstt.tar.gz)               echo "rss-to-telegram" ;;
              *)                                echo "$1" ;;
            esac
          }
          send_archive_alert() {
            local obj="$1" verdict="$2" reason="${3:-}" detail="${4:-}"
            if [ -n "${TELEGRAM_BOT_TOKEN:-}" ] && [ -n "${TELEGRAM_CHAT_ID:-}" ]; then
              local run_id="${GITHUB_RUN_ID:-manual}"
              # 统一 HTML 版式（emoji 标题 + ━━━ 分隔线 + 键值区 + 收尾区）；
              # source 幂等可重复；收尾区由 tg_add_footer 统一产出（勿自造时长/链接格式）
              source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"
              local full_msg=""
              # 标题区分阶段：周期归档与最终归档走两个函数，此前标题完全相同，
              # 读者分不清告警来自哪一轮（调用点里的 "final" 前缀已由本标题承担）
              tg_add_title full_msg "⚠️ 最终归档告警 · $(obj_label "$obj")"
              tg_add_path full_msg "对象" "$obj"
              tg_add_kv full_msg "结论" "$verdict"
              if [ -n "$reason" ]; then
                tg_add_kv full_msg "原因" "$reason"
              fi
              tg_add_path full_msg "Run ID" "$run_id"
              if [ -n "$detail" ]; then
                tg_add_section full_msg "🧾 原始输出"
                tg_add_pre full_msg "$(printf '%s' "$detail" | tail -c 1200)"
              fi
              tg_add_footer full_msg
              send_tg "$full_msg" || true
            fi
          }

          # 归档结果累积（供收尾的「最终归档结果」通知读取）：对象 \t 结论 \t 字节 \t 备注
          # 结论由各归档分支当场判定（ok / fail / skip）——临时包成功上传后即被删除，
          # 事后无法再测大小，所以只能边归档边记，不能等收尾时反推。
          # 单独落文件而不是靠步骤内变量：通知在下一个 step，两者不共享 shell 作用域。
          FINAL_ARCHIVE_RESULTS="/tmp/openclaw-final-archive-results.tsv"
          : > "$FINAL_ARCHIVE_RESULTS"
          # 记录失败不得影响归档本身：本步骤是 set -e，写结果只是旁路副作用，
          # 磁盘写不进去时留一行日志继续（归档成功与否不该被一条统计记录左右）
          record_archive_result() {
            printf '%s\t%s\t%s\t%s\n' "$1" "$2" "${3:-0}" "${4:-}" >> "$FINAL_ARCHIVE_RESULTS" 2>/dev/null \
              || echo "⚠️ 归档结果记录失败（不影响归档）: $1 $2" >&2
          }
          # 快照名单独记（它是主包的副本，混进明细会与主包重复计数）
          FINAL_ARCHIVE_SNAPSHOT="/tmp/openclaw-final-snapshot-name"
          rm -f "$FINAL_ARCHIVE_SNAPSHOT"

          echo "4. Performing final archive to Dropbox..."
          cleanup_archive_outputs() {
            rm -f /tmp/openclaw-final.tar.gz /tmp/rsstt.tar.gz /tmp/CliRelay.tar.gz /tmp/zcode-final.tar.gz 2>/dev/null || true
            sudo rm -f /tmp/CLIProxyAPI.tar.gz 2>/dev/null || true
            rm -rf /tmp/openclaw-archive-stage.* 2>/dev/null || true
            sudo rm -rf /tmp/cliproxyapi-archive-stage.* 2>/dev/null || true
            rm -rf /tmp/clirelay-archive-stage.* 2>/dev/null || true
            rm -rf /tmp/zcode-archive-stage.* 2>/dev/null || true
          }
          free_kb() { df -Pk "${1:-/tmp}" | awk 'NR==2 {print $4}'; }
          require_free_kb() {
            local path="$1" min_kb="$2" label="$3"
            local avail
            avail=$(free_kb "$path")
            if [ "${avail:-0}" -lt "$min_kb" ]; then
              echo "❌ ${label}: insufficient free space at ${path}. available=${avail}KB required=${min_kb}KB" >&2
              exit 1
            fi
            echo "✅ ${label}: free space ok at ${path}. available=${avail}KB required=${min_kb}KB"
          }
          cleanup_archive_outputs
          TMP_OPENCLAW_TAR="/tmp/openclaw-final.tar.gz"

          openclaw_src_kb=$(du -sk "$HOME/.openclaw" | awk '{print $1}')
          if [ -z "${openclaw_src_kb:-}" ] || [ "$openclaw_src_kb" -le 0 ]; then
            echo "❌ final openclaw archive preflight: unable to calculate source size" >&2
            exit 1
          fi
          echo "OpenClaw 待归档目录大小: ${openclaw_src_kb}KB"
          require_free_kb /tmp $((openclaw_src_kb + 1024 * 1024)) "final openclaw archive preflight"
          openclaw_final_output=""
          if openclaw_final_output=$(bash /tmp/create-openclaw-archive.sh "$TMP_OPENCLAW_TAR" 2>&1); then
            openclaw_final_upload_output=""
            echo "5. Resolving archive destination (healthy → main package; unhealthy → failed/ isolate)..."
            ARCHIVE_DEST="dropbox:self-hosted/openclaw.tar.gz"
            HEALTHY_RUN=0
            if [ -f /tmp/run-openclaw-meta.env ] && grep -q '^RECORDED_SUCCESSFUL_OPENCLAW_VERSION=' /tmp/run-openclaw-meta.env; then
              HEALTHY_RUN=1
            else
              FAILED_NAME="$(cat /tmp/failed-archive-name 2>/dev/null || echo "openclaw-failed-$(date -u +%Y%m%d-%H%M).tar.gz")"
              ARCHIVE_DEST="dropbox:self-hosted/failed/${FAILED_NAME}"
              echo "ℹ️ Unhealthy run: main package will NOT be overwritten; failed state goes to $ARCHIVE_DEST"
            fi
            # 体积骤降只记录、不拦截（口径同周期归档）：完整性已由归档脚本的关键文件
            # 校验把关，这里不再拿云端现有包当基准 —— 那会被主动瘦身永久卡死（新包
            # 永远小于瘦身前的旧包，主包冻结、每轮重复告警，2026-09-12）。
            if [ "$HEALTHY_RUN" = "1" ]; then
              final_new_size=$(stat -c%s "$TMP_OPENCLAW_TAR" 2>/dev/null || echo 0)
              final_remote_size=$(rclone ls "$ARCHIVE_DEST" 2>/dev/null | awk '{s+=$1} END {print s+0}')
              if [ "$final_remote_size" -gt 0 ] && [ "$final_new_size" -lt "$((final_remote_size * 60 / 100))" ]; then
                echo "ℹ️ 体积较云端下降超 40%（new=${final_new_size}B remote=${final_remote_size}B），已通过完整性校验，正常覆盖"
              fi
            fi
            if openclaw_final_upload_output=$(rclone copyto "$TMP_OPENCLAW_TAR" "$ARCHIVE_DEST" -v 2>&1); then
              echo "final openclaw archive/upload ok → $ARCHIVE_DEST: ${openclaw_final_output}"
              # 大小必须在 rm 之前取——成功上传后临时包即被删除，事后测不到
              openclaw_final_size=$(stat -c%s "$TMP_OPENCLAW_TAR" 2>/dev/null || echo 0)
              if [ "$HEALTHY_RUN" = "1" ]; then
                record_archive_result "openclaw.tar.gz" ok "$openclaw_final_size" "覆盖主包"
                echo "6. Versioned snapshot (every healthy run, keep 3)..."
                SNAP_VERSION="$(grep '^RECORDED_SUCCESSFUL_OPENCLAW_VERSION=' /tmp/run-openclaw-meta.env | tail -n 1 | cut -d= -f2-)"
                SNAP_NAME="openclaw-$(date -u +%Y%m%d-%H%M)-v${SNAP_VERSION}.tar.gz"
                if rclone copyto "$TMP_OPENCLAW_TAR" "dropbox:self-hosted/snapshots/${SNAP_NAME}" -v 2>&1; then
                  echo "✅ Versioned snapshot uploaded: dropbox:self-hosted/snapshots/${SNAP_NAME}"
                  printf '%s\n' "$SNAP_NAME" > "$FINAL_ARCHIVE_SNAPSHOT"
                else
                  echo "⚠️ Versioned snapshot upload failed (non-fatal)."
                fi
                rclone lsf dropbox:self-hosted/snapshots/ --files-only 2>/dev/null | sort -r | tail -n +4 | while read -r OLD_SNAP; do
                  rclone deletefile "dropbox:self-hosted/snapshots/${OLD_SNAP}" 2>/dev/null || true
                done
              else
                record_archive_result "openclaw.tar.gz" ok "$openclaw_final_size" "写入失败现场 · 主包未覆盖"
                rclone lsf dropbox:self-hosted/failed/ --files-only 2>/dev/null | sort -r | tail -n +3 | while read -r OLD_FAILED; do
                  rclone deletefile "dropbox:self-hosted/failed/${OLD_FAILED}" 2>/dev/null || true
                done
                echo "ℹ️ Failed-run archive kept (2 max) in failed/; main package untouched."
              fi
              rm -f "$TMP_OPENCLAW_TAR"
            else
              rc=$?
              echo "⚠️ final openclaw.tar.gz upload failed (rc=$rc): ${openclaw_final_upload_output}"
              record_archive_result "openclaw.tar.gz" fail 0 "上传失败 · 退出码 $rc"
              send_archive_alert "openclaw.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${openclaw_final_upload_output:-无输出}"
            fi
          else
            rc=$?
            echo "⚠️ final openclaw.tar.gz archive failed (rc=$rc): ${openclaw_final_output}"
            record_archive_result "openclaw.tar.gz" fail 0 "归档失败 · 退出码 $rc"
            send_archive_alert "openclaw.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${openclaw_final_output:-归档脚本无输出}"
          fi

          echo "4b. Final archive of ZCode personal data (~/.zcode/cli)..."
          if [ -d "$HOME/.zcode/cli" ]; then
            TMP_ZCODE_FINAL_TAR="/tmp/zcode-final.tar.gz"
            if zcode_final_output=$(bash /tmp/create-zcode-archive.sh "$TMP_ZCODE_FINAL_TAR" 2>&1); then
              zcode_final_new_size=$(stat -c%s "$TMP_ZCODE_FINAL_TAR" 2>/dev/null || echo 0)
              zcode_final_remote_size=$(rclone ls "dropbox:self-hosted/zcode.tar.gz" 2>/dev/null | awk '{s+=$1} END {print s+0}')
              # 体积骤降只记录、不拦截（口径同周期归档）：完整性由 create-zcode-archive.sh
              # 的 db.sqlite 校验把关，不再拿云端现有包当基准。
              if [ "$zcode_final_remote_size" -gt 0 ] && [ "$zcode_final_new_size" -lt "$((zcode_final_remote_size * 60 / 100))" ]; then
                echo "ℹ️ zcode 体积较云端下降超 40%（new=${zcode_final_new_size}B remote=${zcode_final_remote_size}B），已通过完整性校验，正常覆盖"
              fi
              zcode_final_upload_output=""
              if zcode_final_upload_output=$(rclone copyto "$TMP_ZCODE_FINAL_TAR" "dropbox:self-hosted/zcode.tar.gz" -v 2>&1); then
                echo "final zcode archive/upload ok: ${zcode_final_output}"
                record_archive_result "zcode.tar.gz" ok "$zcode_final_new_size"
                rm -f "$TMP_ZCODE_FINAL_TAR"
              else
                rc=$?
                echo "⚠️ final zcode.tar.gz upload failed (rc=$rc): ${zcode_final_upload_output}"
                record_archive_result "zcode.tar.gz" fail 0 "上传失败 · 退出码 $rc"
                send_archive_alert "zcode.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${zcode_final_upload_output:-无输出}"
              fi
            else
              rc=$?
              echo "⚠️ final zcode.tar.gz archive failed (rc=$rc): ${zcode_final_output}"
              record_archive_result "zcode.tar.gz" fail 0 "归档失败 · 退出码 $rc"
              send_archive_alert "zcode.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${zcode_final_output:-归档脚本无输出}"
            fi
          else
            echo "ℹ️ ~/.zcode/cli 不存在（本机从未部署 ZCode），跳过 ZCode 最终归档"
            record_archive_result "zcode.tar.gz" skip 0 "本机未部署 ZCode"
          fi

          MIN_SIZE=$((5 * 1024))

          echo "5. Archiving AI gateway persistent data (dual-track by active backend)..."
          ACTIVE_BACKEND="clirelay"
          if [ -f /tmp/active-ai-backend.env ]; then
            ACTIVE_BACKEND=$(grep '^ACTIVE_BACKEND=' /tmp/active-ai-backend.env | tail -n 1 | cut -d= -f2- || true)
          fi
          ACTIVE_BACKEND="${ACTIVE_BACKEND:-clirelay}"
          echo "当前生效 AI 网关: ${ACTIVE_BACKEND}"

          if [ "$ACTIVE_BACKEND" = "clirelay" ]; then
            # CliRelay 主用：pg_dump（postgres 仍在运行）→ 打包上传 CliRelay.tar.gz → compose down
            if [ -f /tmp/local_CliRelay/docker-compose.yml ]; then
              clirelay_final_output=""
              if clirelay_final_output=$(bash /tmp/create-clirelay-archive.sh /tmp/CliRelay.tar.gz 2>&1); then
                actual_size=$(stat -c%s /tmp/CliRelay.tar.gz)
                if [ "$actual_size" -ge "$MIN_SIZE" ]; then
                  clirelay_final_upload_output=""
                  if clirelay_final_upload_output=$(rclone copyto /tmp/CliRelay.tar.gz dropbox:self-hosted/CliRelay.tar.gz -v 2>&1); then
                    echo "final CliRelay archive/upload ok: ${clirelay_final_output}"
                    record_archive_result "CliRelay.tar.gz" ok "$actual_size"
                    rm -f /tmp/CliRelay.tar.gz
                  else
                    rc=$?
                    echo "⚠️ final CliRelay.tar.gz upload failed (rc=$rc): ${clirelay_final_upload_output}"
                    record_archive_result "CliRelay.tar.gz" fail 0 "上传失败 · 退出码 $rc"
                    send_archive_alert "CliRelay.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${clirelay_final_upload_output:-无输出}"
                  fi
                else
                  echo "⚠️ CliRelay 压缩包小于 5KB，放弃覆盖云端数据: size=${actual_size}"
                  record_archive_result "CliRelay.tar.gz" fail 0 "压缩包小于最小阈值"
                  send_archive_alert "CliRelay.tar.gz" "归档失败" "压缩包小于最小阈值 · 放弃覆盖云端数据" "size=$actual_size min=$MIN_SIZE output=/tmp/CliRelay.tar.gz"
                fi
              else
                rc=$?
                echo "⚠️ final CliRelay.tar.gz archive failed (rc=$rc): ${clirelay_final_output}"
                record_archive_result "CliRelay.tar.gz" fail 0 "归档失败 · 退出码 $rc"
                send_archive_alert "CliRelay.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${clirelay_final_output:-归档脚本无输出}"
              fi
              echo "Stopping CliRelay compose stack after final archive..."
              docker compose -p clirelay -f /tmp/local_CliRelay/docker-compose.yml --project-directory /tmp/local_CliRelay down 2>/dev/null || true
            else
              echo "⚠️ /tmp/local_CliRelay 不存在，跳过 CliRelay 归档"
              record_archive_result "CliRelay.tar.gz" fail 0 "部署目录未初始化"
              send_archive_alert "CliRelay.tar.gz" "归档失败 · 退出码 3" "CliRelay 本地部署目录未初始化 · /tmp/local_CliRelay 不存在 · 通常表示 CliRelay 启动步骤被跳过或失败" "CliRelay 本地部署目录未初始化"
            fi
          elif [ -d /tmp/local_CLIPProxyAPI ]; then
            echo "5a. Archiving CLIProxyAPI persistent data (fallback backend, with clirelay auths)..."
            echo "=== local CLIProxyAPI tree before archive ==="
            find /tmp/local_CLIPProxyAPI -maxdepth 4 | sort || true
            cliproxyapi_src_kb=$(sudo du -sk /tmp/local_CLIPProxyAPI | awk '{print $1}')
            if [ -z "${cliproxyapi_src_kb:-}" ] || [ "$cliproxyapi_src_kb" -le 0 ]; then
              echo "❌ final CLIProxyAPI archive preflight: unable to calculate source size" >&2
              exit 1
            fi
            echo "CLIProxyAPI 待归档目录大小: ${cliproxyapi_src_kb}KB"
            require_free_kb /tmp $((cliproxyapi_src_kb + 512 * 1024)) "final CLIProxyAPI archive preflight"
            cliproxyapi_final_output=""
            if cliproxyapi_final_output=$(bash /tmp/create-cliproxyapi-archive.sh /tmp/CLIProxyAPI.tar.gz 2>&1); then
              actual_size=$(stat -c%s /tmp/CLIProxyAPI.tar.gz 2>/dev/null || echo 0)
              if [ "$actual_size" -ge "$MIN_SIZE" ]; then
                cliproxyapi_final_upload_output=""
                if cliproxyapi_final_upload_output=$(rclone copyto /tmp/CLIProxyAPI.tar.gz dropbox:self-hosted/CLIProxyAPI.tar.gz -v 2>&1); then
                  echo "final CLIProxyAPI archive/upload ok: ${cliproxyapi_final_output}"
                  record_archive_result "CLIProxyAPI.tar.gz" ok "$actual_size"
                  sudo rm -f /tmp/CLIProxyAPI.tar.gz
                else
                  rc=$?
                  echo "⚠️ final CLIProxyAPI.tar.gz upload failed (rc=$rc): ${cliproxyapi_final_upload_output}"
                  record_archive_result "CLIProxyAPI.tar.gz" fail 0 "上传失败 · 退出码 $rc"
                  send_archive_alert "CLIProxyAPI.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${cliproxyapi_final_upload_output:-无输出}"
                fi
              else
                echo "⚠️ CLIProxyAPI 压缩包小于 10KB，放弃覆盖云端数据: size=${actual_size} min=${MIN_SIZE}"
                record_archive_result "CLIProxyAPI.tar.gz" fail 0 "压缩包小于最小阈值"
                send_archive_alert "CLIProxyAPI.tar.gz" "归档失败" "压缩包小于最小阈值 · 放弃覆盖云端数据" "size=$actual_size min=$MIN_SIZE output=/tmp/CLIProxyAPI.tar.gz"
              fi
            else
              rc=$?
              echo "⚠️ final CLIProxyAPI.tar.gz archive failed (rc=$rc): ${cliproxyapi_final_output}"
              record_archive_result "CLIProxyAPI.tar.gz" fail 0 "归档失败 · 退出码 $rc"
              send_archive_alert "CLIProxyAPI.tar.gz" "归档失败 · 退出码 $rc" "归档脚本执行失败，详见原始输出" "${cliproxyapi_final_output:-归档脚本无输出}"
            fi
          else
            echo "⚠️ /tmp/local_CLIPProxyAPI 不存在，跳过 CLIProxyAPI 归档"
            record_archive_result "CLIProxyAPI.tar.gz" fail 0 "数据目录未初始化"
            send_archive_alert "CLIProxyAPI.tar.gz" "归档失败 · 退出码 3" "CLIProxyAPI 本地数据目录未初始化 · /tmp/local_CLIPProxyAPI 不存在 · 通常表示 CLIProxyAPI 容器启动步骤被跳过或失败" "cliproxyapi_dir_missing"
          fi

          echo "6. Archiving rss-to-telegram data..."
          if [ -d /tmp/local_rsstt ]; then
            echo "=== local rss-to-telegram tree before archive ==="
            find /tmp/local_rsstt -maxdepth 4 | sort || true
            rsstt_src_kb=$(sudo du -sk /tmp/local_rsstt | awk '{print $1}')
            if [ -z "${rsstt_src_kb:-}" ] || [ "$rsstt_src_kb" -le 0 ]; then
              echo "❌ final rsstt archive preflight: unable to calculate source size" >&2
              exit 1
            fi
            echo "rsstt 待归档目录大小: ${rsstt_src_kb}KB"
            require_free_kb /tmp $((rsstt_src_kb + 512 * 1024)) "final rsstt archive preflight"
            sudo rm -f /tmp/rsstt.tar.gz
            rsstt_tar_output=""
            # 目录里只剩占位 .keep = 本轮容器没起来（凭据缺失/启动失败）：
            # 此时打包上传只会用空包覆盖云端 rsstt.tar.gz，直接跳过并说明原因
            rsstt_data_files=$(sudo find /tmp/local_rsstt -type f ! -name '.keep' 2>/dev/null | wc -l)
            if [ "$((rsstt_data_files + 0))" -lt 1 ]; then
              echo "⚠️ rss-to-telegram 本轮没有产生数据文件，跳过归档，云端 rsstt.tar.gz 保持原状"
              record_archive_result "rsstt.tar.gz" skip 0 "本轮未产生数据"
              send_archive_alert "rsstt.tar.gz" "归档跳过" "本轮 rss-to-telegram 未产生数据 · 云端归档保持原状未覆盖" "data_files=0 dir=/tmp/local_rsstt"
            elif rsstt_tar_output=$(sudo tar -czf /tmp/rsstt.tar.gz -C /tmp/local_rsstt . 2>&1); then
              # 同上：root 产出的包由 runner 身份的 rclone 上传，显式放开读权限
              sudo chmod 644 /tmp/rsstt.tar.gz 2>/dev/null || true
              rsstt_validate_output=""
              if rsstt_validate_output=$(sudo tar -tzf /tmp/rsstt.tar.gz 2>&1); then
                actual_size=$(stat -c%s /tmp/rsstt.tar.gz 2>/dev/null || echo 0)
                echo "rsstt archive size: ${actual_size} bytes"
                if [ "$actual_size" -ge "$MIN_SIZE" ]; then
                  rsstt_upload_output=""
                  if rsstt_upload_output=$(rclone copyto /tmp/rsstt.tar.gz dropbox:self-hosted/rsstt.tar.gz -v 2>&1); then
                    echo "final rsstt archive/upload ok"
                    record_archive_result "rsstt.tar.gz" ok "$actual_size"
                    sudo rm -f /tmp/rsstt.tar.gz
                  else
                    rc=$?
                    echo "⚠️ final rsstt.tar.gz upload failed (rc=$rc): ${rsstt_upload_output}"
                    record_archive_result "rsstt.tar.gz" fail 0 "上传失败 · 退出码 $rc"
                    send_archive_alert "rsstt.tar.gz" "上传失败 · 退出码 $rc" "rclone 上传失败" "${rsstt_upload_output:-无输出}"
                  fi
                else
                  echo "⚠️ rsstt 压缩包小于 10KB，放弃覆盖云端数据: size=${actual_size} min=${MIN_SIZE}"
                  record_archive_result "rsstt.tar.gz" fail 0 "压缩包小于最小阈值"
                  send_archive_alert "rsstt.tar.gz" "归档失败" "压缩包小于最小阈值 · 放弃覆盖云端数据" "size=$actual_size min=$MIN_SIZE output=/tmp/rsstt.tar.gz"
                fi
              else
                rc=$?
                echo "⚠️ final rsstt.tar.gz validation failed (rc=$rc): ${rsstt_validate_output}"
                record_archive_result "rsstt.tar.gz" fail 0 "归档校验失败 · 退出码 $rc"
                send_archive_alert "rsstt.tar.gz" "归档校验失败 · 退出码 $rc" "tar 列表校验失败" "${rsstt_validate_output:-无输出}"
              fi
            else
              rc=$?
              echo "⚠️ final rsstt.tar.gz archive failed (rc=$rc): ${rsstt_tar_output}"
              record_archive_result "rsstt.tar.gz" fail 0 "归档失败 · 退出码 $rc"
              send_archive_alert "rsstt.tar.gz" "归档失败 · 退出码 $rc" "tar 创建失败" "${rsstt_tar_output:-无输出}"
            fi
          else
            echo "⚠️ /tmp/local_rsstt 不存在，跳过 rss-to-telegram 归档"
            record_archive_result "rsstt.tar.gz" fail 0 "数据目录未初始化"
            send_archive_alert "rsstt.tar.gz" "归档失败 · 退出码 3" "rss-to-telegram 本地数据目录未初始化 · /tmp/local_rsstt 不存在 · 通常表示 rss-to-telegram 容器启动步骤被跳过或失败" "rsstt_dir_missing"
          fi

      # 最终归档结果汇总（规范 · 结果通知）：归档步骤边归档边把结果写进 /tmp 的结果文件，
      # 这里统一渲染成一条通知。单独成 step 而不是并进归档步骤——归档步骤多处 exit 1
      # （预检失败 / 磁盘不足），写在步骤末尾的通知会随之被跳过；always() + 结果文件
      # 让「没产出明细」也能有交代（那类失败在归档步骤内不产生任何通知）。
      - name: Notify OpenClaw final archive result
        if: ${{ always() }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TG_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TG_RUN_STARTED_AT: ${{ github.run_started_at }}
          ARCHIVE_STEP_CONCLUSION: ${{ steps.final_archive.conclusion }}
        run: |
          # 刻意不用 set -e：本步骤的任务是把已归档的结果送出去，任何一处判断失误都不该
          # 变成「静默不发」；发送失败由末尾的 || echo ::warning:: 留痕（规范 · 发送层）
          set -uo pipefail
          # 凭据校验在 source 之前（规范 · 接线）
          if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
            echo "⚠️ Telegram secrets missing, skip final archive result notification."
            exit 0
          fi
          source "${GITHUB_WORKSPACE}/.github/scripts/telegram/tg_notify.sh"

          RESULTS_FILE="/tmp/openclaw-final-archive-results.tsv"
          SNAPSHOT_FILE="/tmp/openclaw-final-snapshot-name"

          msg=""
          # 归档步骤在记录任何结果前就结束了（预检 exit 1 / 运行被取消）：降级为「未完成」
          if [ ! -s "$RESULTS_FILE" ]; then
            case "${ARCHIVE_STEP_CONCLUSION:-}" in
              success)   _verdict="归档步骤已结束，但未产出归档明细" ;;
              failure)   _verdict="归档步骤执行失败" ;;
              cancelled) _verdict="运行已被取消" ;;
              skipped)   _verdict="归档步骤被跳过" ;;
              *)         _verdict="归档步骤未执行" ;;
            esac
            tg_add_title msg "⚠️ OpenClaw 最终归档未完成"
            tg_add_kv msg "结论" "$_verdict"
            tg_add_note msg "本轮没有产出归档明细，云端归档保持上一轮状态。"
            tg_add_footer msg
            send_tg "$msg" || echo "::warning::TG 通知发送失败（不影响任务）"
            exit 0
          fi

          # 逐条读结果并计数；条目边读边构建。明细是固定几项的结构性清单（最多 5 项），
          # 全量展示不折叠（规范 · 折叠规则）——折掉任何一项都会让读者误判归档是否完整。
          ok_n=0; fail_n=0; skip_n=0; total_bytes=0
          entries=""
          # read 的退出码兜底（|| [ -n "$_obj" ]）：末行缺尾换行时 read 返回非 0 但变量已赋值，
          # 只看退出码会静默丢掉最后一个归档对象——而「少一项」正是这条通知要防的误判。
          while IFS=$'\t' read -r _obj _verdict _bytes _note || [ -n "${_obj:-}" ]; do
            [ -z "$_obj" ] && continue
            case "$_bytes" in ''|*[!0-9]*) _bytes=0 ;; esac
            case "$_verdict" in
              ok)
                ok_n=$((ok_n + 1))
                total_bytes=$((total_bytes + _bytes))
                tg_add_entry entries "$_obj" "✅ $(format_bytes "$_bytes")" "$_note"
                ;;
              skip)
                skip_n=$((skip_n + 1))
                tg_add_entry entries "$_obj" "⏭️ 跳过" "$_note"
                ;;
              *)
                fail_n=$((fail_n + 1))
                tg_add_entry entries "$_obj" "❌ ${_note:-失败}"
                ;;
            esac
          done < "$RESULTS_FILE"

          # 标题按严重度降级（规范 · 状态图标）：全败 ❌ > 有失败 ⚠️ > 全成 ✅
          if [ "$fail_n" -gt 0 ] && [ "$ok_n" -eq 0 ]; then
            tg_add_title msg "❌ OpenClaw 最终归档失败"
          elif [ "$fail_n" -gt 0 ]; then
            tg_add_title msg "⚠️ OpenClaw 最终归档完成 · 部分失败"
          else
            tg_add_title msg "✅ OpenClaw 最终归档完成"
          fi

          # 计数只列非零项，避免「失败 0」这种与标题自相矛盾的写法（规范 · 计数与量化）
          result_parts=""
          if [ "$ok_n" -gt 0 ]; then result_parts="成功 $ok_n"; fi
          if [ "$fail_n" -gt 0 ]; then result_parts="${result_parts:+${result_parts} · }失败 $fail_n"; fi
          if [ "$skip_n" -gt 0 ]; then result_parts="${result_parts:+${result_parts} · }跳过 $skip_n"; fi
          tg_add_kv msg "结果" "$result_parts"
          if [ "$ok_n" -gt 0 ]; then
            tg_add_kv msg "大小" "合计 $(format_bytes "$total_bytes")"
          fi
          # 快照是主包的副本，单列一行、不并入明细与合计（否则与主包重复计数）
          if [ -s "$SNAPSHOT_FILE" ]; then
            tg_add_path msg "快照" "$(cat "$SNAPSHOT_FILE")"
          fi

          tg_add_section msg "📦 归档明细 · $((ok_n + fail_n + skip_n))"
          tg_add_block msg "$(tree_lines "$entries")"
          tg_add_footer msg
          send_tg "$msg" || echo "::warning::TG 通知发送失败（不影响任务）"

      - name: Trigger next OpenClaw run
        if: ${{ always() && steps.keepalive.conclusion == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          echo "Triggering next openclaw.yml run. GitHub Actions concurrency will keep it singleton."
          gh workflow run openclaw.yml \
            --repo "${{ github.repository }}" \
            --ref "${{ github.ref_name }}"

      # - name: Sync ~/.openclaw to Dropbox
      #   if: always()
      #   run: |
      #     echo "Syncing ~/.openclaw to Dropbox..."
      #     rclone sync ~/.openclaw dropbox:self-hosted/openclaw \
      #       --exclude "logs/**" \
      #       --exclude "cache/**" \
      #       --exclude "browser/**/Crashpad/**" \
      #       --exclude "browser/**/GPUCache/**" \
      #       --exclude "browser/**/Code Cache/**" \
      #       --exclude "browser/**/GrShaderCache/**" \
      #       --exclude "browser/**/GraphiteDawnCache/**" \
      #       --exclude "**/*.lock" \
      #       --exclude "**/*.pid" \
      #       --exclude "**/*.jsonl.lock" \
      #       -v
