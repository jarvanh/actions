name: proxy-speedtest-gitee

on:
  schedule:
    # 每 6 小时测一次：UTC 02:00, 08:00, 14:00, 20:00（北京 10:00, 16:00, 22:00, 04:00）
    - cron: "0 2,8,14,20 * * *"
  workflow_dispatch:

jobs:
  speedtest:
    concurrency: proxy-speedtest-gitee-singleton
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Run proxy speedtest
        env:
          PROXY_SPEEDTEST_DETACH: '0'
          GITEE_PRIVATE_TOKEN: ${{ secrets.GITEE_PRIVATE_TOKEN }}
          PROXY_SPEEDTEST_SUB_URLS: ${{ secrets.PROXY_SPEEDTEST_SUB_URLS }}
          PROXY_SPEEDTEST_GITEE_OWNER: ${{ secrets.PROXY_SPEEDTEST_GITEE_OWNER }}
          PROXY_SPEEDTEST_GITEE_REPO: proxy-speedtest-temp
          PROXY_SPEEDTEST_SIZE_MIB: '10'
          PROXY_SPEEDTEST_PUSH_TIMEOUT: '30'
          PROXY_SPEEDTEST_CLONE_TIMEOUT: '30'
          PROXY_SPEEDTEST_DIRECT_BASELINE_TIMEOUT: '60'
          PROXY_SPEEDTEST_DIRECT_BASELINE_MAX_ATTEMPTS: '5'
          PROXY_SPEEDTEST_MODE: push-only
          GH_TOKEN: ${{ secrets.PAT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python .github/scripts/proxy-speedtest/speedtest_gitee.py
