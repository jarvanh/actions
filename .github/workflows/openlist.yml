name: openlist

on:
  # schedule:
  #   - cron: "0 1,7,13,19 * * *"
  #   - cron: "0 * * * *"
  workflow_dispatch:
  # workflow_run:
  #   workflows:
  #     - t
  #   types:
  #     - completed  # 工作流完成后触发
  watch:
    # types: started

jobs:
  build:
    concurrency: openlist-singleton
    runs-on: ubuntu-latest
    # timeout-minutes: 60
    steps:
      - name: rclone-install
        run: |
          curl -O https://beta.rclone.org/rclone-beta-latest-linux-amd64.zip
          unzip rclone-*-amd64.zip
          cd rclone-*-linux-amd64
          sudo mv rclone /usr/bin
          sudo chmod +x /usr/bin/rclone
      - name: rclone-config
        env:
          RCLONE_CONFIG: ${{ secrets.RCLONE }}
        run: |
          mkdir -p ~/.config/rclone/
          cat << EOF > ~/.config/rclone/rclone.conf
          $RCLONE_CONFIG
          EOF
      - name: rclone-run
        run: |
          echo "==========onedrive==========="
          rclone lsf onedrive:
          echo "==========dropbox==========="
          rclone lsf dropbox:

          sudo apt-get install fuse3
          sudo mkdir -p /dropbox
          sudo nohup rclone mount dropbox: /dropbox --allow-non-empty --no-gzip-encoding --umask 000 --allow-other --attr-timeout 10m --vfs-cache-mode full --vfs-cache-max-age 5m --vfs-read-chunk-size-limit 10G --buffer-size 100M --vfs-cache-max-size 10G &
      - name: Run openlist container
        run: sudo docker run -d --restart=unless-stopped -v /dropbox/self-hosted/openlist/data:/opt/openlist/data -p 5244:5244 -e PUID=0 -e PGID=0 -e UMASK=022 --name="openlist" openlistteam/openlist:latest
      - name: sleep
        run: |
          sudo sleep 1m      
      - name: Verify docker container is running
        run: docker ps

      - name: install cloudflared
        run: |
          sudo mkdir ~/.cloudflared/
          sudo rclone copy dropbox:self-hosted/.cloudflared ~/.cloudflared -vv
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb
      - name: Check if the tunnel is running
        env:
          TUNNEL_NAME: "openlist" # 替换为你的隧道名称
        run: |
          # 获取隧道列表
          OUTPUT=$(cloudflared tunnel list)
          
          # 查找目标隧道的行
          TUNNEL_LINE=$(echo "$OUTPUT" | grep -w "$TUNNEL_NAME")
          if [ -z "$TUNNEL_LINE" ]; then
            echo "The tunnel '$TUNNEL_NAME' does not exist. Proceeding."
            exit 31
          fi
  
          # 提取 CONNECTIONS 列并检查是否为空
          CONNECTIONS=$(echo "$TUNNEL_LINE" | awk '{if (NF>=4) print $4; else print "-"}')
          if [ "$CONNECTIONS" == "-" ]; then
            echo "The tunnel '$TUNNEL_NAME' exists but has no active connections. Proceeding."
          else
            echo "The tunnel '$TUNNEL_NAME' is running with connections: $CONNECTIONS. Stopping actions."
            exit 0
          fi

      - name: run cloudflared
        run: |
          cloudflared tunnel route dns openlist openlist.'${{ secrets.VD }}'.eu.org
          nohup cloudflared tunnel run --url http://127.0.0.1:5244 openlist &
          sudo sleep 5m
      
      - name: Install required tools
        run: |
          echo "Installing ffmpeg, jq, coreutils..."
          sudo apt-get update && sudo apt-get install -y ffmpeg jq coreutils
      
      - name: rclone-sync-with-logging
        env:
          PROCESSED_FILES_LOG: "processed_videos.log"
        run: |
          # 发送日志到Telegram的函数
          send_log_to_telegram() {
            local log_file="$1"
            local task_name="$2"
            local exit_code="$3"
            
            # 检查文件大小
            local file_size=$(stat -c%s "$log_file" 2>/dev/null || stat -f%z "$log_file" 2>/dev/null || echo 0)
            
            if [ -f "$log_file" ] && [ "$file_size" -gt 0 ]; then
              # 获取最后3000个字符作为摘要
              local log_summary=$(tail -c 3000 "$log_file" 2>/dev/null || echo "无法读取日志文件")
              
              # 构建消息
              local message="❌ $task_name 同步失败 (退出码: $exit_code)\n\n"
              message+="📁 日志文件: $log_file\n"
              message+="📊 文件大小: $(numfmt --to=iec-i --suffix=B "$file_size" 2>/dev/null || echo "${file_size}B")\n\n"
              message+="📋 错误摘要:\n\`\`\`\n$log_summary\n\`\`\`"
              
              # 发送摘要消息
              curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendMessage \
                -d chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                -d text="$message" \
                -d parse_mode="Markdown" || true
              
              # 如果文件小于50MB（Telegram文档限制），发送完整日志文件
              if [ "$file_size" -lt 50000000 ]; then
                # 发送文档
                curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendDocument \
                  -F chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                  -F document=@"$log_file" \
                  -F caption="📁 完整日志文件: $task_name" || true
                
                echo "已发送完整日志文件到 Telegram"
              else
                # 文件太大，发送压缩版本
                echo "日志文件太大 ($file_size 字节)，尝试压缩..."
                
                # 压缩日志文件
                local compressed_log="${log_file}.gz"
                gzip -c "$log_file" > "$compressed_log" 2>/dev/null
                local compressed_size=$(stat -c%s "$compressed_log" 2>/dev/null || stat -f%z "$compressed_log" 2>/dev/null || echo 0)
                
                if [ "$compressed_size" -lt 50000000 ]; then
                  # 发送压缩后的日志
                  curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendDocument \
                    -F chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                    -F document=@"$compressed_log" \
                    -F caption="📁 压缩的日志文件: $task_name (原大小: $(numfmt --to=iec-i --suffix=B "$file_size" 2>/dev/null || echo "${file_size}B"))" || true
                  
                  rm -f "$compressed_log" 2>/dev/null || true
                else
                  echo "压缩后的文件仍然太大，无法通过Telegram发送"
                  curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendMessage \
                    -d chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                    -d text="⚠️ 日志文件过大 ($(numfmt --to=iec-i --suffix=B "$file_size" 2>/dev/null || echo "${file_size}B"))，无法通过Telegram发送。请在OneDrive查看完整日志。" || true
                fi
              fi
            else
              # 没有日志文件
              curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendMessage \
                -d chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                -d text="❌ $task_name 同步失败 (退出码: $exit_code)\n\n⚠️ 没有生成日志文件" || true
            fi
          }
          
          # 视频文件分割函数
          split_large_video() {
            local remote_path="$1"
            local remote_source="$2"  # 例如: onedrive
            local local_file_path="$3"
            local file_name="$4"
            
            echo "开始分割视频文件: $file_name"
            
            # 基础文件名和扩展名
            local base_name="${file_name%.*}"
            local extension="${file_name##*.}"
            
            # 获取视频时长（秒）
            echo "获取视频时长..."
            local duration=$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 "$local_file_path" 2>/dev/null || echo 0)
            
            if [ "$(echo "$duration > 0" | bc)" -eq 1 ]; then
              echo "视频时长: $duration 秒"
              
              # 计算文件大小和所需分片数
              local file_size=$(stat -c%s "$local_file_path" 2>/dev/null || stat -f%z "$local_file_path" 2>/dev/null)
              local max_part_size=3750000000  # 3.5GB，留出安全余量
              local n=$(( (file_size + max_part_size - 1) / max_part_size ))
              
              # 确保至少分割成1部分
              if [ $n -lt 1 ]; then
                n=1
              fi
              
              echo "文件大小: $(numfmt --to=iec-i --suffix=B "$file_size" 2>/dev/null || echo "${file_size}B")"
              echo "将分割成 $n 个部分"
              
              # 计算每段的时长（秒）
              local segment_time=$(echo "scale=2; $duration / $n" | bc)
              echo "每段时长: ${segment_time}秒"
              
              # 使用 ffmpeg 分割视频（复制流，避免重新编码）
              echo "开始分割视频..."
              ffmpeg -i "$local_file_path" -c copy -map 0 -segment_time "$segment_time" -f segment -reset_timestamps 1 "${base_name}_part%03d.${extension}"
              
              if [ $? -eq 0 ]; then
                echo "视频分割完成，开始上传分割后的文件..."
                
                # 上传分割后的文件到原目录
                for part in "${base_name}_part"*."${extension}"; do
                  if [ -f "$part" ]; then
                    rclone copyto "$part" "${remote_source}:${remote_path}/${part}"
                    echo "已上传: $part"
                    
                    # 验证文件已上传
                    rclone lsf "${remote_source}:${remote_path}/${part}" >/dev/null 2>&1
                    if [ $? -eq 0 ]; then
                      echo "✅ 验证成功: $part 已上传"
                    else
                      echo "❌ 验证失败: $part 上传可能有问题"
                    fi
                  fi
                done
                
                # 删除云端的原始大文件
                echo "删除原始文件: ${remote_source}:${remote_path}/${file_name}"
                rclone deletefile "${remote_source}:${remote_path}/${file_name}"
                
                # 清理本地文件
                rm -f "$local_file_path"
                rm -f "${base_name}_part"*."${extension}"
                echo "视频分割和清理完成"
                
                # 记录已处理的文件
                echo "$(date +%Y-%m-%d_%H:%M:%S) - ${remote_source}:${remote_path}/${file_name}" >> "$PROCESSED_FILES_LOG"
                
                return 0
              else
                echo "❌ ffmpeg 分割失败"
                return 1
              fi
            else
              echo "❌ 无法获取视频时长"
              return 1
            fi
          }
          
          # 分析错误日志，查找失败的大视频文件
          process_failed_videos() {
            local log_file="$1"
            local remote_source="$2"
            local task_name="$3"
            
            echo "分析错误日志，查找失败的视频文件..."
            
            # 从日志中提取失败的视频文件路径
            # 查找包含错误信息且包含视频文件的行
            local failed_videos=$(grep -i "error\|failed\|too large\|size" "$log_file" | grep -E "\.(mp4|mkv|mov|avi|flv|webm|m4v|wmv|mpg|mpeg)$" | head -20 || true)
            
            if [ -n "$failed_videos" ]; then
              echo "找到可能失败的视频文件:"
              echo "$failed_videos"
              
              # 提取具体的文件路径
              local file_patterns=$(echo "$failed_videos" | grep -oE "[a-zA-Z0-9_\-./]+\.(mp4|mkv|mov|avi|flv|webm|m4v|wmv|mpg|mpeg)" | sort -u)
              
              for file_pattern in $file_patterns; do
                # 清理文件路径
                local cleaned_path=$(echo "$file_pattern" | sed 's/^.*\///')
                
                # 尝试查找完整的文件路径
                local full_path=""
                
                # 根据任务名确定搜索路径
                case "$task_name" in
                  "backup")
                    search_paths=("backup")
                    ;;
                  "task0")
                    search_paths=("0")
                    ;;
                  "task2")
                    search_paths=("2")
                    ;;
                  "task3")
                    search_paths=("3")
                    ;;
                  *)
                    # 默认在所有可能的路径中查找
                    search_paths=("backup" "0" "2" "3")
                    ;;
                esac
                
                # 在可能的路径中查找文件
                for search_path in "${search_paths[@]}"; do
                  echo "在路径 ${remote_source}:${search_path} 中查找文件: $cleaned_path"
                  local found_file=$(rclone lsf "${remote_source}:${search_path}" --include "*${cleaned_path}*" 2>/dev/null | head -1)
                  
                  if [ -n "$found_file" ]; then
                    # 获取文件大小
                    local file_info=$(rclone size "${remote_source}:${search_path}/${found_file}" 2>/dev/null || echo "0")
                    local file_size=$(echo "$file_info" | grep "Total size" | grep -oE '[0-9]+' || echo "0")
                    
                    # 检查文件大小是否超过3.5GB
                    if [ "$file_size" -gt 3500000000 ]; then
                      echo "找到大视频文件: ${search_path}/${found_file} (大小: $file_size 字节)"
                      full_path="${search_path}/${found_file}"
                      break
                    fi
                  fi
                done
                
                if [ -n "$full_path" ]; then
                  echo "尝试分割大视频文件: $full_path"
                  
                  # 创建临时目录
                  local temp_dir="temp_video_split_$(date +%s)"
                  mkdir -p "$temp_dir"
                  
                  # 下载文件
                  echo "下载文件: ${remote_source}:${full_path}"
                  rclone copy "${remote_source}:${full_path}" "$temp_dir/"
                  
                  local local_file="$temp_dir/$(basename "$full_path")"
                  
                  if [ -f "$local_file" ]; then
                    # 分割视频
                    if split_large_video "$(dirname "$full_path")" "$remote_source" "$local_file" "$(basename "$full_path")"; then
                      echo "✅ 视频分割成功: $full_path"
                      # 发送成功通知
                      curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendMessage \
                        -d chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                        -d text="✅ 已自动分割大视频文件:\n${remote_source}:${full_path}\n文件大小: $(numfmt --to=iec-i --suffix=B "$file_size" 2>/dev/null || echo "${file_size}B")" \
                        -d parse_mode="Markdown" || true
                    else
                      echo "❌ 视频分割失败: $full_path"
                    fi
                  else
                    echo "❌ 下载文件失败: ${remote_source}:${full_path}"
                  fi
                  
                  # 清理临时目录
                  rm -rf "$temp_dir" 2>/dev/null || true
                fi
              done
            else
              echo "未找到可识别的大视频文件错误"
            fi
          }
          
          # 统一的错误日志处理函数
          sync_with_logging() {
            local source_path="$1"
            local dest_path="$2"
            local task_name="$3"
            
            echo "开始同步: $source_path -> $dest_path"
            
            # 生成带时间戳的错误日志文件名
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            LOG_FILENAME="${task_name}_sync_error_${TIMESTAMP}.log"
            
            # 执行同步命令，记录日志
            echo "同步日志将保存在: $LOG_FILENAME"
            rclone sync "$source_path" "$dest_path" \
              --progress \
              --ignore-errors \
              --verbose \
              2>&1 | tee "$LOG_FILENAME"
            
            # 检查同步结果
            SYNC_STATUS=${PIPESTATUS[0]}
            
            # 上传错误日志到onedrive根目录
            echo "上传日志到: onedrive:/$LOG_FILENAME"
            rclone copyto "$LOG_FILENAME" "onedrive:/$LOG_FILENAME" || echo "上传日志文件失败，继续执行"
            
            # 清理旧日志（最多保留7个），使用list命令避免删除错误
            echo "清理旧日志..."
            rclone lsf onedrive: --files-only 2>/dev/null | \
              grep "^${task_name}_sync_error_.*\.log$" | \
              sort > /tmp/log_list.txt 2>/dev/null || true
            
            if [ -f /tmp/log_list.txt ]; then
              LOG_COUNT=$(wc -l < /tmp/log_list.txt 2>/dev/null || echo 0)
              if [ "$LOG_COUNT" -gt 7 ]; then
                DELETE_COUNT=$((LOG_COUNT - 7))
                echo "需要删除 $DELETE_COUNT 个旧日志文件"
                head -n "$DELETE_COUNT" /tmp/log_list.txt | while read -r old_file; do
                  echo "删除旧日志: $old_file"
                  rclone delete "onedrive:/$old_file" 2>/dev/null || echo "删除旧日志失败: $old_file，跳过"
                done
              fi
              rm -f /tmp/log_list.txt 2>/dev/null || true
            fi
            
            # 发送通知
            if [ "$SYNC_STATUS" -eq 0 ]; then
              echo "同步成功: $source_path"
              curl -s -X POST https://api.telegram.org/bot'${{ secrets.TELEGRAM_BOT_TOKEN }}'/sendMessage \
                -d chat_id='${{ secrets.TELEGRAM_CHAT_ID }}' \
                -d text="✅ $task_name 同步完成" || true
            else
              echo "同步失败: $source_path (退出码: $SYNC_STATUS)"
              # 调用函数发送日志到Telegram
              send_log_to_telegram "$LOG_FILENAME" "$task_name" "$SYNC_STATUS"
              
              # 如果同步失败，尝试分析日志，处理失败的大视频文件
              echo "尝试分析失败原因，处理大视频文件..."
              process_failed_videos "$LOG_FILENAME" "onedrive" "$task_name"
            fi
            
            # 清理本地临时文件
            rm -f "$LOG_FILENAME" 2>/dev/null || true
            
            return 0
          }
          
          # 初始化已处理文件日志
          echo "=== 初始化已处理文件日志 ===" > "$PROCESSED_FILES_LOG"
          
          # 执行所有同步任务
          echo "开始执行所有同步任务..."
          
          # 同步 backup
          echo "=== 开始同步 backup 任务 ==="
          sync_with_logging "onedrive:backup" "openlist:aliyundriveCrypt/backup" "backup"
          
          # 同步 0
          echo "=== 开始同步 0 任务 ==="
          sync_with_logging "onedrive:0" "openlist:123panCrypt/0" "task0"
          
          # 同步 1
          echo "=== 开始同步 1 任务 ==="
          sync_with_logging "onedrive:1" "openlist:123panCrypt/1" "task1"
          
          # 同步 2
          echo "=== 开始同步 2 任务 ==="
          sync_with_logging "onedrive:2" "openlist:aliyundriveCrypt/2" "task2"
          
          # 同步 3
          echo "=== 开始同步 3 任务 ==="
          sync_with_logging "onedrive:3" "openlist:aliyundriveCrypt/3" "task3"
          
          # 同步 4 (如果被注释掉，可以取消注释)
          # echo "=== 开始同步 4 任务 ==="
          # sync_with_logging "onedrive:4" "openlist:baidupanCrypt/4" "task4"
          
          # 同步 5
          # echo "=== 开始同步 5 任务 ==="
          # sync_with_logging "onedrive:5" "openlist:baidupanCrypt/5" "task5"
          
          echo "所有同步任务完成！"
