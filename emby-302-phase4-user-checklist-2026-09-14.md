# emby 302 · Phase 4 用户实验清单（2026-09-14）

> 计划真源 `emby-302-remediation-plan-2026-09-13.md` §4。这是 Phase 4 里**唯一需要你本人做**的部分。
>
> 为什么非你不可：`[起播]` 出画探针测的是**客户端侧**（客户端建连 → 取首字节 → 缓冲 → 解码），
> 302 生效后视频流不再经过服务器，ge2o 一个请求都看不到。这段一直是空白，**必须有真实播放才产出数据**。

## 准备

- 地址：你平时用的 `e.<VD>.eu.org`
- 客户端：**你平时看片的那个**（手机 / 电视 / 浏览器都行）。换设备 = 换变量，测出来没意义
- 挑片：**挑一个本轮还没播过的条目** —— 探针按条目去重（`/tmp/pl-seen`），本轮播过的不会再启动
- 避开服务重启的约 14 分钟窗口（重启期间服务不可用；当前轮约 12:23 收尾、12:40 前后恢复）

## 步骤（约 10 分钟）

**1. 播一个视频**
点播放，**让它真的出画面**，出画面后**再播 30 秒以上**，别一出画面就退。
（探针会从该条目本轮第一次请求视频流起，最长盯 45 秒。）

**2. 等 TG 播放通知**
几秒到一分钟内会收到一条播放通知，里面有两块关键内容：
- `⏳ 起播等待`（树形）—— 服务器侧各段耗时拆解
- 底部 `▶ 打开直链`

**3. 把通知内容回贴给我**（截图或复制文字都行）。**这一步最关键。**

**4. 做直链对照（计划 4.2）**
同一台设备、同一个网络，点通知里的 `▶ 打开直链`，记录现象：

| 现象 | 结论方向 |
|---|---|
| 秒开 | 问题在 Emby / 播放器侧，继续查服务器 |
| 也卡几秒 | 客户端 → 微软 CDN 这条路由的问题，仓库侧无解 |
| 直链秒开但 Emby 要等 10 秒 | Emby 多了一次往返，按「起播等待」树逐项消 |

**5. 把第 4 步的现象也回贴给我。**

## 我拿到后会做什么

- 从那一轮的 run 日志里读 `playlog.log` 的 `[起播] item=N 客户端出画 +Xs`
  —— 这是「点播放 → 出画面」唯一能拿到的客户端侧数字
- 与 ge2o 分段耗时统计、`⏳ 起播等待` 树对齐，给出「这 10 秒里服务器占多少 / 客户端占多少」的归因
- 按计划 §6 P4 验收：**≥1 条 `[起播]` 读数 + 归因结论落文档**

## 可选（需要你明确授权才做）

计划 4.3 的 direct A/B：`gh workflow run emby.yml -f playback_mode=direct`。
它会让**视频流经 runner 中转**（真实流量走 runner，行为与旧版一致）。
不做也不影响 Phase 4 收口。

## 两个提醒

- 别为了测试特意换设备 / 换网络 —— 那会引入新变量，结论就不可比了
- 如果 45 秒内没出画面，探针会记「客户端出画未观测到」——
  **这也是有效数据**（说明卡在客户端侧），照样贴给我
