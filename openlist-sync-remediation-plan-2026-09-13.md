# openlist.yml 同步修复实施计划（2026-09-13）

- 代码基线：起步于 `5c01d32`（`task_engine.sh` 与被剖析的 run 34728107625 所在 commit 零差异，结论可平移）；**截至 2026-09-19 已落地到 `0b0a1aa`**（Phase 1 全量 + Phase 2 + 2026-09-15 九项 + 2026-09-19 F22/F10/F17，见 §进度日志）
- 依据：仓库根报告三份（`openlist-sync-glm-f-report` / `openlist-sync-feasibility-report` / `openlist-sync-assessment-ds4.1-report`，均为 2026-09-13）+ 对 HEAD 的代码核验 + 审核新发现（§1.4）
- **本文档是执行蓝图：与报告冲突时以本文档为准；本文档与代码冲突时现场查证并更新本文档进度。**

---

## 0. AI 接手须知（每次开工先读这一节）

**任务**：修 `openlist.yml` 网盘同步——让它从「每轮 5.5h 全被硬杀、零真实落盘、游标卡死在同一个后端」变成「能优雅到站、指标可信、开始真正搬数据」。判断标准只有 §6 的退出标准 A–E，别自创口径。

**⏱ 当前状态快照（2026-09-19 —— 交接/隔天回来先读这一段）**：

- **★ 一句话结论（2026-09-18 五次修订 + 2026-09-19 生产验证 —— 每版均被后续实测推翻/修正，本版有终轮生产实证）**：
  ① **「文件同步不了 / 修复也失败」→ 真堵点是「`API mkdir` 报 200 但目录不落盘」（假成功）**，
     而**不是** 409 语义、**也不是**"目录可写性预检误判"（后者已由主轮 `35308273431` 证伪，
     见 §12.14.6）。
     - 早先归因（作废）：把 `_fix_probe_dir_writable` 的写探针视为误判源
       （[file_fix.sh:865](file:///workspace/.github/scripts/openlist/file_fix.sh#L865)）。
       三态解耦（`296a3c3`）正是按此归因做的，但**生产实测一次都没触发**
       （`exists_but_readonly` = 0 次）⇒ 该归因**不成立**（§12.14.6）。
     - 当前证据：短哈希目录 `5/<深层>/5058f1af`（**全新名字**）经 `API mkdir HTTP_CODE:200`
       后 `lsd` 仍读不到 ⇒ 目录**根本没建出来** ⇒ 预检判 `unwritable` **是对的**。
     - ⇒ 堵点在**更底层**：`API mkdir` 的成功回报**不可信**。只要它成立，
       任何"换目录"兜底（短哈希 / 跳出子树）都是徒劳。
     - ✅ **矛盾已消解（§12.14.8，run `35322561888`）**：`5058f1af` **不是深层路径**，
       而是 `md5(".")[0:8]` 的确定性产物、落点就在 `dest_path` 直属
       （`openlist:wopan175/5/5058f1af`）⇒ 它与 `ol2p_*` **同深度同形状**。
       深度分层专项同轮实测：**历史名能建、同深度全新名能建、深度 +1 也能建，
       唯独"进生产 L2 子树 `5/1024j-视频-pornhub-channel/` 建"直接 409 Conflict**
       ⇒ **「深度」「名字」「时间窗口」三个猜想全部否定，坐实「路径局部（该 L2 子树坏）」**。
       ⇒ §12.14.3 与 §12.14.6 不矛盾：**差异是当轮窗口不同**，不是深度。
     - ⚠️ **新暴露的隐患（口径不一致）**：同一条坏子树，**OpenList 原生 API 报 `HTTP_CODE:200`
       （假成功）**，而 **rclone/WebDAV `mkdir` 报 `409 Conflict`（硬失败）**
       ⇒ 这解释了三态解耦为何一次没触发（它依赖 rclone/`lsd` 口径，生产走的是 API 兜底那条路）。
       **修法须先统一这个口径。**
     - ⚠️ 前三版结论作废（§C 的「后端健康时 100% 成功」仍成立，那是**不用写探针**的直传路径）。
  ② **「速度快不快」→ 瓶颈已确认是 wopan 网盘配额，非代码可解**（见下条定案）。
  ③ **「修复产物改回原名会不会消失」→ 不会**（`35239688191` E2 实测，三时点真值复核）。
     ⚠️ **但"改回原名"这一步本身曾写错子命令（2026-09-19 实测发现并修，见 §进度日志）**：
     还原的改名分支原用 `rclone move`，而 move 的 dst 被当目录 ⇒ 会建出
     `<原文件名>.mp4/<短哈希名>.mp4`。已全部改为 `rclone moveto`，并有
     `test_restore_real_local.sh`（真 rclone 实测，19 断言）锁住。
  ④ **【2026-09-18 新增】「mkdir 假成功」已定性为「特定路径个体问题」，非通用缺陷** ——
     这是本轮最重要的新增事实，它**改变了修法方向**（见下"下一步"）。

  🔒 **L2 假成功的最终定性（2026-09-18，两次独立同构实测）**：
  把故障路径 `openlist:wopan175/5/<L2 段>/aeon-marks` 拆成三层逐级探测，
  并在 `5/` 下用 4 个**互不相关**的名字做同构矩阵：

  | 实验 | 轮次 | 实测 | 判读 |
  |---|---|---|---|
  | L1(`5/`) mkdir+lsd | `35291770701` / `35295422027` | rc=0 · 409=0 · **lsd 存在=1** | 挂载根与第一层**健康** |
  | L2(`5/<L2 段>`) mkdir+lsd | 同上（两轮） | rc=0 · 409=0 · **lsd 存在=0** | ★ **mkdir 回报成功但目录不落盘** |
  | **新建 4 个无关顶层目录**（`5/ol2p_*`） | `35294896071` / `35295178274` | **4/4 真成功** · lsd 存在=1 · 409=0 | ★ **非通用缺陷：新建顶层目录本身没问题** |
  | 已存在目录写文件 | `35295178274` | rc=0 · 写入后 lsf 可见=1 | 「新目录建不出」≠「后端不可写」 |
  | L2 下兄弟目录（chilibomba/rolakiki） | `35291770701` / `35295422027` | 均 rc=1 · 409=1 · lsd 存在=0 | L2 故障**污染其整个子树** |
  | 末层名字 4 形态（原样/+后缀/ASCII/短哈希） | `35291770701` | **4/4 全 OK** | **与末层名字无关** |

  ⇒ **结论**：不是"该挂载下新建顶层目录一律假成功"（通用缺陷**已否定**），
  而是**特定的 L2 目录 `5/<L2 段>` 处于一种"mkdir 假成功"的坏状态，并把失败传染给其整棵子树**。
  同后端同一时刻，`5/ol2p_*` 能正常建、`5/<L2 段>/*` 一律 409 ⇒ **失败是路径局部的，不是挂载级的**。
  这与 §12.13.6「按原名拒收、改名即绕」的结论**方向一致但层次不同**：那次是**文件**层，
  这次定位到**目录**层，且同样不是"后端整体不可用"。

  ⇒ **下一步（按证据修正，勿再走弯路）**：
  - **不要再做**：① 继续加 409 语义分支（从未触达）；② 在"通用缺陷"假设下换驱动/换建目录通路
    （已被 `35294896071` 否定——同挂载新建目录是好的）。
  - ✅ **已做**：把「目录级故障」从「不可写」里**解耦出来** —— 三态化已落地（`296a3c3`，§进度日志）。
  - ⏸️ **暂停（2026-09-18，§12.14.5 取证后）**：**兜底目录跳出故障子树**。
    取证 run `35306329978` 实测 `dest_path` 自身**完全可写**、连"不跳出"的落点都建得出来
    ⇒ **生产故障形态（L2 坏）本轮未复现** ⇒ 「跳出 vs 不跳出」对照**没有判别力**，
    **不能据此改落点**（改了也无法验证）。探针 `diag_escape_probe.sh` 留作
    **故障复现当轮的取证工具**，届时再跑才有判决力。
  - **仍待回答**（本次未覆盖）：该 L2 目录为何进入"假成功"状态（网盘侧残留？配额？名字历史？）。
    但这属**追因**而非**修法**——修法只需"绕开坏子树"，不依赖追因结论。

  🔑 **★★ 修法优先级（2026-09-18 生产验证后重排 —— 原第一名已被证伪）**：

  ⚠️ **原"最高优先级：把『目录建不出』与『目录不可写』解耦"已降级 —— 生产实证它治不了病**
  （主轮 `35308273431` 首次验证，§12.14.6）：三态解耦**行为正确**（判 `unwritable` 是对的，
  因为那个短哈希目录**确实不存在**），但故障形态是「**兜底目录本身也建不出来**」，
  而三态只解决「**建得出但写不进**」⇒ **一次都没触发**（`exists_but_readonly` 全程 0 次）。
  ⇒ 该改动**保留**（正确性修复，对真·只读目录有效），但**不再是主攻方向**。

  🔑 **最高优先级：统一「mkdir 成败」的判据口径（API vs WebDAV）**（§12.14.8 新暴露）。
  同一条坏子树 `5/1024j-视频-pornhub-channel/`，**OpenList 原生 API 报 `HTTP_CODE:200`**
  （假成功，生产走的就是这条兜底路），而 **rclone/WebDAV `mkdir` 报 `409 Conflict`**（硬失败）。
  ⇒ 两条通路回报**互相矛盾** ⇒ "建成了没有"**无法判断**：
  三态解耦依赖 rclone/`lsd` 口径 ⇒ 生产全程未触发（§12.14.6）。
  - ✅ **已落地第一步（commit `4ebb39e`）**：4 处 API-200 分支统一接**存在性复核**
    （`_fix_dir_exists_or_conflict`）⇒ "API 200"不再被当作"建成"。
    ⚠️ **这不解决同步失败本身**（坏子树建不出就是建不出），修的是**判据可信度**
    —— 省掉 ~2 分钟白等、日志如实、并为下面"跳出故障子树"提供**可信判据基础**。
  - ⏭️ **下一步**：~~在可信判据之上做「兜底目录跳出故障子树」~~ ⇒
    ❌ **该修法已撤销**（§12.14.9）：核算证明**兜底落点 `dest_path/<md5(dir)[0:8]>` 本来
    就在 `dest_path` 直属**（与故障子目录**平级**，从不进入其内部）
    ⇒ "现有兜底落在同一棵坏子树里"是**错误命题**（文档里流传 6 处，源自一次未核算的推断）。
    **落点无缺陷 ⇒ 不改落点、不改还原链。**

  🔑 ~~**次高：兜底目录跳出故障子树**~~ ⇒ ❌ **已撤销**（§12.14.9）：
  §12.14.8 确实证明了「`dest_path` 可写、`L2 子树` 409」这一**对照成立**，
  但**兜底落点本来就在 `dest_path` 直属**，**无需"跳出"** —— 该修法针对的问题不存在。
  原表述"现有落点 `dest_path/5058f1af` 本身没问题…但须配套做"亦随之作废。

  ⚠️ **已作废的方向**：① ~~"兜底目录名带随机盐"~~（§12.14.7 推测，§12.14.8 变量①否定）；
  ② ~~"按深度分层找规律"~~（§12.14.8 深度猜想否定 —— 深度 +1 仍能建）；
  ③ ~~"查清 `API mkdir` 200 不落盘的成因"~~（降为独立追因课题，**不阻塞修法**）；
  ④ ~~"兜底目录跳出故障子树"~~（**§12.14.9 撤销：落点本就在 `dest_path` 直属**）。

  🔑 **③ 追因课题已推进到"可观测性"层（§12.14.10，2026-09-18）**：
  生产 `file_fix.sh` 两处 API 分支**把响应体丢弃**（只留 `tail -n 1` 的 `HTTP_CODE`），
  而 `diag_write_probe.sh` W2 **保留了 body** ⇒ **③ 在现有日志里信息论上不可追**（缺原始数据，
  非缺分析）。⇒ **最小动作**：生产两处 + pipeline 一处，把 API 响应体前 N 字节
  一并写进日志（纯增量、不动控制流，与 W2 同口径）。
  🔒 **但 ③ 已判定"不做"**（§12.14.11）：查清 API 200 的成因**不改变任何行动**（见下）——
  故该"最小动作"**暂缓，不再推进**（无行动价值）。

  🔑 **★★ 二选一已判死（§12.14.11，run `35334119443`）—— 落点无缺陷，是坏窗口**：
  本次在**真实兜底落点** `5/5058f1af` 上实测「mkdir + 写一个真文件 + 全路径直读」：
  **copyto rc=0 · 409=0 · stat=1 ⇒ 建得出**且**写得进**；E1a 历史名能建、E1b 全新名 3/3 真成功、
  E3 窗口稳定。⇒ **兜底落点、目录名、写入通路全都正常** ⇒
  **生产失败只发生在"坏窗口"**（§12.14.6 那轮），是**后端当轮窗口问题**，
  **不是代码缺陷、不是落点错误**。且**会自愈**（本轮即健康窗口，同一条落点完全可写）。
  - ✅ **对用户目标的含义**：文件同步能力**本身没问题**（健康窗口 100%、落点可写）。
  - ⚠️ **剩余风险 = 后端当轮窗口**：坏窗口整轮零产出，**代码无法挽救**。
  - ⇒ **正确策略转为「坏窗口期自我保护」**（F5/F6 熔断：识别 → 及早收手 → 下轮重试），
    **不再继续改代码追因**。与 §A 对「批次性假成功」的裁定同一逻辑。
  - ⚠️ **停止条件已明确**：③（API 200 成因）**不做** —— 查清也不改变行动（§12.14.11 方法论）。

  🔑 **★★ 快测复核（fix-check，§12.14.12，run `35336292127`）—— 端到端能修好，但口径需注意**：
  `openlist-fix-check` 12 分钟实测 **成功 1 / 失败 4**：
  - ✅ **成功那 1 个端到端实证兜底可用**：原目录 409 → API 200 被复核拦下（§12.14.8 修复生效）
    → base64URL 同样被拦 → 折叠 `2042c299` → 预检可写 → 落盘 → **重启后仍在**。
  - ⚠️ **4 个失败是 fix-check 口径产物，非生产 bug**（已核对到行）：fix-check 的**叶子单元**
    把 `leaf_rel` 剥成"叶子内相对路径"，当叶子就是文件所在目录时只剩文件名 ⇒
    `file_dir_rel="."` ⇒ 走根目录折叠（`5058f1af`）。生产不会这样 —— 缺失清单来自
    `rclone lsf -R`（**任务根相对、含完整目录**），`file_dir_rel="."` 只对应真正的根目录文件
    （有意设计、有测试覆盖：`test_hash_dir_fallback.sh` 场景 5）。
  - ⇒ **要测生产真实成功率仍须看主轮**；fix-check 是"能力上限"口径，不等于生产实测。

  🔑 **★★ 坏窗口的止损机制已补齐并经生产验证（§4 · F22 + F10 + F17，2026-09-19，run `35405370217`）**：
  既然"坏窗口代码救不了、只能自我保护"，本轮把**保护机制的缺口**填上：
  - **F22（写探针周期性重探）**: 探针的「可写」结论只在 t=0 有效（实测同一路径单文件
    顺序写全过、生产同路径 1034 文件全 405）⇒ 批次循环每开一批前按时间（默认 30min）
    清缓存重探。✅ **已验证：本轮实测 5 次重探，且恰逢坏窗口**
    （02:17 探针 ✅ → 02:25 重探 ✅ → 02:25–02:46 批次 9/9 假成功 → F5 熔断）
    —— **"探针通过 ≠ 后续写入成功"被生产实证**，正是 F22 存在的理由。
  - **F10（405 快速失败）**: 单批 405 计数达阈值（默认 20）即判"该目录本轮写不进"，
    先于巩固中止剩余批次。✅ **已验证判据正确**：本轮 `405 拒写` 触发 0 次，
    因为全部 80 条 405 都是**删除型**（`Couldn't delete`）——且已更正：它们来自
    **并发的 initial sync 清理**（77/80 是 `.nfo`，集中在 02:21 的 38 秒内），
    **不是还原阶段**（详见 §进度日志 2026-09-19 更正条目）；批次 copy 内 405 = 0
    ⇒ 该批走 F5 而非 F10，两者判据互补不重叠、分工正确。
  - **F17（预览 listing 短超时）**: 预览用专属超时 240s（原 900s）+ 失败快弃。
    ✅ 已生效（预览段约 6 分钟，无 900s 白等）。
  - ⇒ 三者都是**止损/省时**，不改变"坏窗口本身不可修"的结论（见上条）。
    本轮与 F5 协同完成「发现 → 止损 → 让路轮转」闭环。
  - 新增/变更单测：`test_write_reprobe_and_405.sh`（新，16 断言）、
    `test_batch_precheck_circuit_breaker.sh` G10/G11（+9 断言）、
    `test_backend_dead_signal.sh` 6a–6d（+4 断言）；本机套件 **32/32 全绿**、
    CI `tests.yml` **32/32 全绿**。

  ⚠️ **不要把 `OPENLIST_DRIVER_READY_WAIT` 调短**：生产已盲等 60s+10s=**70s**
  （[openlist_driver.sh:663-667](file:///workspace/.github/scripts/openlist/openlist_driver.sh#L663-L667)/[713](file:///workspace/.github/scripts/openlist/openlist_driver.sh#L713)），
  实测就绪只需 ~10s；调短会引入新的时序脆弱性。详见 §12.14.4。

- **同步链活跃**：主轮 `35239780581`（`70fc685`，含全部 409 修复）**failure · timeout 330min**
  —— 409 修复首次进生产轮，结论见 §12.14；
  此前 D4 验证轮 `35162246938` `success`（09-16 23:25 → 09-17 02:39）、
  schedule 轮 `35168662780` 亦 `success`；`35186977864` failure；
  主轮 `35211665935` 已按用户指示主动取消（让位诊断）。
  **cron `0 * * * *` 常态兜底**；手动跑
  `gh workflow run openlist.yml`（跑量用默认 320min；`sync_budget_min=60~90` 只适合验证）。
- **代码侧：已识别的失败形状全部修完**（Phase 0/1/2 + 09-15 九项 + 09-16/17 十余项 +
  09-19 F22/F10/F17）；回归 **CI `tests.yml` 32/32 全绿**（09-16 新增 `test_blacklist_crossrun_method2.sh`，
  09-19 新增 `test_write_reprobe_and_405.sh`，锁定「拉黑→跨轮→触达方法2」与「405 洪泛/重探到期」契约）、
  `command not found` 命中 0（flake 名单见 `AGENTS.md`；
  `test_progress_no_orphans` 的 T4 为已知时序 flaky，复跑即过）。
- **数据侧：仍在长跑**。近 12 轮合计 **72.8GB**（均 6.1GB/轮，方差大 —— 含多轮验证性短轮/取消轮）；
  最好一轮 **46.02GB / 63min**（`pair_parallel=2`）。剩余量最后一次量化是 **10.1TB**；
  `skip_preview=true` 后 trend 不再记 remaining ⇒ **当前没有更准的剩余量**。
  ⚠️ **2026-09-17 裁定：不再为此专门重测** —— 准数不改变任何行动（见下方剩余任务 A 表）。
- **★★ 速度瓶颈已确认（2026-09-17 定案，勿重走弯路）：瓶颈是网盘配额，不是代码。**
  - **实测值**：wopan 单挂载 **≈2.8 MiB/s**（≈10.5GB/h）；`transfers=6 × subdir_parallel=2
    = 12 流` 已是实测拐点，再提并发无收益。
  - **旁证一（并发已到顶）**：把并发翻倍不涨速，说明瓶颈在**对端给的带宽**而非本地流水线。
  - **旁证二（跨后端也一样）**：两后端并发实测**共享同一瓶颈且有害**
    （两后端合计 **0.50** < 单后端 **1.20** MiB/s，§12.8）⇒ 瓶颈不在单个挂载配置。
  - **旁证三（其他环节都不是瓶颈）**：`baidupanCrypt` ≈7 MiB/s、runner 出口 **25.86 MiB/s**，
    均远高于 wopan 的 2.8 MiB/s ⇒ 排除 runner / 本地磁盘 / 另一后端。
  - ⇒ **结论：调并发参数、换调度策略、优化代码路径都无法提速**。工期 6–8 周由 wopan
    配额直接决定，属于**外部约束**。
  - ⇒ **提速已无代码侧动作可做**：瓶颈在对端配额，本地任何优化都不改变速率。
    唯一的理论"省时"途径是减少重传白烧，而该项（批次性假成功）**已收敛并移除**（见 §C）
    ⇒ **本项无待办**。
- **工期（由最慢后端决定 —— 用户要求每个后端都有副本）**：wopan 单挂载 ≈2.8 MiB/s（≈10.5GB/h）
  ⇒ 10.1TB 纯传输 ≈44 天；叠加每轮 20–35% 非传输开销 ⇒ **6–8 周**（乐观 ~5 周）。
  其余环节均非瓶颈（依据见上条旁证三）。
- **剩余任务（2026-09-17 收敛后重写 —— 逐项核过，不再照抄旧状态）**：

  **A. 已消解 / 已完成（不必再做，勿再列为待办）**
  | 项 | 收敛结论 | 依据 |
  |---|---|---|
  | ~~① 分卷假成功~~ | ✅ **结案**：根因翻案（§12.12）→ Step A 判据修正+命名统一（§12.13.1）→ 定向诊断「按原名拒收、改名即绕」（§12.13.5 D1–D3）→ **生产主轮实证方法2 能落盘**（D4，run `35162246938`）。**Step B（7z 分卷）/ Step C（fs/put API）经数据裁定不需要**（方法3/4 出现 0/2 次） | §12.13 |
  | ~~② 重测剩余量~~ | ✅ **撤回**：知道 9TB 还是 10.1TB **不改变任何行动**——工期由 wopan 配额（2.8 MiB/s）卡死，量级与"继续/终止"判断均不变。属仪表盘需求，非工程需求 | 2026-09-17 用户裁定 |
  | ~~④ C 判据复核~~ | ✅ **不需要**：其前提 bug（折叠落盘列举因 `OPENLIST_RCLONE_LISTING_TIMEOUT` 裸数字而静默失败）**早已修复并加回归锁**（`1669` 行），其后多轮已自然跑过；且 C 已于 09-15 达标收口 | §12.6 / §6 |
  | ~~⑤ 吞吐补测 baidu~~ | ✅ **已达成**：`baidupanCrypt` 实测单对 57min 传 23.8GiB ≈ **7.1 MiB/s**（远高于 wopan 1.7–1.9）；可写性表（§7 F14）七项全 ✅ | §12.8 / §7 |

  **B. 等自然发生即可（不必专门投入）**
  - **③ F6 强证据门生产验证**：需真实"后端真死"样本，不可主动构造；已有 17 项测试覆盖
    （弱证据不落盘 / 本轮仍让路 / PERSIST 逃生口 / TTL 4h）。**等复现再验**。

  **C. 核心问题已解决实证（2026-09-17 定案 —— 用户最关心的「同步不了/修复也失败」）**
  - **结论：后端健康时已解决**（两连轮 `方法耗尽 = 0`、成功率 `100%`）。
    ⚠️ **重要限定（2026-09-17 补）**: 这不是"任何情况下都解决"——后端**目录层**故障时
    会整轮零落盘（`35186977864` 实测，根因 409 误判，见 §12.14 与 §0 一句话结论）。
    ⚠️ **限定补充（2026-09-19，run `35405370217`）**: 即便健康窗口也**存在约 0.6% 的
    间歇性持久化失败**（三组复核 `19/19`、`195/195`、`178/178 通过 177 / 失败 1`）
    ⇒ **"落盘成功"≠100% 绝对**。已由「失败清单 + 黑名单」兜住、下轮重传，
    **不造成永久损失，不做修法**（详见 §进度日志 2026-09-19 更正条目）。
    准确表述: **代码的修复能力已达标；剩余风险来自后端可用性，而非方法缺失。**
    | 轮次 | 修复管线 | 方法耗尽 | 成功率 | 批次真值 |
    |---|---|---|---|---|
    | `35162246938`（手动） | 尝试 7 · 失败 6 | **0** | **100%**（46/46） | 批次1: 81 成功 / 0 直接失败 |
    | `35168662780`（schedule） | 尝试 257 · 失败 0 | **0** | **100%**（247/247） | 批次2: **86/86 全部真实落盘** |
    - `方法耗尽 = 0` 是**唯一**代表"同步永远完不成"的判据（4 种方法 × 原目录与兜底目录全试过
      仍失败）。两轮均为 0 ⇒ **不存在修不好的文件**。
    - `本轮修复成效: 成功 247（新落盘 247 · 沿用上轮 0） · 缺失 45 · 未修复 0`（run 35168662780）
      —— 缺失 45 全部是**时间预算将尽、下轮接力**（`⏳ 时间预算将尽…下轮接力` 逐条可见），
      **不是失败**。这是排期问题，不是能力问题。
  - ~~**批次性假成功**~~ → **✅ 已移除，不再是待办项**（2026-09-17 定案）。
    它是**已收敛的成本项，不是未解故障**：机制清楚（某时段后端整体写不进 → 落盘未持久化 →
    重启后暴露 → 拉黑 + 换方法重试），已被**判据修正 + 跨轮黑名单 + 同轮换方法**三重兜住。
    **自愈闭环已实证**：`ph5f12a2558ec9e` 被 `拉黑 方法2·短名直传` 后，同轮
    `✅ 修复成功 · 方法4·分卷·短名` ⇒ 假成功**不再造成永久损失**，只多花一次重传
    （残余量 ~1 文件/轮，本轮全库仅 1 例）。
    ⇒ **不再追查"什么条件触发该时段"**：即便查清也必须重传，对工期的影响只是那一份文件的
    带宽，**不改变任何行动**（与 §A ②「重测剩余量」同一裁定逻辑）。

  **D. 已推迟（有理由，保持）**: ~~F10 405 fail-fast~~（**已于 2026-09-19 落地**，见 §4 · F10）、
  F13 僵尸 run 心跳、F12 后半（刷新就绪轮询）。

  **E. 小尾巴（低优先）**: V3 那 3 个 ph 文件（`ph5f12b2166785f` / `ph5c947a5f33a9f` /
  `ph59c40cb5598b1`）在 D4 轮日志中未出现（不在该轮缺失清单），归属待确认——可顺手查，
  不影响结论。
- **09-16 的三个关键产出**：① **修复能力验证通道** `openlist-fix-check`（首次实测那批"修不好"
  文件: **3 个其实已修好**、**3 个分卷假成功**）；② 挖出并修掉一个**生产 bug** ——
  `OPENLIST_RCLONE_LISTING_TIMEOUT` 注入裸数字被传给 rclone `--timeout`（要求带单位）⇒ 折叠落盘
  列举一直静默失败 ⇒ **折叠成果从未被记账**（幽灵落盘 + 每轮重传）；③ **分卷假成功根因翻案**
  （§12.12）+ 命名口径统一与判据修正（§12.13）。

**当前阶段**（2026-09-15 更新；更早的行保留供追溯）：

| 阶段 | 状态 |
|---|---|---|
| **2026-09-15 · 九项修复（今日）** | ✅ 全部落地并 push: 折叠落盘改延迟复核、F6 强证据门 + TTL 4h、`sync_budget_min` 短轮 + 派生阈值随预算缩放、失败传播两层修复（批次独立标志）、F9 批次字节进趋势、`transfers` 4→6、缓存刷新等待 60→40、**`pair_parallel` 默认 1→2**、对级字节记账、子目录级 sync 硬上限 |
| **当前生效的旋钮（接手先知道这些默认值）** | `transfers=6` × `subdir_parallel=2` = 12 流/挂载（实测拐点）· `pair_parallel=2`（两挂载并行，实测额度独立 ≈1.9×）· `sync_budget_min=320`（验证用 60）· `fs_refresh_sleep=40` · `skip_preview=true` |
| **实测天花板（2026-09-17 定案）** | 单挂载 12 流 ≈1.8–2.8 MiB/s；两挂载并行合计 ≈3.4 MiB/s；**runner 出口 25.86 MiB/s（不是瓶颈）**；`baidupanCrypt` ≈7 MiB/s（不是瓶颈）⇒ **瓶颈锁定在 wopan 网盘配额**。剩余 10.1TB 仍需**数周**后台长跑（6–8 周）。**结论：非代码可解，调参/换调度均无效**（详见 §0 速度瓶颈定案） |
| **退出标准 A–E 现状** | A/B/D/E **实质达标**、C **达标（2026-09-15 收口）** ⇒ **A–E 目前全部达标**（依据见 §6 与 §12） |
| Phase 0 解堵 + 基线 | ✅ 完成 |
| Phase 1（F0–F5） | ✅ 全部落地并 push，**已在生产验证**: run 34770092689 首次拿到非硬杀的 success，F1/F5/F0/F4 实锤生效（逐条证据见 §6） |
| Phase 2 第一批（F6/F7/F8/F11） | ✅ 落地（`e5296ed` / `3737688`），**已多轮生产验证**（F7 新落盘口径、F8 源端失败记 unknown、F11 SESSION_HOLD 兜底均已生效） |
| F6 触发源接线（F5 全拒 → `SYNC_BACKEND_DEAD`） | ✅ 落地 `acbbff5`（**2026-09-14 补 push**，此前只在本机 commit、origin/main 没有，见进度日志）；把熔断点从 ~2.5h 提前到批次 1 巩固结束，见 §4 · F6。**已生产验证**：run `35405370217` 实测 F5「9/9 全拒」触发后正常标注失败并让路轮转 |
| Phase 2 剩余 | F9 **前提已核实、范围收窄**（见 §4 · F9）；~~F10~~ **已于 2026-09-19 落地**（§4 · F10）；F12 后半 / F13 **评估后推迟**（理由见各自小节） |
| F3 第二步 | 根目录文件的短哈希兜底 —— ✅ **已落地（2026-09-16，`2313423`）**：原判"从未复现、收益存疑"**已作废**（那是修复管线拿不到预算时的观测；留出修复尾段后实测 `无目录可换` **150 次/轮**，且占该轮修复失败的绝大多数），见 §4 · F3 与 §12.10 |
| **F21 写探针判死信号键错位** | ✅ 落地（2026-09-14，本轮）。**这是「死后端钉住游标 ≈44h」的直接成因**，修完判死信号才真正到得了轮转/跨轮熔断，见 §4 · F21 |
| **F19 8005 重试前写探针短路** | ✅ 落地（2026-09-14，本轮）。后端真死时单次重试实测烧 48min ⇒ 探针不过就收手，见 §4 · F19 |
| **F20 验证起跑规程（规程，非代码）** | ✅ 写进 §0 开工四步第 5 步：队列恒 1 深（GitHub 自动顶掉被取代的 pending），延迟上限 ≈6.3h，要更快就主动取消在跑轮 |
| Phase 3 | F14 三后端写入自检起；**Gate 1 已逼近**（见 §7）。**F16 已按用户指示取消**（后端一个都不削减），**F15 不再需要批准** |

**开工四步**（一个观察周期 5.5–6h）：

1. `git fetch` 看落后数；`gh run list --workflow=openlist.yml --limit 5 --json databaseId,status,conclusion,event,createdAt,headSha` —— **先核对 `headSha` 是不是你要验证的那版代码**：schedule 轮钉的是「创建那一刻」的 main sha，新 push 只影响之后创建的轮次。
2. `gh run view <id> --log > /tmp/ol.log`（in_progress 的取不到日志，等结束再取）后按 §6 关键词 grep。
3. 本机 `rclone cat onedrive:/logs/sync_state/{task_rotation.json,trend.jsonl,backend_dead.json}` 看游标 / 趋势 / 跨轮熔断命中。
4. 对照 §6 记录增量 → **更新本文档的复选框与 §进度日志**。这是唯一的跨会话进度真源（`.codebuddy/memory/` 是本机私有记忆，不在 git 里，别的 AI 读不到）。
5. **验证起跑规程（要立刻验新代码时用）**：GitHub 的 concurrency **会自动取消被顶替的 pending 轮**（实测 `34787645966` 被 `34793014398` 顶掉即此行为）⇒ 队列**恒为 1 深、且总是最新创建的那个**，不会积压成"落后 N 轮"。新代码进生产的真实延迟 = **下一次 cron（≤1h）＋ 在跑轮剩余时间（≤5.3h）**，上限约 6.3h。要压到分钟级就主动干预：先 `gh run list --workflow=openlist.yml --status pending --json databaseId` 取 id 逐个 `gh run cancel`，必要时再 `gh run cancel <在跑的 id>`（在跑轮若正烧在死后端，取消它损失极小），最后 `gh workflow run openlist.yml` 用"此刻"的 main 起跑。注意取消在跑轮会丢掉该轮已完成但未持久化的进度（游标/marker 是增量持久化，损失有限）。
6. **修复能力验证载体（2026-09-16 起）**：要回答"某个文件到底能不能修好"，走
   `gh workflow run openlist-fix-check.yml`（独立 workflow，见 §12.11）—— 定点、分钟级、
   真值复核、逐文件 `VERDICT` 行；回归测试仍走 `tests.yml`，后端诊断仍走 `openlist-diag.yml`。
7. **两种"不等 5.5h"的取日志方式（2026-09-15 起，用户要求）**——验证一律先用它们，长轮只用于跑量：
   - **调试轮（视任务规模，10min–2h）**: `run_mode=调试 · 修复管线测试` + `fix_test_task=<task>` +
     `fix_test_max=<n>` + `force_sync=true` —— 只跑单任务: 逐子同步对 diff → 修复管线
     （**含目录级批量折叠**，它跑在 fix_max 截断之前）。
     **实测（2026-09-15 · task3）: 不是"几分钟"** —— 两侧递归列举（源端 OneDrive + 目标端
     crypt）本身就要十几到几十分钟，且会遍历该任务的**全部**子同步对（`fix_max` 只约束
     逐文件条数，不限子同步对数量；step 超时 120min）。强项是跳过 initial sync 传输直达修复，
     适合验证修复管线/折叠/落盘记账；**拿不到**批次/吞吐/熔断数据（`OPENLIST_FIX_TEST_MODE=1`）。
     **要真正的分钟级就选窄任务**: 注册表里有单子目录对（如 `task0-wopan175` =
     `onedrive:0/j-1024j-视频-pornhub-favorites` → `openlist:wopan175/0/...`），列举量小、
     几分钟出结论；大任务（task3/task4）光列举就 30-60min+。
   - **小时级**: `sync_budget_min=<分钟>`（默认 320，上限 320）—— 完整链路、预算缩短（如 60）。
     派生阈值（批次最小片长 / 尾部预留）随预算等比缩放（`task_engine.sh _budget_scaled`），
     所以短轮仍会正常开批次、走完整链路。代价: setup(~4min) + 收尾(~8min) 固定开销占比升高
     （60min 轮约 20%，320min 轮约 4%）⇒ **跑量用 320，验证用短轮**。
   - 两者都先 `gh run cancel <在跑轮>` 腾并发位，否则会 pending 到长轮结束。

**红线**（§8，无例外）：run_mode 只允许「同步」与「调试 · 修复管线测试」；动通知必跑 `bash skills/telegram-notify-audit/scripts/render_preview.sh`；改完跑全套串行回归（数量会变，不写死；判据「除环境假红外全 `EXIT=0`」） + 全部日志 `grep "command not found"` 必须为空；push 前 `git fetch` 并更新本文档。

**必须问用户的**（AI 权限外，见 §9）：**wopan176 的 OpenList 驱动登录令牌是否需要人工重抓**——2026-09-14 用户已答「账号状态正常」，但 8005 是 **OpenList 驱动层登录令牌**失效，与网盘账号是两回事（详见 §4 · F19 与 §9 第 1 项）。原 §9 的 F16 主副本选型**已取消**（用户要求后端一个都不削减），F15 `transfers` **已授权自行调整**。

---

## 1. 问题总览

### 1.1 三份报告共识（可直接采信）

1. **接力机制健康**（`session_hold`/`self_retrigger` 已修好并验证），marker/游标增量持久化无丢失。
2. **近 12 轮 0 success**；近 4 轮同步 step 耗时精确 330m12s±1s ⇒ 全部被 `timeout-minutes: 330` 硬杀，320min 预算优雅到站从未发生。直接根因：**文件批次循环没有预算闸**（`task_engine.sh` L1459 起，核验属实）。
3. **wopan176Crypt 是主故障后端**，两类失败并存：
   - 硬失败：密文名 >100B（wopan 实测上限）→ 405。文件级超名 24%，目录级超名 74%（父目录超长连坐短名文件）。
   - 软失败（假成功）：rclone 报 `Copied`、重启容器取真值后零落盘。跨轮重复率 32.3%，同一文件 24h 内「成功复制」9 次仍缺失。
4. **熔断粒度过粗**：405 路径特异性被提升为后端级熔断（`OPENLIST_BACKEND_DEAD_THRESHOLD=3`），短路了本可生效的短哈希目录/zip 分卷兜底；写探针探挂载根+lsf 读回不过缓存（假阳性+假阴性并存）。修复率 8.6% 且 183 全是「沿用上轮」。
5. **轮转游标钉死**：`task_rotation.json` cursor=8（task2→wopan176Crypt/2）attempts=6，上限 8（≈44h）；另外 15 对近四轮零执行，**baidupanCrypt / wopan175 / aliyundriveCrypt 可写性零证据**。
6. **指标双失真**：`remaining_bytes` 源端列举失败静默归零（单轮假降 2.49TB）；`transferred_bytes` 强杀丢内存计数（同值重传 churn：18282971×3、171257627×2）。
7. **规模与速率**：源端去重 4.12TiB / 16 对 10.1TB（2.25 倍冗余）；实测净传均值 ≈334MB/轮 ⇒ 均值口径 14.5 年、最好轮 2.9 年。**当前路径下不可能完成。**

### 1.2 报告审核裁定（矛盾处怎么采信）

| 矛盾点 | 报告立场 | 裁定 |
|---|---|---|
| 405 成因 | ds4.1「全路径拒写、与名长无关」vs glm-f/feasibility「路径特异性（名长）」 | **采信后者**。ds4.1 的关键证据（22B 文件名照样 405）被 feasibility 的交叉验证推翻：391 个 22B 文件失败源于**父目录名超长**，ds4.1 把目录级超名误读成了「名长无关」 |
| 短哈希目录可写性 | ds4.1「折叠 Copied 但零落盘 ⇒ 后端全死」 vs glm-f「折叠被熔断没实测过，大概率可写」 | **已裁决（2026-09-14，run 34770092689）：采信 ds4.1 的「后端全死」结论**。F3 第一步生效后，目录级批量折叠**真的被执行了**——`19:32:57 🔀 …（44 个文件）→ 短哈希目录 f22c8c73`、`19:36:03 🔀 …（39 个文件）→ 短哈希目录 cb4f0d57`，合计 83 个文件；两次都在 `19:39:01` 熔断（连续 3 个目录不可写）**之前**，即**未被熔断短路**。结果两处都以 `❌ 批量折叠零落盘（rc=0），该目录退回逐文件修复` 收场（19:34:39 / 19:37:37）。⇒ **「缩短名字即可绕过」在真实路径上被直接证伪**：折叠后的 8 字符目录名拿到了实测写尝试、rclone 报 rc=0，后端照样零落盘。名长不再是必要解释，主因是后端整体不可写（与 §1.4 F0 的 transfers 错位叠加） |
| 能否完成 | ds4.1「永不」 vs glm-f「机制能/速率不能」 vs feasibility「修完 P0 约 1.5–2 个月」 | 分解后一致：后端账号可恢复 + P0 修复 + 冗余削减 ⇒ 月级；账号不可恢复 ⇒ 必须切后端（Phase 3）；都不做 ⇒ 等于永不 |
| 质量排序 | — | feasibility 交叉验证最严谨，作主干；glm-f 的速率/预算数学最好；ds4.1 的单轮取证与缺陷清单（缺陷 3/5/6/7/8）全部有效，但顶层结论「全路径拒写、运维层故障」**过推，不采信** |

### 1.3 审核新发现（三份报告都漏掉，已核验代码）

**F0 · 批次路径 transfers 变量错位（疑似假成功主源，优先级最高的一行改动）**

- `openlist.yml:185` 设置 `OPENLIST_TRANSFERS: ${{ inputs.transfers || '1' }}`（输入默认 1，描述明确「wopan176 保持 1——run 32749862280 整批假成功事故后端」）。
- 但批次 copy（`task_engine.sh:1489`）与巩固串行重试（`:1095`）读的是 **`OPENLIST_TARGET_TRANSFERS:-4`**——该变量全库无人设置 ⇒ **批次路径实际一直 transfers=4**，而日志（`:1495`）硬编码打印「transfers=1」掩盖了这一点。
- 批次循环头注释（L1480-1483）自述：「批次 copy 此前无 transfers 限制（默认 4 并发上传），慢后端来不及持久化 → object not found / PUT 假成功——正是批次 exit=4 与预览差值不动的直接诱因之一」。**即低并发保护加错了变量名，从未生效。**
- 这解释了 34728107625 的形态闭环：批次 copy transfers=4 假成功 → 巩固发现 1037 缺失 → 串行重试也 transfers=4 假成功（`retry_copied=1037`）→ 重启取真值仍 1037 缺失 → `BATCH_BACKEND_DEAD` 判据要求 `retry_copied -eq 0` 恒不触发 → 照常开批次 2/3。
- 注意：`sync_engine.sh:195`（初始 sync）与 `file_fix_pipeline.sh:412`（折叠）读的是正确的 `OPENLIST_TRANSFERS`，只有 task_engine 的两处错位。

### 1.4 本计划不解决的事

wopan176 账号本身的配额/限流/封禁/token 状态——需用户登 OpenList 管理面或网盘账号后台确认（§9）。100B 上限与假成功可能同源于此。

---

## 2. Phase 0 · 解堵与基线（无代码改动，立即执行）

- [x] **0.1 取消僵尸 run**：2026-09-14 00:15 +08 已 cancel `34752801560`（in_progress 5h29m）与 `34763535089`（pending 1h31m），两者均 completed/cancelled。副作用: 自续触发判定「人工取消，不接力」，接力链中断。
- [x] **0.2 基线快照**：已存 `/tmp/openlist-remediation-baseline.txt`（task_rotation cursor=8/attempts=6、trend.jsonl 末 3 条、近 5 轮结论、run 34752801560 六项取证）。
- [x] **0.3 不手动触发新同步**：Phase 1 落地前未触发。Phase 1 全量 push 后（2f60f26）于 2026-09-13 16:56Z 手动 dispatch `34770092689` 开观察轮——接力链已断且 cron 1h 未见新 run。

## 3. Phase 1 · P0 修复（恢复优雅到站 + 熔断准确，逐项 commit）

完成本阶段全部条目后统一跑 §8 回归再 push。

### F0 批次路径 transfers 统一（一行改动，先做）✅ 已落地（339d58a）

- 位置：`task_engine.sh:1095`、`:1489`。
- 改法：两处 `OPENLIST_TARGET_TRANSFERS:-4` → `OPENLIST_TRANSFERS:-1`；`:1495` 与 `sync_engine.sh:200` 的日志改为打印实际值（`transfers=${_ol_transfers}`），消除硬编码烟幕；同步修正 L532-535 注释。
- 验收：日志出现真实 transfers 值；下一轮 wopan176 批次的顽固缺失数显著下降（对照基线 1037）。
- 风险：极低。串行重试本来就是「串行兜底」语义，transfers=1 与其本意一致。

### F1 批次循环预算闸（近 4 轮 failure 的直接根因）✅ 已落地（339d58a）

- 位置：`task_engine.sh` `sync_by_file_batches` 批次循环（`:1459` `for i in $(seq 0 $batch_num)`，`if [ -s "$bf" ]` 块内、批次启动前）。
- 改法：
  1. 循环头加 `sync_budget_stop && { SYNC_TIME_EXHAUSTED=1; break; }`（与同步对级 `:171` 同款文案「优雅收摊」）。
  2. 批内修复管线置位的 `SYNC_TIME_EXHAUSTED=1` 必须阻止下一批开启：循环头加 `[ "${SYNC_TIME_EXHAUSTED:-0}" = "1" ] && break`。
  3. 新增 `OPENLIST_BATCH_MIN_SLICE_SECONDS`（默认 3600）：批次的最小工作片。600s 假设已被「一批 = copy+巩固+修复 ≈ 1h+」实测推翻（批次 1 实测 1h17m）。其他层仍用 600s。
- 验收：新增测试用例（deadline 已过 → 不开新批、优雅返回、SYNC_TIME_EXHAUSTED=1）；下一轮日志出现「时间预算将尽，优雅收摊」且 conclusion=success。
- 风险：闸过紧导致零批开跑——320min 预算下预留 60min 片长不会误伤正常批次。

### F2 逐文件修复循环熔断短路（单轮省 ~119min）✅ 已落地（339d58a）

- 位置：`file_fix_pipeline.sh:762-767`（时间预算检查旁并列）。
- 改法（ds4.1 草案可直接用，`_fix_backend_root_of` 在 `file_fix.sh:725` 已有）：
  ```bash
  local _fix_be_root; _fix_be_root=$(_fix_backend_root_of "$dest_path")
  if [ "${_BACKEND_DEAD[$_fix_be_root]:-0}" = "1" ]; then
    echo "🛑 后端 ${_fix_be_root} 本轮已熔断，跳过剩余 $((_cb_total - _cb_done)) 个文件" | tee -a "$LOG_FILENAME"
    SYNC_BACKEND_DEAD=1   # 注意：不要置 SYNC_TIME_EXHAUSTED——「预算到点」与「后端故障」两种出口语义不得混用
    break
  fi
  ```
- 验收：单测（预置 `_BACKEND_DEAD[root]=1` → 循环立即退出且 `SYNC_BACKEND_DEAD=1`）。

### F3 熔断与名长解耦（修 8.6% 修复率，本阶段最复杂）✅ 第一步已落地（2f60f26）

- 位置：`file_fix.sh:819-833`（后端熔断计数）+ 名长诊断消费路径。
- 原则：
  1. 名长诊断已命中「密文名 > 后端已接受最长（100B）」的目录/文件，属**内容性**失败：不进入 `_BACKEND_DEAD` 连续计数，直接走短哈希目录折叠 / zip 分卷等替代形态（不被熔断短路）。
  2. 熔断计数只收集「短名（含折叠后的 8 字符哈希目录）也写不进」的情形。
  3. 补「文件位于目标端根目录、无目录可换」死角：根目录文件也有短哈希兜底目录。
- 验收：调试模式对 wopan176 跑含超长名的任务（`fix_test_max=20`）：短哈希目录有**实测写尝试**（日志见 rclone 调用而非「熔断推断」）；修复成功率（新落盘口径）> 8.6% 基线。
- 风险：改判定最深的逻辑。建议两步走：先做「不计数+不短路」（小 diff），根目录兜底第二步再做。

**第一步验收已达标（2026-09-14，run 34770092689 实测）**：短哈希目录拿到**实测写尝试**——两次目录级批量折叠（44 + 39 = 83 个文件）都落在熔断（19:39:01）**之前**，日志见 `🔀 目录级批量折叠 … → 短哈希目录 f22c8c73 / cb4f0d57` 及其后的 rclone 调用，不是「熔断推断」。修复成功率仍未达标的**原因已不在本项**（折叠路径自身也零落盘，见 §1.2 裁决行）。

**第二步（根目录短哈希兜底）本轮评估后推迟 —— 该死角从未真实复现**：

- 缺口位置确认无误：`file_fix.sh:1023-1027`，`file_dir_rel = "."` 或空时直接 `_HASH_DIR_FAIL_REASON="无目录可换"` 并 `return 1`。
- 但 `run 34770092689` 全量日志里 `无目录可换` **出现 0 次** ⇒ 根目录文件这一形态在本轮 1036 个触碰文件里一个都没走到。
- 收益亦存疑：根目录文件唯一可换的元素就是**文件名**，而方法 2 `copyto_shorthash`（`file_fix.sh:937-`）已在同一目录里把文件改成 8 字符哈希名；再套一层短哈希**目录**只会让路径**更长**（`<dest>/<原名>` → `<dest>/<8hex>/<原名>`），与「压短路径」的目的相反。只有当「目标端根目录不可写、其下新建子目录却可写」这种反常后端行为成立时才有收益，目前零证据。
- **重启条件**：日志里真正出现 `无目录可换`（或根目录文件其余方法全败）再落地，并同步补单测。现在动手属于「为不存在的问题加分支」，违反 §6 迭代纪律。

### F4 写探针分层（探针判死即整轮省 5.5h）✅ 已落地（2f60f26）

- 位置：`openlist_driver.sh:380-434` `_backend_write_probe`。
- 改法：1) 探针目标从挂载根 `${root}/${probe_name}` 改到**真实任务子路径**（`${dest_path}` 自身或其一级子目录）；2) `:409` 的 lsf 读回前先调 OpenList `/api/fs/refresh` 刷新缓存（否则 PUT 假成功活在缓存里，读回拦不住——该函数注释声称能拦，实测拦不住）。
- 验收：探针结论按（后端 × 路径深度）分层记录；34728107625 形态（探针通过 89s 后即大量 405）不再出现。

### F5 后端写入全拒判据（假成功下恒不触发）✅ 已落地（339d58a）

- 位置：`task_engine.sh:1118-1133`。
- 现状：判据要求 `retry_copied -eq 0`，假成功时 `retry_copied=1037` ⇒ 恒假（该轮 grep「后端写入全拒」= 0）。
- 改法：判定下移到 stubborn 算出之后（`:1139-1151`），判据改 `stubborn_n >= touched_n && touched_n >= 3`。**保留语义**（L1125-1127 用户规格）：熔断只中止剩余批次，本批顽固缺失仍转修复管线，不豁免。
- 验收：同形态轮次在批次 1 巩固后立即中止批次 2/3（省 ~80min），单测覆盖「假成功（retry_copied>0 但 stubborn==touched）」用例。

---

## 4. Phase 2 · P1（熔断传播 + 指标可信 + 防僵尸）

### F6 后端熔断跨轮持久化 ✅ 已落地（e5296ed）+ ✅ 加固（5e51f55，2026-09-15）

**加固：判死信号分级 + TTL 收敛**（用户同意）。起因是一次实测**误判污染**：4af1cbc
回归期间，修复管线的目录级熔断（连续 3 个目录不可写）把**健康**后端 `wopan175` 判死
（同一后端 A 轮刚真实搬了 13.08GB），经 F6 写进 `backend_dead.json` 后其 6 个同步对被
整体跳过 12h —— **一次误判的代价是整个后端停摆一个 TTL 周期**。

- **只允许强证据跨轮持久化**：
  - 强 = 写探针判不可用（真实 PUT + 刷新服务端缓存复核 / 重启后复核，3 处）
        + 批次巩固「后端写入全拒」F5（容器重启后复核、触碰文件 100% 未落盘）
  - 弱 = 修复管线目录级熔断（依赖"探针文件可见性"，列表未就绪时假阴性 = 本次成因）
  - 弱证据**仍本轮熔断让路**（原设计不变），只是不写 `backend_dead.json`；
    `OPENLIST_BACKEND_DEAD_PERSIST=all` 为强制持久化的逃生口。
- 新增 `SYNC_BACKEND_DEAD_STRONG`，与 `SYNC_BACKEND_DEAD` 一起在每对开始前重置
  （跨对泄漏会让弱证据借上一对的强证据落盘）。
- **TTL 12h → 4h**（`OPENLIST_BACKEND_DEAD_TTL` 默认 14400）：4h 仍覆盖"暂态故障 +
  一轮观察"，但误判最多影响下一轮前半段。
- 测试扩到 **17 项**（弱证据不落盘 / 本轮仍让路 / PERSIST 逃生口 / TTL 默认 4h；
  并给场景 2/3/4/5/6 补 `STRONG=1`——否则会因"文件根本没写"而假通过）。
`sync_state/backend_dead.json`：熔断时写挂载根+时间戳+TTL（默认 12h，死后端常是暂态：登录/限流）；下轮 task_engine 入口读取、跳过该后端的同步对（游标后移）。边界：全部 OpenList 后端都死 → 忽略跳过标记并告警（不许全线停摆）。收益：游标从「44h 才让路」降到 1 轮。

**F6 补充 · 触发源接线（✅ 已落地 `acbbff5`，2026-09-14 补 push；2026-09-19 生产验证）**：F6 原先只认 `SYNC_BACKEND_DEAD=1`，而该标志的两个来源都不好用——① 入口写探针（`sync_engine.sh:152`）在 run 34770092689 里**通过**了（探针打的是任务子路径、后端仍照收 405/假成功）；② 修复管线的目录级熔断（`file_fix_pipeline.sh:781`）同轮实测晚至 19:39、开跑约 2.5h 后才触发。于是"整批 1036 个触碰文件零落盘"这种最硬的证据反而不会让后端进入跨轮熔断。现把 F5 的「后端写入全拒」判据（`task_engine.sh` `_batch_consolidate`，`_truth_confirmed` + `stubborn_n >= touched_n >= 3`，即容器重启复核后本批 100% 未落盘）同时置 `SYNC_BACKEND_DEAD=1`，熔断点从 ~2.5h 提前到批次 1 巩固结束（实测 1h17m）。语义不变：本批顽固缺失仍转修复管线（F5 既有用户规格）。

### F7 修复成效拆分（让零产出告警真正生效）✅ 已落地（3737688）
`file_fix_pipeline.sh:1126-1136` 的 `add_ok` 含「沿用上轮修复」→ 拆 `ROUND_FIXED_NEW`（本轮新落盘）/ `ROUND_REUSED`（沿用）；`openlist.yml:503` 告警判据只看 NEW。收尾通知两数分列展示。

### F8 源端清单失败 → remaining 记 unknown ✅ 已落地（3737688）
`task_preview.sh:137-143`（源失败静默 `src_json="[]"`）→ 加 `PREVIEW_FAIL_SRC_PAIRS` 计数 + 与目标端同款通知提示；`sync_trend.sh:50` 任一源端失败时写 null，不参与求和。趋势曲线不再出现 TB 级假进展。

### F9 字节计数落盘 ✅ 最小修复已落地（6056952，2026-09-15）
记账点 `task_engine.sh:316`、`:869` 改为即时追加 `/tmp` 计数文件（强杀后收尾仍可读）；顺带排查同值重传 churn（同批内容轮轮重传）。

**前提已核实（2026-09-14，部分不成立，改动范围需收窄）**：趋势口径的记账**本来就是即时落盘**——`sync_trend.sh:42-46` `trend_record_transferred` 每次调用即 `echo >> /tmp/ol_trend_transferred.log`，收尾 `trend_record_and_notify` 只做 `awk` 求和（`sync_trend.sh:81-84`），所以"强杀丢内存计数"对 trend 这条链**不成立**（硬杀轮的 trend 样本确实写出来了，见 §6 · C）。F9 真正剩两件：
1. **核对覆盖面**：确认所有传输路径都调了 `trend_record_transferred`（已知 `task_engine.sh:437`、`:1001`；批次路径 `:1738` 只把字节累加进 `batch_transferred_bytes` 供进度行展示，是否另需记入趋势待查）。
2. **churn 已确诊（新证据）**：`trend.jsonl` 里 `transferred_bytes` 出现**逐字相同的重复值**——`18282971` 连续三轮（09-11 11:36 / 09-11 21:45 / 09-12 06:22）、`171257627` 连续三轮（09-13 05:18 / 10:53 / 16:17），而同期 `remaining_bytes` 摆动达 2.5TB。被 330min 硬杀的三轮不可能凑出字节级完全一致的真测量 ⇒ **同一批内容每轮被重新"传输"一次**（rclone 照报 Copied、后端零落盘），与 §6 · C「transferred 2.08GB vs 真实落盘 0」同一根因。**结论：`transferred_bytes` 是"rclone 声称量"，不是"落盘量"**，C 达标必须换判据（如用巩固/修复管线的新落盘计数），不能靠修 F9 的落盘时机。

**覆盖面已核实（2026-09-14 第三轮；结论：批次路径整体不计入趋势）**：

- 全库 `trend_record_transferred` 调用点只有两处：`task_engine.sh:437`（`_sync_task_finalize` 尾部，取 `SYNC_TRANSFERRED_BYTES`）与 `:1001`（顶层最终完整同步尾部）。**批次路径没有任何一处调用它。**
- 机制（已核到行）：`sync_by_file_batches` 把每批字节累加进 `batch_transferred_bytes`（`:1749`），但**只**喂给进度行的 `⬆️` 字段（`:1820-1823`），从不回写 `SYNC_TRANSFERRED_BYTES`；函数尾部（`:1855-1862`）为省一次全量扫描把 `OPENLIST_FIX_TEST_MODE` 临时置 1 再调 `sync_with_logging`，而该模式会 `: > "$LAST_ATTEMPT_LOG"` 跳过真实传输（`sync_engine.sh:170-174`）→ 日志为空 → `SYNC_TRANSFERRED_BYTES=0`（`sync_notify.sh:197`）→ `_sync_task_finalize` 记 0。
- 结论：趋势里的 `transferred_bytes` 只反映"未经批次的直接 sync 路径"，而**批次路径正是本域的主传输通道** ⇒ 该数字同时具备「含假成功（偏高）」与「漏批次量（偏低）」两种失真，双重不可用。
- ~~**不改代码**~~ → **已改（2026-09-15，`6056952`）**：原判断"属指标定义、非失败形状"在**短轮**下不再成立 ——
  短轮几乎每轮都走批次路径，趋势因此**恒为 0**、拿不到任何吞吐数据（run 34920298417 实锤:
  实际落盘 51 个文件而 trend 记 0）。最小改法即本节预写的方案: `sync_by_file_batches` 尾部
  把 `batch_transferred_bytes` 并入 `SYNC_TRANSFERRED_BYTES`，**必须在尾部那次 fix_test
  `sync_with_logging` 之后赋值**（否则被其 0 覆盖）；另补 `total_transferred +=`（递归收尾会
  用它覆盖前者）。**口径仍是"rclone 声称量"**（含假成功）⇒ C 判据必须配 truth-check，不能只看它。
- 另修（同日）: **同步对级传输量在 depth=0 收尾显式赋值**（`fce7cf4`）——并行模式下该变量
  从不设置 ⇒ 一对回传 0 B、整轮合计低估一半（`34959561878` 实锤）。

### F10 405 fail-fast ✅ 已落地（2026-09-19）

**2026-09-19 落地**: 判据 + 接线 + 阈值 + 测试全部完成。

- **判据**（`task_engine.sh · _batch_405_flood`）: 本批日志里 `Method Not Allowed` 计数
  ≥ `OPENLIST_405_FAST_FAIL_MIN`（默认 20）即判"该目录本轮确定性拒写"。只数 405，
  **不数 409/423**（那两类是重试可自愈的竞争冲突，已由 `_sync_retry_409/423` 覆盖，
  纳入会把自愈形态误判成死目录 —— 单测 `2f` 反向锁死这条）。
- **接线位置（关键）**: 批次 copy 结束之后、`_batch_consolidate` **之前**。触发即
  跳过本批巩固（重启容器 + 全量 lsf + 串行重试 + 修复管线，动辄数十分钟），
  复用 F5 的既有中止出口（`BATCH_BACKEND_DEAD=1`）中止剩余批次。
- **通知区分**: 新增 `BATCH_ABORT_REASON`，F10 写「405 拒写」、F5 取默认「后端写入全拒」
  —— 两个判据共用出口但 level 不同（F10 目录级、F5 后端级），不区分会把目录级 405
  说成后端全拒，误导排障。
- **与 F22 的关系**: F22 的结论把 F10 从"省 115min 的优化"提升为"唯一能在失效发生后
  及时止损的机制"；两者同轮落地（见 F22）。
- **测试**: `test_batch_precheck_circuit_breaker.sh` G11/G11b（触发中止不跑巩固/剩余批次、
  reason 正确、不误报后端全拒、未达阈值照常三批）+ `test_write_reprobe_and_405.sh`
  2a–2h（阈值边界 19/20、自定义阈值、阈值=0 关闭、409/423 不计入、空日志/不存在文件）。

**验收 ✅（2026-09-19 生产轮 `35405370217`）**: 判据表现正确（**未误触发，且本轮本就不该触发**）——
`405 拒写` 命中 **0** 次、`中止剩余 N 个批次` 仅 **1** 次（来自 F5）。核对到行为的**负例**：
全轮 `Method Not Allowed` 共 80 条**全部**是 `Couldn't delete: Method Not Allowed`（**删除**失败，
不属批次 copy）；批次 1 copy 区间（02:25–02:46）405 计数
= **0**（该批是「PUT 假成功」形态，无 405）⇒ 该批走 F5 而非 F10。⇒ 二者判据**互补不重叠、分工正确**；
待未来出现 405 洪泛轮时方能看到 F10 中止（阈值 20 已远低于历史 K=1034）。

⚠️ **这 80 条的归属已更正（2026-09-19 复核，原判"修复还原阶段"有误）**：按时间线核对，
80 条**全部落在 02:21:03–02:21:41 这 38 秒内**，且 **77/80 是 `.nfo`**（另 3 条为
`…-Frag33`/`-Frag143` 分卷残留与一个短名 `baf9bc4c.mp4`）。发出者是 **02:18:45 起跑的
`task0 initial sync`**（`rclone sync --filter-from …`，同一批里 `Deleted` 成功 222 条 vs
失败 80 条）——它与修复管线**并发**跑，所以删除日志与 `↳ 还原:` 行在日志里交错出现，
**看着像还原阶段，实际不是**。⇒ F10 的结论不变（**不看删除型 405 是对的**），
但"还原链在报 405"这个说法**撤回**；这 80 个被排除文件只是**没删掉、留了残留**
（下轮会重复处理），**不影响 mp4 落盘与还原**。

**原始规格（保留）**: 同一目录连续 N（默认 20）个 `405 Method Not Allowed` → 中止该目录
本轮尝试，不再 2103 个逐个烧完（`34728107625` 烧了 115min）。

**历史推迟记录（保留，理由已随 F22 撤销）**: 曾被推迟两次 —— ① 2026-09-14 因"热路径改动 +
下轮要干净验证 F6/F5 接线"；② 2026-09-15 因"405 路径特异性已由 §11.8 收口、兜底路径已能
救回，价值降为少试几轮"。**F22 的结论（探针只覆盖 t=0）推翻了 ②**：fail-fast 不再是优化，
而是失效发生后的唯一止损手段；归因失真问题已由诊断 workflow（64 秒单独复现）解决。

### F11 SESSION_HOLD 兜底 ✅ 已落地（3737688）
`openlist.yml:565` `|| 'true'` → `|| 'false'`，与声明默认（false、冲刺期窗口全给同步）一致。现状是颗雷：同步 step 一旦以 success 收场，schedule 轮会真执行 hold 5h。

### F12 `_refresh_openlist_cache` 的等待条件化 ✅ 前半已落地（1fdb773 / 17e1e99 / b319b79，2026-09-15）
每子目录前后各一次、一轮 ≈85 次纯 sleep（~85min，预算 26%）→ 改「驱动未就绪时才等待」。落手前先核实现场行数与调用次数。

**2026-09-15 已落地前半**：等待时长可配（`OPENLIST_FS_REFRESH_SLEEP`，默认 60→**40**；
对比轮实测 sleep=30 无 stale 症状）+ **本次零传输时跳过**同步后刷新（无新写入 ⇒ 无 stale 风险）。
量化依据: 一轮 14 次 × 60s = **14min，占 72min 短轮的 19%**（长轮 ~7%）。**后半（按就绪轮询替代
定长等待）仍未做** —— `/api/fs/refresh` 无完成信号，只能启发式轮询，风险见下。

**原始推迟记录（2026-09-14，保留）**：静态调用点只有 **3 处**（`sync_engine.sh:66` 8005 重试内、`sync_engine.sh:157` 每次 `sync_with_logging` 一次、`sync_notify.sh:112`），并非「每子目录前后各一次」；「≈85 次/轮」取决于 `sync_with_logging` 一轮被调多少次，**需从真实日志数一遍再动手**（本节自己写的「落手前先核实」）。推迟的另一理由：`/api/fs/refresh` 是异步接口、**没有完成信号**，「条件化」只能靠启发式（轮询 listing 是否稳定），改错会让 rclone 读到未刷新的 stale listing → 重传风暴，代价远大于省下的 85min。

### F13 僵尸 run 心跳检测 ❌ 仍未做（2026-09-15 复核：观察到的同类形态另有根因）

**2026-09-15 补**: 观察到"看起来像僵尸"的形态 —— run `34967321544` 超预算 17min 仍在跑、
`sync_state` 65min 无写入，而 step 硬杀要到 4h 后。**根因不是僵尸而是"子目录级 sync 没有硬超时"**
（已修 `3fd29b6`）⇒ 该形态已由预算内硬上限覆盖，F13 的"心跳检测"仍无复现样本，继续不做。
自续触发 step 或轮入口：存在 in_progress 且 >45min 无更新的 run → `gh run cancel`。34752801560 形态（step 超时是 330min 后的兜底，不是主防）。

**本轮推迟（2026-09-14）**：自然落点「自续触发 step」被 §8 红线 4 明令禁止改动（接力逻辑已验证健康），只能改走「轮入口新增 step」；且当前**没有**僵尸（在跑的 `34779382573` step 正常推进）。等真正复现僵尸形态再落地，不为不存在的问题加 step。

### F19 8005 重试前写探针短路 ✅ 已落地（2026-09-14，本轮）

- 位置：`sync_engine.sh` `_sync_retry_8005`（`_refresh_ol_drivers` 与整次 `run_rclone_sync_once` 之间）。
- **证据（本轮从 `34770092689` 日志挖出的裁决性一条）**：该轮唯一一次 8005 重试的时间线是
  `18:41:16Z` → `19:29:17Z`（**48 分钟**），期间 `5.978 MiB → 441.257 MiB / 450.089 MiB`
  （`177–313 KiB/s`），而 `xfr#0/1034 → xfr#0/21` —— **`xfr#0` 表示零文件真正传输**，
  即 48 分钟全部花在"把每个文件试一遍再报同一个错"上；末行仍是 `8005 错误仍然存在，继续重试...`
  ⇒ **刷新 token 并没有消除 8005**，登录失效是持续性的。
- 改法：刷新驱动 + `sleep 10` 之后先跑一次 `_backend_write_probe`（打在同步对真实子路径），
  不过就 `SYNC_BACKEND_DEAD=1` 并 `break`，跳过整次重传与随后的 `_refresh_openlist_cache`
  （连带省掉它的无条件 `sleep 60`）。探针 60s 内给出同样结论。
- **必须先 `unset "_BACKEND_WRITE_PROBE_CACHE[$dest_path]"`**：同步前的入口探针已把该键置 1（可写），
  不清就会命中缓存直接返回"可写"，等于没探。
- 单测 `tests/test_8005_probe_gate.sh`（10 断言）；**已负向验证**：整段注释掉后 1a/1b/1c/1d/1e/3 六项变红，恢复后 10 PASS / 0 FAIL。
- **未解决的前置问题**：8005 的**根因**是 OpenList 里 wopan176 的**驱动登录令牌**失效（wopan 令牌有效期
  方法一 7 天 / 方法二 2 个月，且**无法自动续期，只能人工重抓**；重抓时跨端互踢）。F19 只是让失败**变便宜**，
  不修根因——根因在 §9 第 1 项，需用户确认。

### F20 验证起跑规程（规程，非代码）✅ 已写进 §0 开工四步第 5 步

- 起因：用户反馈「进度太慢」。核实后确认**不是**队列积压：GitHub 的 concurrency 会自动取消被顶替的
  pending 轮（`34787645966` 被 `34793014398` 顶掉即实测），队列恒 1 深且总是最新创建的。
- ⇒ 新代码进生产的延迟 = 下一次 cron（≤1h）＋ 在跑轮剩余时间（≤5.3h），**上限约 6.3h**。
  **本文档此前写的「≈2 轮 / 11h」只对紧接着的那一轮成立，不累积**——该说法已在本轮更正。
- 要压到分钟级：取消 pending +（必要时）取消在跑轮 + `gh workflow run`（命令见 §0 第 5 步）。
- **对 F18 的连带影响**：F18（cron `0 * * * *` → `0 */2 * * *`）的原始理由是「run 列表塞满空转的过时排队」，
  而该现象被 concurrency 的自动取消消解了大半（现在只留 cancelled 条目）。且降频会**拉长**新代码进生产的
  延迟上限（1h → 2h）。⇒ **本轮不建议做 F18**；真要清列表，方向应是把 cron 降为纯兜底（如 `0 */6 * * *`）
  并让接力成为主机制，但那要动接力 step（§8 红线 4 禁止）。

### F23 在途传输硬上限（2026-09-14 落地 `4da3c11`；**2026-09-15 补最后一处 `3fd29b6`**）

- **起因（run `34779382573`，`06cc0df`，Phase 1）**: 22:16 起跑、预算至 03:36，但前段被
  预览 36min + wopan175 各子目录 initial sync / truth-check **2h36m** 吃掉，批次 1 到
  **01:33** 才开启（此时只剩 2h3m），自身跑了 **2h14m 仍未完成** → 03:47 撞 step 的
  `timeout-minutes: 330` 被整轮杀掉（conclusion=failure）。**F1 的预算闸只拦得住「新开的
  工作」，拦不住「已在途的传输」。**
- **改法**:
  1. `OPENLIST_BATCH_MIN_SLICE_SECONDS` 默认 3600 → **7200**（实测批次在 wopan176 上
     1h17m、在 wopan175 上 3178 个文件 >2h14m 未完成）。
  2. 新增 `_budget_slice_seconds()` = 预算剩余 − `OPENLIST_BATCH_TAIL_RESERVE`（默认
     2700s）；给**批次 copy** 与**巩固串行重试**套 `timeout <秒>` 硬上限。到点后
     rc=124 走失败分支 → 巩固 → 循环闸优雅收摊，不再拖到 330min。无预算锚点
     （调试/还原模式）输出空串 = 不加包装，行为与旧版一致。
- **验收**: `_budget_slice_seconds` 三态 + 批次预算闸三态（新片长）+ G7「每批 copy 都
  套上硬上限」断言；**2026-09-19 生产验证（主轮 `35405370217`）**：320min 前优雅收摊、
  未再出现 330min 硬杀。
- **已知残留**: 子目录级 initial sync 仍只有 600s 片（单次实测可跑 40min+），长尾仍可能
  小幅越过 320min 预算，目前靠 320min 与 330min 之间那 10min 余量兜着；若下一轮再出现
  硬杀，把子目录层也换成 `_budget_slice_seconds` 包装。

### F22 写探针时序补全 ✅ 已落地（2026-09-19）

- **起因**：`openlist-diag.yml` 首跑证明「单文件、顺序、约 10 次写」在 `wopan176Crypt/2` 上
  **全部成功**（含 128B 长名与覆盖写），而生产在同一路径上是 1034 个文件 / 48 分钟 / 全部 405。
  ⇒ `_backend_write_probe`（F4）的「可写」结论**保质期只有开头那几分钟**：它只在同步
  **开跑前**跑一次，之后整轮复用缓存，天然测不到随时间/量累积的失效。
- **落地改法（2026-09-19）**：
  - `openlist_driver.sh · _backend_write_probe_invalidate <dest_path>` —— 清该路径的探针结论。
    **键口径必须是同步对路径**（与 F21 同口径；按挂载根清 = 清了个不存在的键 = 等于没探）。
  - `task_engine.sh · _write_reprobe_due` —— 时间闸：距上次真探 ≥ `OPENLIST_WRITE_REPROBE_INTERVAL`
    （默认 1800s = 30min）即到期；未设锚点（本轮首次）视为到期（保守重探）。
    间隔≤0/非数字 → 回落默认，脏锚点 → 视为到期。
  - **接线点**：批次循环「刷新驱动 → 清缓存重探 → 批次预检」序列，重探复用预检里的
    `_backend_write_probe`（不额外增加探测次数，只让已有那次变成真探）。
  - 环境变量 `OPENLIST_WRITE_REPROBE_INTERVAL: "1800"`（openlist.yml）。
- **与 F10 的关系**：**F10（405 fail-fast：同一目录连续 N 个 405 → 中止）本来就是同一件事的
  另一种表述**。F22 的结论是 **F10 的优先级应当提高**——它不再只是"省 115min"的优化，
  而是唯一能在失效发生后及时止损的机制。两者同轮落地（互不耦合：F22 管"多久重探一次"，
  F10 管"探不到时怎么止损"）。
- **测试**：`test_write_reprobe_and_405.sh` 1a–1h（到期/未到期/自定义间隔/关闭/脏值）+
  `test_backend_dead_signal.sh` 6a–6d（invalidate 键口径、不动根键、空参安全）+
  `test_batch_precheck_circuit_breaker.sh` G10/G10b（接线：到期清缓存、未到期不动）。
- **验收 ✅（2026-09-19 生产轮 `35405370217`）**：`♻️ … 写探针结论已过期，清缓存后重探`
  实测出现 **5 次**（23:31:41 / 23:31:57 / 00:09:09 / 01:44:26 / 02:25:10），每次紧跟一次真探
  （`💉 后端写探针` + `✅ 后端可写`）；同一同步对内不重复重探 ⇒ 时间戳刷新逻辑正确。
  ★ 该轮**恰逢真坏窗口**（02:25–02:46 批次 1 的 9 个文件「Copied 但重启复核 9/9 未落盘」），
  而 02:17 / 02:25 两次探针**都报可写** ⇒ 生产实证「**探针通过 ≠ 后续写入成功**」，
  正是 F22 存在的理由（失效在 t=0 之后才发生）。

### F21 写探针判死信号键错位 ✅ 已落地（2026-09-14，本轮）

- 位置：`sync_engine.sh` `sync_with_logging` 两处（入口预检失败 `:159`、rc=88 二次预检熔断 `:280`）。
- **这是「死后端钉住游标 ≈44h」的直接成因**，也是本轮最值钱的发现：
  - `_backend_write_probe`（F4）按**「后端 × 路径」**分层缓存，写的是 `_BACKEND_WRITE_PROBE_CACHE[$dest_path]`
    （如 `openlist:wopan176Crypt/2`）—— 全库写入点只有 `openlist_driver.sh:447/452`，键恒为 `$dest`。
  - 而上述两处读取曾用 `_backend_root_of "$dest_path"` 的结果作键（`openlist:wopan176Crypt`）——**键对不上**。
  - ⇒ `SYNC_BACKEND_DEAD` 在这两条路径上**永不置位** ⇒ `run_all_tasks` 既不"立即后移游标"（`:322` 分支），
    也不调 `_backend_dead_mark`（无跨轮 TTL 跳过）⇒ 死后端唯一出路是
    `ROTATION_MAX_CONSECUTIVE_ATTEMPTS`（默认 **8**）阀门 ⇒ **8 × 5.5h ≈ 44h**，与 §1.1 第 5 条实测症状完全吻合。
- 改法：两处读取改用 `$dest_path`（探针实际写入的那个键），并删掉随之无用的 `_be_root` 计算。
- 单测 `tests/test_backend_dead_signal.sh`（8 断言，含 1b/1c 断言"键落在同步对路径、且根键未被写入"）；
  **已负向验证**：把两处读取改回按后端根取后 2/3/5a/5b 四项变红，恢复后 8 PASS / 0 FAIL。
- 附带澄清：`_check_openlist_backend_connectivity` 因**读**探针失败而返回非 0 时，缓存里没有 `0`，
  故不会误置位——该语义被修法保留（新增断言 4 覆盖）。

---

## 5. Phase 3 · 吞吐与结构（决定「能否完成」，含决策点）

### F14 三后端写入自检（先做，是后续一切决策的前提）
调试模式（`run_mode=调试 · 修复管线测试`，`force_sync=true`，`fix_test_max=5`）对 baidupanCrypt / wopan175 / aliyundriveCrypt 各跑一次小任务。它们近四轮零执行、可写性完全未知。产出：三后端可写性证据 + 实测速率。

### F15 transfers 调整（**已授权自行调整**，2026-09-14 用户明示）
原为「`transfers=2` 实验（需用户批准）」。**2026-09-14 用户明确授权「transfers 你可以根据情况调整」⇒ 不再需要批准**。
保留原判据：改 transfers 必须用调试模式先试一批，观察 object-not-found 率与 truth-check 结果；
`openlist.yml` 的输入描述里「wopan176 保持 1」随之作废（改代码时一并更新描述）。
**前置依赖未解除**：wopan176 当前连登录都过不去（8005），此时调 transfers 测不出吞吐差异——
**先解 8005（§9 第 1 项），再动 transfers**。

### F16 冗余削减 ❌ **已取消（2026-09-14 用户指示）**
原文：16 对 10.1TB → 主副本 4.5TB，wopan176 的 7 对降级或暂停。
**用户明示「不要削减后端，全部都是需要的」⇒ 本项整体取消，不再评估。**
连带影响：
- §7 **Gate 3 失效**（它定义在 F16 之后："主副本确立且持续吞吐 ≥3GiB/轮"）——Gate 3 不再有前置项，
  需要重新定义为「16 对全量跑通一轮的实测吞吐」，但**暂不改**，等 Gate 1/2 有结论再定。
- 吞吐目标不变（仍要搬完 ~9.9TB），因此**只能靠修好后端 + 提高单轮有效吞吐**，没有"砍量"这条路。
- 「三个后端都不健康则升级用户」的升级路径**保留**，改挂到 Gate 2。

### F17 预览降耗 ✅ 已落地（2026-09-19）

预览 30m41s 占预算 9.6%，其中 listing 失败拖满 `OPENLIST_RCLONE_LISTING_TIMEOUT=900`。

**2026-09-19 落地（选"加短超时 + 失败快弃"，不选 `skip_preview=true`——后者会让通知失去待传量信息）**:
- `task_preview.sh · _get_listing_json` 改用专属超时 `OPENLIST_PREVIEW_LISTING_TIMEOUT`
  （默认 **240s**，openlist.yml 注入）。预览只做粗粒度估算，不需要 sync pass 那种 900s
  精确列举预算；列不出来本就允许"按未知/全量估算 + 明示 ⚠️"。
- `add_preview_pair` 目标端重试的"超时型失败不重试"判据由**固定 300s** 改为
  **单次尝试耗时 ≥ 预览超时**（`_pv_tmo_s`）。**这条必须同步改**：旧 900s 超时下固定
  300s 恰好能命中，改短超时（240s）后固定 300s 会让每次 240s 超时都不到 300s ⇒ 3 次全跑
  = 720s 白等，与"超时型不重试"的意图正好相反（净效果反而更差）。
- **效果**：单次失败等待 900s → 240s；最坏"3 次重试" → "1 次"，失败对不再在超时上白等。
- 环境变量 `OPENLIST_PREVIEW_LISTING_TIMEOUT: "240s"`（openlist.yml；**必须带单位**，
  rclone `--timeout` 拒绝裸数字——见 `OPENLIST_RCLONE_LISTING_TIMEOUT` 的踩坑注释）。
- **测试**：`test_preview_diff.sh` 全绿（48 断言，含重试/失败/缓存/大清单场景）；
  超时值本身走 workflow 注入，静态检查由现有 `test_fix_check.sh` 的"必须带单位"惯例覆盖。
- **未做（保留为备选）**：跨轮源端 size 缓存 —— 需新增持久化层，收益不确定，暂不做。
- **验收 ✅（2026-09-19 生产轮 `35405370217`）**：预览段未再拖满 900s；环境变量
  `OPENLIST_PREVIEW_LISTING_TIMEOUT: 240s` 已由收尾 dump 确认进入生产。

### F18 cron 降频
`0 * * * *` → `0 */2 * * *`。接力正常时 cron 纯兜底，降频让 run 列表干净；代价是兜底延迟 ≤2h。

---

## 6. Phase 4 · 观察循环与退出标准

每轮观察动作（AI 可全权执行）：

1. `gh run list --workflow=openlist.yml --limit 5` 看 conclusion。
2. `gh run view <id> --log | grep -E "优雅收摊|后端写入全拒|熔断|后端跨轮熔断|跳过让路|修复成功 ·|顽固缺失|transfers=|同步时间预算"`。
3. 本机 rclone 读 `onedrive:/logs/sync_state/`：task_rotation.json 游标移动、trend.jsonl 末条、backend_dead.json 命中情况。
4. 对照 0.2 基线记录增量，更新本文档复选框。

**退出标准（全部满足、连续 ≥3 轮）—— 2026-09-15 现状复核（详见下方各条追加的证据）：**

| 标准 | 现状 | 依据 |
|---|---|---|
| A · success 非硬杀 | **实质达标** | 今日 5 个 60min 短轮全部 `success`、逐级优雅收摊、无硬杀；长轮最近一次完整样本 `34871720751` 亦 success |
| B · 新落盘修复率 / 熔断短路 | **实质达标** | 修复管线稳定出成果（`成功 69 · 缺失 6 · 未修复 0`）；短哈希折叠 + 逐文件方法真实落盘；游标早已离开 #9 |
| C · trend 与真值差 <2x | ✅ **达标（2026-09-15 收口）** | 见 §12: 证伪"尺寸/并发"两个假设后定位真因是**修复管线被预算饿死**；加"修复尾段预留"（`11c1761`）后，验证轮 `34997994883` 收尾 `成功 94（新落盘 94 · 沿用 0）· **缺失 0 · 未修复 0**`，且**假成功检测 0 次**（上一轮是 `新落盘 1 · 未修复 985`） |
| D · 非 wopan176 被执行 | **达标** | `task5-wopan175` 常态执行；并行轮两挂载同时推进 |
| E · 零产出告警 | **达标** | F7 拆分新落盘/沿用 + 告警口径修正；实测已触发（`🚨 修复管线零新增落盘`） |

**【2026-09-19 最新数据点，run `35405370217`（`0b0a1aa`，success，320min 优雅收摊）】**：
A 达标（success、无 330min 硬杀、逐级优雅收摊）；B 本轮撞坏窗口（F5 实测 `9/9 全拒`）⇒
熔断正确短路、游标 3→6 正常轮转**离开**；C 修复成效 `成功 400（新落盘 266 · 沿用 134）· 缺失 1121`，
`方法耗尽 0`、`失败 0`；D 达标（4 个同步对执行，含 baidupanCrypt 79.8+5.5 GiB、wopan176Crypt 15.9 GiB）；
E 未触发零产出告警（因本轮**有**新落盘 266，符合预期）。
⇒ **A–E 全部仍达标**；本轮"缺失高企"的成因是**后端坏窗口**（F5 实测），非代码缺陷。

注: "连续 ≥3 轮" 未逐轮记账 —— 本轮迭代大量主动取消长轮（为换取分钟级反馈），
连续性指标在当前节奏下不具可比性；判据以"是否复现失败形状"为准。

**退出标准（原始记录，保留供追溯）：**

- [ ] A · run conclusion=success（320min 优雅到站，非 330min 硬杀）
      —— 观察轮 34770092689（2f60f26，Phase 1）已达标 1/3: conclusion=success，
      16:56→22:14（5h18m），22:12「时间预算将尽，优雅收摊: 剩余 15 个同步对」，
      22:19 预算到点前正常收摊，全 step success（含收尾/会话保持/Persist/自续触发）。
- [ ] B · wopan176 新落盘口径修复率 >50%，或熔断正确短路且游标 2 轮内轮转离开 #9
      —— 未达标: 该轮批次 1 巩固「后端写入全拒（1036/1036 个触碰文件经复核全部未落盘）」
      ⇒ 整批仍无真实落盘；熔断按 F3 只看短名路径后于 19:39 触发（连续 3 个目录不可写）。
      游标 8→9 已移动（attempts=1）。
      【2026-09-14 补证据】短哈希折叠路径**已被真实尝试、且同样零落盘**：19:32:57 与 19:36:03
      两次目录级批量折叠（44 + 39 = 83 个文件）→ 短哈希目录 `f22c8c73` / `cb4f0d57`，两次都在
      19:39:01 熔断**之前**（即未被短路），结果均 `❌ 批量折叠零落盘（rc=0）`（19:34:39 / 19:37:37）。
      ⇒ B 的「或」分支里「熔断正确短路」成立（F3 第一步达标），但主判据「新落盘口径修复率」仍为 0。
- [ ] C · trend transferred 与 truth-check 确认落盘量差 <2x
      —— 未达标且更需警惕: 该轮 transferred_bytes=2,077,403,120（2.08GB，此前三轮
      恒为 171MB）而真实落盘 0 ⇒ 差值无限大，计数包含了假成功。
      【2026-09-14 补证据】批次 1 巩固的自述就是假成功样本：`本批成功 1036 个 / 直接失败 0 个，
      重启容器校验后端真值` → 校验结果 `后端写入全拒（1036/1036 个触碰文件经复核全部未落盘）`。
      ⇒ rclone 侧「成功 1036、失败 0」与真实落盘 0 可以同时成立：任何基于 rclone 退出码 /
      `Copied` 行数的判据在本后端上都不可信（与 §1.1 第 3 条、§1.3 F0 同源）。
- [ ] D · 非 wopan176 的同步对开始被执行 —— 未达标（该轮仍只碰 wopan176Crypt/2）。
- [ ] E · 零真实落盘轮次能触发零产出告警 —— 该轮未触发（收尾报 `本轮修复成效: 成功 183 ·
      缺失 2146 · 未修复 1963`，而 183 条**全部**是 `♻ 沿用上轮修复`——新落盘 0，持久化验证
      汇总 `复核 183/183 / 通过 183 / 失败 0`——当时尚未部署 F7）；F7 已落地，下轮起看「新落盘」口径。

**2026-09-14 08:05 +08 观察快照（无新完成轮，A–E 无变化）**：

- 最新**完成**轮仍是 `34770092689`（2f60f26，Phase 1）⇒ A–E 证据同上，本轮**无新增数据点**。
- 在跑 `34779382573`（`06cc0df`，**Phase 1 代码**，schedule）：run 创建 09-13 20:00:11Z，
  job 22:14:04Z 才拿到 concurrency 锁开跑（排队 2h14m）；08:05 +08 时 step 18「任务预览与全量同步」
  已跑 1h48m，330min 上限 ⇒ 预计 11:40 +08 前后收场。**它跑的是 Phase 1，不含 Phase 2**，
  观测价值 = 又一个已知形状样本。
- 排队 `34787645966`（`0eef148`，**同为 Phase 1**，06:43 +08 创建，已 pending 1h22m）——
  两个在队轮次都钉在 Phase 2 之前的 sha 上。**但不必人工干预**：在跑轮正常收场后自续触发
  `gh workflow run` 会用「触发那一刻」的 main，届时 Phase 2 全量进入下一轮（同 concurrency 组内
  较旧的 pending 会被顶掉）。
- 本机 rclone 状态：`task_rotation.json` = `{cursor: 9, attempts: 1, updated: 2026-09-13T22:55:23Z}`
  —— 22:55 的写入正是 `34779382573` 的"执行前落盘"（idx 9），即**这一轮又回到上一轮同一对
  wopan176Crypt 上重试**，是 F6 尚未生效的预期形态。`backend_dead.json` **不存在**（F6 从未被
  任何一轮执行过，符合预期）。
- 结论：**Gate 1 仍未到**（要求 Phase 1+2 落地后观察 2 轮，目前 Phase 2 观察 0 轮）。本轮不再
  追加同形状样本的推断，等下一条完整日志。

**2026-09-14 09:10 +08 观察快照（无新完成轮；两项既有认知被实测推翻）**：

- **轮次盘点**（`gh run list`）：最新**完成**轮仍是 `34770092689`（2f60f26，Phase 1，success）⇒ **A–E 无新增数据点**。
  - `34772641066`（2f60f26，Phase 1）→ cancelled；`34787645966`（0eef148，Phase 1）→ cancelled（被更晚的 pending 顶掉）。
  - **在跑** `34779382573`（`06cc0df`，Phase 1）：job 22:14:04Z 拿到 concurrency 锁，同步 step 22:16:54Z 起；320min 预算 ⇒ 预计 **03:36Z ≈ 11:36 +08** 收场。
  - **排队** `34793014398`（`8acfc6f`，含 Phase 2 第一批 F6/F7/F8/F11，**不含 F6 触发源接线**）——它是 Gate 1 的第一个 Phase 2 数据点，会在在跑轮收场后立即开跑（约 03:36Z 起、5.5h 后 ≈ 17:00 +08 收场）。
- **推翻①：`task_rotation.json` cursor=9 不是"又回到 wopan176Crypt"**。注册表索引已逐条导出核对（`SYNC_TASK_REGISTRY` 共 16 对）：
  idx 8 = `task2 → openlist:wopan176Crypt/2`（上一轮实执行的那一对），**idx 9 = `task2-wopan175 → openlist:wopan175/2`**。
  运行日志实锤：`同步对轮转: 本轮从第 9/16 个同步对开始（已连续尝试 6 次）`（17:01:44Z 预览 pass、17:30:32Z 真实 pass 各一次）+ 预览 pass 的 `注册进度: 2/16 onedrive:2 → openlist:wopan175/2`。
  ⇒ 上一轮 §0 写的"cursor 9 = 回到同一对 wopan176Crypt 重试"是**索引误读**；cursor 9 是一个 **wopan175** 对。
- **推翻②：自续触发没有接力**。`34770092689` 尾部实锤 `已有 1 个排队/等待中的运行，跳过接力`（22:13:57Z，护栏 3）。cron `0 * * * *` 会持续把 run 塞进队列，所以"在跑轮收场后自续触发用触发那一刻的 main"**不成立**——接力被护栏跳过，队列里那个**旧 sha 的 pending** 才是下一轮。
  ⇒ **新代码进生产的延迟 ≈ 2 轮（约 11h）**：push 只影响"之后新建的 run"，而队列里已排队的旧 sha pending 会先跑。这正是 `acbbff5`（08:28 +08 commit）至今未被任何一轮执行的原因。**不要再按"push 完下一轮就生效"安排验证节奏。**
- **A–E 逐条（新增实测细节，结论不变）**：
  - A 达标 1/3（证据同上）。新细节：`34770092689` 的**真实 pass 只执行了 1 个同步对**（idx 8）——22:12:09Z `⏳ 时间预算将尽，优雅收摊: 剩余 15 个同步对`（n−i=15 ⇒ i=1）。预览/注册 pass 覆盖全部 16 对但只读。
  - B/C/D/E 全部未达标，证据同下。D 的精确表述：**真实 pass 未执行任何非 wopan176 对**（预览 pass 的 16 对注册不算"执行"）。
- **F6 静态链已复核到行（含两条此前未记录的有利性质）**：
  1. `_backend_dead_mark`（`task_engine.sh:149`）在写文件前**先把 root 塞进内存 `_BACKEND_DEAD_ROUND`** ⇒ 同一轮内后续的 wopan176Crypt 对会被 `:286` 直接跳过，**不必等下轮**（D 有机会在同一轮达标）。
  2. "全线皆死"保护不会误伤 F6：注册表只有 **4 个后端**（aliyundriveCrypt / wopan176Crypt / baidupanCrypt / wopan175），判死 1 个 → `1/4` → 走"跳过让路"分支（`:258` 要求 `_dead_backend_n ≥ _backend_total` 才清空）。
  3. 链路无子 shell：`_batch_consolidate`（`:1755` 直调）→ `sync_by_file_batches` `return 1` 并置 `SYNC_FAILED=1`（`:1780`）→ `_run_registry_entry`（`:309` 直调 `|| true`）→ `run_all_tasks` 的 `elif [ "${SYNC_BACKEND_DEAD:-0}" = "1" ]`（`:320`）→ `_backend_dead_mark`。`SYNC_BACKEND_DEAD` 全库只有 `:308` 一处归零（每对开始前），执行期间无覆盖点。
- **顺带记录（不修）**：`⚠️ raw 计数持续为 0（容器重启后列表未就绪），本轮禁用落盘即时校验` 出现 6 次；`34770092689` 的修复持久化汇总 = `复核 183/183 / 通过 183 / 失败 0`，其中 181 条是 `♻ 沿用上轮修复`（F7 已针对此落地，下轮起看"新落盘"口径）。
- **在跑轮很可能正在执行 `wopan175/2`（待收场日志确认）**：推理链——`34770092689` 收场时游标停在 8/attempts 7（i=0 落盘 `save(8,7)`，i=1 被预算闸 break，未再落盘）；`34779382573` 起手 start=8，i=0 的 idx 8 失败后 `_rot_attempts` 达 8 → 阀门（`:334`）→ `save(9,0)`，i=1 的 idx 9 = `task2-wopan175` → `save(9,1)`（22:55:23Z，与实测 `cursor 9 / attempts 1` 完全吻合）。若成立，则**真实 pass 首次落到非 wopan176 后端**，D 有机会在该轮达标——但它是"轮转自然轮到"而非 F6 之功（该轮不含 F6 接线），**记录时不要把它算成 F6 的功劳**。
- **本轮结论：Gate 1 仍未到**（要求 Phase 1+2 落地后观察 2 轮；目前 Phase 1 观察 1 轮、Phase 2 观察 0 轮）。等 `34793014398` 收场后才有第一个 Phase 2 数据点。

**2026-09-14 10:0x +08 观察快照（仍无新完成轮；「进度慢」归因完成，两处代码修复落地）**：

- **轮次盘点**（`gh run list --limit 8`）：最新**完成**轮仍是 `34770092689`（2f60f26，Phase 1，success）
  ⇒ **A–E 无新增数据点**（A 1/3，B/C/D/E 未达标，证据同 09:10 快照）。
  - **在跑** `34779382573`（`06cc0df`）：job `22:14:04Z` 起，09:0x 时已跑 ~2h50m，320min 预算 ⇒ 预计 **03:36Z ≈ 11:36 +08** 收场。
  - **排队** `34793014398`（`8acfc6f`）：`00:32:26Z` 创建，pending。**注意它不含 `acbbff5`**（F6 触发源接线）——
    `acbbff5` 的 commit 时间是 `00:28:24Z`，而该轮创建于 `00:32:26Z` 却仍钉在 `8acfc6f`（`00:27:52Z` 的 commit）
    ⇒ **schedule 轮取 sha 有几分钟级滞后**，实测 ~4.5min。
  - 关键：**在跑的 `34779382573` 用的是 `06cc0df`，落后 HEAD 15 个提交**（`git merge-base --is-ancestor` 逐条核实：
    不含 `e5296ed`（F6 跨轮持久化）、不含 `acbbff5`（F6 触发源接线））——**它连 F6 都没有**，
    只是"又一个 Phase 1 样本"，观测价值低。
- **「进度慢」的归因（本轮的核心产出，分三层，按影响排序）**：
  1. **根因层（最大）**：wopan176 的 OpenList **驱动登录令牌失效（8005）**，导致该后端**写入全拒**。
     单轮 5.3h 只走 1/16 个同步对，且真实落盘 0 —— **不修这个，其余优化都是在小数点后挪**。→ §9 第 1 项。
  2. **浪费层（本轮已修）**：
     - **F21**：写探针判死信号**键错位** ⇒ `SYNC_BACKEND_DEAD` 永不置位 ⇒ 死后端只能靠 8 次上限阀门脱身
       ⇒ **8 × 5.5h ≈ 44h** 烧在同一个死后端上。修完判死信号才真正到得了轮转/跨轮熔断。
     - **F19**：8005 重试前无探针闸门 ⇒ 单次重试实测烧 **48min** 且 `xfr#0` 零落盘。修完 ~1min 出结论。
  3. **调度层（本轮澄清，非问题）**：**不是**队列积压 —— GitHub concurrency 会自动取消被顶替的 pending
     （`34787645966` 被 `34793014398` 顶掉即实测），队列恒 1 深。延迟上限 ≈6.3h。
     **此前本文档「≈2 轮 / 11h」的估计偏高，已更正**（见 §4 · F20）。
- **两个悬而未决的证据缺口（诊断 workflow 就是为它们建的）**：
  - **容器日志里的原始 8005 行从未进过 run 日志** ⇒ 现在只能从"检测到 wopan176 登录失败 (8005)"
    这条**包装后的提示**反推，看不到 `rsp_code` / `rep_desc` 原文。
  - **探针 ✅ 与真实写入 405 可以同时成立**（`34770092689`：入口写探针放行，随后真实上传全 405）。
    可能解释有二：① 令牌是**时段性**失效（access token 约 5min 过期，刷新失败后整段失效）；
    ② 探针写的是**新建**文件，而真实失败是 **`unchunked simple update`（覆盖写）**。二者修法完全不同，
    `openlist-diag.yml` 的「名长阶梯 + 覆盖写 + 子目录写」三组探针正是用来分开它们。
- **本轮代码改动**（2 处实现 + 2 个新单测 + 1 个新诊断 workflow）：
  `sync_engine.sh`（F19 + F21）、`tests/test_backend_dead_signal.sh`（8 断言）、
  `tests/test_8005_probe_gate.sh`（10 断言）、`.github/scripts/openlist/diag_backend.sh` + `.github/workflows/openlist-diag.yml`。
  两个新单测**均已负向验证**（注入错误形态后分别 4 项 / 6 项变红，恢复后全绿）。
- **本轮结论：Gate 1 仍未到**（Phase 1 观察 1 轮、Phase 2 观察 0 轮）。**且 Gate 1 的第一个 Phase 2 数据点
  （`34793014398`）也不含 F6 触发源接线** ⇒ 要看到 F6 全链生效，得再往后一轮。建议先按 §9 第 1 项解 8005，
  否则后续几轮仍会是"成功到站但零落盘"的样本。

**2026-09-14 10:45 +08 诊断首跑（`openlist-diag.yml`，run 34799967126）—— 决定性，推翻三条既有假设**：

- 耗时 **64 秒**（`02:42:32Z → 02:43:36Z`），跑的是本轮 commit `00ca8ff`。**这就是「验证要排 5.5h」的解法**。
- **存储清单**：10 个存储**全部 `status=work`、无一 disabled**——含 `/wopan176`（WoPan）与 `/wopan176Crypt`。
  ⇒ OpenList 自己**没有**把 wopan176 判为 broken。
- **探针结果（全部通过）**：
  | 探针 | 结果 |
  |---|---|
  | 读 | ✅ 1 个条目 |
  | 短名写（26 B） | ✅ 真落盘 |
  | 名长阶梯 32/64/80/100/112/**128** B | ✅ **六个长度全部真落盘** |
  | 覆盖写（`unchunked simple update` 同形态） | ✅ 通过 |
  | 子目录内写 | ✅ 真落盘 |
- **推翻的三条假设**：
  1. **「密文名 >100B → 405」的名长上限**：128 B **明文**经 `wopan176Crypt`（`filename_encryption` 已配）
     加密后**名字更长**，仍真落盘 ⇒ 本路径上名长**不是**拒写原因。
  2. **「父目录名长连坐」**：子目录内写成功。
  3. **「登录令牌已失效且是持续性的」**：短名写成功 ⇒ 此刻登录与写入都是通的。
- ⚠️ **但不能反推「8005 不存在」**：容器是**全新启动**的，`docker logs` 只有 **8 行**（无历史），
  所以「8005 命中 0 行」是**无信息**，不是阴性证据。判读时别把它当"没有 8005"。
- **真正的解释收敛到「量 / 时长 / 并发」**：探针是**单文件、顺序、总共约 10 次写**；
  生产是 **1034 个文件、48 分钟、transfers=1**。两者可以并存，只要失效是**随时间或量累积**
  发生的（后端限流、风控踢会话、长同步中途 token 失效）。
  ⇒ 这同时解开了「探针 ✅ 与真实写入 405 并存」这个长期悖论：**不是探针探错了地方**
  （F4 已把它修到真实子路径），**而是探针只覆盖 t=0 那一瞬**。
- **连带影响**：
  - **F3 的前提被削弱**（名长不是本路径的原因）⇒ 第二步维持推迟；第一步的"解耦"保留
    （让判据不再依赖错误归因）。
  - **F4 写探针需要时序补全** ⇒ 新增 **F22**（见 §4）。
  - **§9 第 1 项降级**：当前**没有**证据支持"驱动令牌已失效"，改为"先跑持续写入探针定位失效点，
    再决定是否人工重抓"。
- **下一步工具已就位并已跑完**：`diag_backend.sh` 第 9 组**持续写入探针**（150 文件）
  + 第 9b 组**并发写三态探针**，结论见 §11.6（持续写全过；并发在补足重试后可行，
  已据此把 `transfers` 默认提到 4）。

迭代纪律：一轮观察周期 5.5-6h；每轮最多一批修复，push 前过 §8 回归；失败形状复现优先于新功能。

## 7. 决策门

- **Gate 1**（Phase 1+2 落地并观察 2 轮后）：wopan176 truth-check 真实落盘仍为 0 且熔断/轮转工作正常 → 判账号级死亡，进 Phase 3 后端切换。
  - **2026-09-14 更新：Gate 1 判据不成立，不做后端切换。** 用 `openlist-diag` 把三个后端
    逐个测了一遍（每个 5 分钟），**全部可写**：
    | 后端 | 短名写 | 名长 32–128B | 覆盖写 | 子目录写 | 长名阶梯 | 深度阶梯 | 8005 |
    |---|---|---|---|---|---|---|---|
    | `wopan176Crypt/2` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 0 行 |
    | `baidupanCrypt/1` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 0 行 |
    | `aliyundriveCrypt/backup` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 0 行 |
    另有: wopan176 持续写 150/150 全 OK（140s）；aliyundrive 重启后需 ~10s 才可写（+10s 起 OK）。
    ⇒ **不存在"账号级死亡"**；生产里的零落盘由**管线侧原因**造成，且已逐条定位并修掉：
    折叠落盘校验假阴性、目录可写性预检假阴性、路径特异性 405 被提升为后端级熔断、
    批次在途传输被 330min 硬杀、并发无重试。（唯一剩下的真问题是"特定目录名 405"，
    由短哈希折叠兜底，已可用。）
  - **连带修正**: §11.2 杠杆 2（冗余削减）已按用户裁决作废；Phase 3 的"后端切换"不再需要，
    Phase 3 只剩 F14（已完成，即上表）与吞吐/结构优化。
- **Gate 2**（F14 后）：三个备选后端写入全失败 → 账号/配额层问题，升级用户，AI 停手。
- **Gate 3**（F16 后）：主副本确立且持续吞吐 ≥3GiB/轮 → 更新剩余量推演；若仍年级收敛，带数据请用户裁决继续/终止。

## 8. 工程红线（每次改动，无例外）

1. **通知**：动通知文案/版式必遵 `docs/telegram-notify.md`；改完跑 `bash skills/telegram-notify-audit/scripts/render_preview.sh`（16 项校验）。排版/发送一律 source `tg_notify.sh`，不得 curl 直发。
2. **回归**：`tests/` 下全部 `test_*.sh` **串行**跑（`bash test_x.sh </dev/null`）。**2026-09-14 实测基线：24 套 / 22 EXIT=0 / 2 非 0**——`test_marker_skip_guards.sh`（macOS `date` 无 `-d`，环境性）、`test_truth.sh`（需 docker，环境性）；全部日志 `grep "command not found"` 为空。已知 flaky 单独重跑即过：`progress_no_orphans`（T5 时序）、`sync_trend_budget`（macOS `wc` 前导空格）。**另有一类「沙箱假红」**：套件运行期偶见子进程被拦，日志里会出现 `Brokered program policy check unavailable`，见到该标记即单独复跑——2026-09-14 的 `test_batch_consolidate.sh`（12b/13d）与 `test_bulk_hash_fold.sh`（T3a）即此形态，单跑分别 **61 PASS / 0 FAIL**、**29 PASS / 0 FAIL**。跑法注意：zsh 下 `rm -f /tmp/x_*.log` 无匹配会中断整条命令链，日志请写进新建目录。
3. **Git**：push 前先 `git fetch`（main 有并行推送）；commit `fix(openlist): 中文描述` + `- ` 列表正文；禁 force / --no-verify；不 commit 除非任务需要（本计划授权提交）。
4. **不许动**：接力 step 逻辑（已验证健康）、truth-check 重启取真值机制、marker/游标增量持久化设计、「熔断不豁免修复管线」用户规格。
5. **注释**写「为什么」；实现改了同步文件头注释与相关 docs。
6. **dispatch 纪律**：只允许「同步」（默认）与「调试 · 修复管线测试」；**⚠️ 还原 / ⚠️ 灾难恢复模式禁碰**（需用户明示）。run_mode 默认即真实同步数小时；手动触发注意 concurrency 单例排队。

## 9. 需用户协助（AI 权限外）

1. ~~wopan176 的 OpenList 驱动登录令牌需要人工重抓~~ → **已降级（2026-09-14 诊断首跑后）**：
   `openlist-diag.yml` 实测**短名写 / 128B 长名 / 覆盖写 / 子目录写全部真落盘**，
   存储清单 10 个全部 `status=work` ⇒ **当前没有证据支持「令牌已失效」**。
   ✅ **「量/时长累积失效」已由 F22 兜底（2026-09-19）**：不再需要"先跑一次持续写入探针定位
   失效点" —— 批次循环已按时间周期性清缓存重探（`OPENLIST_WRITE_REPROBE_INTERVAL`，默认 30min），
   F10 又在 405 洪泛时快速止损。**故此项已无待用户动作**（见 §4 · F22 / F10）。
   **仅在将来出现"重探通过但持续 405、且重启容器也无效"的新形态时**，才需回头考虑人工重抓。
   （保留原始证据链备查：OpenList 把驱动层 8005 包装成 HTTP 405 返回给 rclone，
   run 日志里只看得到 `405 Method Not Allowed`；两条证据链是：
   - `34770092689` 唯一一次 8005 重试：刷新 token 后重跑 **48min**、`xfr#0` **零文件落盘**、
     末行仍是 `8005 错误仍然存在，继续重试...`（见 §4 · F19）⇒ 刷新并没有消除它，是持续性失效；
   - wopan 令牌有效期有上限（方法一 7 天 / 方法二 2 个月）且**无法自动续期，只能人工重抓**
     （`openlist_driver.sh:297-300` 自带这条运维提示）。
   **若最终确需重抓**：登 OpenList 管理面 → 存储 → wopan176 → 编辑 → 重新获取 token。
   重抓时**跨端互踢**：方法一别登网页版、方法二别登手机 APP，以免把其他健康挂载踢下线。）
   **诊断工具已就位**：`.github/workflows/openlist-diag.yml`（workflow_dispatch，独立 concurrency，
   单次 ~4 分钟）会把容器日志里的**原始 `rsp_code`/`rep_desc`** dump 出来，并跑
   「短名 / 名长阶梯 / 覆盖写 / 子目录写」四组探针 —— 用来在动令牌**之前**先确认到底是不是登录失效
   （而不是名长），避免又一次按错方向修。
2. ~~F16 主副本选型（保哪个后端的 4.5TB）~~ → **已取消**（用户指示：后端一个都不削减，见 §5 · F16）。
3. ~~F15 wopan176 transfers=2 实验批准~~ → **已授权自行调整**（见 §5 · F15）。
4. ~~报告三份与本计划文件是否入库~~ → **已入库**（2026-09-14，本文件与三份报告一并提交）。
5. **（新）是否允许「主动取消在跑轮以立刻验证新代码」**：队列恒 1 深、新代码进生产延迟上限 ≈6.3h
   （见 §4 · F20）。若用户接受丢掉在跑轮**未持久化**的进度，AI 可按 §0 第 5 步把验证压到分钟级。
   **AI 不会自行取消** —— 取消在跑的生产同步属于对外部系统的动作，等用户点头。

## 10. 接手指令模板（可直接粘给另一个 AI）

> 继续跟进 openlist 同步修复。先读仓库根 `openlist-sync-remediation-plan-2026-09-13.md` 的 §0（AI 接手须知，含当前旋钮默认值与实测天花板）、§6（退出标准 A–E 现状）、§8（红线）、§9（需用户协助）与文末进度日志，再读 `.github/scripts/openlist/` 的代码。
> 然后：
> 1. `gh run list --workflow=openlist.yml --limit 5 --json databaseId,status,conclusion,event,createdAt,headSha` 看最新轮 conclusion，**并用 headSha 确认它跑的是哪版代码**（schedule/自续轮钉的是"创建那一刻"的 main sha）；`gh run view <id> --log` 按 §6 关键词 grep。
> 2. **验证一律用短轮，别等 5.5h**：`sync_budget_min=60`（完整链路、预算缩短）或
>    `run_mode=调试 · 修复管线测试` + `fix_test_task=<窄任务>` + `force_sync=true`（直达修复管线）。
>    要立刻验新代码就先 `gh run cancel <在跑轮>` 腾并发位（队列恒 1 深）。
> 3. **测量纪律（踩过）**：吞吐/并发类测量必须**与主轮互斥**（两个 runner 各起自己的容器但打同一网盘账号，
>    主轮在传会把各档位压平而误判"已饱和"）；诊断 workflow 是独立的 `openlist-diag.yml`，
>    用 `thru_mb`/`thru_levels`/`xback_transfers` 取数。
> 4. 对照 §6 退出标准 A–E 记录进度（把实测证据写进对应条目，别只打勾）；出现新失败形状就地定位修复，遵守 §8 红线。
>    C 的长尾已于 2026-09-15 收口（见 §12.9）；当前无"已定位未修"的带宽浪费问题，出现新失败形状就地定位。
> 5. **改默认值前必须显式验证**：把 `pair_parallel` 之类直接改默认曾导致回归报警（只验证串行语义的测试失败 + `command not found`）⇒ 先显式参数跑一轮验证，通过再翻默认，并把依赖旧默认的测试显式声明。
> 6. 收尾前：更新本计划文档的复选框、进度日志与阶段表，跑 §8 回归套件（全套串行，数量不写死；`command not found` 扫描为空）再 push。
> 7. 需要用户做的事只有 §9 那几项，别自行代答。

---

## 11. 提速方案（2026-09-14 提出；**2026-09-14 用户已裁决**，见 §11.6）

> 说明: 本节按时间**追加**，小节编号非顺序（11.6 之后又补了 11.7–11.13），以内容为准。
> 当前生效的提速配置见 §0 的「当前旋钮」行。

### 11.1 先把「慢」量化（实测，非估算）

| 项 | 实测值 | 来源 |
|---|---|---|
| 单轮墙钟 | 5.5h（320min 预算 + ~10min setup） | `34779382573` / `34770092689` |
| 单轮覆盖同步对 | **1 个**（16 对轮转 ⇒ 绕一圈 ≈ 16 轮 ≈ **4 天**） | `task_rotation.json` cursor 8→9 |
| 单轮真实传输 | **≈ 2.06 GB**（5570 条 `Copied (new)`；trend 口径，被杀批次字节未计入） | `34779382573` |
| 有效吞吐 | **≈ 0.10–0.15 MB/s**（`transfers=1` 串行，单文件均值 ~370 KB） | 同上 |
| 待传量 | **9.887 TB** | trend 末条 |
| 日吞吐 | **≈ 9 GB/日**（2.06GB/轮 × 24h÷5.5h ≈ 4.36 轮/日） | 推算 |
| 当前速率外推 | **≈ 3.0 年** | 9887GB ÷ 9GB/日 ≈ 1098 天 |

**结论: 慢的不是调度，是「单位时间吞吐」+「每轮固定开销」。** 修 bug 只能让轮次"不白跑"，
不能让它"跑得快"。

### 11.2 四个杠杆（按 收益/成本 排序）

1. **并发上传（收益最大、成本最低，用户已批准，受 §11.6 的 423 约束）** —— `transfers: 1`
   是历史保护性设置，依据是 run `32749862280` 的整批假成功事故，**那次发生在 wopan176**；
   诊断已推翻「名长/路径/量/时长」解释，且持续写 150/150 全过、给出串行基线 ≈1.07 文件/s。
   并发写首测失败但错误点是**父目录 mkdir 竞争（423 Locked）**，已加三态探针待复测。
   - 若可通过「先建目录 / 给足重试」消除 ⇒ 提 `transfers` 到 4。**吞吐 ×4（上限）。**
   - 若三态全 FAIL ⇒ 并发不可用，转杠杆 3/4。
2. ~~冗余削减（F16）~~ —— **作废：用户明确"全部后端都需要，不能削减"**，10.1TB 全量保留。
3. **多后端并行分片（覆盖提速，中等成本）** —— concurrency 单例把 16 对串成一条队。
   若按后端分片（`concurrency: openlist-<shard>`，每 shard 只跑自己后端的同步对），
   3–4 个 run 可同时跑。成本: `sync_state`（游标/趋势/熔断）与 Dropbox 上的 OpenList
   DB 回传需按 shard 隔离，否则互相覆盖。**覆盖速度 ×3–4。**
4. **每轮固定开销（免费，有取舍）** —— 预览 36min/轮（≈11%）。`skip_preview=true` 直接省掉；
   代价是通知里失去「待同步量/预计跳过」信息，且剩余量会记 unknown（见 F8）。
   另: 4800s 探测类开销（`_refresh_openlist_cache` 的 sleep 60，F12）也在同一档。

### 11.3 组合效果（粗算）

| 组合 | 日吞吐 | 10.1TB 需 |
|---|---|---|
| 现状 | ~9 GB | ≈ **3.0 年** |
| 只做 1（并发 ×4） | ~36 GB | ≈ 9 个月 |
| 只做 4（省预览） | ~10 GB | ≈ 2.8 年（但每轮多 ~11% 干活时间） |
| 1 + 4 | ~40 GB | ≈ **8 个月** |
| 1 + 3 + 4 | ~120 GB | **≈ 3 个月** |

（杠杆 3 是覆盖提速，上限受后端总带宽约束，乐观值。并发倍数按线性外推，
实际受 §11.6 的 423 锁约束。）

### 11.6 用户裁决与诊断结论（2026-09-14）

**用户裁决**:

1. **全部后端都需要，不做冗余削减** ⇒ **杠杆 2（F16）作废**，16 对 10.1TB 全量都要同步。
2. **批准 transfers 并发实验**。
3. **`skip_preview` 默认打开** ⇒ 已改默认 `true`（省 ~11% 预算/轮）；注意取值必须走
   `github.event.inputs.skip_preview`（用 `inputs.` 时 boolean false 会被 `||` 吞掉，
   手动传 false 也会被跳过，与 `session_hold` 同坑）。
4. **同意停止用「等 5.5h 一轮」做验证** ⇒ Gate 1 改以诊断证据为准。

**诊断结论（`openlist-diag` run `34812308774`，wopan176Crypt/2，~5 分钟）**:

- 短名写 OK、名长阶梯 32/64/80/100/112/128B **全 OK**、覆盖写 OK、子目录写 OK。
- **持续写 150/150 全 OK，耗时 140s** ⇒ **「量/时长」不是失效原因**（至少到 150 文件量级），
  且给出串行基线 **≈1.07 文件/s（0.93s/文件）**。
- **并发写 transfers=4 / 20 文件 / 新目录 → 7 个失败，`Update mkParentDir failed: Locked: 423 Locked`**。
  关键: 报错点是**父目录创建**，不是文件写 ⇒ 423 很可能是 4 个 worker 同时 mkdir
  同一个尚不存在的目录造成的竞争锁，而非"后端不允许并发"。已加三态判别探针
  （新目录×4并发×retries1 / 已存在目录×4并发×retries1 / 新目录×4并发×retries3）待复测。
- **三态复测（run `34814298033`）已给出结论 —— 并发可用**:

  | 形态 | 结果 |
  |---|---|
  | 新目录 × transfers=4 × retries1 | ❌ 423 Locked（mkParentDir 竞争） |
  | **已存在目录 × transfers=4 × retries1** | ✅ 通过 |
  | **新目录 × transfers=4 × retries3** | ✅ 通过 |

  ⇒ 423 只是**父目录 mkdir 竞争**，给足重试即可绕过。**已落地 `9351e09`**:
  - `rclone_flags.sh`: `--retries 1→3`、`--low-level-retries 3→5`（两个常量都改）；
  - `task_engine.sh`: 批次 copy / 巩固串行重试同样改为 `--retries ${OPENLIST_RETRIES:-3}`
    + `--low-level-retries ${OPENLIST_LOW_LEVEL_RETRIES:-5}`；
  - `transfers` 默认 **1→4**（workflow 入参默认、脚本兜底、workflow 注入兜底三处一致
    —— 兜底留 1 会让 schedule 轮退回串行）；
  - 历史包袱处理: run `32749862280` 的 wopan176 整批假成功成因仍未定位，但现在有
    truth-check + F5 全拒熔断 + F6 跨轮熔断兜底，假成功会被当场暴露并熔断；
    某后端若仍现假成功，用入参单独降回 1。

### 11.8 405 归因排查（诊断逐条证伪，2026-09-14）

生产 run `34779382573` 里对 `wopan175/2/1024j/动漫本子/路人女主/已解压/(CSP6) [流石堂
(流ひょうご)] 淫らな彼女達の作りかた (冴えない彼女の育てかた) [中国翻訳]/` 的写入
恒定 `405 unchunked simple update failed`，而同一对 1004 个文件在上一层目录正常落盘。
用 `openlist-diag` 逐条排除：

| 假设 | 探针 | 结论 |
|---|---|---|
| 密文名 > 100B | 名长阶梯 32/64/80/100/112/128B | ❌ 全部 OK（证伪） |
| 覆盖写被拒 | 覆盖写探针 | ❌ OK（证伪） |
| 父目录名长连坐 | 子目录内写 + 父目录名 10/20/30/40/50/60 中文字阶梯 | ❌ 全部 OK（证伪） |
| 路径深度 | d1..d5 逐层加深 | ❌ 全部 OK（证伪） |
| 重启后驱动未就绪 | docker restart 后 +0/+10/+30/+60s 各写一次 | ❌ **+0s 就 OK**（证伪） |
| 并发触发限流 | 3 态并发探针 | ⚠️ 仅"新目录+重试1"失败（423 mkdir 竞争），补重试即可 |
| **真实目录名本身**（含空格/圆括号/方括号/`☆`/日文假名，且多层长名叠加） | 用**真实路径**作 diag 目标重放 | 见下方"真实路径复现" |

**真实路径复现（决定性的最后一步）**: 直接把 diag 的 `target` 指到那个真实目录
（`.../动漫本子/路人女主/已解压/(CSP6) [流石堂 (流ひょうご)] 淫らな彼女達の作りかた
(冴えない彼女の育てかた) [中国翻訳]`）→ **短名写 FAIL / 名长阶梯 FAIL / 覆盖写 FAIL /
子目录写 FAIL / 父目录名长阶梯全 FAIL(409) / 深度阶梯全 FAIL(409) / 重启后写入全 FAIL(405)**。
⇒ **该目录确实整体不可写，且在真实路径上稳定复现**（合成路径全过）。

**二分（两轮 diag 收口）**:

1. **不是路径上下文**: 把 target 换成它的真实祖先 `…/动漫本子/路人女主/已解压`，
   在其下写合成长名（10/20/30/40/50/60 中文字）与 5 层深路径 → **全部 OK**
   （run `34819021050`）⇒ 405 与祖先路径、累积长度、深度都无关。
2. **不是单个字符集**: 等长换字符集（纯中文 40 字 / 中文+空格 / 中文+全角圆括号 /
   中文+半角括号 / 中文+方括号）→ **全部 OK**；把**那个真实名字原样**放到探针父目录下
   （全新位置）→ **FAIL(405)**（run `34819769598`）
   ⇒ 触发条件是**这个具体字符串本身**，且可脱离原位置复现（不是那个目录的历史状态）。
   **尚未继续二分到具体子串**（需若干轮 diag）；对修法无影响。

**结论与修法**: 这类"特定目录名"就是写不进，且无法从命名规则上规避（目录名由源端
决定）。**唯一可行修法是短哈希折叠** —— 而折叠本身是好的（见下），此前只是被误判为失败。

**同时查出一个独立的、影响更大的 bug（已修 `待提交`）——折叠落盘校验假阴性**:

`openlist:wopan175/2/1024j/动漫本子/5b32587f`（run `34779382573` 里被判"批量折叠零落盘"
的那个短哈希目录）用 diag 复查 → **✅ 可读 18 个条目、短名写 OK**。也就是说**折叠其实
成功落盘了**，是校验读早了：OpenList 对**新建目录**的列表有缓存延迟，折叠刚写完立刻
`rclone lsf` 会返回 0 条，于是被判"零落盘"→ 退回逐文件修复（白跑）且成果不进 marker
（后端有文件、账上没有 = 幽灵落盘，下一轮再折一次）。这直接解释了"修复率长期 8.6%"。

落地:
- `file_fix_pipeline.sh` 折叠落盘校验: 读空 → 刷服务端缓存（`_ol_refresh_path_cache`）
  重读，最多 3 次才定论；日志记录每次重读。
- `file_fix.sh` 新增 `_probe_file_visible`（带"刷缓存 + 重读"的探针可见性判定），
  `_fix_probe_dir_writable` 的缓存口径与真值口径两处读回都改用它——同类假阴性在
  目录可写性预检里会让**可写目录被整体跳过**。
- 重试次数可配（`OPENLIST_PROBE_READ_RETRY` / `OPENLIST_PROBE_REFRESH_WAIT`）:
  单测没有 `_ol_refresh_path_cache`，兜底等待会把套件从秒级拖到 2 分钟（实测），
  两个相关测试设 `OPENLIST_PROBE_READ_RETRY=1`。
- 测试: `test_hash_dir_fallback.sh` 新增场景 14（读空→刷缓存→第 2 次可见即判可写 /
  确实刷了 1 次 / 始终不可见仍判不可见）。

### 11.9 「分片并行」可行性结论（2026-09-14，**跨 job 方案不可行，改走 job 内并行**）

原本设想的杠杆 3 是「按后端分片、多 run 并行」。读过 workflow 后结论是**跨 job 并行
在当前基础设施上不可行**：

1. **隧道守卫明确禁止两实例共存**（`检查 Cloudflare 隧道状态` step）:
   `隧道已有活跃连接 → 其他实例正在服务，终止本次运行避免两端争写同一份数据`（exit 1）。
   这不是随手加的限制，它保护的是"两个 job 各有自己的 OpenList 容器 + 各自的 DB 副本，
   同时写同一份 Dropbox 数据"。
2. **DB 单份**: 每个 job 启动时拉 `dropbox:self-hosted/openlist/data`、收尾回传；
   两 job 并发 = 后写者覆盖先写者（丢配置/缓存/令牌变更）。
3. **sync_state 单份**: 游标 `task_rotation.json`、`trend.jsonl`、`backend_dead.json`
   都是"读-改-写"，并发会丢更新。
4. `concurrency: openlist-singleton` 且 **schedule 轮无法传 inputs** ⇒ 分片组名只能
   手动 dispatch 才生效。

要跨 job 分片，需要用户侧基建: 建 N 条隧道 + N 个 DNS 路由、DB 按分片拆/限定
单一 owner、state 文件名按分片加后缀。**成本高、风险中，不建议现在做。**

**可行且便宜的替代: job 内并行（同一 job 跑多个同步对）** ——
同一个容器、同一份 DB、同一条隧道，天然满足隧道守卫的约束（仍是单实例）。
复用已有的 `OPENLIST_SUBDIR_PARALLEL` 机制（worker 子进程 + 状态文件 + 父进程统一渲染），
把并行粒度从"一对内的子目录"提到"多个同步对"，并按后端分配 worker（同一后端不并行，
避免把单后端打爆）。收益: 不同后端互相独立时，覆盖速度近线性。

**执行顺序建议**: 先看 B 轮（transfers=4）实测倍数 →
① 若已达 ≥2×，先吃这个收益，job 内并行列为下一步；
② 若并发受限（后端总带宽或锁），则 job 内并行是唯一剩下的加速手段。

### 11.12 管线利用率：一轮 21% 花在盲等上（2026-09-14 静默间隔分析，已部分处理）

对 run `34793014398`（334min 轮次）做"≥55s 无日志输出"的静默间隔分析，合计 **151min**：

| 归类 | 时长 | 段数 | 性质 |
|---|---|---|---|
| 盲等: 缓存刷新 | **42.1min** | 42 | 主体是**两次 `rclone size`**（刷新前后各一次，各可耗 120s），不是 sleep |
| 盲等: 驱动就绪 | **29.0min** | 22 | 容器重启后的 `sleep 60`（已改为自适应轮询，见下） |
| 预览/注册（逐对） | 17.5min | 4 | `skip_preview` 默认开启后应大幅下降 |
| rclone 报错/重试 | 6.9min | 3 | 传输失败的退避 |
| token 保鲜循环 | 2.6min | 1 | |
| 其余（传输进度行之间） | ~50min | ~53 | 传输本身 |

**已做**（`4af1cbc`）: 新增 `_wait_driver_ready`（轮询"目标路径可列出"当就绪信号，3s 一次），
两处容器重启后的 `sleep 60` 改用它；**上界仍 60s**，最坏情况与旧行为一致，只赚不亏。

**未做（下一轮候选，需单独设计）**: 无 —— 缓存刷新那 42min 的主体（两次全量
`rclone size`）已确认**只用于日志对比、不参与控制流**，已默认跳过（5c9989a，
`OPENLIST_CACHE_REFRESH_COUNT=1` 可恢复排查）。剩余盲等只有缓存刷新后的
`sleep 60`（等待异步刷新完成，缩短会落入"读太早"类问题，保留）。

**方法论沉淀**: 用"静默间隔 + 按前一条消息归类"定位时间黑洞，比读日志猜高效得多——
脚本已在本会话验证可用（python 解析 `build\tSTEP\t<ISO ts> <msg>`，注意 `%f` 只吃 6 位小数，
日志有 7 位，需截断）。

### 11.11 提速的天花板在哪里（2026-09-14 实测收口，**结论级**）

三组实测把"还能不能更快"钉死了：

| 测量 | 结果 | 含义 |
|---|---|---|
| 单后端 `transfers=1` | 0.32–0.60 MiB/s | 单流慢 |
| 单后端 `transfers=4` | 0.86–**1.20** MiB/s（**1.7–3.8×**） | 提并发有效，但**不是线性** ⇒ 存在聚合上限 |
| **runner 出口带宽**（同 6 MiB 传 Cloudflare） | **19.12 MiB/s** | 出口**不是**瓶颈 |
| **跨后端并发**（wopan175 + baidupanCrypt 同时各跑一条 transfers=4） | A=0.55、B=0.25、**合计 0.50 MiB/s** | **低于**单后端独跑 1.20 ⇒ 两后端**共享**同一条瓶颈，跨后端并行**无增益且有害** |

⇒ **瓶颈是从 runner 到国内网盘的那条国际链路（共享），既不是 runner 出口，也不是管线。**
换算: 10.1TB ÷ 1.2 MiB/s ≈ **130 天**（100% 利用率、且只用一个后端的上限）。

**连带结论**:

1. **job 内多同步对并行（`3f53957`，已实现、默认关闭）的前提被实测推翻** —— 它假设"不同
   后端有各自独立的带宽额度"，而实测两后端共享同一条瓶颈。故**保持默认 1 不启用**；
   实现与测试保留（机制本身无害，且若将来换成两条真正独立的链路即可直接用）。
2. **管线侧还剩约 2× 的利用率空间**: 生产单轮实得 ≈0.66 MB/s（13.08GB/5.5h），
   对照单后端上限 1.2 MiB/s ⇒ 还有 ~1.8× 可从"减少非传输阶段"（truth-check 重启、
   巩固、修复管线）里挤出来，但**天花板就是 1.2 MiB/s，不是 4–8×**。
3. **真正的提速只能改网络路径**（工程外）:
   - 用**离目的地更近的 runner**（用户已有 RDP / Tailscale 那台机器，若在境内则效果最大）；
   - 或给上传链路挂一个**对国内路由更好的代理/VPS**；
   - 或接受现状重算工期（≈130 天起，实际因利用率约 200 天）。
4. **此前"3–4 周/7–12 个月"的乐观估算作废** —— 那是按"并发线性叠加"外推的，
   实测只到 1.7–3.8× 且有共享上限。

### 11.10 已落地的加速项与换算（2026-09-15 更新：**实测 4.15×，工期 ≈1.4 个月**）

**实测验证（run 34871720751，6441561 = 今日全部修复）**:
- **单轮传输 54.37 GB**（transfers=1 基线 13.08GB 的 **4.15×**）⇒ transfers=4 + subdir 深度放宽 ≤1 的并发组合**按预期生效**
- **修复管线首次完整工作**: 成功 69（新落盘 69 · 沿用 0）· 缺失 6 · 未修复 0 —— 此前长期"成功 X 全是沿用/0 修复"
- 文件列表获取不完整 0 次；重启后列表未就绪 7 次（4af1cbc 回归，已回滚 eb29536）；折叠"零落盘"仍有 4 次（凑齐轮询 6×30s 对部分目录不够，见下）

| 项 | 改动 | 机制 |
|---|---|---|
| 并发上传 | `transfers` 1→**4** | 大文件流是**带宽主导**（≈374KiB/s/流），并发按流叠加 |
| 子目录并行 | `subdir_parallel` 1→**2**，**深度放宽到 ≤1** | 顶层子目录 worker 并行；**实测原条件 depth=0 对"顶层只有 1 个子目录、第二层才有 ≥2 个"的常见结构永不成立**（run 34826097133 并行标记 0 次），放宽后该层（动漫本子/动漫图片/…）真正并行。与 transfers 相乘 ⇒ **单后端 8 个并发 PUT** |
| 预览/注册 | `skip_preview` 默认 **true** + **注册模式跳过源端全量列举** | 预览 36min + 仅注册 31min ≈ **67min/轮（20%）全部消失**；后者（31min）是对大源做全量递归列举只为面板"源端 X GB"展示，零控制流依赖 |
| 拆分/批次阈值 | **统一 20GB**（原子目录拆分错用文件批次的 5GiB） | ≤20GB 的目录直接整体同步，不再逐层下钻；批次变大 ⇒ 批次间巩固（含容器重启）减少 4 倍 |
| 在途保护 | F23 + 重试 3/5 | 不再被 330min 硬杀、423 mkdir 竞争靠重试兜过 |
| 折叠 | 落盘校验改为「凑齐期望条数」轮询（6×30s） | 修复"折叠成功被误判失败"（长期 8.6% 修复率的真因之一）；**仍有 4 次零落盘**（可见性延迟 >3min 的目录），下一轮 rclone sync 会看到它们已在后端 → 记录自愈；若持续出现再把上界调大 |

**换算**（以实测 54.37GB/轮为基）:

- 54.37GB/轮 × 4.36 轮/日 ≈ **237 GB/日** ⇒ 10.1TB ≈ **42 天（1.4 个月）**
- 对照: transfers=1 基线 ≈57GB/日 ≈ 6 个月；今日修复前的"wopan176 假成功时代" ≈3 年

| 项 | 改动 | 机制 |
|---|---|---|
| 并发上传 | `transfers` 1→**4** | 单条流实测 ≈1.07 文件/s（0.93s/文件）是**往返延迟主导**（文件小、大小无关），并发生效 |
| 子目录并行 | `subdir_parallel` 1→**2**，**深度放宽到 ≤1** | 顶层子目录 worker 并行；**实测原条件 depth=0 对"顶层只有 1 个子目录、第二层才有 ≥2 个"的常见结构永不成立**（run 34826097133 并行标记 0 次），放宽后该层（动漫本子/动漫图片/…）真正并行。与 transfers 相乘 ⇒ **单后端 8 个并发 PUT** |
| 预览/注册 | `skip_preview` 默认 **true** + **注册模式跳过源端全量列举** | 预览 36min + 仅注册 31min ≈ **67min/轮（20%）全部消失**；后者（31min）是对大源做全量递归列举只为面板"源端 X GB"展示，零控制流依赖 |
| 拆分/批次阈值 | **统一 20GB**（原子目录拆分错用文件批次的 5GiB） | ≤20GB 的目录直接整体同步，不再逐层下钻；批次变大 ⇒ 批次间巩固（含容器重启）减少 4 倍 |
| 在途保护 | F23 + 重试 3/5 | 不再被 330min 硬杀、423 mkdir 竞争靠重试兜过 |
| 折叠 | 落盘校验读空改为刷缓存重试（4×15s） | 修复"折叠成功被误判失败"（长期 8.6% 修复率的真因） |

**换算**（基线已上调：A 轮 13.08GB/轮，不是 2.06GB —— 健康后端 wopan175 的实测）:

- A 轮（transfers=1，无今日修复）: 13.08GB/5.5h ≈ **57 GB/日** ⇒ 10.1TB ≈ **6 个月**
- 8 条流 + 注册/预览省 67min + 拆分开销下降: **天花板 1.2 MiB/s ≈ 100 GB/日 ≈ 3.3 个月**；
  按管线利用率 50→70% 估，现实区间 **4–6 个月**

### 11.13 补充：缓存刷新的两次全量 size 默认跳过（5c9989a）

静默间隔分析里"盲等: 缓存刷新 42min"的主体不是 sleep 60，而是**两次全量 `rclone size`**
（刷新前后各一次，单次最长 120s）——而它们**只用于日志对比**（`before_count !=
after_count` 仅影响一条 warning，不参与任何控制流）。已默认跳过
（`OPENLIST_CACHE_REFRESH_COUNT=1` 可打开排查"缓存是否真的过期"），
POST 刷新 + 等待语义不变，预计省 ~30min/轮。

| 项 | 改动 | 机制 |
|---|---|---|
| 并发上传 | `transfers` 1→**4** | 单条流实测 ≈1.07 文件/s（0.93s/文件）是**往返延迟主导**（文件小、大小无关），并发生效 |
| 子目录并行 | `subdir_parallel` 1→**2** | 顶层子目录 worker 并行（既有机制），与 transfers 相乘 ⇒ **单后端 8 个并发 PUT** |
| 预览 | `skip_preview` 默认 **true** | 省 36min/轮 ≈ 11% 预算，全部转为传输时间 |
| 在途保护 | F23 + 重试 3/5 | 不再被 330min 硬杀、423 mkdir 竞争靠重试兜过 |

**换算**（按文件数与延迟外推，非带宽外推）:

- 现状: ≈9 GB/日（2.06GB/轮 × 4.36 轮/日）
- 并发 ×4 + 子目录 ×2 = 8 条流 ⇒ 乐观 ≈70 GB/日；受后端总带宽与固定开销拖累，
  **保守按 3–5× 估 ≈27–45 GB/日**
- ⇒ 10.1TB 由 **≈3.0 年** 收到 **≈7–12 个月**

**仍不够快的原因**: 一轮只覆盖 **1 个同步对**，且有大量与传输无关的固定开销
（truth-check 重启、巩固、修复管线）。8 条流的收益被这些开销吃掉一部分。

**下一个杠杆（job 内多同步对并行）**: 把并行粒度从"一对内的子目录"提到"多个同步对"
（复用同一 worker 机制，按后端分配）。这样固定开销互相重叠，覆盖率也上去。
**优先级: 先看 B 轮实测倍数（§11.7），再决定是否投入。**

### 11.7 提速效果的判定口径（下一轮起）

同一同步对（`task2 → openlist:wopan175/2`）上做 A/B：

| 指标 | A 组（transfers=1） | B 组（transfers=4） |
|---|---|---|
| 轮次 | `34793014398`（8acfc6f，在跑） | `34815057776`（9351e09，排队） |
| 看什么 | 文件数/耗时 ⇒ 文件·秒速率 | 同口径对比，期望 ≥2–3× |
| 兜底判据 | truth-check「重启前后列表一致」 | 同左；若出现假成功 ⇒ F5/F6 应触发熔断 |

注意 B 组跑的是**下一个同步对**（游标在该轮开始时已后移），两者不是严格同对；
若差异不明显，用两轮各自的 `Copied (new)` 条数与 `Transferred` 速率归一化后再比。

### 11.4 建议的路线调整

1. **停止用「等 5.5h 一轮」做验证** —— 已有 `openlist-diag.yml`（64 秒）与
   `调试 · 修复管线测试`（分钟级），迭代成本已经打到可忽略。
2. **Gate 1 判据调整**：原写「Phase 1+2 落地并观察 2 轮」（≈11h）。现在能 4 分钟直接
   测出后端可写性与失效点，**建议改为以诊断证据为准**，不再用时间兑换样本。
3. **优先级重排**：并发实验（杠杆 1）＞ 冗余削减（杠杆 2，需选型）＞ 假阴性探针修复
   （见 11.5）＞ F10 fail-fast ＞ 其余 F 项。
4. **不建议**再投入 F12/F18 这类"省几分钟"的项，直到杠杆 1/2 定论。

### 11.5 顺带发现的必须修项（来自 `34779382573` 的取证）

`_fix_probe_dir_writable` 对 `wopan175/2/1024j/动漫本子/…` 报「❌ 目录不可写（重启后探针
消失）」+「批量折叠零落盘」，但**同一目录有 1004 个真实文件落盘**（truth-check 确认）
⇒ 目录可写性探针存在**假阴性**，直接导致 84 个文件被判"无目录可换"而进不了修复。
这与 F22（探针时序）不同，是**探针正确性**问题，优先级应提到 F22 之前。

## 进度日志

- 2026-09-13 · 计划制定（基于三报告 + HEAD 核验 + F0 新发现）。Phase 0-4 未开始。
- 2026-09-14（+08）· Phase 0 完成 + Phase 1（F0-F5）全量落地并 push。
  - 现状取证（run 34752801560，Phase 1 前最后样本）: 修复成效 成功 183 / 缺失 2126 / 未修复 1943，成功数与上轮逐字相同 ⇒ **新落盘 0**；批次 1 顽固缺失 1037（与上轮同值，F0 假成功闭环复现）；grep「后端写入全拒」= 0 条（F5 旧判据恒不触发，实锤）；13:48Z 起熔断刷屏致短哈希目录全部"未试写"（F3 实锤）；日志硬编码 transfers=1 掩盖实际 4（F0 烟幕实锤）。
  - 落地: `339d58a`（F0 transfers 统一 + F1 批次预算闸 + F1 配套 finalize 不写成功 marker + F2 熔断短路 + F5 全拒判据下移）、`2f60f26`（F3 名长解耦第一步 + F4 写探针分层到任务子路径 + 复核前刷缓存）。
  - 回归: 21 套串行，18 EXIT=0；`marker_skip_guards` / `sync_trend_budget` / `truth` 为既有环境失败（已对拍 HEAD 确认）；`command not found` 扫描为空（顺带补了该测试缺失的 `tg_add_entry_text` / `_batch_budget_stop` stub）。
  - 退出标准 A-E 当前**全部未满足**（基线见 `/tmp/openlist-remediation-baseline.txt`）；观察轮 `34770092689` 已于 2026-09-13 16:56Z dispatch，按 §6 观察。
- 2026-09-14（+08）· Phase 2 第一批落地（F6/F7/F8/F11）+ 观察轮 1 结果回读。
  - 落地: `3737688`（F7 修复成效拆分 ROUND_REUSED + 收尾看"新落盘"、F8 源端失败记 unknown、
    F11 SESSION_HOLD 兜底 'true'→'false'）、`e5296ed`（F6 后端熔断跨轮持久化
    `sync_state/backend_dead.json` + TTL 12h + 全线皆死保护 + 新测试 10 项）。
  - 观察轮 `34770092689`（2f60f26）**new: A 达标 1/3** —— 首次非硬杀的 success 收场，
    F1 预算闸生效；同时 **F5 首次真实触发**（「后端写入全拒（1036/1036）→ 中止剩余 2 批」，
    省约 80min）；F0 生效（日志打印真实 transfers=1）；F4 生效（写探针改到任务子路径
    `openlist:wopan176Crypt/2/1024j/动漫本子`，探针通过）。但 **B/C/D/E 全部未达标**:
    批次整批无真实落盘、transferred 2.08GB vs 落盘 0、仍只碰 wopan176Crypt、告警未触发。
  - 注意: 观察轮用的是 2f60f26（Phase 1），F7/F8/F11/F6 要等后续轮次（当前在跑的
    `34779382573` = 06cc0df、排队 `34787645966` = 0eef148，均只含 Phase 1）。
  - 计划文件 §6 · 退出标准处已按轮次逐条记录实测证据。

- 2026-09-14（+08，第二轮）· 观察快照 + F6 触发源接线落地。
  - **无新完成轮**: 最新完成轮仍是 `34770092689`（Phase 1），A–E 无新增数据点（逐条证据
    见 §6 末尾的「2026-09-14 08:05 观察快照」）。在跑 `34779382573` = `06cc0df`（Phase 1，
    step 18 已 1h48m，330min 上限 ⇒ 约 11:40 +08 收场），排队 `34787645966` = `0eef148`
    （同为 Phase 1）。**两个在队轮次都钉在 Phase 2 之前的 sha**，但无需人工干预: 在跑轮正常
    收场后自续触发会用「触发那一刻」的 main，Phase 2 全量进下一轮。
  - rclone 侧: `task_rotation.json` = `cursor 9 / attempts 1 / updated 22:55:23Z` —— 该写入正是
    在跑轮的"执行前落盘"（idx 9），即本轮又回到上一轮同一对 wopan176Crypt 重试；
    `backend_dead.json` **不存在**（F6 从未被执行过，符合预期）。
  - **落地 `acbbff5`**（F6 触发源接线，见 §4 · F6 补充）: F5「后端写入全拒」同时置
    `SYNC_BACKEND_DEAD=1`，把 F6 的熔断点从 ~2.5h 提前到批次 1 巩固结束。**这是本轮唯一的
    代码改动**——F10/F12/F13 评估后推迟（理由写进各自小节），因为下一轮要干净地验证
    F6/F5 接线（Gate 1 的唯一输入），再塞热路径改动会让归因失真。
  - **F9 前提已核实、范围收窄**（见 §4 · F9）: 趋势记账本来就是即时落盘（`sync_trend.sh:42-46`
    每次调用即追加 `/tmp` 文件），「强杀丢内存计数」对 trend 这条链不成立；**新证据**是
    `transferred_bytes` 出现逐字相同的重复值（`18282971`×3、`171257627`×3）而同期 remaining
    摆动 2.5TB ⇒ 同一批内容每轮被重新"传输"，`transferred_bytes` 是「rclone 声称量」而非
    「落盘量」，C 达标必须换判据。
  - **静态核验（待下轮实测确认）**: F6 的信号链能穿过函数边界——`_fix_probe_dir_writable`
    （`file_fix.sh:738`）在 `file_fix_pipeline.sh:377`、`file_fix.sh:1101/1276` 都是普通调用
    （无管道/命令替换），`while ... done < <(...)` 走进程替换在主 shell 执行，故
    `_BACKEND_DEAD` / `SYNC_BACKEND_DEAD` 能传到 `run_all_tasks` 的 `elif`；批次路径
    `sync_by_file_batches`（`task_engine.sh:755/769`）同样无子 shell，中止出口置 `SYNC_FAILED=1`
    后走 `_backend_dead_mark`。**串行（默认 subdir_parallel=1）成立；并行 worker 是子 shell，
    标志传不回父级**，与既有认知一致。
  - 回归: 22 套串行 / 20 EXIT=0；非 0 仅 `marker_skip_guards`（macOS 无 `date -d`）、
    `truth`（需 docker）；`command not found` 扫描为空。§8 · 回归基线数字已按实测更新
    （旧文写「19 个测试 / 16 EXIT=0」与现场对不上）。

- 待办/未做（2026-09-14 第二轮更新）:
  - **等观测（最高优先）**: 下一轮（首次含 Phase 2 全量代码）是 Gate 1 的唯一输入——看 F6 是否
    写入 `sync_state/backend_dead.json`、`run_all_tasks` 是否打印「后端跨轮熔断: N/M 个后端在
    TTL 内被判死」与「⏭ 同步对轮转…跳过让路」，以及退出标准 D（非 wopan176 的同步对开始被执行）
    能否首次达标。`gh run view <id> --log` 后按 §6 关键词 grep。
  - F3 第二步（根目录文件的短哈希兜底）。
  - Phase 2 剩余: F9（范围已收窄，见 §4 · F9）、F10 / F12 / F13（推迟理由已写进各自小节）。
  - Phase 3: F14 三后端写入自检起（含 Gate 1/2）。
  - **Gate 1 判据提醒**: 要求 Phase 1+2 落地后**观察 2 轮**。目前 Phase 1 观察 1 轮、Phase 2
    观察 0 轮，**未到**；下一轮若仍复现「wopan176 整批零真实落盘 + 熔断/轮转工作正常」，
    才可判账号级死亡并转 Phase 3 后端切换。

- 2026-09-14（+08，第三轮）· 观察快照 + 两项认知纠正 + 补 push 滞留 commit。
  - **无新完成轮**，A–E 无新增数据点（逐条证据见 §6 · 「2026-09-14 09:10 观察快照」）。
    轮次盘点: 在跑 `34779382573`（`06cc0df`，Phase 1）、排队 `34793014398`（`8acfc6f`，**Phase 2 第一批**，
    即 Gate 1 的第一个 Phase 2 数据点）、`34772641066`/`34787645966` 已 cancelled。
  - **发现并修复一个流程缺陷**: `acbbff5`（F6 触发源接线）**只在本机 commit、从未 push**——
    上一轮日志写"落地"但 origin/main 上没有它（`git rev-list --left-right --count origin/main...HEAD` = `0 1`）。
    本轮已补 push，并把上一轮遗留未提交的计划文档改动一并入库。**教训: 收尾务必核对 push 真的成功
    （沙箱会拦 push），别只写"已落地"。**
  - **纠正①**: cursor=9 不是"回到同一对 wopan176Crypt 重试"，而是 `task2-wopan175 → openlist:wopan175/2`
    （注册表 16 对索引逐条导出核对 + 日志 `注册进度: 2/16 onedrive:2 → openlist:wopan175/2` 实锤）。
  - **纠正②**: 自续触发**没有**接力——`34770092689` 尾部实锤「已有 1 个排队/等待中的运行，跳过接力」（护栏 3）。
    ⇒ 新代码进生产延迟 ≈ 2 轮（~11h），"push 完下一轮就生效"的假设不成立。
  - **F9 覆盖面已核实**（写进 §4 · F9）: `trend_record_transferred` 全库只有两处调用点，**批次路径整体
    不计入趋势**（`batch_transferred_bytes` 只喂进度行；尾部那次 `sync_with_logging` 跑在
    `OPENLIST_FIX_TEST_MODE=1` 下、日志被置空 ⇒ 记 0）⇒ 该指标同时偏高（含假成功）与偏低（漏批次量）。
    **不改代码**（属指标定义问题，且 C 达标已定要换判据）。
  - **F6 链路复核到行**（写进 §6）: `_backend_dead_mark` 会同步更新内存熔断表 ⇒ 同轮即可跳过后续同后端对；
    注册表 4 个后端 ⇒「全线皆死」保护不误伤；链路无子 shell；`SYNC_BACKEND_DEAD` 无执行期覆盖点。
  - **本轮零代码改动**（§6 迭代纪律: 失败形状复现优先于新功能；下一轮要干净验证 F6/F5 接线）。
  - 回归（§8）: 22 套串行 / **20 EXIT=0 / 2 非 0**（`marker_skip_guards` 无 `date -d`、`truth` 需 docker，
    均为既有环境性失败）；`progress_no_orphans` 单跑即过（PASS=16 FAIL=0，确认 flake）；
    **全部日志 `grep "command not found"` = 0 命中**。基线数字与 §8 一致，无需修订。
  - 待办不变: 等 `34793014398`（Phase 2 第一批）收场 → 再等一轮含 `acbbff5` 的 → 才够 Gate 1 的"观察 2 轮"。

- 2026-09-14（+08，第三轮续）· 从上一轮的日志里挖出**裁决性证据** + 两处小改动。
  - **§1.2 悬而未决的「短哈希目录可写性」已裁决（采信 ds4.1「后端全死」）**: `34770092689` 里
    F3 第一步生效后，目录级批量折叠**真的被执行了**——`19:32:57`（44 个文件）→ 短哈希目录
    `f22c8c73`、`19:36:03`（39 个文件）→ `cb4f0d57`，两次都在 `19:39:01` 熔断**之前**（未被短路）；
    结果两处都是 `❌ 批量折叠零落盘（rc=0）`（`19:34:39` / `19:37:37`）。
    ⇒「缩短名字即可绕过」在真实路径上被**直接证伪**；F3 第一步的验收（短哈希目录有实测写尝试）**达标**。
  - ~~**F3 第二步推迟（评估后）**~~ → ✅ **已落地（2026-09-16，`2313423`）**: 原判"`无目录可换` 日志 0 次
    ⇒ 从未复现、收益存疑"**已作废** —— 那次观测发生在**修复管线还拿不到预算**的时期（跑不到这一步）；
    留出修复尾段（`11c1761`）后，实测 run `34959561878` 一轮里 `无目录可换` **150 次**，
    且占该轮修复失败（49 次）的绝大多数 ⇒ 它是**修复失败的主导原因**，不是"存疑收益"。
    原判的后半句（"方法 2 已把文件名改短，再套一层短哈希目录只会让路径更长"）也站不住:
    方法 2 只改**文件名**、路径不变，而**路径**正是 §11.8 证明可被"特定字符串"拒收的那一维 ——
    根目录文件恰恰是唯一**从未试过换路径**的一类。详见 §12.10。
  - **§6 · C/E 补精确证据**: C 的假成功样本 = 批次 1 巩固自述「本批成功 1036 个 / 直接失败 0 个」
    → 复核「1036/1036 全部未落盘」；E 的收尾数字 = `成功 183 · 缺失 2146 · 未修复 1963`，
    而 183 条**全部**是 `♻ 沿用上轮修复`（新落盘 0）。
  - **代码改动（1 处文案 + 1 处测试）**:
    1. `task_engine.sh`「跳过让路」文案去掉「上轮判死」——F6 触发源接线落地后判死也可能发生在
       **同一轮**的批次巩固里，原文案与事实相反；下一轮就会命中这个形态，先修掉免得日志误导。
    2. `tests/test_backend_dead_round.sh` 场景 5 补 **5c/5d**: 断言 `_backend_dead_mark` 之后
       **同一轮内**后续同后端对（p2）当场被跳过、且日志含「跳过让路」。原场景 5 只断言落盘，
       把内存熔断表那行删掉测试照样全绿（假通过）——而退出标准 D 正是靠这条性质在同一轮成立。
       **已负向验证**: 注掉 `_BACKEND_DEAD_ROUND["$root"]="$now"` 后 5c/5d（连带 4a/5b/6b）变红，
       恢复后 **12 PASS / 0 FAIL**。
  - **`AGENTS.md` 回归基线纠正**: 原文「22 套中 19 套 `EXIT=0`」+ 列 4 个非 0（19+4≠22）自相矛盾；
    实测应为 **20**（非 0 仅 `marker_skip_guards`、`truth` 两项环境性；`progress_no_orphans`、
    `sync_trend_budget` 为 flaky）。已改为与 §8 一致。
  - 回归（§8）: 22 套串行 / **20 EXIT=0 / 2 非 0**（同上两项环境性）；本轮 `progress_no_orphans`
    直接 EXIT=0（未触发 flake）；**`command not found` 扫描 0 命中**。耗时 8m42s。
  - 待办不变: 等 `34793014398`（Phase 2 第一批）收场 → 再等一轮含 `acbbff5` 的 → 才够 Gate 1 的"观察 2 轮"。

- 2026-09-14（+08，第四轮）· 用户答「账号正常 / 后端不削减 / transfers 可调 / 进度太慢」后，
  把「慢」归因到底并落地两处代码修复 + 一个独立诊断 workflow。
  - **用户指示（本条为规格变更，实现须服从）**：
    1. 「wopan176 账号状态正常」—— **但这不等于 8005 消失**：8005 是 OpenList **驱动层登录令牌**失效，
       与网盘账号是两件事。§9 第 1 项已按此改写（附两条证据链 + 需用户执行的具体动作）。
    2. 「不要削减后端，全部都是需要的」⇒ **F16 冗余削减整体取消**（§5 · F16 标 ❌，连带 Gate 3 失效待重定义）。
    3. 「可以另外写一个测试 workflows」⇒ 新增 `openlist-diag.yml`。
    4. 「transfers 你可以根据情况调整」⇒ **F15 不再需要批准**（§5 · F15 已改写；但前置依赖 8005 未解，
       此时调 transfers 测不出差异，故本轮不动）。
  - **F21 写探针判死信号键错位（本轮最值钱的发现）**：`_backend_write_probe` 按**同步对路径**缓存
    （`openlist_driver.sh:447/452`，键恒为 `$dest`），而 `sync_with_logging` 两处读取曾按
    `_backend_root_of` 的**后端根**取 —— **键对不上 ⇒ 判死信号恒丢 ⇒ `SYNC_BACKEND_DEAD` 永不置位**
    ⇒ 既不"立即后移游标"、也不调 `_backend_dead_mark` ⇒ 死后端只能靠 `ROTATION_MAX_CONSECUTIVE_ATTEMPTS`
    （默认 8）阀门脱身 ⇒ **8 × 5.5h ≈ 44h**。**这正是 §1.1 第 5 条「游标钉死」的直接成因，
    此前一直被归因于"熔断器判据不对"，方向偏了。** 修法：两处读取改用 `$dest_path`。
  - **F19 8005 重试前写探针短路**：`_sync_retry_8005` 刷新驱动后直接跑整次重传。实测（`34770092689`）
    单次重试 `18:41:16Z → 19:29:17Z` = **48min**，`450.089 MiB` 重传但 `xfr#0`（**零文件落盘**），
    末行仍 `8005 错误仍然存在` ⇒ 刷新 token 无效。改法：刷新后先 `unset` 缓存再跑 `_backend_write_probe`，
    不过就置 `SYNC_BACKEND_DEAD=1` 并 `break`（连带省掉 `_refresh_openlist_cache` 的无条件 `sleep 60`）。
  - **新增单测 2 个，均已负向验证**：
    `test_backend_dead_signal.sh`（8 断言；注入"按后端根读"后 2/3/5a/5b 变红）；
    `test_8005_probe_gate.sh`（10 断言；注释掉闸门后 1a/1b/1c/1d/1e/3 变红）。
  - **新增独立诊断 workflow**（用户授权）：`.github/workflows/openlist-diag.yml` +
    `.github/scripts/openlist/diag_backend.sh`。最小 setup、**独立 concurrency `openlist-diag`**
    （绝不与 `openlist-singleton` 互等）、不装 cloudflared/不接管隧道/不回传数据库、`timeout-minutes: 40`、
    单次 ~4min、上传 artifact。四组探针：短名 / 名长阶梯（32–128B）/ 覆盖写（`unchunked simple update` 同形态）/
    子目录写；并把容器日志里的**原始 `rsp_code`/`rep_desc`** dump 出来 —— **这是当前最大的观测盲区**
    （主 run 日志里只有包装后的 405）。
  - **纠正上一轮的两处误读（都已写进 §6 快照与 §4 · F20）**：
    1. **不是队列积压**：GitHub concurrency **会自动取消被顶替的 pending 轮**（`34787645966` 被
       `34793014398` 顶掉即实测）⇒ 队列恒 1 深、总是最新创建的 ⇒ 新代码进生产延迟上限 **≈6.3h**，
       **不累积**。上一轮写的「≈2 轮 / 11h」偏高。
    2. **schedule 轮取 sha 有分钟级滞后**：`acbbff5` commit 于 `00:28:24Z`，而 `00:32:26Z` 创建的
       `34793014398` 仍钉在 `8acfc6f`（`00:27:52Z`）⇒ 滞后 ~4.5min。
  - **在跑轮的代码版本核实**：`34779382573` = `06cc0df`，**落后 HEAD 15 个提交**，
    `git merge-base --is-ancestor` 逐条确认**不含 `e5296ed` / `acbbff5`** ⇒ 该轮连 F6 都没有，
    只是"又一个 Phase 1 样本"。
  - **新增「验证起跑规程」写进 §0 第 5 步**（非代码）：要立刻验新代码就取消 pending（必要时连在跑轮）
    + `gh workflow run`。**AI 不自行取消在跑的生产同步**，已列为 §9 第 5 项等用户点头。
  - **对 F18 的连带修正**：其原始理由「run 列表塞满过时排队」已被 concurrency 自动取消消解大半，
    且降频会**拉长**新代码进生产延迟 ⇒ **本轮起不建议做 F18**（理由写进 §4 · F20）。
  - **AGENTS.md 待同步**：§8 回归基线套数已从 22 变 24（新增两个测试文件），AGENTS.md 与本文件
    「22 套中 20 套 EXIT=0」需一并改为 24 套口径（本轮随代码一起改）。
  - **`` 词边界坑（本机 BSD grep）**：`grep -oE '\b(4[0-9]{2})\b'` **静默零匹配**（与已知的 `\S` 同类）。
    诊断脚本里改用「码 + 空格 + 字母」形态 `(4[0-9]{2}|5[0-9]{2}) [A-Za-z]`，顺带避开进度行 `450.089 MiB`。
  - 回归（§8）: **24 套串行 / 22 EXIT=0 / 2 环境性非 0**（`marker_skip_guards` 无 `date -d`、`truth` 需 docker）；
    **`command not found` 扫描 0 命中**。另有 2 项在套件运行期被沙箱拦子进程造成假红
    （`batch_consolidate` 12b/13d、`bulk_hash_fold` T3a，日志里都带 `Brokered program policy check unavailable`），
    **单独复跑均全绿**（61/0、29/0）⇒ 判定为环境假红，非本轮改动回归。两个新测试 `test_backend_dead_signal.sh`、
    `test_8005_probe_gate.sh` 首跑即 EXIT=0。

### 2026-09-14（+08）第二轮 · 并行会话（本会话: 观察轮 `34779382573` 取证 + F23）

- **观察轮 `34779382573`（`06cc0df`）新失败形状**: 前段被预览 36min + wopan175 各子目录
  initial sync / truth-check **2h36m** 吃掉 ⇒ 批次 1 到 01:33 才开启（只剩 2h3m）、自身跑
  **2h14m 未完成** ⇒ 03:47 撞 330min 被整轮杀掉。**F1 预算闸只拦得住新开的工作、拦不住
  在途的传输** → 本轮新增 **F23**（见 §4）并落地 `4da3c11`（片长 3600→7200 +
  `_budget_slice_seconds` 给批次 copy / 巩固串行重试套 `timeout` 硬上限）。
- **退出标准 D 的口径修正**: 该轮执行的是 `task2 → openlist:wopan175/2`（写探针打在
  `openlist:wopan175/2/1024j/…`）⇒ **非 wopan176 的同步对确实被执行了**，D 已有实测证据；
  但「连续 ≥3 轮」远未达成。
- **⚠️ 更正上一版结论（「该轮零真实落盘」是错的）**: 该轮 **wopan175 在真实搬数据** ——
  全轮 **5570 条 `Copied (new)`**；每个子目录的 truth-check（重启容器取真值）都报
  「✅ 重启前后列表一致，无假成功污染」（1004 / 144 / 355 / 64 / 829 个文件确实在后端），
  直到 03:47 被超时切断时 rclone 仍在持续写入。「成功 0 · 缺失 84 · 未修复 84」是
  **修复管线**口径（那 84 个顽固文件），不代表整轮。
  ⇒ 该轮是「**有产出的轮次被硬杀**」，F23 的价值比原先判断更高。
- 该轮 trend: `transferred_bytes=2,064,871,588`、`remaining_bytes=9,887,180,404,143`。
  注意 trend 字节**偏低**：被杀批次的字节计数丢在内存里（F9 未做）。
- **另一处待查的假信号**: 同轮里 `_fix_probe_dir_writable` 对 `动漫本子/…` 报
  「❌ 目录不可写（重启后探针消失）」+「批量折叠零落盘」，但同一目录有 1004 个真实
  文件落盘 ⇒ **目录可写性探针存在假阴性**，是 84 个文件进不了修复的直接原因（见 §11）。
- 该轮虽 conclusion=failure，但收尾 / Persist / 自续触发仍是 success（接力链健康），
  硬杀的实际损失只有 conclusion 与在途批次。
- **并行会话冲突提醒**（工程约定）: 同一工作区可能同时有多个 AI 会话在改 openlist。
  提交前必须 `git status` + 逐文件 `git diff` 确认改动归属，**只 add 自己的文件**；
  否则会把别人未完成的实现/文档一起提交。（本会话已按此执行: `4da3c11` 只含
  task_engine.sh + 两个测试文件。）
- **2026-09-14（+08）第三批（本会话）· 注册阶段提速 + 拆分/批次阈值统一 20GB**：
  - `eb0ca9b`：①仅注册模式（TASK_REGISTER_ONLY=1）跳过 `_get_source_size_with_excludes`
    ——该全量递归列举只产出面板"源端 X GB"，31min/轮纯展示成本；预览模式保留
    （缓存命中零成本）。②回退跨轮源端大小缓存（e15bb02，改动①后成死代码）。
    ③**拆分/批次阈值统一 20GB**（用户指定）：openlist 目标的子目录拆分此前错用
    `OPENLIST_BATCH_BYTES`（5GiB，文件批次的本意）⇒ >5GiB 的目录全被逐层下钻；
    两处（子目录拆分 :913 与文件批次 :1700）都改用 `SYNC_SPLIT_THRESHOLD_BYTES`
    （workflow 已设 20GB），`OPENLIST_BATCH_BYTES` 引用与 5GiB 注释清理。
  - 依据（B 轮 34826097133 取消于 2h 处的日志）: transfers=4 ✓ / skip_preview ✓ /
    剩余量 unknown ✓ / **subdir_parallel=2 完全没生效**（depth=0 条件对"顶层 1 个
    子目录、第二层 ≥2 个"的结构永不成立）/**折叠校验 2×5s 仍判零落盘**（可见性
    延迟 >> 15s）/ 仅注册 31min。
  - 修复: 43f62ab（depth≤1 + SUBDIR_PARALLEL_DONE 防级联；折叠校验 4×15s 可配）+
    eb0ca9b（本条）。
  - 工期基线再上调: **A 轮单轮真实传输 13.08GB**（817 个 Copied、truth-check 通过，
    transfers=1、无今日修复）⇒ 57GB/日 ≈ 6 个月；今日全部修复后按天花板
    1.2 MiB/s ≈ 3.3 个月、按利用率 50-70% ≈ 4-6 个月。
- 待办: B' 轮（首个带全部修复的完整轮）跑完后按 §11.7 对比；job 内多同步对并行
  的前提（跨后端独立带宽）已被证伪，仅当网络路径改造后才有意义。
- **2026-09-15 · 提速实测验证通过（重大节点）**: 首个带全部修复的完整轮
  `34871720751`（6441561）单轮传输 **54.37GB**（transfers=1 基线 13.08GB 的 4.15×），
  且修复管线首次完整工作（成功 69 新落盘 · 缺失 6 · 未修复 0）。
  工期重算: 10.1TB ≈ **42 天**（原"3 年"）。
  剩余问题: ①折叠"零落盘"仍有 4 次（凑齐轮询 6×30s 对部分目录不够，候选: 上界调大
  或折叠校验改延迟复核）②7 次「重启后列表未就绪」（4af1cbc 回归，已回滚 eb29536，
  该轮是最后一次带它）③405 特定目录名二分暂缓。
- **2026-09-15 · 发现并清掉一条跨轮熔断误判**: `backend_dead.json` 首次出现
  `openlist:wopan175` 判死（dead_at=1789404921 = 09-14 16:55:21Z）。溯源: 该标记由
  run 34837308985（带 4af1cbc 回归的那轮）在 12:18 触发「后端 wopan175 连续 3 个目录
  不可写」——与"重启后列表为空 ⇒ 目录判不可写"是同一机制 ⇒ 判定为**误判**（wopan175 在
  A 轮跑出 13.08GB）。已于 09-14 23:1xZ 把 backend_dead.json 清空为 `{}`（若真死，F5/F6
  会在数分钟内重新判死）。要点: 4af1cbc 的回归不止拖慢一轮，还会通过 F6 污染跨轮状态 12h。
- **2026-09-15 · F6 加固（`5e51f55`，用户同意）**: 判死信号分级（只有写探针/F5 全拒
  复核这类**强证据**才写跨轮熔断；修复管线的目录级熔断只在本轮让路）+ TTL 12h→4h。
  起因: 09-14 实测一次误判污染 —— 4af1cbc 回归把健康后端 wopan175 判死，经 F6 让该后端
  6 个同步对整体跳过 12h。测试扩到 17 项（含弱证据不落盘、PERSIST 逃生口、TTL 默认值，
  并修掉场景 3「因文件没写而假通过」）。回归 26 套 22 EXIT=0、cnf=0（当时套件数，现已增加）。
- **2026-09-15 · 折叠落盘改「延迟复核」（`8486ea1`）**: 上一完整轮仍有 4 次「批量折叠零落盘」。
  就地调大轮询上界不可靠（可见性延迟实测可达小时级: 5b32587f 在 11h 后才读到 18 个文件），
  而旧行为"读空 ⇒ 退回逐文件修复"是双重损失（逐文件打的是**原目录**=405 那条路径；
  折叠成果不进 marker ⇒ 幽灵落盘）。改为: 折叠时登记 → 处理完全部目录后统一复核一次 →
  可见即补记 marker 并把文件从缺失清单移除，仍不可见才真正退回逐文件修复。
  记账抽成 `_bulk_fold_record_landed`（正常路径与复核共用；全局出参而非命令替换，
  否则子 shell 吞掉 `FIXED_THIS_RUN`）。测试 38 项（T14/T15 + mock 支持模拟延迟可见）。
- **2026-09-15 · 阶段耗时分解（run `34871720751` 日志，证据驱动）**: 全轮 16:56:53→22:27:40
  （5h31m）。最大静默间隔全部是**真实数据传输**，不是代码开销:
  | 间隔 | 内容 |
  |---|---|
  | 743s | 折叠 sync 一个大视频（6 文件目录） |
  | 622s | 逐文件修复一个电影（短哈希名法）→ 落盘确认 |
  | 433s / 374s | 逐文件修复 → **假成功 → 拉黑方法 2 → 换方法** |
  | 406s / 369s | 折叠/分卷上传（733MB 单卷） |
  ⇒ **瓶颈已是带宽本身**（单流 ≈0.6–1.2 MiB/s），不是流程开销；「跨后端并发」实测已关闭
  （§11.9: 两后端合计 0.50 MiB/s < 单后端 1.20 ⇒ 共享同一瓶颈）。代码侧剩余可做的只有
  "少走弯路"（如本次折叠复核），量级提升需网络路径改造。
  **顺带核实（避免误改）**: 折叠用整目录 `rclone sync`，曾怀疑"把已落盘文件重复上传"——
  该轮 4 次折叠的目录都是"整目录仅 6 个文件"的小 release 目录（cnt=6 ≈ 全目录），
  重复上传假设不成立，未改动。
  - **调优候选（待下一轮数据决定，未改）**: `OPENLIST_FOLD_VERIFY_TRIES` 默认 6×30s=3min 是
    纯 sleep；有了延迟复核后，就地轮询的边际价值只剩"提前记账"，而复核（数分钟后）能
    覆盖同样的情况 ⇒ 可降到 2×30s，每轮省 ~10min（4 个读空目录 × 2.5min ≈ 3.6% 预算）。
    暂不改的理由: 保持本轮改动集最小、归因干净；等下一轮看延迟复核的实际命中率再定。
- **2026-09-15 · 短轮快速迭代（`db77d72`，用户要求"不要每轮跑那么久"）**:
  - workflow 新增入参 **`sync_budget_min`**（默认 320，上限 320=step 超时 330min 留余量；
    非正整数回落默认并告警）。原先预算锚点是硬编码 19200s、日志也硬编码"320min"。
  - **派生阈值随预算等比缩放**（`task_engine.sh _budget_scaled`）: 批次最小片长
    = budget×3/8（19200→7200 精确还原原值）、尾部预留 = budget×9/64（19200→2700 精确还原），
    下限 900s / 600s；显式环境变量优先。**不改会怎样**: 45min 预算 + 固定 120min 片长 ⇒
    `_batch_budget_stop` 恒真 ⇒ 一个批次都不开，而折叠/修复管线/熔断都在批次之后 ⇒
    短轮退化成空轮；尾部预留按 45min 算则吃掉整个预算、每次传输被压到 60s 兜底。
  - §0 新增第 6 条「两种不等 5.5h 的取日志方式」: 分钟级（`调试 · 修复管线测试` +
    `fix_test_task` + `force_sync=true`，覆盖折叠/修复/记账）+ 小时级（`sync_budget_min`）。
  - 测试新增 7 项（默认预算精确还原 / 60min 与 30min 缩放与下限 / 超长预算不超原默认 /
    显式设置优先）。回归 26 套 23 EXIT=0、cnf=0（当时套件数，现已增加）。
- **2026-09-15 · 自查修掉一个"改动本身失效"的 bug（`12be71f`）**: `_budget_scaled` 原先在
  **source 时**求值，而 workflow 的真实顺序是「先 `source /tmp/load_all.sh`，再 export
  `OPENLIST_SYNC_BUDGET_SECONDS`」⇒ 缩放值永远按默认 320min 算 ⇒ 60min 短轮仍拿到
  7200s 片长 ⇒ `_batch_budget_stop` 恒真 ⇒ **一个批次都不开**，短轮空转且日志上看不出来
  （正是这次要防的失效模式）。改为运行期惰性求值 `_batch_slice_effective()`，并加回归锁
  （source 之后改预算 → 派生值必须跟着变）。**教训**: "默认参数 + 环境变量覆盖"的实现，
  必须确认**赋值时机**与调用方的 export 顺序一致 —— 写死 source 时求值 = 埋雷。
- **2026-09-15 · 调试轮实测（task3，取消于 85min）**: `调试 · 修复管线测试` 确实直达修复
  管线（跳过 initial sync 传输），逐子同步对修好了缺失文件（marker 逐个更新: fc2/天使
  ひなたちゃん 5 条、fc2/巨乳… 11 条、影视_电影_* 多个、fc2/カナヲちゃん 62KB marker…），
  手法是既有方法 2（短哈希文件名直传）→ 落盘确认。**但耗时不是"几分钟"**: 单个重子同步对
  （大电影 + 持久化复核 + 容器重启）要 30min+，task3 全部子对跑完会撞 120min step 超时。
  结论: 调试轮适合"验证修复管线/折叠/记账"，**不适合验证吞吐/批次/熔断**；要分钟级必须选窄任务。
- **2026-09-15 · 短轮验证通过 + 暴露并修掉三个真 bug（`4c9271d`）**: 60min 预算轮
  `34920298417` **58 分钟 success 收场**（对比 5.5h）—— 预算行「同步时间预算: 60min（至 03:16 UTC）」、
  「批次 1: 本轮剩余预算内最多传输 2670s」（**批次正常开启**，证明派生阈值按 60min 缩放生效）、
  逐级优雅收摊（修复管线 → 子目录 → 最终同步 → 同步对）全部命中，无硬杀。
  该轮 51 个文件经批次路径真实落盘（`Copied (new)`）。**但暴露三个真 bug**:
  1. **批次失败被洗成成功**（最严重）: `_sync_task_finalize` 递归收尾无条件用
     `failed_subtasks` 重算 SYNC_FAILED，而**文件批次路径的失败不体现在 failed_subtasks**
     （叶子任务没有子目录）⇒ 40/40 未落盘 + 后端写入全拒，子任务却报成功:
     父级不聚合失败（整轮报 success）、且 `run_all_tasks` 的 `elif SYNC_BACKEND_DEAD`
     分支**永不求值** ⇒ 既不让路也不调 `_backend_dead_mark` ⇒ **F6 跨轮熔断彻底失效**
     （`backend_dead.json` 长期为空，实测该轮什么都没记）。修: `failed_subtasks>0 ||
     SYNC_FAILED!=0` 才判失败；marker 门槛改用重算后的 SYNC_FAILED。
     **更正一处**: 提交信息里写"会写成功 marker 被 1d-skip"——实测**没有**写
     （`save_sync_marker` 自带"目标端文件数 < 源端"守卫拦住了），实际损害是
     「整轮报成功」+「F6 永不触发」两项；marker 门槛改动属纵深防御。
  2. **F5「后端写入全拒」假判死**: 该轮批次 copy 在 2670s 处被预算硬上限 timeout 截断
     ⇒ 40/40「未落盘」被读成后端拒收，而**同一后端本轮另有 51 个文件真实落盘**。
     不排除的后果: 经 F6 写进 `backend_dead.json`，让健康后端在 TTL(4h) 内被整轮跳过。
     修: 判死前置条件（① 本批 rc=124 被 timeout 截断；② 可用工作片 ≤
     `OPENLIST_F5_MIN_SLICE`=300s，重试被压到 60s 兜底）→ 跳过判定；rc 由调用方传入。
  3. **「零新增落盘」告警口径误导**: 告警说"实际没有任何数据落盘"，而同轮 51 个文件经
     批次路径真实落盘 ⇒ 文案改为明确"口径仅覆盖修复管线"并指向 trend/truth-check。
  - 测试: `test_batch_consolidate` +10g/10h（67/0）、`test_autosplit_fallthrough` +9/10（29/0）。
  - **顺带发现（待办）**: 该轮 trend `transferred_bytes=0` 而实际落了 51 个文件 ——
    正是 §F9 记录的"批次路径不喂 SYNC_TRANSFERRED_BYTES"，短轮下会让趋势恒为 0，
    优先级应上调（否则短轮拿不到吞吐数据）。
- **2026-09-15 · F9 最小修复（`6056952`）**: 短轮 `34920298417` 实锤 trend `transferred_bytes=0`
  而该轮 51 个文件经批次路径真实落盘。根因就是 §F9 早记录的那条（批次路径从不喂
  `SYNC_TRANSFERRED_BYTES`），但**短轮让它的严重性上升**：短轮几乎每轮都走批次路径 ⇒ 趋势
  恒为 0、拿不到吞吐数据（长轮里批次比重低时不明显，所以之前优先级被低估）。
  修法: `sync_by_file_batches` 尾部两处赋值（必须在尾部那次 fix_test `sync_with_logging`
  **之后**，它会清零）—— `total_transferred += batch_transferred_bytes`（递归收尾会用它
  覆盖 SYNC_TRANSFERRED_BYTES）+ `SYNC_TRANSFERRED_BYTES = batch_transferred_bytes`（顶层用）。
  口径仍是"rclone 声称量"（含假成功），C 判据必须配 truth-check。
  测试: `test_batch_precheck_circuit_breaker` 新增 G8/G8b（3 批 × 1.5MiB → 4718592 / 解析不到 → 0）。
- **待办（新，优先级上调）**: ① §11.6 的"吞吐阶梯"仍只有 1/4 两档、6 MiB 载荷太小
  （噪声盖信号），要拿到"transfers 能不能再提"的结论需改成 64–128 MiB + 多档（1/2/4/8/12/16）；
  ② 自续接力**不传预算**（红线禁止改自续 step）⇒ 短轮之后会接一个默认 320min 轮，
  要连续短轮得手动 dispatch（或在 §0 第 6 条里注明）。
- **2026-09-15 · 吞吐阶梯改造（`f55806e`）**: 原版载荷 6 MiB、只测 transfers=1/4 两档 ——
  6 MiB 在 1 MiB/s 下只有 6 秒，握手占比过大 ⇒ 实测给"4× 并发只 1.67×"而同期的真实轮次
  是 4.15×，两者对不上就是载荷太小的锅，导致"提并发到底有没有用"一直悬着。
  现在: `DIAG_THRU_MB`（默认 64，1 MiB/文件贴近生产形态）、`DIAG_THRU_LEVELS`（默认 "1 4 8"）、
  `DIAG_THRU_TIMEOUT`（默认 900s，45s 探针超时对大载荷必然失败）；每档输出 MiB/s + 单流速率 +
  耗时，末尾 awk 给**拐点判读**（相邻档吞吐比 <1.2 = 已饱和）；跨后端段同步改用同一载荷。
  **测量方法学（重要）**: 必须在**没有主轮在传**时跑 —— 两个 runner 各起自己的 OpenList
  容器但打**同一个网盘账号**，主轮在跑会争用同一条瓶颈、把各档位压平从而误判"已饱和"
  ⇒ 2026-09-14 的"跨后端共享瓶颈"结论是在有主轮并行的条件下测得的，**需在隔离条件下复核**。
- **2026-09-15 · 短轮二验（`34926236845`，58min success）+ 又一层失败传播 bug（`46f5fe0`）**:
  - ✅ **F5 假判死闸门生产验证通过**: 日志「⚠️ 批次 1 巩固: 本批传输被预算硬上限截断
    （timeout rc=124）→ 跳过「后端写入全拒」判定」——**没有**产生全拒结论、没有污染
    `backend_dead.json`（仍为 `{}`）。
  - ✅ 预算/派生阈值/逐级优雅收摊全部再次命中（「60min（至 04:50 UTC）」「批次 1: 最多传输 2701s」）。
  - ❌ **但同步对被判成功、游标 13→14 前移**（批次 1 失败、成功 0/4、251 个缺失文件被当
    "已同步"交给下轮）。上一版修复（`4c9271d`）只把 `SYNC_FAILED` 从"被 failed_subtasks
    无条件重算"里救出来，**而 `SYNC_FAILED` 本身会被后续 sync 尝试清零**（`sync_notify`
    开头把它重置为"这一次的结果"，fix_test 模式=0）⇒ 信号仍丢。
  - 修法（`46f5fe0`）: 独立标志，不依赖 `SYNC_FAILED` 存活 —— `SYNC_FAILED_BATCH`
    （子任务作用域，父级每子任务前重置）+ `SYNC_FAILED_BATCH_PAIR`（同步对粘性，
    只在每对开始前重置，供 `sync_task` 尾部兜底）+ 父级分类条件带批次标志兜底。
  - **加永久诊断行**: `同步对轮转判据: idx=.. SKIPPED=.. FAILED=.. PARTIAL=..
    BACKEND_DEAD=.. BATCH_FAILED=.. BATCH_FAILED_PAIR=.. attempts=..` —— 这次为查
    "失败却没让路"只能反推、耗时很长；有这行一眼可定位。**未查明的真因**: 究竟是哪一次
    sync 尝试把 SYNC_FAILED 清零（候选: 批次函数尾部的 fix_test `sync_with_logging`、
    finalize 的最终完整同步）——下一轮用该诊断行确认。
  - 测试: `test_autosplit_fallthrough` +场景 11（SYNC_FAILED 被清零后仍不写成功 marker、
    仍按失败走切割检查，mock 支持 `SBB_CLOBBER`），31/0。
- **2026-09-15 · 隔离条件重测吞吐阶梯**: 派发 `34937414236`（`thru_mb=64`、
  `thru_levels="1 4 8"`）—— 先取消在跑长轮与排队轮，保证**没有主轮争用同一条账号带宽**。
  原 §11.9 的"跨后端共享瓶颈"结论是在有主轮并行的条件下测得的，本次为复核。
- **2026-09-15 · 隔离吞吐阶梯实测（决定性，回答"transfers 还能不能提"）**: 改造后的阶梯
  （64 MiB 载荷、1 MiB/文件）在**取消在跑轮与排队轮**后跑了两次（保证没有主轮争用同一条
  账号带宽）:
  | transfers | 1 | 4 | 8 | **12** | 16 | 24 |
  |---|---|---|---|---|---|---|
  | MiB/s | 0.51 | 1.33 | 1.78–2.13 | **2.78** | 2.78 | 3.05 |
  | 单流 | 0.51 | 0.33 | 0.22–0.27 | 0.23 | 0.17 | 0.13 |
  ⇒ **拐点在 12 流**（4 流的 2.1×）；12→16 零增益、16→24 只 +10%，且 423 mkdir 碰撞
  风险随并发上升 ⇒ **提并发有效，但上限就是 12 流左右**。
  落地 `8780271`: `transfers` 默认 **4→6**（× `subdir_parallel=2` = 12 流，正好拐点），
  入参选项扩到 1/2/3/4/5/6/8/12，5 处脚本兜底 + workflow 注入兜底同步改。
  **方法学更正**: §11.9 的"跨后端共享瓶颈（合计 0.50 < 单后端 1.20）"是在**有主轮并行**的
  条件下测得的；本次隔离测量（8 流 1.78–2.13 MiB/s）与生产 8 流 2.64 MiB/s 量级一致
  ⇒ 该结论需按隔离口径重测才可作数，而它正是 `pair_parallel` 启用与否的唯一依据。
  **吞吐上限的现实含义**: 12 流 ≈ 2.78 MiB/s ⇒ 10.1TB 纯传输约 **42 天**（与当前工期估算
  一致）；想再上一个量级只能换网络路径（不是加并发）。
- **2026-09-15 · 短轮三验（`34940234180`，`8780271`，72min success）—— 多项修复生产验证通过**:
  - ✅ **F9 生效**: trend `transferred_bytes=**6,711,600,481**`（6.71GB / 72min ≈ 1.55 MiB/s）
    —— 此前批次重的轮次恒为 0，现在批次路径的字节进趋势了。被取消的那轮（`17b0606`）
    也记了 5.84GB，两轮互证。
  - ✅ **transfers=6 生效**: 日志 7 处 `transfers=6`、0 处 `transfers=4`。
  - ✅ **F5 假判死闸门**: 该轮无 `后端写入全拒`、无 timeout 截断 ✓。
  - ✅ **新诊断行可用**（这次直接给出结论，不用再反推）:
    `同步对轮转判据: idx=14 SKIPPED=0 FAILED=1 PARTIAL=1 BACKEND_DEAD=0 BATCH_FAILED=0
     BATCH_FAILED_PAIR=0 attempts=2` ⇒ 失败被正确识别、**游标没有前移**（此前的
    "失败却前移"本轮未复现）。注意: 该轮**没走到批次路径**（只有子目录拆分），
    所以 `46f5fe0` 的批次失败传播仍是单测覆盖（场景 11），生产待遇。
  - **新发现（本阶段第三大固定开销）**: 「等待缓存刷新完成 (60s)」一轮被调 **14 次 = 14min**，
    占该 72min 轮的 **19%**（长轮 ~7%）。已做 F12 前半（`1fdb773`）: 等待可配
    `OPENLIST_FS_REFRESH_SLEEP`（默认仍 60）+ 本次零传输时跳过同步后刷新。
    **调小默认值的测量设计（下一轮做）**: 以本轮基线（kate-bloom 1 / rolakiki 8 /
    rustudio 8 / kenneth-play 12 个缺失）对比，若缺失数/修复触发量显著上升即说明
    "递归刷新 60s 不够"的反面——即不能缩短。
- **2026-09-15 · F12 可测量化 + 诊断探针自伤修复**:
  - `b319b79`: 缓存刷新等待暴露为入参 **`fs_refresh_sleep`**（默认 60；非正整数回落 60）——
    调优必须"可测量地"做（对比缺失数/修复量），不能默默改默认值。
  - `1cb8c82`: **修一个自己造成的探针失效** —— 阶梯改造把载荷名从 `t01.bin` 改成
    `t001.bin`（三位补零），而「出口带宽基准」探针硬编码找旧名 ⇒ 静默 SKIPPED
    （近两次诊断都缺这一项）。改为独立 8 MiB 载荷（1 MiB 在高速率下噪声大）。
    教训与"改字符串要 grep 全仓"同源: **改文件名/格式要检查所有按名引用它的地方**。
  - 对比轮 `34949253305`（`fs_refresh_sleep=30`，60min 预算）在跑 —— 与上一轮
    （sleep=60，基线: kate-bloom 1 / rolakiki 8 / rustudio 8 / kenneth-play 12 个缺失）
    对比缺失数与修复触发量，判断能否缩短或需不需要做成"轮询就绪"。
  - 该轮时间分布（静默间隔法）: 固定开销 ≈24min/72min（**33%**）—— 缓存刷新 14min、
    驱动就绪盲等 4min（`自适应轮询已回滚`，勿动）、驱动初始化 4min、容器重启 2min；
    其余为真实传输（含分卷上传 511s 聚合）与修复管线。
- **2026-09-15 · 缓存刷新等待对比实验（定稿，`17e1e99`）**: 同一同步对相邻两轮:
  | 指标 | 基线 `34940234180`（sleep=60） | 对比 `34949253305`（sleep=30） |
  |---|---|---|
  | 刷新等待 | 14 次 × 60s = **14min** | 6 次 × 30s = **3min** |
  | 零传输跳过刷新 | —（新功能） | **6 次** ✓ |
  | **重启前后列表不一致** | 0 | **0** ✓ 无 stale 症状 |
  ⇒ 默认取 **40**（两侧留 1/3 余量，省 ~5min/轮；短轮占比 19%→13%）。
  **进一步压缩的判定条件**: 高写入轮（≫2GB）里若"缺失数/修复触发量异常抬升"或出现
  「重启前后列表不一致」即回退 60。
- **2026-09-15 · 轮转判据行已两次直接给答案**: 三轮的判据分别是
  `FAILED=1 PARTIAL=1 attempts=2` → `attempts=4`（游标**全程未前移**）——
  该同步对（idx=14，task5 一支）连续多轮因**修复管线被预算切在尾部**而无法完成，
  attempts 累积到阀门（8）后由轮转让路。这不是 bug 而是长尾形态: 每轮只能修掉一部分，
  但每轮都在真实推进（缺失数在降）。**若要加速这一支**，可考虑给它单独多轮连续预算
  （短轮 + 不轮转）而不是等 8 轮阀门。
- **2026-09-15 · 跨后端复核（隔离条件，`34956588037`）—— §11.9 的旧结论需改写**:
  - **出口带宽基准（修好探针后首次拿到）: 25.86 MiB/s** ⇒ **runner 出口不是瓶颈**
    （网盘侧 2–3 MiB/s 才是）⇒ "换 runner 区域/走代理/自建 runner"这条路**排除**，
    §11.9 里"仅当网络路径改造后才有意义"的判断要改成"网络路径不在瓶颈上"。
  - 跨后端并发（两侧各 4 流、各 32 MiB，隔离）: A(wopan176Crypt)=1.39、B(wopan175)=1.39、
    **合计 2.78 MiB/s**；同一轮阶梯 transfers=4 单跑 = 1.28 MiB/s ⇒ 各自**保持独跑速率**
    （无相互降速）⇒ 表面看"挂载额度独立、pair_parallel 有效"。
  - **歧义已判别（决定性，`34957585599`）**: 两侧各 12 流（合计 24 流）→
    A=1.68 + B=1.88 = **合计 3.37 MiB/s**，而同轮单后端 12 流只有 1.78（≈1.9×）
    ⇒ **两挂载额度相互独立，账号级共享天花板的假设被推翻**。
    （4+4 流时两侧各 1.39 vs 单跑 1.28 也同向 ✓ 两次独立测量互证。）
    **⇒ §11.9 的"跨后端共享瓶颈、并行无益"结论作废**（那是主轮并行时的污染样本），
    `pair_parallel` 是真实的 ~1.9× 杠杆。落地纪律: 先 `pair_parallel=2` 显式跑一轮
    生产验证（该路径从未按默认跑过，改默认值会连带打破只验证串行语义的回归测试），
    验证通过再翻默认值。
- **2026-09-15 · 待验证**: `pair_parallel=2` 验证轮（60min 预算）—— 游标 idx=14/15 正好是
  **同一任务的两种后端挂载**（`task5`→wopan176Crypt 与 `task5-wopan175`→wopan175），
  正是"跨挂载并行"的目标形态；验证点: ①吞吐是否 ≈ 单对的两倍 ②进度渲染是否正常
  ③容器独占锁（truth-check 重启）是否与 worker 互斥 ④marker 并行落盘是否冲突
  ⑤轮转游标是否按并行语义推进（整批后推进到"本轮未启动的第一个"）。
  **（已由下一条解决：验证通过并启用默认值。）**
- **2026-09-15 · pair_parallel 验证 + 启用（`98a93f0`）+ 修对级记账（`fce7cf4`）**:
  - 验证轮 `34959561878`（显式 `pair_parallel=2`，60min，success）: **并行路径正常** ——
    「🔀 并行同步对: 上限 2 个（同一后端不并行），本轮候选 16 个」+ 两对**同秒起跑**
    （wopan176Crypt 第 15/16、wopan175 第 16/16）；marker 层面可见两个挂载的同一子目录
    并行推进；游标按并行语义推进（→ 第 1/16 个）✓ 无报错。
  - 同轮暴露 **对级字节记账错**（已修 `fce7cf4`）: 「并行同步对完成: 第 16/16 个（wopan175 ·
    partial · **0 B**）」，而该对日志明确写着「本轮传输 10 个文件」——`SYNC_TRANSFERRED_BYTES`
    在 depth=0 收尾**从未被设置** ⇒ 取到"最后一个子同步的值"或 0 ⇒ 父级合计低估一半，
    而它直接影响趋势样本与工期分母。修法: depth=0 两条路径都显式赋值。
  - **默认值已翻到 2**（依据=配额独立实测 + 验证轮通过）：吞吐增益的最终确认看下一轮
    （记账已修，届时"合计传输量"可信）。
  - **纪律记录**: 第一次直接改默认值 → 回归立刻报警（2 个只验证串行语义的测试失败 +
    cnf 20 条）⇒ 架构变更必须"先显式参数跑验证轮，通过再翻默认"，并把依赖旧默认的测试
    显式声明（本轮的教训已写进注释与测试）。
- **2026-09-15 · 记一个瞬态失败（非代码问题）**: run `34966900190` 在「校验 OpenList 容器运行状态」
  步骤失败 —— 「HTTP 就绪，但管理面登录失败（检查 OPENLIST_ADMIN_PASSWORD …）」，
  两次登录尝试合计只等 ~17s。**重试即过**（下一轮 `34967321544` 同 sha、步骤 6/7/8 全 success）。
  判读: 容器冷启动（拉大 DB 后管理面子系统未就绪）导致登录端点超时，不是密码不匹配
  （若是密码问题不会"重试即过"）。**处置**: 遇到该形态直接重跑，别去动 secrets；
  若要减少复现，可考虑把 `wait_openlist_ready` 的登录重试次数/间隔调大（当前 ~2 次×15s）。
- **2026-09-15 · F23 最后一处漏网修复（`3fd29b6`）**: run `34967321544`（60min 预算）实测
  **超预算 17min 仍在跑**（step 12:13:12Z 起、预算 13:13:12Z 到点、13:30Z 仍在运行；
  sync_state 自 12:26Z 起 65min 无写入），而 step 硬杀要到 17:43Z ⇒ 会白占队列 ~4h。
  根因: 批次 copy 与巩固重试早已套 `timeout`（`_budget_slice_seconds`），**唯独
  `run_rclone_sync_once` 的 `rclone sync`（子目录级 initial sync）没有硬超时** ——
  预算闸只能拦"新开的工作"，拦不住"在途的单次 sync"（§F23 的教训最后一处）。
  修法: 在该 `rclone sync` 前套 `timeout $(预算剩余 − 尾部预留)`，rc=124 → 失败分支 →
  各级预算闸优雅收摊；无预算锚点（调试/还原）不包装、行为不变。
  **验证点（下一轮）**: 60min 轮的同步 step 应在 ~60min+尾 内收场，不再超预算。
- **2026-09-15 · 待确认清单（下一轮一并看）**: ① 硬上限是否生效（不再超预算）
  ② `pair_parallel=2` 默认值下的**合计传输量**（记账已修，看 trend 字节是否 ≈ 单对的两倍）
  ③ 对级 `.done` 回传的 transferred 是否两对都非 0。
- **2026-09-15 · 硬上限生效后的残留边界（实测）**: 加完子目录 sync 硬上限后，60min 预算的轮
  实测**超预算 8min**（step 13:35:56Z 起、预算 14:35:56Z 到点、14:44Z 仍在跑），而此前是
  **无界 17min+**。8min 落在**尾部预留**内（60min 预算的预留 = `_budget_scaled` 下限 600s=10min）
  ⇒ 属设计内（预留就是给收尾/在途文件用的）。
  **仍未被包装的路径**: 逐文件修复的单次操作（copyto / zip_split 等，单文件 1GB+ 时 10–20min）
  —— 它们的**片闸**只检查"下一个文件前"是否该收摊，拦不住"在途的那一个文件"。
  当前可接受（越界被一个文件的传输时间 + 预留共同界定，且远离 step 的 330min 硬杀）；
  若要更紧，可给 `file_fix.sh` 的各方法调用也套 `_budget_slice_seconds`（改动面较大，暂不做）。
- **2026-09-15 · 超预算在跑的最后一处无界阶段（`03dbb60`）**: run `34975551022`（60min 预算）
  实测超预算 **21min+** 仍在跑（此前同类 17min+）。定位: **持久化复核的「假成功重试」循环**
  （`OPENLIST_PERSIST_RETRY_ROUNDS` 默认 11）—— 每轮 = 逐文件换方法重试（分钟级/文件）+
  一次容器重启复核，**原本完全没有预算约束**。已加 `sync_budget_stop` 闸；提前 break 安全
  （剩余 pending 走循环后同款清理: 移出 marker + 记失败清单 + 黑名单，下轮继续）。
  ⇒ 至此"预算闸只拦得住新开的工作、拦不住在途/循环"这条教训的四类落点（批次 copy、
  巩固重试、子目录 sync、假成功重试循环）**全部收口**。

## 12. C 判据专项：「重传但未落盘」长尾（2026-09-15 开工）

### 12.1 实证（run `34940234180` 日志，决定性）

```
本轮传输 15 个文件，目标视图 4（缓存口径）—— 重启容器取后端真值
重启后目标视图: 13 → 3
⚠️ 检测到 10 个假成功文件（重启后从列表消失 → 未持久化），已暴露为缺失，将由下方 diff 送修复管线
─── task5_..._little-liliana 缺失文件修复 · 共 10 个 ──────
⏳ 修复管线预算将尽，优雅收摊: 剩余 10 个文件交下轮接力（状态已增量持久化）
```
同轮另有一处 `12 个假成功`（12 个文件全部未落盘）、一处 `✅ 重启前后列表一致`（干净）。
名长诊断: **密文名均未超限** ⇒ 排除名长/路径因素（§11.8 的"特定目录名 405"不是这批的原因）。

### 12.2 机制（修复前为什么会无限循环）

1. `rclone sync` 报 `Copied` 但后端不落盘（**假成功**）；
2. truth-check（重启容器取真值）识别出来 → 转修复管线换方法；
3. **修复管线被预算饿死**（该轮 3 处收摊，37+10+11 个文件全部顺延下一轮）；
4. 下一轮 diff 又把同一批文件当缺失 → **重新走 sync 重传一遍**（≈1.8GB/轮白烧），回到 1。

⇒ 每轮都在重传同一批文件，却永远轮不到修它。（2026-09-15 已由修复尾段预留打破，见 §12.9）

### 12.3 两个候选修法（2026-09-15 定夺：候选 A 已落地并验收，见 §12.8/§12.9；候选 B 未做，暂无必要）

- **候选 A（预算分配）**: 给修复管线留出预算尾段 —— 子目录循环的闸从"全局最小片 600s"改为
  `max(600s, 预算 × OPENLIST_REPAIR_RESERVE_PCT%)`。代价: 无修复需求的轮次会空转尾段。
- **候选 B（不再重传已知假成功）**: 把 truth-check 标记的假成功文件记进该对的修复状态，
  下一轮 **sync 阶段直接跳过它们**（不再重传），预算全给修复管线换方法。代价: 需新增"已知假成功"
  清单的持久化与过期/清退逻辑（若后端后续恢复，要能重新纳入）。

### 12.4 已就绪的测量工具

`openlist-diag.yml` 新增 **大文件落盘阶梯探针**（`2b90863`）: `big_mb`（默认 `16 64 256` MiB）逐个上传，
每个都做「上传后 lsf（缓存口径）」+「**重启容器**后 lsf（后端真值口径）」两步校验，
不仅可定位是否存在**尺寸阈值**，还可用 `big_transfers` 对比"并发是否推高假成功率"。

### 12.5 为什么先测量再改

生产平均 `2.434 GiB / 15 文件 ≈ 166MB`，正好落在 64–256 MiB 之间 ⇒ 阈值很可能在此；
若实锤，最简单的修法可能是**调低既有的分卷阈值**（`LARGE_FILE_THRESHOLD_BYTES` 现为 4GB，远高于此）。
**不要凭"看起来像"就改热路径** —— 这与 §11 的方法学教训一致。

### 12.6 并行默认启用后的首轮（`34985212438`，`03dbb60`，60min success）—— C 大幅好转

| 指标 | 之前（单对） | 本轮（pair_parallel=2） |
|---|---|---|
| 单轮传输 | 6.71 GB | **46.02 GB**（42.864 GiB / 63min ≈ 12.2 MiB/s） |
| 分对回传 | — | baidupanCrypt **23.767 GiB** + wopan176Crypt **19.097 GiB** |
| **假成功率** | 坏子目录 10/15（67%）、12/12 | **3 / 1268（0.2%）**，多数子目录「✅ 重启前后列表一致」 |

- 两对都记 `failed`（部分子目录未完成），但**传输量本身是真实落盘的**（真值复核几乎全清）
  ⇒ **C 判据的"重传不落盘"形态本轮未复现**（此前集中在 `task5/1024j-视频-pornhub-channel/*` 那批
  平均 ~166MB 的文件上）。
- **后端间速度差异很大**: baidupanCrypt 单对 57min 传 23.8GiB（≈7.1 MiB/s），
  远高于阶梯实测的 wopan 挂载（1.7–1.9 MiB/s/挂载）⇒ 后续阶梯**应补测 baidu 挂载**，
  且工期节奏由**最慢的后端**决定（用户要求全部后端都要有副本）。
- **该轮**暴露的问题（已在 §12.9 由 `34997994883` 收口）: 收尾 `成功 136（新落盘 1 · 沿用上轮 135）· 缺失 1121 · 未修复 985`
  ⇒ **修复管线仍被饿死**（新落盘只有 1 个）：预算优先给了大块传输，长尾缺失文件排不上。
  ⇒ 候选 A（给修复管线留预算尾段）的价值因此上升 —— 已落地，见 §12.3 与 §12.8。

### 12.7 尺寸假设被推翻（`34993430318`，隔离测量）

`大文件落盘阶梯（transfers=1）: 16MiB:OK 64MiB:OK 256MiB:OK`
（每档都做了「上传后 lsf」+「**重启容器**后 lsf」两步 ⇒ 256MiB 是真落盘）
⇒ **文件大小不是"重传不落盘"的原因** —— 生产均值 ~166MB 落在被证伪的区间内。
（同轮出口带宽基准 22.18 MiB/s，与 25.86 同量级，再次确认 runner 出口不是瓶颈。）

**下一个决定性测试（在跑 `34995335073`）**: 大文件 × **transfers=6**（生产当前并发）。
探针此前用 transfers=1，而生产自 `8780271` 起默认是 6 ⇒ 若 256MiB 在并发 6 下变成假成功，
则根因是**并发 × 大文件**（而不是尺寸本身），修法方向随之改变（见下方判读）。

### 12.8 两个假设均被证伪 ⇒ 真因是"修不完"，候选 A 已落地（`11c1761`）

- `34995335073`（隔离）: `大文件落盘阶梯（transfers=6）: 64MiB:OK 256MiB:OK`
  ⇒ **尺寸与并发都不是原因**（"大文件写不进"假设作废）。坏传输窗口里 rclone 报
  `Attempt 2/3 succeeded`（重试成功）却仍有 10 个假成功 ⇒ 属后端侧既有假成功形态，
  靠真值复核 + 修复管线兜底，**关键是别把它饿死**。
- 落地候选 A: 子目录循环（串行 + 并行派发）的闸改为 `_subdir_budget_stop`
  —— 预留「预算 × `OPENLIST_REPAIR_RESERVE_PCT`%」（默认 25%，上限 60%，不低于全局
  最小片）给尾部。320min 轮 = 4800s（80min）、60min 轮 = 900s（15min）。
- **验收口径（已由 `34997994883` 满足，见 §12.9）**: 与上一轮对比 —— 收尾的「新落盘 / 未修复」是否
  从 `新落盘 1 · 未修复 985` 明显改善（重点看**未修复**是否下降），
  以及"停止开新子目录"的时刻是否提前到"剩余 25%"处。
  ⇒ 实测: `新落盘 94 · 缺失 0 · 未修复 0`、假成功 0 次；新闸在"剩余 25%"处如期触发
  （日志 `尾部 900s 留给修复管线`）。

### 12.9 ✅ C 判据收口（验证轮 `34997994883`，`11c1761`，60min success）

| 指标 | 上一轮（无预留，`34985212438`） | 本轮（预留 25%，`34997994883`） |
|---|---|---|
| 收尾成效 | 成功 136（新落盘 **1** · 沿用 135）· 缺失 1121 · 未修复 **985** | 成功 94（新落盘 **94** · 沿用 0）· **缺失 0 · 未修复 0** |
| 假成功检测 | 3 / 1268 | **0 次** |
| 单轮传输 | 46.02 GB | 1.44 GB（本对可传内容较少 + 预留提前收摊） |

- 新闸按预期触发: `⏳ 时间预算将尽，停止开新子目录（未同步的留给下轮接力；尾部 900s 留给修复管线）`。
- ⇒ **"重传但未落盘"长尾已收口**：不是写不进，是**修不完**；留出预算尾段后修复管线能当轮清空。
  退出标准 C 判为达标（A/B/D/E 此前已实质达标 ⇒ **A–E 目前全部达标**）。
- **结论（2026-09-16）: 不做 15% 平衡实验，默认保持 25%。**
  - **预留不是浪费**: 修复提前完工 → 本轮收尾 → 自续触发下一轮立刻接管那段时间（接力无缝）。
    严格说不是"零成本"，而是**把传输窗口从本轮挪到下一轮**，代价仅为重付一次 setup
    （驱动初始化 120s×2 + 容器启动 + DB 拉取 ≈ 5–6 分钟），相对 5.5h 轮次可忽略。
  - **实测显示预留被用满、没有过剩，故无下调动机**: `34997994883` 预算 60min、
    `duration_s`=3850s(64min) —— 子目录循环在剩余 900s 时停止后，修复阶段把**整段 900s
    预留用完**（该轮修好 94 个文件），**没有出现"提前完工"**。下调只会重新引入长尾风险
    （未修复 985 的教训）。
  - 补充: **短轮测 15% 无效** —— `_repair_reserve_seconds` 有 600s 下限保护，60min 轮的
    15% = 540s 会被抬到 600s、退化成旧闸。
- **零成本观测判据（替代专门实验）**: 看长轮 `duration_s` 是否接近预算 320min（19200s）:
  - 接近（≥18500s）→ 预留刚好，**保持 25%**
  - 明显小于（如 ~14000s，提前约 80min）→ 修复确实提前完工、预留过剩，届时再下调
    5–10 个点（25 → 20）
  - 下调时必须用**同一游标位置的同一同步对**数据对比，避免不同对的差异干扰
  - 触发前提: **连续多轮**观测到 `duration_s` 明显小于预算才考虑下调
- 后续建议: 阶梯补测 **baidupanCrypt**（本轮实测它单对 57min 传 23.8GiB ≈7.1 MiB/s，
  远高于 wopan 挂载）⇒ 后端速度差异大，工期由**最慢后端**决定。


### 12.10 修复能力取证（2026-09-16）+ 根目录折叠落地（`2313423`）

**取证（近三轮日志轻量统计）**:

| 轮次 | 修复成功 | 修复失败 | 方法耗尽 | 「无目录可换」 | 「目标目录不可写」 |
|---|---|---|---|---|---|
| `34959561878` | 29 | 49 | 6 | **150** | 50 |
| `34940234180` | 43 | 5 | 5 | 0 | 0 |
| `34985212438` | 5 | 0 | 0 | 0 | 0 |

- 49 次失败 vs 150 次「无目录可换」记录（每文件约 3 条: Step 2 尝试 + Step 5 尝试 + 失败清单）
  ⇒ **该轮修复失败几乎全部是根目录文件**。
- **真·方法耗尽（`所有修复方法均失败`）经查也是根目录文件**: 排除清单里它们是
  `- /<文件名>.mp4`（**无目录前缀** = 目标端根目录），例:
  `OnlyTeenBlowjobs - Cute Teen Is Not So Innocent-ph5ebc05eca6793.mp4`（246.6 MiB），
  且日志有 `⛔ 上轮假成功（替代路径已消失），拉黑 修复方法4·短哈希分卷` ——
  它们试过的**全是"换名字"的方法，从未试过"换路径"**。
- ⇒ **修复失败的主因单一且明确: 根目录文件缺"换路径"这一维**。已落地（`2313423`）:
  根目录文件折叠到 `dest_path/<hash8>/<file>`（哈希源用常量 `.`，因 `.`/`..` 是保留名
  ⇒ 不会与真实子目录的折叠值撞车；折叠目标自带 dest_path 前缀 ⇒ 无需按对加盐）。
  防删除 `--filter-from`（`- /<hash8>/<文件>`）**天然覆盖**（实现前已核实 = 本项最大回归风险）。

**下一轮验证点**: ①「无目录可换」是否归零 ②根目录折叠是否产生真实落盘
（日志 `目标端根目录折叠为短哈希目录 <hash>` + `✅ 折叠落盘`）③真·方法耗尽数是否下降
④收尾新增的「修复管线: 尝试 N · 失败 F（其中方法耗尽 E · 顺延下轮 D）· 成功率 P%」是否可用。

**取证产出的顽固文件清单（供人工判断，未盲目改热路径）**:

真·方法耗尽（`所有修复方法均失败`）共 5 个（run `34940234180`），**全部在子任务根目录**:

| 文件 | 大小 | 已试方法 |
|---|---|---|
| `OnlyTeenBlowjobs - Cute Teen Is Not So Innocent-ph5ebc05eca6793.mp4` | 246.6 MiB | 方法 1/3/4 已被**上轮假成功**拉黑；方法 2（短哈希名）本轮又假成功（raw 未增长 1→1） |
| `A cute maid who provides pornographic services to the host-ph5f12b2166785f.mp4` | 201.6 MiB | 同上形态 |
| `…Does Porn On The Side - BananaFever-ph5c947a5f33a9f.mp4` | — | 同上 |
| `…and Riley Reyes (Sex Hack How To) [ph59c40cb5598b1].mp4` | — | 同上 |
| `…thering Footjob with a Huge Cumshot-ph60a8026e45d5e.mp4` | — | 同上 |

- 共同点: **在子任务根目录**（排除项形如 `- /<文件名>`，**无目录前缀**）+ `.mp4` + 200–250MB
  + 名长诊断判「密文名 107B > 后端已接受最长(100B)」（该批同时触发名长拉黑）。
- **它们从未试过"换路径"**: 日志里既无「目录级兜底」也无「无目录可换」—— 因为 Step 5 兜底的
  进入条件里也有一道根目录闸，且**静默跳过**（连日志都不打）⇒ 已由 `eac4043` 修。
- 判读: 这是"根目录 + 名长"叠加的形态，现在两条路都通了（折叠换路径 + 短哈希名）。
  若下一轮仍修不好，才说明是**后端对这几个文件本身**的问题 —— 届时按原因分类再动，不预设改法。

**第二步修复（`eac4043`，CI 全绿）**: Step 5 的第二道闸放开 + 文案链路同步 ——
`docs/telegram-notify.md` **4 处样例**随行为更新（「无目录可换」→「备用目录也写不进去」，
因为根目录文件现在与子目录文件**同口径**、会真去建+探备用目录；按规范先改文档）；
测试补 5i~5k 覆盖 Step 5 那条路径（原测试只覆盖 Step 2 ⇒ 第一版漏改才没被本地发现，
是 CI 的 12f 文案断言兜住的）。


## 12.11 修复能力定点验证规程（openlist-fix-check，2026-09-16 建）

**载体**: `.github/workflows/openlist-fix-check.yml`（仅手动 · concurrency 独立组 `openlist-fix-check`
· job 40min · 自带容器副本、不装 cloudflared/不碰隧道/不回传 DB）+ 驱动脚本
`.github/scripts/openlist/fix_check.sh` + 测试 `tests/test_fix_check.sh`（32 项，被 CI glob 自动纳入）。
与 `openlist-diag` 的容器 setup 段**同源内联**（不抽 composite action —— 抽取要改已在工作的诊断链路，
收益只是省 30 行复制；两处都加了"改一处需同步"注释）。

**它回答什么**: 对指定文件跑**生产同款修复管线**，逐文件输出
`VERDICT|<状态>|<方法>|<文件>|<替代路径>|<原因>`：

| 状态 | 含义 |
|---|---|
| `fixed` | 真值复核通过（重启容器后仍可见），已按生产口径写 marker |
| `fake_success` | rclone 报成功但重启后消失（未落盘） |
| `failed` | 4 种方法 + 换目录兜底全试过仍失败（原因在第 6 段） |
| `already_ok` | 原路径已在 / marker 已记账（文件以替代形态落盘） |
| `not_found` | 清单条目在源端匹配不到（清单需修正） |

**退出码**（可当"修复能力回归门"）: 0=全 fixed/already_ok · 1=有 failed/fake_success · 2=有 not_found 或环境问题。

**两种模式**: `mode=list`（指定清单: `task` + `subdir` + `files` 每行一个文件名/密文名后缀，分钟级）
与 `mode=diff`（两侧递归列举求差集，覆盖全但大任务 30–60min）。

**口径要点（写清避免误读）**:
- **默认重置历史黑名单**（`reset_blacklist=是`）⇒ 测的是**能力上限**（4 方法 + 换目录全试），
  不是"生产下一轮会走的那条路"（生产会跳过历史上被拉黑的方法）。
- **真值复核是唯一判据**（`truth_restart=是`，重启容器后再列一次）—— 这批文件的失败形态正是
  "rclone 报成功但后端没落盘"，只看 rc 会把假成功记成 fixed。
- **叶子同步单元**（★ 最容易搞错）: 修复与 marker 记账都落在"生产实际用的叶子单元"上
  （`task` 逐层 `_${subdir//\//_}` + dest 逐级拼接；脚本按**已有 marker 反查**，无 marker 时按最深目录推定），
  否则"真修了"下一轮仍会被当缺失重传（幽灵落盘）。
- **真修并记账**（用户确认）: 复核通过的条目按生产口径写 marker ⇒ 验证即产出，下轮不重传。
- **带宽纪律**: 主轮在传时跑会偏慢（同一网盘账号），OneDrive 列举还可能被限流 —— 脚本会把列举
  错误**显式打出来**并区分"列举失败 vs 路径不存在"（不再吞成"0 个文件"）。

**首次实测（`35053515218`，2026-09-16；对象 = run 34940234180 那 5 个"真·方法耗尽"文件）**:

| 结果 | 文件 | 说明 |
|---|---|---|
| `already_ok` ×3 | `kate-bloom/OnlyTeenBlowjobs…ph5ebc05eca6793.mp4` · `aeon-marks/AVN.footpunkz…ph60a8026e45d5e.mp4` · `kenneth-play/…ph59c40cb5598b1-poster.jpg` | **已经修好并记账**（marker 里有替代形态）⇒ 那批"方法耗尽"已被后续轮次解决 |
| `fake_success` ×3 | `rolakiki/A cute maid…ph5f12b2166785f.mp4` · `rustudio/Cute Petite Brunette…ph5c947a5f33a9f.mp4` · `kenneth-play/Anal Play 101…ph59c40cb5598b1.mp4` | 全部是**方法 3 `zip_split_original`（分卷）**，真值复核判定 `分卷缺失` |

⇒ **可执行结论**: 剩下的"修不好"形态是**分卷上传的持久化失败**（分卷 parts 上传后重启即消失），
既不是"方法全都试过"（`failed=0`）也不是"清单对不上"（`not_found=0`）。
下一步取证方向: 分卷粒度/分卷数（`OPENLIST_SPLIT_PART_BYTES` 默认 1GB）与该形态是否应直接跳过方法 3 走方法 4。

**顺带产出（重要）**: 首次实跑暴露并修掉两个 rclone 参数 bug —— 其中 `OPENLIST_RCLONE_LISTING_TIMEOUT`
注入裸数字 `900` 被传给 rclone 的 `--timeout`（要求带单位）⇒ **生产的折叠落盘列举一直静默失败**
（三轮日志 `✅ 折叠落盘` 出现 0 次 = 折叠成果从未记账）。已修（注入 `900s`）并加回归锁；
折叠"零落盘"的成因判断也随之更正（主因是参数错误，可见性延迟是第二成因）。

- **2026-09-16 · 修复能力验证通道 + 生产 bug（`35b2ae0`/`21a3a83`/`1404286`/`54990b6`/`3141042`）**:
  - 新建 `openlist-fix-check.yml` + `fix_check.sh` + `tests/test_fix_check.sh`（32 项）: 定点验证
    "文件到底能不能修好" —— 两种模式、重置黑名单测能力上限、**重启容器真值复核**、通过才写 marker、
    逐文件 `VERDICT` 行、退出码语义化。关键设计: **叶子同步单元**（按已有 marker 反查，否则真修了
    也会被当缺失重传）。规程见 §12.11。
  - **首次实测（`35053515218`，6 候选）**: `already_ok` ×3（**其实已修好并记账**）+ `fake_success` ×3
    （全为**方法 3 分卷**，判 `分卷缺失`）⇒ 剩余"修不好"形态 = **分卷持久化失败**。
  - **生产 bug（已修）**: `OPENLIST_RCLONE_LISTING_TIMEOUT` 裸数字 → rclone `--timeout` 失败 ⇒
    折叠落盘列举静默失败 ⇒ 折叠成果从未记账（旁证: 三轮日志 `✅ 折叠落盘` 0 次）。另修
    `--retry`→`--retries`（新脚本 3 处），并加**回归锁**（注入值必须带单位、脚本内无裸数字、无 `--retry`）。
  - **诚实更正**: 折叠"零落盘"此前归因"列表可见性延迟"（并据此做了延迟复核）—— 延迟确实存在
    （11h 后才读到文件），但**主因是参数错误**；注释已更正，延迟复核保留（第二成因）。
  - CI: `progress_no_orphans` 的 **T4 偶发时序红**（同 commit 4 次里 2 次，重跑即绿）已记入 `tests.yml`。

### 12.12 「分卷假成功」专攻：根因是**落盘即时校验的口径错**，不是分卷机制（2026-09-16）

**取证（三次独立复现，逐字同构）**：`35053515218`（`1404286`）→ `35074878571`（`4e739e0`，
当前 main，含根目录折叠）→ `35079518553`（同 sha，单文件）。三次日志的**每一行都相同**：

```
✅ 目录可写（重启后探针仍在）           ← 目录侧完全健康
▶ 修复方法1·copyto 原名                 64s → 🔴 假成功 raw 未增长 52→52 → 拉黑
▶ 修复方法2·短哈希名                   5.3s → 🔴 假成功 raw 未增长 52→52 → 拉黑
▶ 修复方法3·zip 分卷                   71s → ✅ 落盘确认 raw=53 → 判成功
真值复核（重启容器）                    → ❌ 未持久化 · 分卷缺失 → fake_success
```

**根因一：方法 1/2 是被"计数未增长"否掉的，不是被后端拒收否掉的。**

- 判据 `_confirm_persist_by_count`（`file_fix.sh:639-668`）读的是 `_raw_dir_count` →
  `rclone size` 的**文件计数**，刷新后 `sleep 5` 就读，**预算是 40 次**（本轮日志「预算 40」）。
- 三轮里 `52→52` 出现 **6 次**（3 文件 × 2 方法），**计数一次未动**；而方法 2 只花 **5.3s** 就
  rc=0 —— 200MB 文件不可能 5 秒传完 ⇒ 那是**幂等跳过或立即失败**，计数不动是必然结果，
  **不能据此判"假成功"**。
- 代码注释自己记过同类事故（run 31928671112「同文件连传 9 个方法，全部真实落盘又全部被误判」）
  ⇒ 这层快筛的**假阴性风险是已知的**，而本形态正是踩中它。
- **后果是级联的**：方法 1/2 被误判 → `_blacklist_add` 拉黑 → 只剩方法 3/4。而方法 3 一"成功"
  就 `return 0`（`file_fix.sh:998-999`）⇒ **方法 4 从未被尝试过**（三次日志「修复方法4」0 次）。

**根因二：方法 3 的"通过"依据，恰好是同一个不可信计数器的必然增长。**

- 分卷是 **1 个文件**（`1 / 1`），`_confirm_persist_by_count` 看"目录文件数 +1"⇒ **52→53 必然成立**，
  与是否真落盘无关。逐卷的 `rclone size` 比对（`file_fix.sh:515`）同样是**缓存口径**。
- §12.11 已证这批文件的失败形态正是"rclone 报成功但后端没落盘"⇒ **用缓存口径验收 = 拿嫌疑人口供定罪**。

**根因三：根目录折叠路径根本没被触发（新证据，推翻本项的预期）。**

- `4e739e0` 已含 `2313423`/`eac4043`（Step 2 + Step 5 两道闸放开），但三次日志
  「目录级兜底」「短哈希目录」「无目录可换」**全部 0 次**。
- 原因清楚：Step 2 预检判**可写**（探针重启后仍在，真值口径）⇒ 不进换目录分支；Step 5 兜底
  要求「4 种方法全败」⇒ 而方法 3 已"成功" ⇒ **兜底同样不进**。
- ⇒ 折叠是"方法全败后才走"的最后兜底，**分卷假成功把这条兜底路给短路了**。这解释了为什么
  §12.10 的改动在这批文件上没有任何表现。

**结论（回答"分卷也不行还有什么办法"）**：不是"4 条路都试过都不行"，而是
**只有方法 3 被真正执行过，它的"成功"是假象，而兜底换目录因这个假象被短路。**
修法优先级：

1. **写入类方法（1/2）不用目录计数判假成功** —— 改用大小精确匹配（同
   `_persist_verify_entries` 的 `is_transformed=0` 分支口径），或计数未增长时**先刷缓存重读**再定论。
2. **分卷的验收改为"期望卷数 + 每卷大小"**，别用"目录文件数 +1"（分卷天然会让计数涨）。
3. **方法 3 之后不再 `return 0` 就收工** —— 至少在真值复核前不把方法 3 当终局，给方法 4 与折叠留机会
   （或把"分卷类"的即时校验改成"复核失败则继续同轮换方法"，而不是等真值复核才发现）。

**未验项（诚实标注）**：本轮**没有**证明"方法 1/2 若判成功就真能落盘"。三次实验里方法 1/2 的
上传结果都被计数器掩盖，需要先把判据改成大小匹配、再复跑同三文件才能回答"是不是所有方法都不行"。

---

## 12.13 修复方法扩充 + 命名统一（2026-09-16，用户已批方案）

**用户决策**：① 按计划扩方法；② 命名统一为 `方法N·动作·变体`（中文可读）；
③ **目录折叠暂不动**（用户明确："文件修复已经把文件改了，目录折叠还要把目录也改了" ——
尽量不做目录级改动，先看新方法效果）。

**方案文件**：`.trae/documents/openlist-fix-method-expansion-plan.md`（含完整现状分析、
改动清单、决策记录 D1–D10、验证步骤 V1–V5）。

### 12.13.1 Step A（已实施）· 命名统一 + 落盘判据修正

**A-1 命名统一为单一口径**（此前"日志短标签 / marker 全名"两套并存）:

| 层 | 函数 | 形态 |
|---|---|---|
| 展示层（日志/通知） | `_fix_method_short` | `方法3·分卷·原名` |
| 持久化层（marker/黑名单） | `_fix_method_desc` | `方法3·分卷·原名（zip 压缩 + 分卷切割，…）` |

**★ 改名的高风险点（本项最容易出事的地方，已在 §2.6 单列）**：
`_fix_method_desc` 的输出是**持久化身份**，不是日志文案 —— 它
① 写进 marker 的 `fix_blacklist` 跨轮读回比对；② 写进 `method_id`；
③ 被 `restore_info.jq` 用 **`test()` 子串匹配**分类还原形态。
⇒ 改名**必须保留** `分卷切割` / `短哈希文件名` / `短哈希目录 ` / `base64URL 编码目录 `
四个子串，否则还原元数据静默退化成 `kind=copy`（还原脚本错误）。

**A-2 黑名单跨命名版本兼容**：新增 `_fix_method_norm`，
把任意表示（语义 ID / 新描述 / 历史全名）归一到语义 ID；
`_blacklist_add` 改存**归一 ID**，`_fix_method_blocked` 两侧归一后比对。
⇒ 历史 marker 条目（旧全名）与新条目都能命中，**不会因改名静默失效**。

**A-3 落盘判据修正（§12.12 的根因修复）**：新增 `_confirm_persist_by_size`
（目标端字节数 == 期望字节数，同 `_persist_verify_entries` 的 `is_transformed=0` 口径）；
方法 1/2 的成功判据改为 **`尺寸匹配` 或 `计数增长`（取或）**。
理由：本层目标是消除**假阴性**（该判成功却判失败 → 级联误杀好方法，
实测 3 次；同类事故记录见 run 31928671112）；假阳性由**重启容器真值复核**兜底（既有设计）。

**A-4 配套改动**：`file_fix.sh` 内 4 处硬编码 `文件修复方法N 成功/失败` 改调 `_fix_method_short`；
`_confirm_persist_by_count` 加"单独使用会误杀"警示；README · 两套方法N 表更新命名要求；
`restore_info.jq` / `sync_marker.sh` / `scan_fix_signatures.py` / `file_restore.sh` 注释同步；
`test_method_id_naming.sh` 重写为 31 断言（含 restore 子串锁 + 跨版本黑名单兼容）。

**验证**：`test_method_id_naming.sh` **31 PASS / 0 FAIL**，并**已负向验证**两处：
① 去掉 `分卷切割` 子串 → 1c/1d 变红；② 黑名单去掉归一 → 6a/6f 变红（暴露重复条目）。
`restore_info.jq` 用新旧两套方法文本各跑一次分类，四种形态**全部正确**
（copy / short_hash_rename / split_zip ×2），历史格式亦兼容。
回归：`test_fix_check` 32/0、`test_hash_dir_fallback` 62/0、`test_fix_pipeline_optimizations` 38/0。

### 12.13.2 Step B / C（待做 → **必要性存疑，见 §12.13.4 V3 实测**）

- **Step B**：分卷改用 `file_split.sh` 的 7z 原生分卷（`-t7z -mx=0 -v`，
  产物 `.7z.001` 天然可逆，去掉手工倒序改名段；**不引入** `.sha256`/`.restore.txt`
  —— 会改变 `alternative` 语义与防删除 filter）；粒度仍用 `OPENLIST_SPLIT_PART_BYTES`。
  ⚠️ V3 实测后必要性下调：B 只改打包形态，**不改写入路径**，大概率同样无法持久化。
- **Step C**：新增 API 写入方法 5/6（`api_put_original` / `api_put_shorthash`，
  经 `POST /api/fs/put` 绕开 WebDAV；必须带 `Content-Length`、`File-Path` 必须 URL 编码）。
  依据：现 4 种方法**全部经 WebDAV**，而目录创建早已用 API 兜底 —— 这个不对称正是 405/8005 的来源层。
  ⚠️ V3 实测后定位下调：写入已证明能进 OpenList（真实流量 + 即时可见），卡点在
  **OpenList → 后端**那一层，换 API 入口大概率过不去同一层；仅剩"排除 WebDAV 层"的诊断价值。

### 12.13.3 本项**有意不做**的事

- **不改 `return 0` 控制流**（§12.12 曾建议"方法 3 之后不 return"）：先修判据让"成功"可信；
  若 Step A 复验后仍见"方法 3 判成功而未落盘"，再回改控制流（列为决策点）。
- **不动目录折叠**（用户决定）。

### 12.13.4 Step A 复验（V3）· 修法换了但没落盘 —— 假设翻案（2026-09-16）

**run 记录**：`35085545044`（参数错：files 用逗号拼接成一行，而候选匹配按**行**做
basename/子串比对 → `not_found` 退出码 2。教训：`files` 必须**换行分隔**）；
`35085986592`（`137c005`，换行分隔 3 文件，跑通全流程）。

**时间线**（第二个 run）：预检重启后**探针仍在**（小写入能持久化）→ 方法 1 三次上传
**88s/211MB、73s/163MB、166s/530MB（≈2.2–3.2MB/s 真实流量）** → 即时 `尺寸匹配` 全过
→ 真值复核重启容器 → **3 个全部 `期望N 实际0`** → `fake_success`×3（方法 1）。

**结论**：
1. **"方法 1 被误判"假设被推翻**：即时层计数判据确实误判过（§12.12），但"未落盘"的
   **最终判决是真的** —— 数据进了 OpenList（真实流量 + 即时可见）却从未到达后端，
   重启即蒸发。唯一可信的持久化判据是**重启后真值复核**（既有设计，正确）。
2. **卡点不在方法层**：同目录 5616 个文件绝大多数早已同步成功 ⇒ 后端写入通路没坏；
   探针（无害小文件）能持久化 ⇒ 小写入也没坏；坏的恰好是**这 3 个文件**，
   换任何写入方法都过不去 ⇒ 疑点集中在**后端对这批内容/文件名的拒收**
   （内容审核类静默吞掉：真实流量收下、即时可见、后端不留）。
3. **Step A 的产出依然成立**：尺寸判据在生产验证按设计工作（真实字节即时可见，
   远优于间接计数）；命名统一端到端生效（日志/判决行/拉黑均为 `方法1·原名直传`）。
4. **本轮只有方法 1 被尝试**：新判据下方法 1"即时成功"→ 直接进真值复核，
   方法 2/3/4 未被触达（fix-check 每轮一轮修复，拉黑后下一轮换方法）。

**⇒ 决策点（用户已选"定向诊断"）**：见 §12.13.5 的 D1/D2 两步诊断与结果。

### 12.13.5 拒收归因专项诊断（2026-09-16，用户选定向诊断）

**D1 · 随机内容对照**（run `35114399854`，`openlist-diag.yml`，打到**问题目录本身**
`.../rolakiki`，`big_mb="64 256 512"`）:
`大文件落盘阶梯（transfers=1）: 64MiB:OK 256MiB:OK 512MiB:OK`
⇒ 一次性排除三个假设：**目录状态**、**文件大小**（512MiB 远大于失败文件的 163–530MB
区间下沿仍 OK）、**随机内容**。候选变量只剩**真实视频内容**与**文件名/扩展名**。

**★ 载体教训（实测，别再踩）**: `fix-check` **结构上不能当排除链用** ——
run `35116778989` 设 `reset_blacklist=否` 想让管线从方法 2 起步，结果仍走方法 1。
原因见 [fix_check.sh 头注释](file:///workspace/.github/scripts/openlist/fix_check.sh#L39-L49)：
它是「能力上限口径」，① 复核判假成功时**不落盘新黑名单**；② 不调
`_flush_blacklist_to_marker`；③ 容器跑完即销毁，marker 里的黑名单一并蒸发。
⇒ 每轮**必然**从方法 1 开始。要看方法 2/3/4 的表现得等**生产主轮**（那里黑名单独持久化）。

**D2 · 真实内容换名对照**: 由专项脚本 `diag_reject.sh`（随 vzd/并行提交 `3e513fa` 落地，
**A 原名 / B 改名 / C 随机** 三组对照 + **三时点复核**①即时②延迟③重启后真值）
承担，run `35122248482`。三时点设计特意区分「延迟落盘」与「拒收」——立即重启会把
OpenList→后端的异步推送未完成**误判成拒收**，这是纯两步校验（如我一度打算加的第 14 节）
的方法性缺陷；故本节直接复用更强版本，**未重复造轮子**。

判读口径（脚本 R4 段自动给出）:
| B 改名组真值 | C 随机组真值 | 结论 |
|---|---|---|
| 存活 | 存活 | **按文件名/状态拒收** ⇒ 改名可绕 ⇒ 方法 2（短哈希名）应能修好 |
| 蒸发 | 存活 | **按内容拒收坐实** ⇒ 换任何写入方法都过不去 ⇒ 止损有据 |
| （C 蒸发） | — | 通路/目录异常，本实验作废，先查常规探针 |

**★ D2 实测结果（run `35122248482`，判决性）**:

| 组 | 三时点轨迹 | 结果 |
|---|---|---|
| A 原名 `A cute maid…ph5f12b2166785f.mp4` | 可见→可见→**缺失** | 蒸发（复现 V3） |
| B 改名 `oldiag_ren_0_<ts>.mp4`（同内容） | 可见→可见→**可见** | **存活** |
| C 随机（同字节数 211355567 B） | 可见→可见→**可见** | 存活（通路正常） |

⇒ **按文件名/状态拒收，改名可绕过**。推论：

1. **这类文件可修，不是"云端拒收·不可修"** —— 止损方案（§12.13.4 决策点）据此否决。
2. **扩展名不是触发因素**：B 组保留 `.mp4` 仍存活 ⇒ 触发的是**文件名字符串本身**
   （或该原名在后端残留的坏状态；A 组是覆盖写路径，两种解释本实验无法再分）。
   **方法 2（`copyto_shorthash`，`<md5前8位>.<扩展名>`）与 B 组形态一致 ⇒ 应能修好。**
3. **对 §12.12 的历史结论要重新审视**：那里"方法 2 只跑 5.3s 即判假成功"的结论出自
   fix-check，而 fix-check **结构上每轮必从方法 1 起步**（见上节载体教训），
   其"方法 2 失败"记录的可信度需按新证据重估。
4. **B/C 步骤据此重新定位**：Step C（换 API 入口）**无必要**——卡点在名字不在写入通道；
   Step B（7z 分卷）**必要性进一步下降**（分卷也换名，但改名直传更简单，方法 2 即可）。

**⇒ 下一步真问题转移**: 既然方法 2 能修好，关键变成「**管线能否可靠触达方法 2**」——
即方法 1 假成功后的拉黑是否真的跨轮持久化（历史上有 `run 31917285452` 拉黑 5 条最终
保存 0 条的丢失事故）。这比再加新方法重要得多。

**★ D3 · 方法2 确切形态验证（run `35161267696`，判决性中的判决性）**:

D2/§12.13.6 证明的是"换个**中性时间戳名**可绕"，但生产真正要走的是方法 2
`copyto_shorthash` 的 **`<md5前8位>.<ext>`**。若后端按名字模式/长度拒收，8 位 hex
未必与时间戳名等价 —— 故新增 `DIAG_REJECT_NAME_MODE=shorthash` 档，专验方法 2 的确切形态。

| 组 | 名字 | 真值 |
|---|---|---|
| A 原名（211MB 原视频） | 原名 | 可见→可见→**缺失**（蒸发，3rd 次复现） |
| **B 改名（shorthash 形态）** | `2976df90.mp4` | 可见→可见→**可见** ✅ |
| C 随机对照（同字节数） | `oldiag_rand_*.bin` | 可见→可见→**可见** |

⇒ **方法 2 的确切命名形态可以落盘并存活** —— 这不是"理论上改名可绕"，而是
**"方法 2 现在就能修好这类文件"**。配合 C 组存活（通路正常），止损方案彻底否决。

**★ D4 · 生产主轮验证（run `35162246938`，`5428461`，2026-09-16 23:25 → 09-17 02:39 success）**:

派发参数: `sync_budget_min=90`、`self_retrigger=false`、`skip_preview=true`。

**结论: D3 在生产环境得到确认 —— 方法2 真能修好，且是主力方法。**

**① 方法使用分布**（全轮日志词频）:
| 方法 | 出现 |
|---|---|
| 方法1·原名直传 | 6 |
| **方法2·短名直传** | **88** |
| 方法3·分卷·原名 | 0 |
| 方法4·分卷·短名 | 2 |

**② 方法2 通过真值复核的铁证**（`02:19:10`，重启容器后后端真值口径）:
```
✅ 通过 · 方法2·短名直传 · … Artistic Scene By Cherry Grace [ph5c61298184190].mp4 · 276479642B
✅ 通过 · 方法2·短名直传 · …d for Hot Blonde in Leather Skirt [ph6071548cbc181].mp4 · 277832669B
✅ 通过 · 方法2·短名直传 · …Tasty Compilation of Cherry Grace [ph5e4300586b6a0].mp4 · 475864013B
✅ 通过 · 方法2·短名直传 · … - Hot Handjob with Ruined Orgasm [ph5efc0ed26ffad].mp4 · 305624962B
持久化验证汇总: 复核 4/4 / 通过 4 / 失败 0
```
⇒ **字节数完整、替代路径在后端重启后真实存在** —— 不是即时缓存假象。

**③ 假成功有新形态，且是"批次性"而非"方法性"**:
- `02:15:34 批次 1 巩固: 修复复核 0 个真实落盘 / **41 个假成功已剔除**` —— 这 41 条是
  **上一轮**遗留条目（方法2×40 / 方法4×1），本轮复核时全部剔除并拉黑、下轮换方法。
- 同一轮内 `02:16-02:19` 新修复的 4 个方法2 条目 → 真值复核 **4/4 通过**。
- ⇒ **方法2 本身可用**；假成功呈**时段/批次聚集**（某段时间后端整体写不进），
  与 §12.12 的"时段性"判断一致，**不是方法2 的固有缺陷**。
- ⚠️ 这解释了为何 fix-check（单点、即时）与主轮（长时段、批间）结论看似冲突：
  两者测的是不同时间窗的后端状态。

**④ 收尾**: 时间预算用尽优雅收摊，剩余交下轮接力；`修复成效: 成功 46（新落盘 46 ·
沿用上轮 0） · 缺失 25 · 未修复 0`；`修复管线: 尝试 7 · 失败 6 · 成功率 100%（新落盘 46/46）`。

**⑤ 观察目标（V3 那 3 个 ph 文件）**: 本轮日志中**一次未出现**（`ph5f12b2166785f` /
`ph5c947a5f33a9f` / `ph59c40cb5598b1` 词频 = 0）—— 它们不在本轮的缺失清单内
（可能已在别的轮次被处理、或不在本轮 task 的 auto-split 子目录内）。**待后续轮次确认**。

**★ 管线侧已锁定（2026-09-16，新增 `test_blacklist_crossrun_method2.sh`）**:
既然改名可绕，就验证「拉黑 → 跨轮回填 → 触达方法 2」这条链是否可靠（历史丢失事故
`31917285452` 的正面防线）。新增套件锁 C1–C5 五条契约，本机 **29/29 套件 EXIT=0**。
**已负向验证**（防假绿灯）: ① 去掉 `_fix_method_norm` 归一 → C3 变红；
② `_blacklist_add` 株连拉黑短哈希类 → C2/C4 变红。
⚠️ 教训: C3/C4 输入**必须用 marker 里的旧全名形态**，不能用内存归一后的短 ID——
用短 ID 时去掉归一仍 PASS（断言抓不住），改用旧全名才正确变红。
**→ 用户已拍板（2026-09-16 下午）：先做定向诊断**，载体已落地（见 §12.13.5）。

### 12.13.5 定向诊断落地: 拒收归因专项 `diag_reject.sh`（2026-09-16 下午）

- **载体**: `openlist-diag.yml` 新增 `reject_src` / `reject_max` / `reject_wait` 输入；
  `reject_src` 非空 ⇒ 走 `.github/scripts/openlist/diag_reject.sh`（与常规 13 组互斥，
  专项要的是「内容变量」的干净归因，混跑常规探针的写入会污染三时点判读）。
- **三组对照**（全部落在 `$TARGET/oldiag_reject_<ts>/`，变量分离）:
  A 组原名直传（贴近生产，含覆盖写路径）· B 组同内容改名 `oldiag_ren_<i>_<ts>.<原扩展名>`
  · C 组与 B[0] **同字节数**的随机数据（对照通路，补上第 13 组「尺寸随机全 OK」
  从未测过内容变量的缺口）。
- **三时点复核**: ① 上传后即时（缓存口径）→ ② 等 `reject_wait` 秒（默认 120，
  防「异步推送未完成」被误判成拒收）→ ③ 重启容器（真值口径，与 truth-check 同哲学）。
- **判读矩阵**（报告自动出结论行）:
  C 蒸发 ⇒ 通路异常作废 · B 存活 ⇒ **改名可绕**（修复管线可加「改名直传」方法；
  全存活时附注 V3 未复现）· B 蒸发 + C 存活 ⇒ **内容拒收坐实**（止损有据）·
  A/B 全被受理层拒绝 ⇒ 先修 WebDAV 层。
- **产物处置**: A/B/C 一律尽力删除（含 B——即使 B 落盘也删，结论已记录，
  不留生产树污染源）；残留以 `oldiag_reject_` 前缀可辨识，不参与同步。
- **回归**: `tests/test_diag_reject.sh` **18 PASS / 0 FAIL**（mock rclone/docker/sleep，
  六判读分支 + 产物形态/重启/清理核验）；`bash -n` 与 YAML 解析通过。

### 12.13.6 ★ 定向诊断实测（run `35120784651`，2026-09-16 16:16Z）: **按原名拒收，改名即绕**

**判决性结果**（目标 `openlist:wopan176Crypt/5`，V3 同款两个源，三时点 trace = 即时→延迟120s→重启真值）:

| 组 | 内容 | 名字 | 真值 |
|---|---|---|---|
| A0 | 原文件（211MB） | **原名** | ❌ 可见→可见→**缺失** |
| B0 | 同内容 | `oldiag_ren_0_<ts>.mp4` | ✅ 存活 |
| A1 | 原文件（164MB） | **原名** | ❌ 可见→可见→**缺失** |
| B1 | 同内容 | `oldiag_ren_1_<ts>.mp4` | ✅ 存活 |
| C | 随机数据（211MB，与 B0 同字节数） | 随机名 | ✅ 存活 |

**结论（推翻「内容拒收」假设）**:
1. **不是内容拒收**——同内容改名后真值存活（B 存活 2 / 蒸发 0），且同尺寸随机对照（C）也存活；
   A 组两个文件**全部蒸发**，与 V3 完全同构（非偶然，2/2 复现）。
2. **是「原名 / 原文件状态」拒收** ⇒ **改名可绕**。这是可落地的修法方向（不是止损）。
3. 名字长度不是变量（B 组名字更短，但 V3 的「密文名未超限」早已排除纯长度）——
   **待收窄**: 到底是「这个具体名字」还是「该文件在后端已存在/曾失败的残留状态」
   （如后端留了坏条目）？下一步建议: 同一 A 组文件传到**同内容但路径层级不同**的位置，
   或把 B 组产物改名回原名的反向实验（确认是名字本身还是目录内既有状态）。

**产物处置**: 清理干净（无残留告警）；专用诊断目录 `oldiag_reject_<ts>` 已 purge。

### 12.14 ★ 409 Conflict 语义修复 + 目录层收手（2026-09-17，run `35186977864` 驱动）

**发现（用户提问驱动）**：用户问「目标端在增长吗 / 修复的文件改回原名会消失吗」，
并要求"要真实数据，不要依靠记忆和文档"。取证时撞见上一轮（09-17 05:45→13:30）
的 **failure**，与 §C 刚下的「已解决」结论矛盾，遂深挖。

**实测数据（非推断，全部来自 run 日志原文）**：

| 指标 | 值 |
|---|---|
| `修复管线: 尝试 142 · 失败 141（方法耗尽 0 · 顺延下轮 0） · 成功率 0%（新落盘 0/20）` | **整轮零落盘** |
| `本轮修复成效: 成功 0（新落盘 0 · 沿用上轮 0） · 缺失 20 · 未修复 20` | 20 个文件全灭 |
| `409 Conflict` | **2062 次**（含 `wopan` 系后端命中；确切挂载名因日志过期**已无法复核**） |
| `Update mkParentDir failed` | **1088 次** |
| `423 Locked` | 167 次 |
| `8005 登录失败` | 仅 14 次，且 `T12:34` 后消失 ⇒ **瞬时抖动，非持续故障** |
| 结局 | `The action '任务预览与全量同步' has timed out after 330 minutes` |

⚠️ **这些计数的可追溯性已失效**：`35186977864` 的日志现已过期（`gh run view --log`
返回 0 行，且无 artifact），上表数值**取证于日志有效期内**，**此后无法复查**。
run 元信息仍在（job `build` failure，07:56→13:30）。⇒ 本文档后续**不再以此批计数
作为关键论据**，只作当时形态的记录；如需新证据，须在故障复现时重新取证。

**关键矛盾（用户后台看驱动正常 vs 日志全灭，二者都对）**:
`8005` 只有 14 次瞬时抖动（与后台"正常"一致）⇒ **不是驱动/账号死**；
真正的失败点是**目录创建层**的 409/mkParentDir，而**管理后台不检查这一层**。

**根因：409 的语义在全库被误判为「后端死」**
- `openlist_driver.sh` 的 `_classify_probe_failure` 把 `conflict|mkParentDir` 归为
  `backend` → `_pre_webdav_health_check` 判定「后端异常」→ **跳过整轮同步**。
- 但 409 在 MKCOL 语义下最常见的原因是**资源已存在**（RFC 4918），是**幂等成功**，
  根本不该跳过；另一来源是并发 mkdir 争用（与 423 同因）。
- 各处 `api/fs/mkdir` 只认 `HTTP_CODE:(200|201|204)`，`409` 一律判失败。

**日志实锤（同一文件内自相矛盾）**:
```
⚠ mkdir | Attempt 3/3 failed: Conflict: 409 Conflict
⚠ mkdir 失败 exit=1，尝试 OpenList API...
OpenList API mkdir 响应: HTTP_CODE:200
OpenList API mkdir 成功                  ← dir_ok 被"骗"成 1
📁 目标目录已就绪
🔎 预检目录可写性: ...
⚠ 目录探测 | Failed to copy: Update mkParentDir failed: Conflict: 409 Conflict
   缓存口径: rc=1, 探针可见=0
↻ 重启 OpenList 容器...
```
⇒ **`dir_ok=1` 是假阳性**（API 回 200 但目录并不存在），后续 4 种方法全部撞
同一堵 `mkParentDir` 墙。**这正是用户问的「目录都创建失败了，咋还进行文件修复」。**

**三处修复**（均带逃生口，可回退）:

| # | 修复 | 位置 | 开关 |
|---|---|---|---|
| ① | **409 = 目录已存在 ⇒ 幂等成功**（用 lsd **读操作**复核，与"真建不出来"可区分） | `file_fix.sh` 4 处建目录点（Step1 / API / base64URL / 短哈希）+ `_bulk_fold_ensure_dir` | `_FIX_MKDIR_409_SEMANTICS=0` |
| ② | **409 与 423 同款整轮重试**（此前只有 423 有重试，409 直接赔掉整轮） | `sync_engine.sh` 新增 `_sync_retry_409`，紧接 `_sync_retry_423` 调用 | `OPENLIST_409_RETRY_ATTEMPTS` / `_SLEEP_SECONDS` |
| ③ | **目录层连续失败达阈值即收手**（不再逐个文件空转 6 小时） | `file_fix_pipeline.sh` `_FIX_MKDIR_FAIL_STREAK` | `OPENLIST_MKDIR_FAIL_STREAK`（默认 3，0=关） |

**分类器与预检的配套改动**:
- `_classify_probe_failure` 新增 `conflict` 类（从 `backend` 拆出，置于其前）。
- `_pre_webdav_health_check` 对 `conflict` 做**二次判定**: 目录存在 ⇒ **放行**（不是跳过）。
  理由: 旧行为跳过 = 整轮零落盘；放行的最坏代价只是后续写入真失败转修复管线，
  而修复管线本就有完整失败处理。**不对称，故必须选放行。**

**回归锁定**: 新增 `tests/test_mkdir_409_semantics.sh`（**19 断言 / 0 失败**），
覆盖 D1 读操作判据 / D2 分类器 / D3 预检放行 / D4 收手阈值 / D5 接入点计数 / D6 整轮重试。
**负向验证已做**（三处分别注入回归，均正确变红）:
- 移除 `echo conflict` → `FAIL: D2a / D2b`
- 移除收手 `break` → `FAIL: D4c`
- 移除 `_sync_retry_409` 调用 → `FAIL: D6b`

本机全套 **30/30 EXIT=0**（原 29 套 + 本套），`command not found` 扫描为空。

**验证环已跑（2026-09-17 15:21Z，run `35239688191`，提交 `70fc685`，目标 `openlist:wopan175`）**:
新增 `diag_409_semantics.sh`（E1 语义判决 + E2 改名实验），两组实跑结果:

| 实验 | 实测 | 判读 |
|---|---|---|
| E1a（**已存在**目录再 mkdir） | rc=0 · 409特征=**0** | 静默成功，**未复现 409** |
| E1b（**不存在**目录 mkdir） | rc=0 · 409特征=**0** | 正常成功 |
| E1c（刚建出目录再 mkdir） | rc=0 · 409特征=**0** | **静默成功**（非 409） |
| E2（短名 → 改名回原名，三时点） | 即时/延迟120s/重启真值 **原名均可见**，短名无残留 | ✅ **改名安全** |

**E1 结论：本次未复现，属「无结论」而非「推翻」。** 三种形态都没回 409。
⚠️ **注意本实验与 `35186977864` 的故障不同后端/不同时段**: 本诊断打的是
**`openlist:wopan175`**（手选），而 `35186977864` 的 409 来自主轮轮转的
`wopan176Crypt` 系 —— 不能据"wopan175 未复现"推断"wopan176Crypt 也不复现"。
两者只能各自陈述: 本诊断**在 wopan175 上单次未复现**，故 409 的**触发条件仍未知**
（可能与特定后端、时段、并发争用有关）。要定案须在**同一后端**于故障时段复测。
唯一可迁移的正向信号是 **E1c 返回 0 而非 409**：该后端对「重复 mkdir 已存在目录」
通常**静默成功**，与 RFC 4918 的 409 语义并不一致 —— 这至少说明"409=已存在"
**不是所有后端的统一行为**。

**⚠️ 上述「修复保留」的判断已被生产轮推翻（2026-09-17 20:56Z，run `35239780581`）**：
该轮 `70fc685`（含全部 409 修复）实跑 5h34m，结局 `timed out after 330 minutes`。实测：

| 指标 | 值 | 含义 |
|---|---|---|
| 幂等放行日志（`mkdir 报错但目录实际存在`） | **0 次** | **409 修复从未被触达** |
| `409 Conflict` | 1876 次 | 409 仍在稳定复现 |
| `Conflict`/`mkParentDir` | 1815 次 | — |
| 目录层收手（`_FIX_MKDIR_FAIL_STREAK`） | 4 次 | ✅ 该修复生效 |
| `本轮修复成效` | 成功 1（新落盘 1）· 缺失 1 | 分母仅 1，"成功率 100%"是假象 |
| `修复管线` | 尝试 14 · 失败 13 | 绝大多数文件**未进入修复管线** |

⇒ **「放行分支有 `lsd` 兜底所以无害」是纸面推理**：该分支根本没走到，保留它不解决任何问题。

**真瓶颈已定位：目录可写性预检把「409 建目录失败」判成「目录不可写」，直接跳过全部 4 种方法。**
生产日志逐字序列（`20:08:45`，`wopan175`）:
```
⚠ mkdir | Attempt 3/3 failed ... Conflict: 409 Conflict
⚠ mkdir 失败 exit=1，尝试 OpenList API...
OpenList API mkdir 响应: HTTP_CODE:200        ← API 说建成了
OpenList API mkdir 成功
📁 目标目录已就绪: openlist:wopan175/5/.../aeon-mar…/.
   🔎 目录可写性（沿用本轮结论 0，已重启确认）   ← 但可写性探针判死
🔀 原目录不可写（已重启确认）→ 跳过原目录的 4 种方法
```

**根因（代码级，已核实到行）**：`_fix_probe_dir_writable`（[file_fix.sh:865](file:///workspace/.github/scripts/openlist/file_fix.sh#L865)）
用 `rclone copyto` 写探针 + `_probe_file_visible` 读回来判目录可写性。生产轮实测
**12 次缓存口径探测中 11 次 `rc=1, 探针可见=0`** —— 写探针与 mkdir 是**同一条会回 409 的写路径**，
所以「目录建不出来」必然导致「探针写不进、判不可写」，进而跳过 4 种方法。**这是同一个故障的两次计费。**

**次生断点（代码级）**：`_fix_probe_dir_writable` 只 `return` 一个 bit（[file_fix.sh:976](file:///workspace/.github/scripts/openlist/file_fix.sh#L976)），
可信度 note（`已重启确认` / `未经重启确认（缓存口径，不可信）`）**不进返回值**。
调用方 [file_fix.sh:1484](file:///workspace/.github/scripts/openlist/file_fix.sh#L1484) 的 `if ! _fix_probe_dir_writable`
对两者**同等对待**——note 只用于日志文案（[file_fix.sh:1493-1498](file:///workspace/.github/scripts/openlist/file_fix.sh#L1493-L1498)）。
而同一文件的后端熔断处（[file_fix.sh:959](file:///workspace/.github/scripts/openlist/file_fix.sh#L959)）
**确实**做了 `elif [ "$note" = "已重启确认" ]` 区分 ⇒ **同一可信度契约，两处落实不一致**。
生产轮结论分布：`可写=0（已重启确认）` 7 次 · `可写=0（未经重启确认）` 4 次 · `可写=1（已重启确认）` 1 次
—— 说明重启复核**是能跑通的**（8 次成功），故本次并非"不可信结论被采信"为主因；
**主因是"即使已重启确认，探针写的仍是同一条 409 路径"** ⇒ 复核也只会确认"写不进"。

⇒ **修正后的结论**：409 SEMANTICS 开关**保留但降级**（它从未被触达，收益未经证实）；
**首要待查项改为「目录可写性预检的可信度」**——需回答：探针写失败到底代表"目录不可写"，
还是只是复现了同一条 409 写路径？若属后者，则 `_fix_probe_dir_writable` 的判据链需要重设计
（例如改用只读 `lsd` 判存在性 + 试写文件判可写性，把"建目录失败"与"目录不可写"解耦）。

**E2 结论（判决性，回答用户 Q2）**：**「修复产物改回原名会消失」不成立**。
三时点（即时 → 延迟 120s → 重启真值）原名**全程可见**，且短名无残留 ⇒
改名是**原子且可持久化**的，restore 还原步骤风险低。
边界已写进脚本判读注脚: 本实验用**隔离目录 + 中性载荷**（原设计要手给生产文件路径，
但 run 日志对远端路径是**截断展示**，从日志反推不可靠；且生产文件名多为敏感词，
不适合写进 workflow 输入）⇒ 变量是「改名操作」本身而非文件内容；若需在
生产语义（长敏感名 + crypt 挂载）下复测，再手工给 `DIAG_409_SRC`。

**顺带修掉的报告措辞 bug**: 原判读在「B 存活」时注脚写「所有产物真值存活 ⇒ 未复现」，
与本形态（A 全蒸发）自相矛盾，会把「原名拒收坐实」读成「现象未复现」。
已改为**仅当 A 组真值也存活**时才判未复现，并加反向断言（3d）锁死（19 PASS / 0 FAIL）。

- **2026-09-16 · 「分卷假成功」专攻 → 根因翻案 + Step A 落地（`§12.12`/`§12.13`）**:
  - **三次独立复现，逐字同构**（`35053515218` / `35074878571` / `35079518553`）:
    方法 1 真上传 **64s** → `raw 未增长 52→52` → 判假成功 + 拉黑；方法 2 只跑 **5.3s**
    → 同样判假成功 + 拉黑；方法 3 → `落盘确认 raw=53` 判成功 → 真值复核 `分卷缺失`。
  - **根因（不是"分卷不行"）**: ① 方法 1/2 是被**不可信的目录计数**否掉的（计数不动是
    "幂等跳过/立即失败"的必然结果，不构成假成功证据）；② 方法 3 的通过依据是同一计数器的
    **必然 +1**（分卷=1 个文件）⇒ 判据恒真；③ 方法 3 成功即 `return 0`
    ⇒ **方法 4 与目录折叠从未被尝试**（日志各 0 次）—— 这解释了 §12.10 的改动为何毫无表现。
  - **方案已由用户批准**（`.trae/documents/openlist-fix-method-expansion-plan.md`）。
  - **Step A 已实施**: 命名统一为 `方法N·动作·变体`（展示层 `_fix_method_short` /
    持久化层 `_fix_method_desc`，废除双口径）+ 黑名单跨版本归一（`_fix_method_norm`）
    + 新增 `_confirm_persist_by_size` 并把方法 1/2 判据改为"尺寸匹配 **或** 计数增长"。
  - **★ 改名的高风险点已识别并加锁**: `_fix_method_desc` 是**持久化身份**（写 marker
    黑名单 + 被 `restore_info.jq` 子串匹配分类），故保留 `分卷切割` / `短哈希文件名`
    等分类子串，并把这条约束**写成测试断言**（test 2a–2e）。
  - **验证**: `test_method_id_naming.sh` **31 PASS / 0 FAIL**，**已负向验证**两处
    （去子串 → 1c/1d 红；去归一 → 6a/6f 红）。`restore_info.jq` 新旧两套文本分类全对。
    回归 `test_fix_check` 32/0、`test_hash_dir_fallback` 62/0、`test_fix_pipeline_optimizations` 38/0。
  - **★ 已判决（2026-09-16 16:29Z，run `35120784651`，§12.13.6）**: 定向诊断（A 原名 /
    B 同内容改名 / C 同尺寸随机，三时点真值复核）实测 —— **A 组 2/2 全部蒸发，
    B 组 2/2 存活、C 存活 ⇒ **不是内容拒收，是「原名/原文件状态」拒收，改名即绕**。
    ⇒ 修法方向明确（加「改名直传」方法），**不是止损**。待收窄: 是名字本身还是
    目录内既有坏状态。
- **2026-09-16（下午跟进）· 主轮 `35059689111` failure 归因: 330min 硬线杀收尾，「在途等待」吃穿尾部预留**:
  - 时间线（`4e739e0`）: 05:32 预算锚点（`320min 至 10:52`）→ 05:38 起自起容器后
    「raw 计数持续为 0 ⇒ 本轮禁用落盘即时校验（信任 rc，重启复核兜底）」降级按设计触发
    → 10:42 sync 硬上限 60s（尾部预留 500s 按设计递减）→ 10:46 修复管线优雅收摊
    （剩余 400 文件交下轮）+ 停止分发新同步对（「**在途的等待完成**」）
    → **11:02:28 `##[error] timed out after 330 minutes`**（05:32:28 起跑 + 330min，秒级吻合）。
  - **预算闸本身按设计工作**: 停分发 / 状态增量持久化 / 收尾与清理 · Persist · 自续 step 全 ✓，
    持久化损失有限。缺口在**「在途等待」不受预算约束**——等在途 sync 对完成 + 收尾
    超出尾部预留（330−320=10min），撞上硬线被杀。与 §1.1 旧「全程硬杀」不同:
    传输工作已优雅到站，只是收尾被截（conclusion 仍是 failure，且 trend 样本计入）。
  - **加重因素（红线违规，另一会话所为）**: V3 复验 `35085986592`（10:37–10:53，
    含 truth_restart 重启容器）与主轮收尾段**并行**——在途 sync 对在容器重启窗口挂起/重试，
    等待被拉长。fix-check/diag 必须与主轮错开的红线再次实证。
  - 数据点: 修复管线 尝试 51 · 失败 6 · 成功率 6%（新落盘 108/1642）。
  - 观察点: 「在途等待」是否要加预算上限（到站后不再无限等在途对）——待接棒轮
    `35085306967`（`137c005`，11:04 起跑，~16:24 到站）确认无外部干扰下是否同形态再定。
    （该轮已于 16:14Z 为让位定向诊断被主动取消，未达观察条件；结论转由后续轮次确认。）

- 2026-09-17（+08）· **二轮收敛：回答用户核心问题「文件同步不了 / 修复也失败」**。
  - 触发：用户指出「最重要就是解决文件无法同步，甚至修复也失败的问题」，并要求**用数据回答
    现在到底解没解决**（此前我对该问题的结论是照抄过期 §0，未逐项核证）。
  - 取证（两连轮，`gh api .../jobs/<id>/logs` 全量拉取后 grep）:
    - `35162246938`（手动，09-16 23:25→09-17 02:39）: `修复管线: 尝试 7 · 失败 6（方法耗尽 0 ·
      顺延下轮 0） · 成功率 100%（新落盘 46/46）`；`本轮修复成效: 成功 46（新落盘 46 ·
      沿用上轮 0） · 缺失 25 · 未修复 0`；批次 1 成功 81 / 直接失败 0。
    - `35168662780`（**schedule**，09-17 00:57→07:55，新命名代码）: `修复管线: 尝试 257 ·
      失败 0（方法耗尽 0 · 顺延下轮 0） · 成功率 100%（新落盘 247/247）`；
      `本轮修复成效: 成功 247（新落盘 247 · 沿用上轮 0） · 缺失 45 · 未修复 0`；
      **批次 2 `本批 86 个传输全部真实落盘（重启后仍在）`**。
  - **判决**：`方法耗尽 = 0`（4 方法 × 原目录与兜底目录全试过仍失败 = 唯一代表"永远修不好"
    的判据）两轮均为 0，成功率 100% ⇒ **不存在修不好的文件，用户最关心的问题已解决**。
    `缺失 45` 属 `⏳ 时间预算将尽…下轮接力`（逐条可见），是排期而非能力问题。
  - **自愈闭环实证**：`ph5f12a2558ec9e` 被 `⛔ 拉黑 方法2·短名直传` 后，同轮
    `✅ 修复成功 · 方法4·分卷·短名` ⇒ 拉黑→换方法重试链路真实生效，假成功不再造成永久损失。
  - **「批次性假成功」定性修正**（§0 剩余任务 C）: 从"唯一真实未解项"**降级为已收敛成本项** ——
    机制已清楚、三重兜底（判据修正 + 跨轮黑名单 + 同轮换方法），残余仅为重传开销，
    实测 ~1 文件/轮。查"触发时段条件"即便查清也仍需重传，不改变任何行动 ⇒ **撤出待攻坚列表**。
  - **文档失职纠正**：此前仅向文档尾部追加 §12.13.x，未回头收敛 §0，且汇报时照抄过期 §0。
    本轮把结论**回写 §0**（快照一句话结论 + 剩余任务 C 表），已推送。
  - **本轮收尾定案（用户指令：更新进度报告、移除"批次性假成功"项、确认速度瓶颈）**：
    - **移除「批次性假成功」待办项**：§0 剩余任务 C 中该条已划删除线并标 ✅ 已移除；
      文档全文仅剩 2 处**历史叙述**保留该词（§12.2 机制还原、本进度日志），属归档性质，不改。
    - **确认速度瓶颈**：§0 新增「★★ 速度瓶颈已确认」定案段，列三条旁证 ——
      ① 并发翻倍不涨速（本地流水线非瓶颈）② 跨后端并发共享同一瓶颈且有害
      （0.50 < 1.20 MiB/s）③ 其他环节均非瓶颈（baidupanCrypt 7 MiB/s、runner 出口 25.86 MiB/s）。
      ⇒ 锁死于 **wopan 网盘配额（≈2.8 MiB/s）**，**调参/换调度/优化代码路径均无效**，
      工期 6–8 周属外部约束。状态表「实测天花板」行同步改为 2026-09-17 定案口径。
    - **收束后待办全景**：A 表 4 项已消解、B 表 1 项等自然复现、C 表核心问题已解决
      ⇒ **无待攻坚项**；余 D 表（已推迟 3 项）、E 表（3 个 ph 文件归属，低优先）。
      即：**当前不存在需要写代码去解决的问题**，只剩按配额速率长跑。

- 2026-09-17（+08）· **409 Conflict 语义修复 + 目录层收手**（用户提问驱动，§12.14）。
  - 触发：用户问「目标端文件/容量有在增长吗」「修复的文件改回原名会消失吗」，
    并要求**要真实数据、不靠记忆和文档**。取证时撞见 `35186977864` **failure**，
    与 §C 刚下的「已解决」矛盾 ⇒ 深挖，结论修正。
  - 取证（全量拉 run 日志后 grep，非推断）: `409 Conflict` **2062 次**、
    `Update mkParentDir failed` **1088 次**、`423 Locked` 167 次；
    而 `8005 登录失败` **仅 14 次且 T12:34 后消失** ⇒ **不是驱动死**（与用户后台
    看到的"正常"一致），失败点在**目录创建层**，管理后台不检查这层。
    结局 `成功率 0%（新落盘 0/20）`、`timed out after 330 minutes`。
  - **根因**: 全库把 `409/mkParentDir` 归为「后端异常」→ 跳过整轮；而 409 在 MKCOL
    语义下常见含义是**资源已存在**（幂等成功）。各处 `api/fs/mkdir` 只认 200|201|204。
  - **日志实锤**: `mkdir 3/3 失败 409` → `API mkdir 响应 HTTP_CODE:200` → `目录已就绪`
    → 紧接着 `预检目录可写性: Update mkParentDir failed: 409` ⇒ **`dir_ok=1` 是假阳性**，
    4 种方法全撞同一堵墙。这正是用户问的「目录都创建失败了，咋还进行文件修复」。
  - **修复三处**（均带逃生口）: ① 409=已存在 ⇒ 幂等成功（lsd 读操作复核；file_fix.sh
    4 处 + `_bulk_fold_ensure_dir`；开关 `_FIX_MKDIR_409_SEMANTICS`）
    ② `_sync_retry_409` 与 423 同款整轮重试（`sync_engine.sh`）
    ③ `_FIX_MKDIR_FAIL_STREAK` 目录层连续失败达阈值即收手（`file_fix_pipeline.sh`）。
  - **回归**: 新增 `test_mkdir_409_semantics.sh` **19 断言 / 0 失败**；
    **负向验证三处均正确变红**；本机全套 **30/30 EXIT=0**，`command not found` 为空。
  - **验证环已跑（run `35239688191`，提交 `70fc685`，目标 `wopan175`）**: 新增
    `diag_409_semantics.sh`（E1 语义判决 + E2 改名实验）。
    **E1 未复现 409**（E1a/E1b/E1c 全 409=0）⇒ 「无结论」而非「推翻」。
    ⚠️ 该诊断打的是**手选的 `wopan175`**，与 `35186977864` 故障所在的
    `wopan176Crypt` 系**不同后端/时段**，不能互相推断；409 触发条件仍未知。
    唯一正向信号是 E1c 对「已存在目录重复 mkdir」**静默成功**（非 409），
    与 RFC 4918 不符 ⇒ 至少说明"409=已存在"**不是所有后端的统一行为**。
    修复**保留**（放行分支有 `lsd` 兜底，其余三处是防御性改动，不依赖 409 语义），
    但**不得再称「已解决」**。
    ⚠️ **该「保留」判断随即被生产轮推翻（见下条）**。
    **E2 判决「改回原名会消失」不成立**：三时点（即时/延迟 120s/重启真值）
    原名全程可见、短名无残留 ⇒ 改名原子且可持久化，restore 风险低。
    边界: E2 用**隔离目录 + 本地中性载荷**（原设计要手给生产路径，但 run 日志对
    远端路径是截断展示、从日志反推不可靠；且生产文件名多为敏感词）⇒
    变量是「改名操作」本身；生产语义复测需手工给 `DIAG_409_SRC`。
  - ⚠️ **取证时效提醒**: `35186977864` 的日志已过期（现取不到、无 artifact），
    §12.14 上表那批计数（2062/1088/167）**已无法复查**，只能作当时形态记录，
    不再充当关键论据。
  - **★ 409 修复首次进生产轮即被证伪（2026-09-17，run `35239780581`，提交 `70fc685`）**:
    实跑 5h34m → `timed out after 330 minutes`（failure）。**幂等放行日志 0 次**
    ⇒ 409 修复**从未被触达**；`409 Conflict` 仍 1876 次、`Conflict`/`mkParentDir` 1815 次。
    `本轮修复成效: 成功 1（新落盘 1）· 缺失 1`，`修复管线: 尝试 14 · 失败 13`
    —— 绝大多数文件**未进入修复管线**。
  - **真瓶颈定位**: 生产日志逐字序列（`20:08:45`，`wopan175`）为
    `mkdir 409` → `API mkdir HTTP_CODE:200`（API 说建成了）→
    `🔎 目录可写性（沿用本轮结论 0，已重启确认）` → `🔀 原目录不可写 → 跳过原目录的 4 种方法`
    → 短哈希目录**同样 409** → `兜底终止`。
    根因是 `_fix_probe_dir_writable`（file_fix.sh:865）的写探针走**与 mkdir 同一条
    会回 409 的写路径** ⇒「建目录失败」必然导致「探针写不进、判不可写」，
    **同一个故障被计费两次**，并因此跳过全部 4 种方法。
    本轮 12 次缓存口径探测中 11 次 `rc=1, 探针可见=0`。
  - **次生断点（可信度契约两处落实不一致）**: `_fix_probe_dir_writable` 只 `return`
    一个 bit（file_fix.sh:976），note（已重启确认 / 未经重启确认）**不进返回值**；
    调用方 file_fix.sh:1484 对两者同等对待（note 仅用于文案 1493-1498），
    而后端熔断处 file_fix.sh:959 **确实**做了 `elif [ "$note" = "已重启确认" ]` 区分。
    生产轮结论分布：可写=0（已重启确认）7 · 可写=0（未经重启确认）4 · 可写=1（已重启确认）1
    ⇒ 重启复核能跑通（8 次成功），故**主因不是"不可信结论被采信"，而是探针本身
    复现同一条 409 路径**（复核也只确认"写不进"）。
  - **修正后结论**: 409 开关**保留但降级**（从未触达，收益未证实）；
    **首要待查项改为「目录可写性预检的可信度」**——须回答"探针写失败是否等于目录不可写"，
    若否，则判据链需重设计（如只读 `lsd` 判存在 + 试写文件判可写，把两者解耦）。
    **教训**: 前两版 §0 结论均被后续实测推翻 —— 单点诊断（裸 mkdir 语义）脱离了
    真实故障链路（预检 → 判死 → 跳过方法），故"未复现"无法证明修复有效。

#### 12.14.1 目录可写性预检的判据验证（2026-09-18，run `35289924584`）

**问题**：`_fix_probe_dir_writable` 的写探针失败，到底代表"目录不可写"，还是只是
复现了同一条会回 409 的写路径？若是后者，则生产里"跳过 4 种方法"是误判。

新增 `diag_write_probe.sh`（W1 复现 / W2 显式 API mkdir 后写 / W3 `--no-check-dest` 绕隐式
mkParentDir / W4 隔离目录机制测试），打真实故障路径（路径经 shell 变量拼装，避免把敏感段
写进可见文本）：

| 实验 | 实测 | 判读 |
|---|---|---|
| W1 `lsd` 存在性 | rc=3（**目录不存在**） | "目录已就绪"是 API 200 的假阳性 |
| W1 `mkdir` | rc=1 · http=409 | 复现生产形态 |
| W1 `copyto` 写探针 | rc=1 · http=409 · mkParentDir=1 | **探针确实走同一条 409 路径** |
| W3 `--no-check-dest` | 仍 rc=1 · 409 · mkParentDir=1 | "绕开隐式 mkParentDir"假说**被否定** |
| W4 隔离目录写入 | **rc=0** · mkParentDir=0 | **后端对已存在目录可写** |

⇒ 两个假设被否定：①"后端整体不可写"（W4 rc=0，且同期生产有一份 17GB `emby-backup.tar.zst`
经方法1·原名直传成功落盘并通过真值复核）；②"绕过隐式 mkParentDir 即可写"（W3 仍 409）。
⇒ 问题收敛为一句：**这个特定路径的目录建不出来**。

#### 12.14.2 目录名/路径变量实验（2026-09-18，run `35291770701`）

新增 `diag_dirname_probe.sh`（D1 父层递进 / D2 末层名字 4 形态 / D3 生产同级兄弟），
用**变量分离**区分「名字相关 / 路径相关 / 后端残留」：

```
D1: L1(5)          mkdir rc=0 · 409=0 · lsd 存在=1   ← 健康
D1: L2(<L2 段>)    mkdir rc=0 · 409=0 · lsd 存在=0   ← ★ mkdir 假成功
    ⇢ 第一层失败点 = L2
D2: 原样/ +后缀/ ASCII/ 短哈希    4/4 全 OK（lsd 存在=1）
D3: chilibomba      rc=1 · 409=1 · lsd 存在=0
D3: rolakiki        rc=1 · 409=1 · lsd 存在=0
    ⇒ D3 结果: 0/2 个兄弟目录存在/可建
```

⇒ **名字相关被否定**（D2 原样名在隔离处能建）；**失败定位到 L2 层**；
**L2 的故障污染其整个子树**（D3 兄弟全 409）。
但这是**单点**（只试了一个 L2 名字）—— 按"单点外推"的教训，不能就此断言"通用缺陷"。

#### 12.14.3 ★ L2 假成功的普遍性判决（2026-09-18，run `35294896071` / `35295178274`）

新增 `diag_l2_probe.sh`（P1 普遍性矩阵 / P2 时间维度 / P3 重试自愈 / P4 已存在目录可写），
在 `5/` 下用 4 个**互不相关**的名字（`ol2p_<ts>_a1` / `ol2p<ts>2` / `ol2p-<ts>-c3` /
`ol2p_<ts>_v4.d`，覆盖下划线/纯数字/连字符/像扩展名四种形态）做同构矩阵。**两轮独立实测**：

| 轮次 | P0 L1 | P1 新建 4 个顶层目录 | P4 已存在目录写文件 |
|---|---|---|---|
| `35294896071` | rc=0 · lsd 存在=1 | **4/4 真成功**（rc=0 · 409=0 · lsd 存在=1） | — |
| `35295178274` | rc=0 · lsd 存在=1 | **4/4 真成功**（rc=0 · 409=0 · lsd 存在=1） | rc=0 · 写入后 lsf 可见=1 |

同时段对照复测故障路径（run `35295422027`）：`L1 存在=1` / `L2 存在=0`（假成功）/
兄弟 `0/2`（409）—— **与 `35291770701` 逐字同构**。

⇒ 🔒 **判决：非通用缺陷**。「该挂载下新建顶层目录一律假成功」**被否定**：
同一个后端、同一个 L1(`5/`)、几乎同一时刻，`5/ol2p_*` 能正常建，
而 `5/<L2 段>/*` 一律 409 ⇒ **失败是路径局部的（该 L2 子树），不是挂载级的**。
P2/P3 因 P1 无假成功样本而跳过（设计如此，非缺失）；这也意味着**无法**从本次数据
回答"假成功是延迟落盘还是根本不落"——但该问题对**修法**已不重要（见下）。

**顺带修掉的两个诊断载体缺陷**（都在实跑中暴露）：
- **`workflow_dispatch` 输入数超限**：GitHub 硬上限 **25 个 inputs**，新增 diag_l2 系列后
  达 27 ⇒ 派发直接 `HTTP 422: you may only define up to 25 inputs`。即"加了开关却根本
  派不出去"。已删两个无剩余价值的输入（`diag_409_src` 纯死输入、无代码读取；
  `reject_name_mode` 唯一 shorthash 档已被 §12.13.6 实测覆盖）。
- **专项报告被串名覆盖**：step 级全局 `DIAG_REPORT: /tmp/ol_diag/report.txt` 被**所有**
  专项脚本继承，把 L2 报告也写进 `report.txt`，导致分支收尾 `tail l2_report.txt` 取不到
  （日志里 `=========== L2 普遍性报告尾部 =========== (无报告文件)`）—— **诊断报告取不到 =
  这轮白跑**。已删该全局项（各专项脚本自带正确默认名），并加注释锁死。

**修法方向（按判决修正）**：既然新建目录在**同一挂载的其它路径**上是好的，就不该去换驱动 /
换建目录通路；真正该做的是**让修复管线在遇到"该目录 mkdir 假成功"时，把兜底目录跳到故障子树
之外**（现有短哈希兜底目录落在同一棵坏子树里，故同样 409 —— 这正是生产里"换成短哈希目录
→ 同样 409 → 兜底终止"的原因）。**关键区别**：不再把这类失败解释为"后端不可写"从而跳过
全部方法，而是"换一个已知可写的父目录重试"。

> ⚠️ **上述"现有短哈希兜底目录落在同一棵坏子树里"是错误命题，已由 §12.14.9 证伪
> （2026-09-18）**：`hash_dst_dir = ${dest_path}/${md5(file_dir_rel)[0:8]}`，
> 落点**恒在 `dest_path` 直属**（与故障子目录**平级**，从不进入其内部）
> ⇒ **兜底本来就是"跳出故障子树"的**。保留原文以记录推理轨迹，**判读以 §12.14.9 为准**。

**方法论教训（第三次同类）**：`35291770701` 已能推出"L2 局部故障"，
但若就此写进文档，就是**又一次单点外推**。补做 `35294896071` 只花 3 分钟，
却把"通用缺陷 vs 个体问题"这个**决定修法方向**的分叉一次判死 ——
**先验证普遍性，再定性**，应当成为后续诊断的固定动作。

#### 12.14.4 ★★ 预检时序实证：容器重启后 +0s 探测**必然失败**，+10s 即恢复
（2026-09-18，run `35296822507`，常规 13 组探针，目标 `openlist:wopan175`）

**为什么做这一轮**：用户直接问「文件无法同步的问题解决了吗」，要求**真实数据**。
不能照抄文档，遂跑一轮全量常规探针取**当下**真值。

**实测（原文）**：
```
短名写:         OK
名长阶梯:       32B=OK 64B=OK 80B=OK 100B=OK 112B=OK 128B=OK
覆盖写:         OK
子目录写:       OK
父目录名长阶梯: 10字=OK 20字=OK 30字=OK 40字=OK 50字=OK 60字=OK
字符集阶梯:     纯中文40字=OK …+方括号=OK  混合(贴近真实)=FAIL(405)
路径深度阶梯:   d1=OK d2=OK d3=OK d4=OK d5=OK
★ 重启后写入:   +0s=FAIL() +10s=OK +30s=OK +60s=OK
大文件落盘阶梯: 16MiB=OK 64MiB=OK 256MiB=OK
吞吐阶梯:       transfers=1:0.66MiB/s(单流 0.66, 97s) · transfers=4:2.06MiB/s(单流 0.52, 31s)
吞吐拐点:       未饱和（可继续提并发）
出口带宽基准:   22.71 MiB/s
持续写:         20/20 全部被受理（21s）
并发写:         新目录×4并发×retries1=FAIL(423) · 已存在目录×4并发=OK · 新目录×retries3=OK
★ 列表可见性:   首次非空=10s(20个) · 凑齐20=10s
```

**★★ 核心发现一：重启后确实有一段时间不可写，但生产失败的主因不是这个**

`重启后写入: +0s=FAIL() +10s=OK +30s=OK +60s=OK`
—— 容器刚重启就立刻写会失败，**等 10 秒即恢复**。
`列表可见性: 首次非空=10s · 凑齐20=10s` 是同一现象的独立佐证：后端重启后列表要 ~10 秒才可用。

**⚠️ 但必须纠正一个容易犯的错误推论（我第一版就是这么写的，已核对生产日志后推翻）**：
**不能说"生产失败是因为重启后立刻读"** —— 生产路径实际上**已经等了 70 秒**：
`_restart_openlist_for_truth` 里 `_wait_driver_ready` 盲等 60s
（[openlist_driver.sh:663-667](file:///workspace/.github/scripts/openlist/openlist_driver.sh#L663-L667)）
＋ 路径刷新后 `sleep 10`（[openlist_driver.sh:713](file:///workspace/.github/scripts/openlist/openlist_driver.sh#L713)），
远超实测所需 10s。生产日志逐字核对（`35239780581`，18:29:29→18:30:38）：

```
18:29:29  ↻ 重启 OpenList 容器
18:29:31  驱动就绪等待 60s（盲等）
18:30:28  ⚠️ raw 计数持续为 0（容器重启后列表未就绪）
18:30:38  ❌ 目录不可写（重启后探针消失: 写入未真正落盘）
18:30:38  结论: 可写=0（已重启确认）
```

⇒ 等满 70s 后**探针仍然消失**，说明**写入本身就没落盘**（`copyto` 当时是 409 失败，
探针文件压根没写上去）—— **这是 §12.14.1 里已证的"409 写路径"问题，不是时序问题**。
生产轮结论分布印证：`可写=0（已重启确认）` 7 次 · `可写=0（未经重启确认）` 4 次 ·
`可写=1（已重启确认）` 1 次 —— 复核链路本身是通的（8 次成功完成重启复核），
**复核只是"如实确认了写入没落盘"**。

⇒ **修正后的归因（按证据，勿再走弯路）**：
- **主因仍是 409 写路径**（探针与 mkdir 同一条路，建不出目录 ⇒ 探针必然写不进）
  —— 这一点 §12.14.1 已经证过，本轮生产日志再次印证。
- **新增的"重启后 10s 就绪"是另一件事**：它是一个**真实存在但当前未被生产触发**的风险
  （因为生产已等 70s）。它的价值是**防止未来有人把等待调短**（`OPENLIST_DRIVER_READY_WAIT`
  若被优化成"轮询到 ping 通就走"，就会掉进这个坑），以及解释
  `列表可见性` 那类观测。**不应把它当作生产失败的主因去改。**

**★ 核心发现二：混合字符集名 FAIL(405) —— ⚠️ 复核后判定为「与 §11.8 同源，非新维度」**

字符集阶梯里 5 档全 OK，**唯一失败的恰是"最贴近生产真实文件名"的混合档**。
第一版把这条写成"新线索、未定位"，**复核 `diag_backend.sh` 后已修正判读**：

```bash
_cs_try "中文40字"        "$(printf '测%.0s' $(seq 1 40))"
_cs_try "中文40+空格"      "$(printf '测 %.0s' $(seq 1 20))"
_cs_try "中文40+圆括号"    "$(printf '（测）%.0s' $(seq 1 10))"
_cs_try "中文40+半角括号"  "$(printf '(测)%.0s' $(seq 1 10))"
_cs_try "中文40+方括号"    "$(printf '[测]%.0s' $(seq 1 10))"
_cs_try "混合(贴近真实)"   '(CSP6) [流石堂 (流ひょうご)] 淫らな彼女達の作りかた (冴えない彼女の育てかた) [中国翻訳]'
```

⇒ **前 5 档是"字符集模式"（重复同一字符），第 6 档是"那个真实失败名逐字复刻"**
⇒ 这不是等长的字符集对照，而是 **§11.8 已定性的「特定目录名本身写入失败」再次出现**
（§11.8 已证：把该名字原样搬到全新位置仍然 405，可脱离原目录复现）。
故它**不构成新的"字符集"维度**——它只是同一现象的又一次观测。
⇒ **本项撤销"未定性线索"的定性**；F10（405 fail-fast）的优先级**不因此上调**。
若仍想分离"字符集 vs 具体串"，正确做法是**等长、同结构、只换字符**的对照
（如把该名里的假名换成等长汉字），而非拿真名当一档——**该实验设计留待需要时再做**。

**其余结论（用于排除，均非瓶颈）**：
- 名长 / 父目录名长 / 路径深度 / 覆盖写 / 子目录写 / 大文件尺寸 —— **全部通过**
  ⇒ 这些维度都不是当前故障原因（此前文档中对"名长阈值"的怀疑应降级）。
- 并发写 423 是**父目录 mkdir 竞争**（已存在目录并发 OK、新目录 retries3 OK）
  ⇒ 与 409 同源，属并发争用，重试可解。
- 吞吐未饱和（4 流 2.06 MiB/s），但**出口带宽基准 22.71 MiB/s** ⇒ 瓶颈确在 wopan 侧
  （与 §0 速度定案一致）。

**★ 由本轮证据修正后的修法优先级**：
1. **把"目录建不出"与"目录不可写"解耦**（最高，仍是 §12.14.1 的结论）：
   探针失败时**不能直接判死**，应先只读判存在性、并尝试"换个可写父目录"——
   因为探针失败已被证明只是复现 mkdir 的 409，**不代表目录不可写**。✅ **已落地**（`296a3c3`）。
2. **兜底目录跳出故障子树**（见 §12.14.3 判决）：生产里
   `🔀 目录级兜底: 目标端根目录折叠为短哈希目录 5058f1af` 之后**仍 409**
   （18:30:39-41 三次 `mkdir+hash` 全败）⇒ 兜底仍落在同一棵坏子树里。
   ⏸️ **但取证后暂停**（§12.14.5）：`35306329978` 实测 dest_path 自身可写、
   故障未复现 ⇒ 对照无判别力，改了也无法验证。留待故障复现当轮再取证。
   > ❌ **本项已撤销（§12.14.9，2026-09-18）**：上面那句"⇒ 兜底仍落在同一棵坏子树里"
   > 是**未核算的推断**。核算后：`hash_dst_dir = ${dest_path}/${md5(file_dir_rel)[0:8]}`
   > ⇒ 落点**恒在 `dest_path` 直属**，与故障子目录**平级**。**该修法针对的问题不存在。**
3. **不要**把 `OPENLIST_DRIVER_READY_WAIT` 调短（当前 70s 总等待是安全裕度，
   实测就绪需 10s；调短会引入新的时序脆弱性）。
4. ~~混合字符集 405 需先诊断再定修法~~ → **✅ 撤回**（§12.14.4 复核）：
   该"混合档"用的是**真实失败名逐字复刻**，本质是 §11.8 已定性的"特定目录名本身"，
   不构成新维度 ⇒ 无需新诊断、无需新修法。

**⚠️ 仍需同时修的语义隐患**：`note="已重启确认"` 只表示"重启过"、
**不等于**"结论可信"。上例中它如实反映了"写入没落盘"，但**一旦出现"重启后列表未就绪"
造成的读失败，它会把"读不到"误标成"确认不可写"**，而后端熔断计数
（[file_fix.sh:959](file:///workspace/.github/scripts/openlist/file_fix.sh#L959) 只认这个词）
会据此误判后端死。建议改名/分档（如 `已重启确认-写入失败` vs `已重启确认-列表未就绪`）。

#### 12.14.5 ★ 「兜底目录跳出故障子树」可行性取证：**前提本轮不成立，故障未复现**
（2026-09-18，run `35306329978`，`diag_escape_probe.sh`，目标 `openlist:wopan175`，dest_path=`5`）

**为什么必须先取证**：§12.14.3 判的是「假成功**是路径局部的**」，但由此推出的
「**跳出去就一定能写**」**从未被实测过**——它是修法②的全部依据。按 §12.14.3 自己写下的
教训（先验证普遍性、避免单点外推），在改还原链（`restore_info.jq` 的 `hash_dir` 分支、
`alternative` 的相对路径语义）之前，必须先坐实这个前提。

**新增 `diag_escape_probe.sh`**（E1 祖先层阶梯 / E2 同层对照 / E3 跳出层写文件 /
E4 还原路径可行性 / E9 清理，前缀 `ol2e_<ts>_`）：

```
E1[上跳 0 · dest_path 自身]  wopan175/5/ol2e_<ts>_u0    mkdir rc=0 · lsd 存在=1   ← 可写
E1[上跳 1 · 挂载根]          wopan175/ol2e_<ts>_u1      mkdir rc=0 · lsd 存在=1   ← 可写
E1 判读: 最深可写层 = 上跳 0 层
E2[不跳出 · dest_path 下]    wopan175/5/ol2e_<ts>_in    mkdir rc=0 · lsd 存在=1   ← ★ 可写
E2 判读: ⚠️ 不跳出的落点本轮**可写** ⇒ 未复现生产故障
E3 跳出层写文件:             copyto rc=0 · lsf 可见=1
E4 跨层归位:                 move rc=0 · **归位后目标位可见=0** ← ★ 假成功
E9 清理: 2 次 purge 成功 · 0 次失败
```

⇒ 🔒 **判决一（对修法②）：前提不成立，本轮无法支持「必须跳出」。**
`dest_path` 自身（上跳 0 层，= 现有短哈希兜底落点的父层）本轮**完全可写**，
连"不跳出"的落点都建得出来 ⇒ **生产故障形态（L2 坏）在本轮没有被复现**。
按 §12.14.3 的口径，这说明**该 dest_path 当前是健康的**（L2 故障是**间歇性**的，
非持续态）；因此"跳出 vs 不跳出"这组对照**没有判别力**，不能据此改落点。
**修法② 暂停**，改为：把本探针留作**故障复现时的取证工具**（复现当轮再跑，
才有判别力），不做无证据的落点改动。

⇒ ⚠️ **判决二（待证线索，已被自己推翻一次）：跨层 `move` 的可见性结论不可信。**
E4 首版：`rclone move <跳出层>/x <dest_path>/x` **rc=0、409 特征=0**，但**紧接着**的
`lsf` 复核目标位**不可见** ⇒ 当时写成"第三处假成功、还原会静默丢文件"。
**⚠️ 该结论已推翻**：复核**紧接在 move 之后、没有任何等待**，而 §12.14.4 已实测
「列表可见性 首次非空=10s」—— 这个写法**必然**把"列表还没刷新"读成"没到位"，
**把成功误判成假成功**。这是**判据本身的缺陷**（我犯的错），不是被测对象的现象。

⇒ 🔒 **判决二（最终）：还原链没有缺陷，首版结论是判据缺陷造成的误报。**
接线修好后连跑两轮（`35307143381` 带等待、`35307446562` 带等待+stat）：

| 轮次 | 列列举（立即） | 列列举（等 15s） | **目标全路径 stat** | 源仍在 |
|---|---|---|---|---|
| `35307143381` | 不可见 | 不可见 | 未测 | 0 |
| `35307446562` | 不可见 | **仍不可见** | **在（Size=18，与载荷字节数一致）** | 0 |

⇒ **文件其实就在目标位**（载荷正好 18 字节，`stat` 命中的就是它），
只是**目录列列举在 15s 后仍未刷新**；`rclone move` 本身**成功且没有丢文件**。
⇒ **§12.14.5 初版"第三处假成功/静默丢文件"的判决彻底作废**；
`restore_info.jq` 的 `hash_dir` 分支**没有**被本轮证据指出的缺陷，**不改**。

**方法论教训（本轮第 2 条，与第 1 条同源）**：判据的**观测方式**会和判据的**结论**耦合错。
首版用"列列举里有没有"判"文件在不在"，而列列举本身就是**滞后**的（§12.14.4 已实测 10s）；
即使加了 15s 等待也仍然不可见 ⇒ **等待时长不是正解，换观测通道才是**：
改用**按全路径 `lsjson` stat**（直接问后端对象在不在，不经目录列举）——
一测即翻案。⇒ **凡"以列表为准"的判据，都必须先用一个独立的直读判据交叉验证。**

**⚠️ 诊断载体已知不足**：E2 只有「dest_path 自身可写/不可写」二分，**没有**在"已知坏的
L2"上做对照 —— 而 §12.14.3 的坏层是 `5/<L2 段>`（dest_path 的**下一层**），
本探针从 dest_path **向上**跳，方向与坏层位置相反。故即便本轮"跳出可写"，
也**不足以**证明"跳出有用"；**可用的判决必须等故障复现**（届时坏层就是 dest_path 或
其下层，对照才成立）。这是本探针**设计上的局限**，如实记录。

**方法论价值（第四次同类）**：本轮**没有产出任何代码改动**，但它拦住了一次
**基于未验证前提的重构** —— 若直接按 §12.14.3 把落点搬到祖先层并改还原链，
就会在"故障其实没复现"的情况下动到**可逆性最关键的一环**，且改动效果无法被验证。
⇒ **"先验证普遍性，再定性"应升级为"先验证前提，再动手"**：任何由判决推出的修法，
其**前提本身**也要走一次实测。

#### 12.14.6 ★★★ 三态解耦的首轮生产验证：**修复未生效，故障恰是它治不了的那一类**
（2026-09-18，主轮 `35308273431`，sha `60740b01`，`sync_budget_min=90`，`force_sync=true`，**success**）

**为什么这一轮是关键**：核验发现 **`296a3c3`（三态解耦 = §0 的"最高优先级修法"）
此前从未进过任何生产轮** —— 近 4 轮主轮 sha（`aa4b8a99` / `629b843a` / `70fc685e` /
`ebea8aef`）经 `git merge-base --is-ancestor` 逐一核对，**均不含该提交**。
故这是第一次在真实环境检验它。

**整轮结果**：
```
修复管线: 尝试 2 · 失败 2（其中方法耗尽 0 · 顺延下轮 0） · 成功率 0%（新落盘 0/10）
本轮修复成效: 成功 0（新落盘 0 · 沿用上轮 0） · 缺失 10 · 未修复 10
本轮传输 2 个文件，目标视图 225（缓存口径）
```

**★ 决定性观测：三态分支全程未触发。**
```
grep -c "判 exists_but_readonly"         → 0     ← 新增态一次都没出现
grep -c "放行，由 4 种方法的落盘结果定论"  → 0     ← 改动 1c 的放行一次都没走
grep -c "兜底终止"                        → 2     ← 仍走旧路径
```
逐字现场（`06:10` 与 `06:11` 两次同构）：
```
🔎 预检目录可写性: openlist:wopan175/5/1024j-.../k…/5058f1af
⚠ 目录探测 | olprobe_xxxx.txt: Failed to copy: Update mkParentDir failed: 409 Conflict  (×3)
缓存口径: rc=1, 探针可见=0（不作判据）
结论: unwritable（可写=0 · 已重启确认-写入失败）
❌ 短哈希目录不可写（已重启确认-写入失败），兜底终止
❌ 无法换目录（备用目录也写不进去），无法修复文件
```

**根因（代码级，已定位到行）**：三态判定**依赖只读 `lsd` 复核存在性**
（[file_fix.sh:986-987](file:///workspace/.github/scripts/openlist/file_fix.sh#L986-L987) →
[file_fix.sh:884-887](file:///workspace/.github/scripts/openlist/file_fix.sh#L884-L887)）。
本例里**这个目录根本不存在** —— 日志显示 `OpenList API mkdir (短哈希) 响应: HTTP_CODE:200`
（API 报成功）**但 `lsd` 读不到** ⇒ 正是 §12.14.2/12.14.3 的 **`mkdir` 假成功**。
`lsd` 失败 ⇒ 判 `unwritable` ⇒ 不放行 ⇒ 兜底终止。

⇒ **这不是三态逻辑写错了，而是"三态的前提在本例不成立"**：
`exists_but_readonly` 的语义是"目录**存在**但写不进"；本例是"目录**不存在**且建不出"
—— 按定义**就该判 `unwritable`**。⇒ 三态解耦**行为正确**，但它**治不了这个病**：
真正的堵点是**"兜底目录本身也建不出来"**，不是"建得出但写不进"。

⇒ 🔒 **判决：§0 认定的"最高优先级修法"在真实故障面前无效。**
它把"预检误判"当成主因，但生产实测显示：**预检没误判** —— 短哈希目录确实不存在，
判 `unwritable` 是对的。真正的堵点是 **`API mkdir HTTP 200 但目录不落盘`**（假成功），
**且兜底目录落在同一棵坏子树里**（§12.14.3）。
⇒ 这**反过来印证修法②（跳出故障子树）方向是对的**，但它同样卡在
"**新目录根本建不出来**"这个更底层的问题上。

> ⚠️ **本节加粗的两句已被 §12.14.9 证伪（2026-09-18）**：
> ①"兜底目录落在同一棵坏子树里" —— **错**，落点恒在 `dest_path` 直属（平级）；
> ②"印证修法②方向是对的" —— **随之作废**，该修法已撤销。
> 另：本节"`5/1024j-.../k…/5058f1af`"的**路径写法有误** —— `5058f1af` 落在
> `dest_path` 直属（`5/5058f1af`），**不在** `1024j-.../k…/` 之下（§12.14.7 已纠正）。
> 保留原文以记录推理轨迹。

**★★ 矛盾点（必须优先查清，它决定修法方向）**：
本轮短哈希目录名是**全新的**（`5/1024j-.../k…/5058f1af`），**同样中招**假成功
⇒ 至少**不是"某个特定名字"的问题**，更像**"该子树内新建目录一律假成功"**。
⚠️ 这与 §12.14.3「`5/ol2p_*` 能建、非通用缺陷」的判决**表面矛盾**。
差异可能在**层级**：`5/` 直属（§12.14.3 测的）vs `5/1024j-.../k…/` **深层**（本轮）。
⇒ **待查**：是"**深度**相关"、"**特定父目录**相关"，还是 §12.14.3 当轮
`5/` 恰好处于健康窗口？**该矛盾未解前不要改修法方向。**

> ⚠️ **上述"深层/层级"表述已被 §12.14.7 复核推翻（2026-09-18 当日晚）**：
> 核对代码发现 `5058f1af` **不是深层路径**，而是 `dest_path` **直属**（见 §12.14.7）。
> 保留原文仅为记录当时的推理轨迹，**判读请以 §12.14.7 为准**。

**其余同轮观测（旁证）**：
- `重启后目标视图: 225 → 225` · `✅ 重启前后列表一致，无假成功污染` —— 真值复核链路正常。
- `raw 计数持续为 0（容器重启后列表未就绪），本轮禁用落盘即时校验` —— 与 §12.14.4
  "重启后 ~10s 就绪"吻合，机制按预期工作。
- 直传路径仍在工作（`本轮传输 2 个文件`）⇒ §C「后端健康时能同步」的限定仍成立。

#### 12.14.7 ★★★ 矛盾的变量纠正：`5058f1af` 与 `ol2p_*` **同深度同形状**，差的是"名字/时间"
（2026-09-18，**代码级复核 + 设计同构分离专项**；实验已就位但**尚未派发**）

**为什么做这一节**：§12.14.6 把矛盾归因为"层级/深度差"（`5/` 直属 vs 深层），
并据此要求"按深度分层"实验。动手设计前先核对**落点到底在哪一层**，结果该归因**不成立**。

**★ 代码级实锤（本机核对，非推断）**：
```
file_fix.sh:1273-1280  _hash_src="$file_dir_rel"；若 file_dir_rel="."/""
                       则 _hash_src="."（根目录无目录名可折，用固定串）
file_fix.sh:1287       HASH_DIR_REL=$(_hash_dir_rel_for "$_hash_src")
file_fix.sh:1069-1071  _hash_dir_rel_for() { printf '%s' "$1" | md5sum | cut -c1-8; }
file_fix.sh:1293       hash_dst_dir="${dest_path}/${HASH_DIR_REL}"
本机实测: printf '%s' "." | md5sum | cut -c1-8  →  5058f1af
```
生产日志逐字印证（`35308273431`）：
`🔀 目录级兜底: 目标端根目录折叠为短哈希目录 5058f1af（根目录无目录名可折，改为在根下新建）`
⇒ 落点 = `dest_path` + `/5058f1af` = **`openlist:wopan175/5/5058f1af`**。

| 对照项 | §12.14.3 的 `ol2p_*` | §12.14.6 的 `5058f1af` |
|---|---|---|
| 落点层级 | `dest_path`(`5/`) **直属** | `dest_path`(`5/`) **直属** |
| 名字新旧 | 全新（时间戳） | 全新（但**确定性**：`md5(".")`） |
| 名字形态 | `ol2p_<ts>_a1`（字母/下划线/数字） | `5058f1af`（8 位纯 hex） |
| 结果 | **4/4 真成功** | **假成功** |

⇒ 🔒 **"深度/层级"不是变量**（两者同层）。剩下只有两个可能变量：
1. **名字内容 / 是否被建过**：`5058f1af` 是**确定性名字**（每轮重算都一样），
   若该名字在后端已"残留半个对象"或因字符集被拒，就**每轮必然失败**（自我复现）；
   而 `ol2p_*` 带时间戳，**每次都不同**，且从没被建过。
2. **健康窗口**：§12.14.3 与 §12.14.6 相隔数小时，非同时段。

**⇒ 设计并落地同构分离专项 `diag_depth_probe.sh`（本轮未派发）**：
- **E1** 同深度同形状对照：同一轮内既建**历史名 `5058f1af`**、又建 3 个**同深度全新名**
  ⇒ 历史名失败而全新名成功 ⇒ 变量①（名字/残留）；两者都成功 ⇒ 变量②（健康窗口）。
- **E2** 深度阶梯：沿生产真实故障路径（`5/1024j-视频-pornhub-channel/…`）逐级下探，
  每级用"全新名 vs 生产名"对照 ⇒ 分离"深度相关 / 子树整体坏 / 名字相关"。
- **E3** 健康窗口锚点：E1/E2 跑完后**同轮内复测** E1a 落点 ⇒ "先失败后成功"即窗口漂移实锤
  （同轮自证，不靠跨轮对照——这是本设计相对 §12.14.3 的关键改进）。
- **E4** 已存在目录可写性对照（目录能建 ≠ 能写文件）；假成功复核一律**带等待 + 直读 stat**。

**⚠️ 若变量①成立，修法方向要改**：不是"跳出故障子树"（§12.14.3 的推论），
而是「**兜底目录名不能复用确定性历史名**」—— 现有 `md5(".")` 落点一旦坏了就**永远坏**，
须改成**每轮带随机盐/时间戳**的落点。这是一个**比"跳出子树"更小、更精准**的改动面。

**为什么本节先于实验写下来**：§12.14.6 的"深度"归因若不纠正，就会设计出一轮
**变量数错**的实验（把"同深度同形状"误当"不同深度"）——这正是 AGENTS.md
「实验设计要数清变量」教训的第四次应用。**派发前先纠变量，成本为零。**

**状态**：`diag_depth_probe.sh` + workflow 接线已落地（commit `2021b9c`，`diag_l2_n=d` 触发），
`bash -n` / YAML 解析 / inputs 计数（仍 25）/ 4 个判决分支的 rclone stub 自测**全部通过**。
✅ **已派发并出结论**：run `35322561888`（sha `56fcb17`，success）—— **见 §12.14.8**：
本节的推测修法「兜底目录名带随机盐」**被实测否定**（变量①不成立），
真正的结论是「路径局部 + API/WebDAV 口径不一致」。

#### 12.14.8 ★★★★ 深度分层实验判决：**矛盾消解 —— 是「路径局部（生产 L2 子树）」，「深度」「名字」「时间窗口」三个猜想全部被否定**
（2026-09-18，run `35322561888`，sha `56fcb17`，`diag_l2=1` / `diag_l2_n=d` / `diag_l2_l1=5`，**success**）

**为什么能一次判死**：§12.14.7 先把变量纠正到"同深度同形状"，
故本轮四个对照**互斥且完备** —— 无论出现哪种组合，都能唯一归因。**同轮、同容器、同后端**。

**实测原文（逐字）**：
```
E0[dest_path 5]:            mkdir rc=0 · 409=0 · lsd 存在=1     ← 锚点健康
E1a[历史名 5058f1af]:       mkdir rc=0 · 409=0 · lsd 存在=1     ← ★ 生产失败落点，本轮**能建**！
E1b[ol2d_<ts>_n1]:          mkdir rc=0 · 409=0 · lsd 存在=1
E1b[ol2d<ts>2]:             mkdir rc=0 · 409=0 · lsd 存在=1
E1b[ol2d-<ts>-x3]:          mkdir rc=0 · 409=0 · lsd 存在=1     ← P1 同构复现（3/3）
E1 判读: 🔒 同深度名全部真成功（含历史名 5058f1af）
E2-1[dest_path 下·全新 L2 名]: mkdir rc=0 · 409=0 · lsd 存在=1   ← 同层新建**能建**
E2-2[新建 L2 下·全新叶名]:    mkdir rc=0 · 409=0 · lsd 存在=1   ← 深度 +1 **也能建**
E2-1'[生产 L2 名（只读）]:    lsd 存在=0                        ← 生产 L2 **根本不存在**
E2-3[生产 L2 子树内·全新名]:  mkdir rc=1 · http=409 · 409特征=1 · lsd 存在=0  ← ★ 唯一失败项
        ▸ Attempt 3/3 failed with 1 errors and: Conflict: 409 Conflict
        ▸ Failed to mkdir: Conflict: 409 Conflict
E2 判读: 🔒 该 L2 子树整体坏（dest_path 直下能建、进该子树就 409）
E3-2[复测历史名]:           mkdir rc=0 · 409=0 · lsd 存在=1     ← 两次一致成功，窗口稳定
E4 目标全路径 stat:         在=1（直读判据）
E9: 已清理 5 次 purge 成功 · 2 次失败（失败者即坏子树内那两个，符合预期）
```

**🔒🔒🔒 判决（四个猜想逐一裁决）**：

| 猜想 | 检验 | 实测 | 裁决 |
|---|---|---|---|
| ① 名字内容/是否被建过 | E1a 历史名 `5058f1af` | **能建** | ❌ **否定** |
| ② 健康窗口漂移 | E1a 首测 + E3 复测 | **两次都成功** | ❌ **否定**（窗口稳定） |
| ③ 深度/层级 | E2-1(dest_path 下)、E2-2(再深一级) | **都能建** | ❌ **否定**（深度到 +1 仍 OK） |
| ④ **路径局部：生产 L2 子树本身坏** | E2-3（进该子树建全新名） | **409 Conflict** | ✅ **成立** |

⇒ **§0 的"待解矛盾"至此消解**：§12.14.3（`5/ol2p_*` 能建）与 §12.14.6（`5/5058f1af` 建不出）
**并不矛盾** —— 两者**同深度同形状**，**当轮窗口不同**（§12.14.3 那两轮健康、
§12.14.6 主轮落在坏窗口），**而与"深度"无关**。真正的稳定判据只有一条：

> **故障锁定在「生产 L2 子树 `5/1024j-视频-pornhub-channel/`」这一条路径上**；
> 该子树**连自身都不存在**（`lsd 存在=0`），在其内**任何新建都是 409 Conflict**
> （且**不是**假成功 —— 是**硬 409**，这与 §12.14.6 生产日志里的"API mkdir 200"不同）。

**⚠️ 一个必须点出的细节差异**：§12.14.6 生产日志显示 `API mkdir (短哈希) 响应: HTTP_CODE:200`
（API 报成功、`lsd` 读不到 = **假成功**）；而本轮 E2-3 是 **`rclone mkdir` 直接 409**（硬失败）。
⇒ 两条通路（**OpenList 原生 API vs rclone 的 WebDAV mkdir**）对同一坏子树的**回报不一致**：
API 说"成功"、WebDAV 说"409"。**这解释了三态解耦为何没触发**（它依赖 `lsd`/rclone 口径，
而生产走的是 API 兜底那条路）—— 两处口径差本身就是一个**值得单独记下的隐患**。

**⇒ 修法含义（按证据，未改代码）**：
- 修法②（跳出故障子树）**方向被本轮坐实为必要** —— 因为「坏的是特定子树」，
  跳出去（到 `dest_path` 直属，E1 已证可写）确实能绕开。
  ⚠️ 但 §12.14.5 那轮**未复现故障**（当时 `dest_path` 也健康）⇒ 判别力依赖"当轮恰好坏"。
  本轮刚好提供了一次**故障在场的对照**：`dest_path` 可写、`L2 子树` 409 ⇒ **跳出有效**。
- 现有短哈希兜底落点 = `dest_path/5058f1af`（E1a 实测**可写**）⇒ **兜底落点本身没问题**；
  生产里它失败是因为 §12.14.6 那轮的 `5/5058f1af` 也落在坏窗口 ⇒ **仍属窗口问题，非落点问题**。
- ⇒ **"兜底目录名带随机盐"（§12.14.7 的推测修法）不成立**（变量①已被否定）；
  真正要做的是**判据口径统一**（API 与 WebDAV 对 mkdir 成败的回报必须一致）+
  **建后真值复核**（§12.14.4 已有 70s 等待，但复核口径要覆盖 API 路径）。

**⚠️ 未决（留给后续轮）**：坏子树的**成因**（网盘侧残留 / 名字历史 / 配额）仍未查；
但按 §12.14.3 的既定口径，修法只要"绕开"即可，不依赖成因。**成因追查属独立课题。**

**方法论（第 7 次；本轮是"设计对了一次判死"的正面样本）**：
§12.14.7 在派发前先把变量纠正为"同深度同形状"，才使本轮四个对照**互斥完备** ⇒
**一次运行同时否定三个猜想、坐实第四个**。若沿用 §12.14.6 的"深度"归因，
本轮会去测一堆不同深度的目录，**既得不到这个结论、还多花时间**。
⇒ 规则：**"待解矛盾"要先拆成互斥且完备的候选变量，再设计对照**。

**✅ 据此已落地的修法 ①：API mkdir 报 200 必须复核存在性**（commit `4ebb39e`，2026-09-18）
- **根因定位到行**：`file_fix.sh` 里 **4 处** API-200 分支都**只看 HTTP 码**就置 ok，
  而同一函数的其它三条分支（rclone rc=0 / rclone 报错 / API 报 409）**都做了读操作复核**
  ⇒ 唯独 API-200 是口子：
  [file_fix.sh:1340](file:///workspace/.github/scripts/openlist/file_fix.sh#L1340)（短哈希）、
  [file_fix.sh:1504](file:///workspace/.github/scripts/openlist/file_fix.sh#L1504)（Step 1 原目录）、
  [file_fix.sh:1583](file:///workspace/.github/scripts/openlist/file_fix.sh#L1583)（base64URL）、
  [file_fix_pipeline.sh:322](file:///workspace/.github/scripts/openlist/file_fix_pipeline.sh#L322)（批量折叠）。
- **生产实锤**（主轮 `35308273431`，逐字）：
  `06:10:10 ✅ 短哈希目录创建成功 (API)` → `06:13:26 ❌ 短哈希目录不可写…兜底终止`
  ⇒ 白等 ~2 分钟，且日志给出"创建成功"的假象。
- **改动**：4 处统一接 `_fix_dir_exists_or_conflict`（**复用既有**读操作判据，
  无新概念/新依赖/新开关），复核不过则不置 ok，并如实记为「API 报 200 但不存在（假成功）」。
- **诚实限定**：**不会让文件同步成功**（坏子树建不出就是建不出）；它修的是
  **判据可信度** —— 省白等、日志如实、并为后续"跳出故障子树"提供**可信判据基础**。
- **回归**：`test_mkdir_409_semantics.sh` 新增 **D7 组**（D7a/D7b 静态 + D7c/D7d 行为正反向）。
  ⚠️ **D7 初版是假过**（用"该行后 N 行内出现复核"会串到后面 409 分支的复核上），
  已改为**按缩进切分支块**；实测旧代码 D7a=**3/3 全红**、D7b=**1/1 全红** ⇒ 是真守卫。
  本机五套 23/62/38/32/67 全绿；CI `tests.yml` run `35324743387`（sha `4ebb39e`）。

#### 12.14.9 ★★★★ 修法②的假前提被证伪：**兜底目录本来就在 `dest_path` 直属，从未落在故障子树内**
（2026-09-18，**纯代码核算 + 生产日志口径核对**；据此**决定不实施**修法②）

**为什么必须核算**：用户批准执行「兜底目录跳出故障子树」。动手前先核对**落点到底在哪一层**
（沿用 §12.14.7 的教训：设计前先核对待改对象的真实取值）。

**★ 代码级核算（三行公式，本机实测）**：
```
file_fix.sh:1287  HASH_DIR_REL=$(_hash_dir_rel_for "$_hash_src")
file_fix.sh:1069  _hash_dir_rel_for() { printf '%s' "$1" | md5sum | cut -c1-8; }   # ← 定长 8 位
file_fix.sh:1293  hash_dst_dir="${dest_path}/${HASH_DIR_REL}"                     # ← 恒在 dest_path 直属
```

| `file_dir_rel`（相对 `dest_path`） | `HASH_DIR_REL` | 落点绝对路径 | 是否在故障子树内 |
|---|---|---|---|
| `1024j-视频-pornhub-channel/kate-bloom`（坏子树**内**的文件） | `2042c299` | `wopan175/5/2042c299` | ❌ **平级，不在其内** |
| `.`（生产实际触发者，根目录文件） | `5058f1af` | `wopan175/5/5058f1af` | ❌ **平级，不在其内** |

⇒ 🔒 **结论：`hash_dst_dir` 是「8 位定长扁平名 + `dest_path` 前缀」，
与 `file_dir_rel` 的层级**完全解耦**（只把整条相对路径当作**哈希输入**，不作为**路径前缀**）
⇒ **兜底从设计上就是"跳出故障子树"的**。

**生产日志口径核对**：`35308273431` 逐字
`🔀 目录级兜底: 目标端根目录折叠为短哈希目录 5058f1af（根目录无目录名可折，改为在根下新建）`
—— `md5(".")=5058f1af` 吻合，且落点即 `5/5058f1af`（`dest_path` 直属）✓

**⇒ 判决：修法② 的整个前提（"现有兜底落在同一棵坏子树里，故同样 409"）是错误命题。**
该命题在文档中出现 **6 处**（§0 ×2、§12.14.3、§12.14.5、进度日志 ×2），
全部源自 §12.14.5 那次**推断**（见 `mkdir+hash` 三次全败 ⇒ 反推"兜底也在坏子树里"），
**从未被核算过**。§12.14.8 的 E1a 其实已经反证了它：`5/5058f1af` 在健康窗口**可写**。

**⇒ 决定：不实施修法②。**
- 落点无缺陷 ⇒ 改落点（含改 `restore_info.jq` 的 `alternative` 相对口径、
  `--filter-from` 删除保护）**风险大且无收益**。
- 生产里"兜底也 409"的真因已由 §12.14.8 定位：**当轮窗口/子树故障** +
  **API 200 未被复核**（后者已在 `4ebb39e` 修掉）。

**⚠️ 方法论（第 8 次同类，且是"拦下一次无谓改动"的正面样本）**：
文档里流传的"显然"命题，也要**核算一次公式/量取值**再据以改代码 ——
本次若直接执行，会白改还原链（高风险面）、并让"已修好"的假象留在文档里。
⇒ 规则：**凡"某落点/某判据在坏范围里"的论断，必须先写出落点公式并代入真实值验证。**

#### 12.14.10 ★★★ 追因：「API mkdir 报 200 但目录不落盘」的**可观测性缺陷**已被定位（代码级）
（2026-09-18，纯代码取证；**不改代码**，结论是"缺证据 → 先补观测"）

**触发**：§0 已把 ③「查清 `API mkdir` 200 不落盘的成因」降为**独立追因课题**（不阻塞修法）。
本次按"先补，再出改动方案"推进，目标：**先确认我们手上到底有没有能力看见真因**。

**★ 关键发现（代码级，三处对照）**：

| 位置 | 是否保留 API 响应体 | 证据 |
|---|---|---|
| 生产 `file_fix.sh` 短哈希分支（[:1338](file:///workspace/.github/scripts/openlist/file_fix.sh#L1338)） | ❌ **丢弃** | `mkdir_http=$(echo "$mkdir_resp" | tail -n 1)` ⇒ 只剩 `HTTP_CODE:200` 那一行 |
| 生产 `file_fix.sh` Step1 分支（[:1502](file:///workspace/.github/scripts/openlist/file_fix.sh#L1502)） | ❌ **丢弃** | 同上写法 |
| 诊断 `diag_write_probe.sh` W2（[:163](file:///workspace/.github/scripts/openlist/diag_write_probe.sh#L163)） | ✅ **保留** | `say "API mkdir 响应体: $(echo "$_resp" | head -c 200)"` |

⇒ 🔒 **判决：生产日志里"API 报 200"是**唯一可见信息**，而 OpenList 的 `fs/mkdir` 在
`code:200` 的**响应体里通常还带 `message`/`data`**（成功=空对象，异常=错误描述）。
生产把 body 丢掉，等于**把唯一的诊断线索当场销毁** ⇒ 「200 的成因」在现有日志里
**信息论上不可追** —— 不是"没人查"，是"查不了"。

**⇒ 这解释了为什么 §0 把 ③ 反复列为"仍待回答"却始终无进展**：缺的不是分析，是**原始数据**。

**⚠️ 与 §12.14.8 的口径矛盾并非"两个后端不一致"那么简单**：
API（`fs/mkdir`，走 OpenList 自身驱动）报 200、rclone/WebDAV（MKCOL）报 409，
**在"目标目录不可写"这一点的判断上，两者本就该一致**。二者分叉只有两种可能：
① API 侧把错误吞了（返回 200 但内部失败，body 里应有痕迹）；
② API 侧真建了，但**落在缓存/临时层未持久化**（这正是历史上"假成功重传"同源形态）。
**区分这两者，只看 `HTTP_CODE` 永远做不到，必须看 body + 一次真值直读。**

**⇒ 决定：不新增诊断脚本**（`diag_write_probe.sh` W2 已具备"打响应体"的能力，
缺的只是**在故障复现当轮对生产同一条路径**跑一次）。正确动作是在**下一次 L2 故障复现的
主轮**上，从**产物里的 fix.log** 取那两行 `OpenList API mkdir (短哈希) 响应` 的**上文**——
若当时已按本次结论改为**同时打 body**，则真因当场可判。

**⇒ 最小动作（因 ③ 已判"不做"，本例暂缓）**：把 `mkdir_http=$(echo "$mkdir_resp" | tail -n 1)`
改为**同时保留 body 前 N 字节**进日志（与 `diag_write_probe.sh` W2 同口径）。
**这是纯增量观测**：不改任何控制流、不改判据、不影响性能 ⇒ 风险≈0，且是解开 ③ 的**唯一**前提。

**⚠️ 方法论（第 9 次同类，"先确认可观测性，再谈追因"）**：
推进"查成因"之前，先问**"现有日志能不能承载这个成因"**。本次答案是**不能** ——
若直接开始分析"为什么 200"，会在**信息不完整的日志**上反复空转（§0 里 ③ 被列为"仍待回答"
多轮，正是这个形态）。⇒ 规则：**凡"某某成因未知"，先核对"观测点是否记录了成因所需字段"，
缺则先补观测（最小、纯增量），再谈分析。**

#### 12.14.11 ★★★★ 兜底落点真写入实测：**「建得出且写得进」** ⇒ 生产失败是**健康窗口问题**，非落点缺陷
（2026-09-18，run `35334119443`，sha `1b1bc4c`，success；诊断与主轮错开）

**为什么做（回归用户目标"文件同步失败"）**：此前所有诊断只判「目录能不能建」，
但用户要的是**文件能不能落盘**。「建得出目录」≠「写得进文件」。本次在**真实兜底落点**
`dest_path/<hash8>`（即生产失败文件实际走的那条路）上**写一个真文件 + 全路径直读**，
一次性判死"可修 bug vs 后端无解"这个二选一。

**实验（新增 E5 组，接入 `diag_depth_probe.sh`，复用 `diag_l2_n=d`）**：

| 观测 | 实测 | 判读 |
|---|---|---|
| E0 dest_path(`5/`) | rc=0 · lsd 存在=1 | 健康窗口锚点正常 |
| E1a 历史失败名 `5/5058f1af` | rc=0 · lsd 存在=1 | **能建**（历史名不是问题） |
| E1b 同深度全新名 ×3 | 3/3 真成功 | 窗口健康 |
| **E5 兜底落点真写入** | **copyto rc=0 · 409=0 · 全路径 stat=1** | ★ **建得出且写得进** |
| E3 窗口锚点 | 首测即成功 | 窗口稳定（非漂移） |
| E4 对照（E1b 目录写入） | copyto rc=0 · stat=1 | 一致 |

🔒 **判决：兜底落点在健康窗口下「建得出**且**写得进」** ⇒
**生产故障轮里那批失败文件，其兜底落点本身没有缺陷** —— 落点、目录名、写入通路**全都正常**。
⇒ 生产失败**只发生在"坏窗口"**（§12.14.6 那轮），是**后端当轮的窗口问题**，
**不是代码缺陷、也不是落点选择错误**。

**⇒ 这终结了 §0 悬置的"可修 bug vs 后端无解"二选一：答案是**后者性质的窗口问题**，
但**并非无解** —— 因为它会**自愈**（本轮即处于健康窗口，同一条落点完全可写）。

**⇒ 对用户目标的直接含义**：
- ✅ **文件同步能力本身没问题**：健康窗口下 100% 成功、兜底落点完全可写。
- ⚠️ **剩余风险 = 后端当轮窗口**：坏窗口期间整轮零产出，**代码无法挽救**（落点没错、
  方法没错、判据也已修），**只能等窗口恢复**。
- ⇒ **正确策略不是继续改代码，而是"坏窗口期的自我保护"**：
  即 §F5/F6 那类**熔断**（识别坏窗口 → 及早收手 → 不在坏窗口里空烧预算 → 下轮重试）。
  这与 §A 对「批次性假成功」的裁定**同一逻辑**：查清触发条件也不改变行动。

**⚠️ 方法论（第 10 次同类，正面样本）**：用户指出"越走越偏"后，**回到目标的原始判据**
（"文件能不能落盘"而非"目录能不能建"），只做**一个**能判死二选一的实验，
3 分钟拿到决定性结论。⇒ 规则：**诊断前先问"这个实验的结果会不会改变我的行动"**；
不会改变行动的追因（如 API 200 的成因）**不做**。

#### 12.14.12 ★★★★ 快测复核（fix-check）：**成功 1/5**，且失败项的真因是**调用口径差异，非生产 bug**
（2026-09-18，run `35336292127`，sha `4f66ee2`，success；12 分钟）

**为什么做**：§12.14.11 的 E5 只证明"落点可写"，用户要的是"**整个修复流程能不能真修好文件**"。
`openlist-fix-check.yml` 就是干这个的（真写目标端 + 重启真值复核），比主轮快得多。

**结果**：`VERDICT|fixed` **1** · `failed` **4** · `not_found` **0**（退出码 1）。

**★ 成功那 1 个的完整链路（证明兜底能真正救回文件）**：
```
mkdir 409(原目录 kate-bloom) → API 200 → 复核不存在（假成功，§12.14.8 的修复生效）
  → base64URL 也 409 → API 200 → 复核不存在（同上）
🔀 目录级兜底: 整条目录折叠为短哈希 2042c299
   ✅ 短哈希目录已确认存在 · ✅ 短哈希目录可写，4 种方法将在此目录执行
✅ VERDICT|fixed|方法1·原名直传 → 真值复核通过（重启后仍可见）
```
⇒ **兜底链路端到端可用**：建目录 → 预检 → 落文件 → 重启仍可见。

**★ 4 个失败项的真因（代码级核对，非推断）**：日志显示它们的折叠源被当成 `"."`
（`目标端根目录折叠为短哈希目录 5058f1af`）⇒ 落点错误。

| 文件 | 叶子单元 | 传入 `leaf_rel` | `file_dir_rel` | 折叠源 | 结果 |
|---|---|---|---|---|---|
| kate-bloom | `openlist:wopan175/5`（任务根） | `1024j-…/kate-bloom/xxx.mp4`（**含目录**） | `1024j-…/kate-bloom` | `2042c299` | ✅ |
| rolakiki 等 4 个 | `…/5/1024j-…/rolakiki`（**深一层**） | `xxx.mp4`（**仅文件名**） | `"."` | `5058f1af` | ❌ |

**根因（已核对到行，推翻"生产 bug"的初判）**：
- `fix_check.sh:279-288` 的**叶子单元设计**会把 `leaf_rel` 剥成"叶子单元内相对路径"；
  当叶子单元**就是文件所在目录**时，`leaf_rel` 只剩文件名 ⇒
  `file_fix.sh:1418 dirname(failed_file_rel)` = `"."` ⇒ 走**根目录折叠分支**（`_hash_src="."`）。
- **生产不会这样**：`file_fix_pipeline.sh:709` 的缺失清单来自
  `rclone lsf "$source_path" -R --files-only`（**任务根相对、含完整目录**），
  故 `failed_dir_rel` **必然带目录**（除真正的根目录文件）。
  ⇒ `file_dir_rel="."` 在生产里**只对应"文件真在任务根"**这一合法情形
  （该路径是**有意设计且有测试覆盖**的特性：`test_hash_dir_fallback.sh` 场景 5，5a–5k）。
- ⇒ 🔒 **判决：这 4 个失败是 fix-check 调用口径的产物，不是生产 bug。**
  `openlist-fix-check.yml:50` 的 `subdir` 说明也印证该口径（"任务根下的子路径"）。

**⇒ 对用户目标的含义（未被推翻的部分）**：
- ✅ **修复流程本身能救回文件**（1/5 端到端实证：折叠 → 落盘 → 重启仍在）。
- ⚠️ **但这 1/5 不足以证明"生产成功率"**：fix-check 的口径（叶子单元 + 重置黑名单）
  与生产不同，**4 个 failed 不能直接读成"生产也失败"**。
- ⇒ **要测生产真实成功率，仍须看主轮**（`VERDICT` 式的定点验证会系统性高估/低估）。

**⚠️ 方法论（第 11 次同类，"别把工具口径当成被测对象"）**：
我看到 `5058f1af`/`根目录折叠` 时差点直接判成"生产兜底算错折叠源"的 bug；
**核对到行**才发现是 fix-check 的叶子单元口径所致。
⇒ 规则：**凡"某工具跑出的异常"，先核对"该工具的输入口径是否等价于生产"**，
再决定是否归因为生产缺陷。



---

- 2026-09-18（+08）· **三次诊断收敛：目录可写性预检是误判源 → L2 假成功定性为「路径局部」**
  （§12.14.1–12.14.3）。
  - 触发：接续前一轮"真瓶颈是目录可写性预检"的结论，用户批准先挖代码、再做定向诊断。
  - **三轮递进**（每轮都只花 3–4 分钟，靠独立 diag 载体，不占主轮）：
    - `35289924584`（W1–W4）: 探针写失败**确实**复现同一条 409 路径（W1 copyto 409 +
      mkParentDir=1）；"后端不可写"被 W4(rc=0) 否定；"绕隐式 mkParentDir"被 W3 否定。
    - `35291770701`（D1–D3）: 名字相关被否定（原样名隔离处可建）；失败**定位到 L2 层**；
      L2 下兄弟目录**全部 409** ⇒ 故障污染整棵子树。
    - `35294896071` / `35295178274`（P1 普遍性矩阵）: `5/` 下新建 **4 个互不相关的顶层目录
      全部真成功**（两轮独立）⇒ **「通用缺陷」被否定**。
    - `35295422027` 对照复测故障路径: 与 `35291770701` **逐字同构**（可复现，非偶然）。
  - 🔒 **判决**：**非通用缺陷**——同一个后端、同一个 `5/`、几乎同一时刻，`5/ol2p_*` 能建,
    `5/<L2 段>/*` 一律 409 ⇒ 失败**是该 L2 子树的局部状态**，不是挂载级、也不是名字相关。
  - **修法方向随之修正**：不换驱动/不换建目录通路（同挂载新建目录是好的），
    而是**让兜底目录跳到故障子树之外**——现有短哈希兜底目录正好也落在同一棵坏子树里，
    故同样 409，这就是生产里"换成短哈希目录 → 同样 409 → 兜底终止"的直接原因。
    > ❌ **本句已证伪（§12.14.9，2026-09-18）**："现有短哈希兜底目录正好也落在同一棵坏子树里"
    > 是**未核算的推断** —— 落点恒在 `dest_path/<md5(dir_rel)[0:8]>`（直属、平级），
    > **从不进入故障子树内部**。⇒ 该"修法方向"**撤销**。保留原文以记录推理轨迹。
  - **顺带修掉两个诊断载体缺陷**（都在实跑中暴露，属"工具坏了导致白跑"）：
    ① `workflow_dispatch` 输入数达 27 > GitHub 硬上限 25 ⇒ 派发 422，"加开关却派不出去"；
    ② step 级全局 `DIAG_REPORT` 被所有专项继承，把专项报告串进 `report.txt`，
    收尾 `tail <专项>_report.txt` 取不到 ⇒ **报告拿不到 = 该轮白跑**。两者均已修并加注释锁死。
  - **红线记账**：推 `openlist-diag.yml` 时 GitHub 拉起一个 `event=push` 的 openlist-diag
    空 run（`35293663751`，0 jobs / 1ms failure / 无日志，**从未真正执行**，未碰容器与网盘）。
    但 `concurrency: openlist-diag` 跨 run 生效，故仍按红线处理：**取消了主轮 `35293089155`
    （用户指示），确认无活动 run 后才派 diag**。
  - **方法论第 3 次同类教训**：`35291770701` 已足以"看起来"下结论，补做的普遍性矩阵
    只花 3 分钟，却把**决定修法方向**的分叉（通用缺陷 vs 个体问题）一次判死。
    ⇒ **先验证普遍性，再定性**，定为后续诊断的固定动作。
  - **同步链**：主轮 `35293089155` 已按用户指示取消（让位诊断）；此后诊断均**单独串行**，
    未与任何主轮重叠。

- 2026-09-18（+08）· **回答用户「文件无法同步的问题解决了吗」：没有完全解决，且真因刚被挖到**
  （§12.14.4，run `35296822507`）。
  - 触发：用户拒绝照抄文档，要求**真实数据**。遂跑一轮全量常规探针取当下真值。
  - **客观事实**：文件**在同步，但不稳定** —— `35168662780` success 实测**传输 127.6 GiB**、
    247 个新落盘、成功率 100%；`35239780581` 则几乎零产出（1 个同步对就失败）。
    ⇒ 不是"同步不了"，但撞上目录层/预检层故障时**整轮零产出**。
  - **★★ 新发现（本轮实测）**：
    ① `重启后写入: +0s=FAIL() +10s=OK +30s=OK +60s=OK`，`列表可见性: 首次非空=10s`
       ⇒ 后端重启约 10s 才就绪。**但核对生产日志后确认这不是生产失败的主因**：
       生产路径已盲等 60s+10s=**70s**（openlist_driver.sh:663-667/713），等满后探针**仍消失**
       ⇒ 写入本身没落盘（当日 `copyto` 就是 409）——仍是 §12.14.1 的 409 写路径问题。
       该时序项的**唯一价值是提醒不要把 `OPENLIST_DRIVER_READY_WAIT` 调短**。
       （⚠️ 本条第一版写成"生产失败是因为重启后立刻读"，**已核对生产日志后自行推翻**。）
    ② `字符集阶梯` 5 档全 OK，**唯一 FAIL(405) 的是最贴近生产真实文件名的"混合"档** ——
        首版记为"新线索、未定性"；**复核 `diag_backend.sh` 后修正**：前 5 档是"字符集模式"
        （重复同一字符），第 6 档是**那个真实失败名逐字复刻** ⇒ 不是等长字符集对照，
        而是 **§11.8 已定性的「特定目录名本身」再次出现** ⇒ **不构成新维度，该项定性撤销**。
     ③ 名长/父目录名长/路径深度/覆盖写/子目录写/大文件尺寸 **全部通过** ⇒ 这些维度排除。
    ④ 并发 423 是**父目录 mkdir 竞争**（已存在目录并发 OK、新目录 retries3 OK），重试可解。
    ⑤ 吞吐 4 流 2.06 MiB/s、出口基准 22.71 MiB/s ⇒ 瓶颈确在 wopan 侧（与 §0 定案一致）。
    ⑥ 生产兜底路径印证修复方向：`🔀 目录级兜底: 根目录折叠为短哈希目录 5058f1af` 之后
       **仍 409**（三次 `mkdir+hash` 全败）⇒ 兜底仍落在同一棵坏子树里。
       > ❌ **该推断已被 §12.14.9 证伪**：落点恒在 `dest_path` 直属，与故障子目录**平级**。
  - **⚠️ 仍需修的语义隐患**：`note="已重启确认"` 只表示"重启过"、**不等于**"结论可信"
    （一旦"重启后列表未就绪"造成读失败，会把"读不到"误标成"确认不可写"），
    而后端熔断计数只认这个词，会据此误判后端死。
  - **修法优先级（按证据）**：① 把"目录建不出"与"目录不可写"解耦（最高，§12.14.1）；
    ② 兜底目录跳出故障子树（§12.14.3）；③ 不要把 `OPENLIST_DRIVER_READY_WAIT` 调短；
    ④ 混合字符集 405 先诊断再定（不在本轮）。
  - ⚠️ **本条 ①②④ 的后续处置（后补，勿只看本条目）**：① 已落地（`296a3c3`）；
    ② 取证后**暂停**（§12.14.5，故障未复现）；④ **已撤回**（"混合档"是真实失败名
    逐字复刻，属 §11.8 已定性的"特定目录名本身"，非新维度）。

- 2026-09-18（+08）· **修法 ①「建不出目录 ≠ 目录不可写」解耦已落地**（改动 1+1c+3+4）。
  - **改动 1（`_fix_probe_dir_writable` 三态化）**：预检结论由二态 `0/1` 改为
    `ok` / `exists_but_readonly` / `unwritable`，经全局 `_DIR_PROBE_STATE` 透出；
    探针失败时补一次**只读** `lsd` 复核存在性。缓存格式随之变为
    `"<三态>|<可信度标注>"`，读取处兼容旧 `0/1`（`1→ok`；`0→exists_but_readonly`，
    取保守侧：宁可白试一次方法，不可误判"目录不存在"而放弃原路径）。
    开关 `OPENLIST_DIR_PROBE_DECOUPLE`（默认 1；`0` 回退旧二态）。
  - **改动 1c（两个调用方）**：
    · `try_fix_failed_file` Step 2 —— `exists_but_readonly` 时**放行 4 种方法**
      （不再跳过），让方法用落盘结果定论；
    · `_fix_switch_to_hash_dir` 的短哈希目录预检 —— 同态放行，避免把原目录的
      误判复制到兜底目录上；
    · **新增入口侧兜底**：Step 1「目录建不出来」的死路原先直接 return，
      现在也放行一次短哈希兜底（`_fix_switch_to_hash_dir` + 下载 + 跑方法）。
    · `file_fix_pipeline.sh` 的批量折叠调用点 —— `exists_but_readonly` 视为"不动它"
      （折叠不可逆，保守处理）。
  - **改动 3（note 语义分档）**：`已重启确认` 拆为 `-可写` / `-写入失败` / `-读取失败`；
    **后端熔断只认 `-写入失败`**（"读取失败"可能只是重启后列表未就绪；
    `exists_but_readonly` 更不该计入——它是"建不出目录"而非"后端不可用"）。
  - **改动 4**：`openlist_driver.sh` 的 `_wait_driver_ready` 头注加警告——
    不要调短该等待（run `35296822507` 实测「重启后立即写 FAIL → +10s OK」）。
  - **顺带修掉一个真实缺陷（测试逼出来的）**：入口侧兜底在 Step 3 之前跑，
    导致 `_try_fix_methods_round` 读不到 `src_expect_bytes` / `file_md5` ——
    `set -u` 下**整个修复流程当场中止**（测试首次实跑即复现），`set +u` 下则方法 2/3/4
    全空跑。修法：把下载段抽成 `_fix_download_source`，两条路径统一调用。
  - **回归**：`test_hash_dir_fallback.sh` 62 PASS / 0 FAIL（含**新增场景15**：
    探针写不进但目录存在 ⇒ 放行 4 种方法，本次改动的主回归）；关联套件
    `test_bulk_hash_fold` 38 / `test_fix_pipeline_optimizations` 38 /
    `test_mkdir_409_semantics` 19 / `test_backend_dead_round` 17 /
    `test_batch_consolidate` 67 / `test_fix_check` 32 全绿；openlist 全域套件本机全绿
    （仅剩 AGENTS.md 记录的 2 项环境假红）。
  - **未做（按 §12.14.4 优先级）**：② 兜底目录跳出故障子树（当时按 §12.14.3 判为根因，
    理由写作"现有短哈希目录仍落在同一棵坏子树里"）；④ 混合字符集 405。
    ⚠️ **后补（2026-09-18）**：② 取证后**暂停**（§12.14.5）；④ **已撤回**（非新维度）。
    ⚠️ **终补（2026-09-18，§12.14.9）**：② 的上述理由**是错误命题** —— 兜底落点
    `dest_path/<md5(dir_rel)[0:8]>` 恒在 `dest_path` 直属，与故障子目录**平级**，本就已"跳出"。
    ⇒ **② 正式撤销，不实施**（不是暂停）。
    至此 §12.14.4 列的"未做"两项均已收口。
    ⚠️ **再后补（§12.14.6 生产验证）**：本条的"改动 1/1c（三态解耦）"经主轮
    `35308273431` 实测**一次都没触发** ⇒ **对当前生产故障无效**（行为正确，但治不了
    「兜底目录本身建不出」）。**本条目所述改动的优先级作废**，主攻方向改为
    「查清 `API mkdir HTTP 200 但目录不落盘`」。

- 2026-09-18（+08）· **★★★ 三态解耦首轮生产验证：修复未生效，故障恰是它治不了的那类**
  （§12.14.6，主轮 `35308273431`，sha `60740b01`，success）。
  - **触发**：核验发现 `296a3c3`（= §0 原"最高优先级修法"）**从未进过任何生产轮**
    （近 4 轮 sha 经 `git merge-base --is-ancestor` 逐一对核，均不含）⇒ 补跑一轮真实验证。
  - **整轮结果**：`修复管线: 尝试 2 · 失败 2 · 成功率 0%（新落盘 0/10）`；
    `本轮传输 2 个文件，目标视图 225`。
  - 🔒 **决定性观测**：三态分支**全程 0 次触发**（`判 exists_but_readonly`=0、
    `放行…定论`=0），而 `兜底终止`=**2 次** ⇒ 仍走旧路径。
  - **根因（定位到行）**：三态判定依赖只读 `lsd` 复核（[file_fix.sh:986-987](file:///workspace/.github/scripts/openlist/file_fix.sh#L986-L987)）；
    而本例日志显示 `OpenList API mkdir (短哈希) 响应: HTTP_CODE:200` **但 `lsd` 读不到**
    ⇒ 正是 §12.14.2 的 **`mkdir` 假成功** ⇒ `lsd` 失败 ⇒ 判 `unwritable` ⇒ 不放行。
  - ⇒ **三态逻辑没写错，是它的前提在本例不成立**：`exists_but_readonly` 语义是
    "目录**存在**但写不进"，本例是"目录**不存在**且建不出" ⇒ 按定义**就该判 `unwritable`**。
    **它治的是"建得出但写不进"，而生产病是"兜底目录本身也建不出"。**
  - 🔒 **判决与重排**：§0「最高优先级修法」**降级**（保留改动，但不再是主攻）；
    **新主攻 = 查清「`API mkdir` 200 但不落盘」** —— 它是所有"换目录"兜底的共同前提。
  - **★★ 待解矛盾（决定修法方向，未解前勿动）**：§12.14.3 测 `5/ol2p_*` 能建（判"非通用缺陷"），
    §12.14.6 测 `5/<深层>/5058f1af`（**全新名字**）建不出 ⇒ 差异疑在**层级/深度**，
    或 §12.14.3 当轮恰逢健康窗口。⇒ 需一轮**按深度分层**的同构实验来分离这两个变量。
  - **旁证**：`重启后目标视图 225→225 · 无假成功污染`（真值复核链路正常）；
    `raw 计数持续为 0 … 禁用落盘即时校验`（与 §12.14.4 "重启后 ~10s 就绪"吻合）；
    直传路径仍工作 ⇒ §C「后端健康时能同步」的限定仍成立。
  - **方法论（第 5 次同类）**：本次的教训是「**改完必须确认它真的在生产里跑过**」——
    `296a3c3` 合入近 6 小时、被文档标为"最高优先级"，却**从未被执行过一次**；
    若非主动核对 `headSha`，这个"已修复"的假象会一直留着。
    ⇒ 规则：**凡声称"某修法已生效"，必须给出含该提交 sha 的生产轮号与命中计数。**

- 2026-09-18（+08）· **✅ 修法 ① 落地：API mkdir 报 200 必须复核存在性**（commit `4ebb39e`）。
  - **触发**：§12.14.8 暴露"API 报 200 / WebDAV 报 409"口径矛盾；用户批准"先出方案再执行"后下令执行。
  - **根因（定位到 4 行）**：`file_fix.sh` 的 4 处 API-200 分支**只看 HTTP 码**就置 ok，
    而同函数其它三条分支都做了读操作复核 ⇒ 唯独 API-200 是口子
    （短哈希 / Step 1 原目录 / base64URL / 批量折叠 `_bulk_fold_ensure_dir`）。
  - **生产实锤**：`06:10:10 ✅ 短哈希目录创建成功 (API)` → `06:13:26 ❌ …兜底终止`（白等 ~2 分钟）。
  - **改动**：4 处统一接 `_fix_dir_exists_or_conflict`（复用既有判据，无新概念/依赖/开关）；
    复核不过不置 ok，如实记「API 报 200 但不存在（假成功）」。
  - ⚠️ **诚实限定**：**不解决同步失败本身**（坏子树建不出就是建不出），
    修的是**判据可信度** + 为后续"跳出故障子树"提供可信判据。
  - **回归**：`test_mkdir_409_semantics.sh` 新增 **D7 组**。⚠️ **D7 初版是假过**
    （"该行后 N 行内出现复核"会串到后面 409 分支的复核上）—— 已改为**按缩进切分支块**，
    实测旧代码 D7a=3/3、D7b=1/1 **全红**，新代码全绿 ⇒ 是真守卫。
    **方法论（第 8 次）**：**新增守卫后必须拿旧代码验它会红**，否则可能只是装饰。
  - **验证**：`bash -n` 通过；本机五套 23/62/38/32/67 全绿；CI `tests.yml` run `35324743387`。

- 2026-09-18（+08）· **★★★★ 深度分层实验：矛盾消解，三个猜想一次被否、坐实「路径局部」**
  （§12.14.8，run `35322561888`，sha `56fcb17`，success；用户指示"取消主轮，别浪费时间等待"）。
  - **触发**：用户批准"设计深度分层实验，尽量缩短时间"；主轮 `35310637379` 卡在 in_progress
    2h26m 无进展 ⇒ 用户指示取消主轮（已 `gh run cancel`，conclusion=cancelled），立即派发本实验。
  - **四个对照（同轮、同容器、同后端）**：E0 dest_path 健康；**E1a 历史名 `5058f1af` 能建**；
    E1b 同深度全新名 3/3 能建；**E2-1/E2-2 深度 +1 也能建**；
    **E2-3 进生产 L2 子树 `5/1024j-视频-pornhub-channel/` 建全新名 → `mkdir rc=1 · http=409`**；
    E3 复测历史名仍成功（窗口稳定）。
  - 🔒 **判决**：① 名字/残留 ❌ ② 健康窗口 ❌ ③ 深度/层级 ❌ ④ **路径局部（该 L2 子树坏）✅**。
    ⇒ §12.14.3 与 §12.14.6 **不矛盾**（同深度同形状，差的是当轮窗口），§0 的"待解矛盾"**消解**。
  - 🔑 **新暴露的隐患**：同一条坏子树，**API 报 `HTTP_CODE:200`（假成功）** vs
    **WebDAV/rclone `mkdir` 报 `409 Conflict`（硬失败）** ⇒ 口径互相矛盾。
    这解释了 §12.14.6 三态解耦为何 0 次触发（它依赖 rclone 口径，生产走 API 兜底路）。
    **修法优先级据此重排**：最高 = 统一 mkdir 成败口径（API 路径也过真值复核）。
  - **修法含义**：跳出故障子树**方向坐实**（本轮提供了"故障在场"的对照）；
    §12.14.7 推测的"名字带随机盐"**作废**（变量①被否定）；现有落点 `dest_path/5058f1af`
    本身可写，问题在判据口径。
  - **方法论（第 7 次；正面样本）**：§12.14.7 先把变量纠正为"同深度同形状"，
    才使四个对照**互斥完备** ⇒ 一次运行同时否定三个猜想。
    ⇒ 规则：「**待解矛盾先拆成互斥且完备的候选变量，再设计对照**」。
  - **遗留**：E9 清理 5 成功 / 2 失败（失败者即坏子树内那两个自建目录，符合预期，
    因该子树本身不可写）；坏子树**成因**仍未查（列为独立追因课题，不阻塞修法）。

- 2026-09-18（+08）· **★★★ 派发前纠变量：`5058f1af` 与 `ol2p_*` 同深度同形状 —— "深度"不是变量**
  （§12.14.7；`diag_depth_probe.sh` + workflow 接线已落地 commit `2021b9c`，**实验尚未派发**）。
  - **触发**：用户批准"设计深度分层实验，尽量缩短时间"。动手设计前先核对**落点到底在哪一层**。
  - 🔒 **代码级实锤**：`file_dir_rel="."` ⇒ `_hash_src="."` ⇒ `md5(".")[0:8]=5058f1af`
    （本机实测），`hash_dst_dir="${dest_path}/5058f1af"` = **`openlist:wopan175/5/5058f1af`**
    ⇒ 与 §12.14.3 的 `5/ol2p_*` **同深度、同形状、同为全新名字**。
    §12.14.6 原文"`5/<深层>/5058f1af`"的"深层"表述**错误**（生产日志实为根目录折叠，
    已在该节加勘误指引）。⇒ **"深度"不是变量**，候选只剩①名字内容/是否被建过、②健康窗口。
  - **落地同构分离专项**：E1 同深度同形状对照（历史名 vs 3 个同深度全新名）、
    E2 深度阶梯（沿生产真实路径逐级下探）、E3 **同轮内复测**历史名（窗口漂移自证，
    不靠跨轮对照 —— 相对 §12.14.3 的关键改进）、E4 已存在目录可写性；
    假成功复核一律**带等待 + 直读 stat**（AGENTS.md 两条教训的落实）。
  - **验证**：`bash -n` / YAML 解析 / inputs 计数（**仍 25**，复用 `diag_l2_n=d`）/
    rclone stub 跑 4 个判决分支（名字残留、健康窗口、同深度全坏、子树整体坏）**全命中**。
  - ⚠️ **未派发原因**：`AGENTS.md` 规定 diag 必须与主轮错开（同一网盘账号互相干扰），
    主轮 `35310637379` 仍在跑。**待空闲即派**。
  - **方法论（第 6 次同类，但这次在派发前拦下）**：§12.14.6 的"深度"归因若不自纠，
    就会设计出一轮**变量数错**的实验（把"同深度同形状"误当"不同深度"）。
    ⇒ 「**设计实验前先核对待测变量在代码/日志里的真实取值**」，成本为零、收益是避免白跑。

- 2026-09-18（+08）· **修法 ② 的可行性取证：前提不成立，故障本轮未复现 —— 暂停改动**
  （§12.14.5，run `35306329978`）。
  - 触发：修法 ② 的全部依据是「跳出去就能写」这个**推论**，从未实测。按 §12.14.3 的教训
    （先验证普遍性），在动还原链之前先补取证。
  - **新增 `diag_escape_probe.sh`**（E1 祖先层阶梯 / E2 同层对照核心判决项 / E3 跳出层写文件 /
    E4 还原路径可行性 / E9 清理），接入 `openlist-diag.yml`：启动开关**复用 `diag_l2_n`**
    （填 `e`/`escape`），因为 `workflow_dispatch` 已占满 **25 个 input 硬上限**，新增即 HTTP 422。
  - 🔒 **判决一**：E1/E2 双向都**可写**（`dest_path` 自身与挂载根均 `rc=0 · lsd 存在=1`），
    连"不跳出"的落点都建得出来 ⇒ **生产故障形态（L2 坏）本轮未复现** ⇒ 对照无判别力，
    **不支持"必须跳出"**。⇒ **修法② 暂停**，本探针转为**故障复现时的取证工具**。
  - ⚠️ **判决二（首版已自己推翻）**：E4 首次报「跨层 move rc=0 但目标位不可见 ⇒ 静默丢文件」。
    **该结论作废** —— 复核紧接 move、无任何等待，而 §12.14.4 已证实列表可见性要 ~10s，
    该写法**必然把"未刷新"误读成"没到位"**（判据缺陷，非被测现象）。
  - **同轮修正 + 验证**：E4 改为「立即读 → 等 `E4_WAIT`(15s) 再读」+ 源侧判据，
    仍报不可见 ⇒ 再补**目标全路径 `lsjson` stat** 直读判据（不经目录列举），
    判读五态，已用 rclone 桩逐一验证。
  - 🔒 **判决二（最终，连跑两轮 `35307143381` / `35307446562`）**：带等待 + stat 后测得
    **目标全路径 stat 在（Size=18，与载荷字节数一致）**，而列列举 15s 后仍不可见
    ⇒ **文件其实就在目标位，`move` 成功且未丢文件**。**首版"静默丢文件"判决彻底作废**；
    `restore_info.jq` 的 `hash_dir` 分支没有本轮证据指出的缺陷，**不改**。
  - **诊断载体整改（2 条，都是"可观测性缺陷误导修法"）**：
    ① 可见性复核不带等待 ⇒ 把"未刷新"读成"没到位"；
    ② 只靠目录列列举 ⇒ **即使加了等待也翻不了案**，必须改用全路径 `lsjson` 直读。
    ⇒ 教训: **凡"以列表为准"的判据，都要用一条独立的直读判据交叉验证**。

- 2026-09-18（+08）· **④ 混合字符集 405 线索复核：判定为与 §11.8 同源，撤销定性**
  （§12.14.4 核心发现二）。
  - 触发：§12.14.4 把"字符集阶梯唯一 FAIL 的混合档"记为**新线索、未定性**，
    并据此把它列进"未做"清单。复核该实验的**实现**后发现判读有误。
  - **复核 `diag_backend.sh` 的 `_cs_try` 调用**：前 5 档是**字符集模式**
    （`测`×40 / `测 `×20 / `（测）`×10 / `(测)`×10 / `[测]`×10，都是"重复同一字符"），
    第 6 档却是**那个真实失败名逐字复刻**（`(CSP6) [流石堂 …] …`）。
    ⇒ 这**不是等长的字符集对照**——第 6 档与其它档**同时变了"字符集"和"具体串"两个变量**。
  - 🔒 **判决**：第 6 档的 FAIL 就是 **§11.8 已定性的「特定目录名本身写入失败」**
    （§11.8 已证：该名字**搬到全新位置仍 405**，可脱离原目录复现）。
    ⇒ **不构成新的"字符集"维度，④ 撤销**；F10（405 fail-fast）优先级**不因此上调**。
  - **正确做法（留待需要时）**：若真要分离"字符集 vs 具体串"，须做**等长、同结构、
    只换字符**的对照（如把该名里的假名换成等长汉字），**而非拿真名当一档**。
  - **教训**: 实验设计里**变量分离不彻底**，会让"已知现象"伪装成"新线索"，
    进而催生一个**不需要做的诊断/修法**。⇒ 判读前先复核**这一档与其它档到底差几个变量**。
  - **方法论第 4 次同类**：本轮**零代码改动**，但拦住了一次**基于未验证前提的重构**
    ⇒ 教训升级为「**先验证前提，再动手**」（此前是"先验证普遍性，再定性"）。
  - **如实记录载体局限**：E2 从 dest_path **向上**跳，而 §12.14.3 的坏层在其**下一层**，
    方向相反 ⇒ 即使"跳出可写"也不足以证明"跳出有用"，可用判决必须等故障复现。

- 2026-09-18（+08）· **★★★★ 修法 ②「兜底目录跳出故障子树」取证证伪：前提不成立，正式撤销**
  （§12.14.9；零代码改动）。
  - 触发：用户下指令「执行」修法 ②。按"先补，再出改动方案"的既定模式，动手前先核算
    **兜底落点到底在不在故障子树里**（此前 6 处文档都断言"在"，均源自 §12.14.5 那次推断，
    从未核算）。
  - **核算（`file_fix.sh` 真源）**：`_hash_dir_rel_for() { printf '%s' "$1" | md5sum | cut -c1-8; }`
    + `hash_dst_dir="${dest_path}/${HASH_DIR_REL}"`（第 1293 行）。
    ⇒ 落点 = `dest_path` 下**一个 8 字符扁平目录名**，**只与 `md5(file_dir_rel)` 有关、
    与 `file_dir_rel` 的深度无关**。
  - **实证两个极端**：
    ① `file_dir_rel='1024j-视频-pornhub-channel/kate-bloom'` → `md5=2042c299` → `5/2042c299`
    （故障 L2 `5/1024j-...` 的**兄弟**）；
    ② `file_dir_rel='.'` → `md5=5058f1af` → `5/5058f1af`（同样平级）。
    ⇒ **两种输入都落在 `dest_path` 直属，从不落进故障子树内。**
  - 🔒 **判决**：修法 ② 的整个前提（"现有兜底落在同一棵坏子树里，故同样 409"）是**错误命题**。
    兜底**本来就是"跳出故障子树"的**（by design）。⇒ **决定：不实施修法 ②**
    （不是"暂停"）——否则要改还原链（`restore_info.jq` 的 `rclone move` + `--filter-from`），
    **零收益、纯风险**。
  - **文档同步**：新增 §12.14.9 记录证伪；§0 把 ② 从"次高优先级"移入"已作废方向 ④"；
    §12.14.3 / §12.14.5 / §12.14.6 加勘误注；进度日志补本条 + 历史条目（第 2493/3048 行
    两处原始断言）加"已证伪"标记。**保留原文以记录推理轨迹**，判读一律以 §12.14.9 为准。
  - **教训**：**"推论"被反复转抄后会获得"事实"的外表** —— 同一句未核算的断言在文档里
    出现 6 次，就让一个不需要做的重构看起来像"根因修复"。⇒ 动手前**先核算该断言依赖的
    那一行代码**（此处只差一次 `md5sum`）。
  - **方法论第 8 次同类**：本轮**零代码改动**，拦住了一次**基于未验证前提的重构**
    （与上一条同型：先验证前提，再动手）。

- 2026-09-18（+08）· **★★★ 追因 ③ 卡在"可观测性"：生产日志把 API 响应体丢了**
  （§12.14.10；零代码改动，结论是"缺原始数据"而非"缺分析"）。
  - 触发：修法② 撤销后，按 §0 把 ③「查清 `API mkdir` 200 不落盘的成因」作为下一目标推进。
    先核对"手上的日志能不能承载这个成因"。
  - **代码级对照（三处）**：生产 `file_fix.sh` 短哈希分支（1338 行）与 Step1 分支（1502 行）
    都写 `mkdir_http=$(echo "$mkdir_resp" | tail -n 1)` ⇒ **只留 `HTTP_CODE:200`，body 丢弃**；
    而诊断 `diag_write_probe.sh` W2（163 行）**保留了 body**（`head -c 200`）。
  - 🔒 **判决**：OpenList `fs/mkdir` 的 200 响应体本可携带 `message`/`data`（真因线索），
    生产把它**当场销毁** ⇒ ③ 在现有日志里**信息论上不可追**。这解释了为何 ③
    被列为"仍待回答"多轮却零进展 —— **缺的不是分析，是原始数据**。
  - **口径矛盾的两种可能（本次澄清，此前混为一谈）**：API 200 vs WebDAV 409 的分叉
    只有 ①"API 吞了错误"或 ②"API 真建了但未持久化（缓存层）"两种；
    **仅凭 `HTTP_CODE` 永远无法区分，必须 body + 一次真值直读**。
  - **决定：不新增诊断脚本** —— `diag_write_probe.sh` W2 已具备打 body 的能力，
    真正缺的是"故障复现当轮把 body 记进生产日志"。⇒ 最小动作 = 生产 2 处 + pipeline 1 处
    补打 body 前 N 字节（纯增量、不动控制流、风险≈0）；**因 ③ 已判"不做"，此项暂缓**。
  - **方法论（第 9 次同类，"先确认可观测性，再谈追因"）**：推进"查成因"前先问
    **"现有日志能不能承载这个成因"**；缺字段则**先补观测**，否则会在信息不完整的日志上空转。

- 2026-09-18（+08）· **★★★★ 回归目标：兜底落点真写入实测 —— 落点无缺陷，失败是坏窗口**
  （§12.14.11，run `35334119443`，sha `1b1bc4c`，success；诊断与主轮错开）。
  - 触发：用户指出**"越走越偏"**（我一直在追"API 200 的成因"），并问"现在情况如何、接下来怎么做"。
    复盘承认：那是**后端行为**，查清也不改变行动（与 §A 对"批次性假成功"的裁定同逻辑）。
  - **回归目标的原始判据**：用户要的是**文件能不能落盘**，而此前所有诊断只判「目录能不能建」。
    ⇒ 设计**一个**能判死二选一的实验：在**真实兜底落点** `dest_path/<hash8>`
    （生产失败文件实际走的那条路）上「mkdir + **写一个真文件** + **全路径直读**」。
  - **实现**：给 `diag_depth_probe.sh` 补 **E5 组**（不新增脚本、不新增 input，复用 `diag_l2_n=d`）；
    新增回归 `test_depth_probe_e5.sh`（3 静态契约 + 3 行为分支，rclone 桩），本机 **6/6 全绿**；
    关联套件 409/hash_fallback/bulk_hash_fold 均 EXIT=0。commit `65d4f68`。
  - 🔒 **判决（决定性）**：`copyto rc=0 · 409=0 · 全路径 stat=1` ⇒
    **兜底落点「建得出**且**写得进」**。同轮 E1a 历史名能建、E1b 全新名 3/3 真成功、E3 窗口稳定。
    ⇒ **兜底落点、目录名、写入通路全都正常** ⇒ 生产失败**只发生在坏窗口**（§12.14.6 那轮），
    **是后端当轮窗口问题，不是代码缺陷、不是落点错误**；且**会自愈**。
  - **对用户目标的结论**：① 文件同步能力**本身没问题**（健康窗口 100%、落点完全可写）；
    ② 剩余风险 = **后端当轮窗口**（坏窗口整轮零产出，代码无法挽救）；
    ③ 正确策略转为**坏窗口期自我保护**（F5/F6 熔断：识别 → 及早收手 → 下轮重试），
    **不再继续改代码追因**。
  - **⚠️ 操作**：为让诊断与主轮错开，经用户同意**取消了刚起跑的 主轮 `35333778314`**
    （用户裁定"取消主轮，立即跑诊断"），诊断串行完成；之后可随时补跑主轮。
  - **方法论（第 10 次同类，正面样本）**：回到目标原始判据、只做**一个**能改变行动的实验，
    3 分钟拿到决定性结论。⇒ 规则：**诊断前先问"这个实验结果会不会改变我的行动"**；
    不会改变行动的追因（③ API 200 成因）**明确不做**。

- 2026-09-18（+08）· **★★★★ 快测（fix-check）：成功 1/5，端到端证明兜底能救回文件；失败项经核对非生产 bug**
  （§12.14.12，run `35336292127`，sha `4f66ee2`，success，12 分钟）。
  - 触发：用户问"要用主轮吗？要不快速测试一下？"—— 主轮数小时，而 `openlist-fix-check`
    12 分钟就能回答"**整个修复流程能不能真修好文件**"（真写目标端 + 重启真值复核）。
  - **结果**：`VERDICT|fixed` 1 · `failed` 4 · `not_found` 0。
  - ✅ **成功 1 个的完整链路**（端到端实证兜底可用）：原目录 409 → API 200 被**复核拦下**
    （§12.14.8 的修复**在生产同款管线里真实生效**）→ base64URL 同样被拦 → 折叠 `2042c299`
    → 预检可写 → 落盘 → **重启后仍可见**。
  - ⚠️ **4 个 failed 的初判被自己推翻**：我先是判"生产兜底把折叠源算成 `"."`，是 bug"。
    **核对到行**后推翻：`fix_check.sh:279-288` 的**叶子单元**把 `leaf_rel` 剥成叶子内相对路径，
    叶子=文件所在目录时只剩文件名 ⇒ `file_dir_rel="."` ⇒ 根目录折叠分支。
    **生产不会这样**：`file_fix_pipeline.sh:709` 缺失清单来自 `rclone lsf -R`
    （任务根相对、含完整目录）。⇒ 判决：**fix-check 口径产物，非生产 bug**。
  - **对用户目标**：① 修复流程**能**救回文件（1/5 端到端）；② 但这 1/5 **不等于生产成功率**
    （fix-check 是"重置黑名单 + 叶子单元"的能力上限口径）；③ **要测生产真实成功率仍须主轮**。
  - **方法论（第 11 次同类，"别把工具口径当成被测对象"）**：凡"某工具跑出的异常"，
    先核对"该工具输入口径是否等价于生产"，再决定是否归因为生产缺陷。
- 2026-09-19（+08）· **落地全部剩余优化（F22 + F10 + F17），除 cron 降频（用户指示）**
  （§4 · F10/F17/F22；单测见各自小节）。
  - **决策依据**：用户问"代码上已经无能为力了吗" —— 逐项审计后确认：**速度**（配额瓶颈）与
    **坏窗口本身**确实非代码可解；但**止损/省时**尚有两处实质缺口，正是 F22/F10/F17。
    用户指示「执行全部优化，除了 cron 降频」⇒ 落地这三项，**跳过 F18（cron，纯美观）与
    F13（僵尸检测，无复现样本）**。
  - **F22 写探针周期性重探**（§4 · F22）：`_backend_write_probe_invalidate`（driver）+
    `_write_reprobe_due`（engine，默认间隔 1800s）；接线在批次循环「刷新驱动 → 清缓存
    重探 → 批次预检」序列，**复用预检里已有那次探测**（不新增探测次数）。
    解决了「探针可写结论只在 t=0 有效、坏窗口沿用到轮末」的固有缺口。
  - **F10 405 快速失败**（§4 · F10）：`_batch_405_flood`（默认阈值 20，只数 405、
    **不数 409/423**）；接线在**批次 copy 之后、`_batch_consolidate` 之前**，触发即跳过
    本批巩固并复用 F5 中止出口。新增 `BATCH_ABORT_REASON` 区分「405 拒写」（目录级）
    与「后端写入全拒」（后端级），避免通知误报排障方向。
  - **F17 预览 listing 短超时**（§4 · F17）：预览专属超时 `OPENLIST_PREVIEW_LISTING_TIMEOUT`
    （240s，原 900s）；**同步改**失败快弃判据（固定 300s → 单次耗时 ≥ 预览超时），
    否则改短超时会因 300s 判据不再命中而把重试从 1 次涨到 3 次、净效果更差。
  - **测试**：新增 `test_write_reprobe_and_405.sh`（16 断言：阈值边界/自定义/关闭/脏值/
    409 反向锁死）；`test_batch_precheck_circuit_breaker.sh` 增 G10/G10b/G11/G11b（+9）；
    `test_backend_dead_signal.sh` 增 6a–6d（+4，invalidate 键口径/空参安全）。
    **本机全套 32/32 EXIT=0，`command not found` 命中 0**；通知版式 16/16 校验通过。
  - **未做（用户明确排除/评估后不做）**：F18 cron 降频（`0 * * * *`→`0 */2 * * *`，纯减少
    run 列表噪音，代价是兜底延迟 ≤2h）；F13 僵尸心跳（无复现样本）；F12 后半（无完成信号，
    启发式风险大）。
  - ✅ **生产轮验证（2026-09-19，主轮 `35405370217`，sha `0b0a1aa`）**：F22 实锤生效
    （5 次重探 + 恰逢真坏窗口反向印证"探针通过 ≠ 后续写入成功"）；F10 判据正确未误触发
    （批次 copy 区 405=0，80 条全为**并发的 initial sync 清理**删除型 405，非还原阶段——
    见下方更正条目）；F17 生效（预览未再拖满 900s）。
    详见下方 2026-09-19 生产验证条目。
- 2026-09-19（+08）· **★★★ F22/F10/F17 生产验证：F22 实锤生效（5 次重探）且恰逢坏窗口，
  F10 判据正确未误触发，F17 生效**（主轮 `35405370217`，sha `0b0a1aa`，`success`，
  23:21Z→04:43Z，320min 优雅收摊）。
  - **环境变量注入确认**：日志收尾 dump 到 `OPENLIST_PREVIEW_LISTING_TIMEOUT: 240s`、
    `OPENLIST_WRITE_REPROBE_INTERVAL: 1800`、`OPENLIST_405_FAST_FAIL_MIN: 20` ⇒ 三者均进生产。
  - ✅ **F22 实锤生效（5 次重探）**：`♻️ 批次 N: 写探针结论已过期，清缓存后重探（间隔 1800s）`
    出现 5 次（23:31:41 / 23:31:57 / 00:09:09 / 01:44:26 / 02:25:10），每次紧跟一次真探
    （`💉 后端写探针:` + `✅ 后端可写`）。注：23:31 那两条分属**两个不同同步对**
    （baidupanCrypt/0 与 wopan176Crypt/0），各自首次进入批次循环 ⇒ "未设锚点视为到期"分支
    按设计各探一次；后续同一对内不再每批重探 ⇒ **时间戳刷新逻辑正确，无重复重探**。
  - ✅ **★ 恰逢真坏窗口，反向印证 F22 的核心论断**：02:17:47 入口探针探 `baidupanCrypt/0`
    → ✅ 可写（t=0 正常）；02:25:10 F22 到期重探 `wopan175/0/...` → ✅ 可写；
    02:25–02:46 批次 1 传输 → rclone 报 9 个 `Copied (new)` 但**重启复核 9/9 全部未落盘**
    ⇒ 02:46:54 **F5「后端写入全拒（9/9）」触发**，中止剩余 14 批。
    ⇒ 实测坐实「**探针通过 ≠ 后续写入成功**」——失效在 t=0 之后才发生，这正是 F22 存在的理由；
    而本轮 F5 已按设计兜住（标失败 + 让路轮转）。
  - ✅ **F10 判据正确（未误触发，且不该触发）**：`405 拒写` 命中 **0** 次、`中止剩余 N 个批次`
    仅 **1** 次（来自 F5）。核对到行为**符合设计的负例**：全轮 `Method Not Allowed` 共 80 条，
    **全部**是 `Couldn't delete: Method Not Allowed`（**删除**失败，不在批次 copy 里）；
    批次 1 copy 区间（02:25–02:46）的 405 计数 = **0**（该批是"PUT 假成功"，rclone 报 Copied
    但无 405）⇒ F10 只对 405 **洪泛**生效，这批走 F5（要"触碰文件 100% 未落盘"），
    两者判据互补不重叠，**分工完全正确**。写入型 405（`Failed to copy`/`Put`/`mkParentDir`）= 0。
    ⚠️ **80 条的归属后来更正为「并发 initial sync 的清理」，非还原阶段**（见下条）。
  - ✅ **F17 生效**：预览用 240s 短超时（原 900s），本轮预览阶段未再拖满 900s
    （step 18 起于 23:25、首个批次 23:31 即开跑，预览段约 6 分钟，无 900s 白等）。
  - **本轮成效（非代码问题）**：`success` 优雅收摊、无硬杀；`传输 101.206 GiB · 本轮 4 个同步对`
    （`baidupanCrypt` 79.833 GiB + `wopan176Crypt` 15.918 GiB + `baidupanCrypt` partial 5.456 GiB
    + `wopan175` 0 B 被 F5 熔断）；修复成效 `成功 400（新落盘 266 · 沿用 134）· 缺失 1121 · 未修复 721`
    —— 缺失高企的成因是**本轮撞上坏窗口**（F5 实测 9/9 全拒）＋预算被修复尾段耗尽，
    非代码缺陷；下轮游标 → 6/16。
  - **结论**：F22/F10/F17 **三项均在生产验证通过**（F22 正面生效 + F10 正确未误触发 + F17 生效），
    **且 F22 恰逢真实坏窗口、与 F5 协同完成"发现→止损→让路"闭环**。
- 2026-09-19（+08）· **★ 更正一处归因 + 补一条基线（无代码改动，run `35405370217` 复核）**
  —— 起因是用户问"同步/修复/还原都能正常工作吧"，逐环节核对生产日志时发现两处需修正。
  - ❌→✅ **更正：80 条 `Couldn't delete: 405` 不在还原阶段**。原判"修复成功后的
    `rclone move` 还原阶段"**有误**。取证：80 条**全部落在 02:21:03–02:21:41（38 秒内）**，
    **77/80 是 `.nfo`**（另 3 条为 `…-Frag33`/`-Frag143` 分卷残留与短名 `baf9bc4c.mp4`）；
    发出者是 **02:18:45 起跑的 `task0 initial sync`**（`rclone sync --filter-from …`，
    同批 `Deleted` 成功 **222** vs 失败 **80**）。它与修复管线**并发**执行，
    故删除日志与 `↳ 还原:` 行在日志里**交错**出现 ⇒ **"位置相邻"被误读成"同一阶段"**。
    - **影响**：F10 结论**不变**（不看删除型 405 是对的、判据未误触发）；
      撤回的是"还原链在报 405"这个说法。残留只是被排除文件**没删掉**
      （下轮重复处理），**不影响 mp4 落盘与还原**。
    - **方法论（第 12 次同类，"日志相邻 ≠ 同一操作"）**：并发管线下**按时间戳与文件类型**
      定位发出者，不能按**行序相邻**归因——本例中"还原行 → 删除 405 行"紧挨着，
      但两者分属不同进程。
  - 🆕 **补记基线：存在间歇性持久化失败（量级 ~0.6%）**。本轮三组持久化复核：
    `19/19 通过`、`195/195 通过`、`178/178 通过 177 / **失败 1**`
    ⇒ `[持久化异常] 容器重启后复核部分未通过`。⇒ **"落盘成功"不是 100% 绝对**，
    判据是"重启后仍可见"，会有极少量间歇性漏网。
    - **已被现有机制兜住**：该失败项走「轮数耗尽 → 失败清单 + 黑名单已即时写入 marker
      （153 条）」，下轮重传 ⇒ **不造成永久损失**（与 §C「批次性假成功」同一兜底逻辑）。
    - **不做修法**：量级 1/178 且已有兜底，改判据的收益低于引入误判的风险。
- 2026-09-19（+08）· **★★★ 实测挖出一个真 bug：还原用错 rclone 子命令（`move` → `moveto`）**
  （`4620fed`）—— 用户要求"用本地临时目录实测还原、且绝对不动源端"，照做后立刻翻出
  一个**只在真 rclone 下才暴露**的缺陷，**此前全部 mock 测试都不可能发现**。
  - **现象**：`rclone move <src文件> <dst文件>` 的 **dst 被当作目录** —— 目标文件不存在时，
    它建出一个**以目标文件名命名的目录**再把文件塞进去，落点变成
    `<原文件名>.mp4/<短哈希名>.mp4`，**不是**我们要的 `<原文件名>.mp4`。
    实测 **local 与 crypt 远端均复现**（rclone v1.75.1）。
  - **为什么稳定踩中**：还原的目标路径**恰恰就是"此前同步失败、因而一定不存在"的原路径**
    ⇒ 每一次改名类还原都会命中这条语义，不是偶发。
  - **修法**：改名分支一律改用 `rclone moveto`（"文件→文件"的改名语义）。
    反例佐证：`diag_409_semantics.sh` 的改名复核**本就用 moveto**，说明 moveto 才是原本的意图。
  - **改动面（6 处，分两类）**：
    - **真执行**：`file_restore.sh · _restore_one_entry` 改名分支、
      `diag_escape_probe.sh · E4` 归位复核（后者若不改，会把"能归位"**误判成"归位了"**）。
    - **写进 marker 的命令串**（人照抄执行会踩同一个坑）：`restore_info.jq`（hash_dir 的
      steps + script）、`file_fix.sh:1159/1192`、`file_fix_pipeline.sh:398`。
  - **新增测试 `test_restore_real_local.sh`（19 断言，真 rclone、**不 mock**）**：
    覆盖短哈希文件名 / 短哈希目录折叠 / 目录+文件名双改 / 原路径原名 / 替代缺失负例，
    并断言**源端内容与目录结构零修改**（用户硬约束）。修复前 7/16、修复后 **19/19**。
  - **影响面评估（重要）**：该 bug **只影响手动「⚠️ 还原」模式**（`restore_fixed_files`
    仅由该 run_mode 触发）。常态同步轮**不执行还原**——日志里那些 `↳ 还原: rclone move …`
    是**写进 marker 的建议命令串，并未执行**。⇒ **属于潜伏缺陷，至今未造成实际数据错误**；
    谁若照 marker 里的命令手工执行，才会踩到。
  - **方法论（第 13 次同类，"mock 覆盖率 ≠ 正确性"）**：现有还原相关测试**全部 mock 掉 rclone**，
    锁的是"判据与分支"，**锁不到子命令语义**。凡"端到端是否真的做对"的性质，
    必须补一层**真二进制 + 临时目录**的实测——这次正是靠它翻出来的。
- 2026-09-19（+08）· **文档与注释整理（无代码改动）** —— 把"待验证/未跑过"等**已过期表述**
  逐处核到生产证据后更新，消除"看着像待办、其实早已验证"的误导（AGENTS.md 要求的
  「文档→实现」依赖方向：这里只改文档，实现未动）。
  - §0 快照标题日期 `2026-09-17 09:10Z` → `2026-09-19`；一句话结论补"+ 2026-09-19 生产验证"。
  - 代码侧回归数 `29/29` → `32/32`（补 `test_write_reprobe_and_405.sh`，2026-09-19 新增）。
  - Phase 2 第一批（F6/F7/F8/F11）"**尚未被任何一轮跑过**" → "已多轮生产验证"；
    F6 触发源接线"未跑过" → "已生产验证（run `35405370217` 实测 F5 9/9 全拒触发）"；
    §4 · F6 小节标题同步。
  - §4 · F23 验收"实测待下一轮" → 2026-09-19 生产验证（320min 优雅收摊、无 330min 硬杀）。
  - §4 · F22 / F10 / F17 三节各补**验收 ✅（2026-09-19 生产轮）**条目；其中 F22 与 F10 的
    验收文本**各自归位**（早前整理时曾把 F10 的验收误放在 F22 节，已改回正确小节）。
  - §进度日志 2026-09-19 落地条目下的"⚠️ 待生产轮验证" → "✅ 生产轮验证"。
  - 头部代码基线 `f7d222e` → `0b0a1aa`（2026-09-19 最新落地）。
  - 保留项（**不属过期，系有意为之**）：§4 · F23「已知残留」（子目录 600s 片）、
    §12 折叠 `OPENLIST_FOLD_VERIFY_TRIES` 调优候选（"待下一轮数据决定，未改"）、
    §12.14 各条"未验证前提/待复现"（追因课题，等自然复现）、进度日志里的历史轮次计数
    （`29/29`/`30/30` 等是**当时**的快照，非当前值，保留供追溯）。
