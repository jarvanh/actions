--- /tmp/actions_repo/.github/workflows/emby.yml
+++ /tmp/actions_repo/.github/workflows/emby.yml
@@ -99,6 +99,10 @@
           echo "==========/var/lib/emby==========="
           sudo ls -la /var/lib/emby
       - name: run emby
         run: |
+          # 先停止 dpkg -i 启动的默认 emby-server 服务，释放 /var/lib/emby 锁定
+          sudo systemctl stop emby-server || true
+          
           sudo nohup /opt/emby-server/bin/emby-server &
       - name: run cloudflared
