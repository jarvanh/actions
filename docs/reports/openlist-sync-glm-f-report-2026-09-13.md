# openlist.yml 同步方案评估（glm-f-report · 2026-09-13）

证据来源：本地代码（HEAD `5c01d32`；被剖析 run 所在 commit `9548261` 与 HEAD 之间 `task_engine.sh` 零差异，仅 `openlist.yml` 差 9 行，结论可平移）、`gh api` 取到的 run/job/step 元数据、run 34728107625 的剥离回显日志（保留期 25h 内唯一可取的近轮日志）、`onedrive:/logs/sync_state/trend.jsonl`（本机 rclone 只读读取）。所有数字标注出处；测不到的在第 8 节显式列出。

## 1. 结论摘要

1. **调度与接力机制是健康的**：`session_hold`/`self_retrigger` 的 env 默认值修法已生效，近 4 轮全部以 failure 收场的 run 都成功触发了下一轮接力，marker/游标增量持久化无丢失迹象。
2. **但每轮都以失败收场成为常态**：最近 4 轮 failure 的同步 step 耗时全部精确等于 330 分钟（330m12s–330m13s，job API step 元数据），即被 `timeout-minutes: 330` 硬杀，而非 320 分钟预算优雅到站。根因已定位到代码：**文件批次循环内没有预算闸**。
3. **吞吐上不可能按当前路径完成同步**：trend.jsonl 实测净传 0–2.27GB/轮、节奏 ~5.75h/轮，对 7.4–10.1TB 的剩余口径，均值外推需 **14 年+**，最速轮外推也需 **2 年+**。瓶颈是 wopan176Crypt 后端 2.5MiB/s 的实测写速率 + 长密文名 405 拒写，不是调度。
4. **本轮实证了一个修复管线的真 bug**：同一后端上批次 copy 正常落盘（2.5MiB/s 持续上传成功），修复管线却因「后端熔断」对 1037 个缺失文件**未试写**全部放弃（0/1037）。熔断状态被路径特异性 405（密文名超长）误触发，且写探针假阳性（探针通过 1 分钟后实际同步即大量 405）。

## 2. 运行现状：最近 21 轮

`gh run list --workflow=openlist.yml --limit 25`（2026-09-13 23:00 CST 取）：

| 结论 | 轮数 | 备注 |
|---|---|---|
| success | 2 | 全部停在 09-10/09-11 的旧 commit（`b63b2da`/`43edf27`） |
| failure | 8 | 含 2 轮 15s 级快败（如 34685067684，rclone-install 失败） |
| cancelled | 9 | 34660149180 为 6h 上限取消（job 6h0m20s）；其余取消者不可考 |
| pending/in_progress | 2 | 34763535089 / 34752801560 |

**2026-09-11 12:07Z（commit `90fdeb3`）以来的 12 轮：0 success。**

failure 轮的 step 18「任务预览与全量同步」耗时（jobs API steps 元数据）：

| run | step18 耗时 | 判定 |
|---|---|---|
| 34728107625 | 330m12s | `timeout-minutes: 330` 硬杀 |
| 34723502766 | 330m13s | 同上 |
| 34709806073 | 330m12s | 同上 |
| 34694126684 | 330m13s | 同上 |

四轮全部精确到 330min+12s，不是巧合：320 分钟预算从未让 step 在超时前退出。

## 3. 单轮解剖（run 34728107625，日志逐行核对）

时间线（UTC，剥离回显后的真实输出）：

| 时刻 | 事件 |
|---|---|
| 00:29:50 | run 创建（cron `0 * * * *` 创建时刻已漂移近半小时） |
| 05:18:51 | job 才开始跑——**排队 4h49m**（concurrency 单例被前轮占满） |
| 05:21:46 | 同步 step 启动，预算锚点 = 10:41:46（日志第 729 行） |
| 05:23:46–05:54:27 | 任务预览 **30m41s**：16 个同步对逐一 rclone 探测源端；第 14 对 `onedrive:1` 清单获取失败，拖了 15m01s 才放弃 |
| 05:54:32 | 轮转从第 9/16 个同步对开始（**已连续尝试 4 次**——同一同步对连续 4 轮失败） |
| 05:57:44 | 写探针：`✅ 后端可写`（wopan176Crypt） |
| 05:58:55 起 | `task2_1024j_动漫本子` initial sync，持续 `405 Method Not Allowed`（长密文路径），烧掉 ~3h10m |
| 09:08–09:10 | yande.re 等目录修复循环：名长诊断 `密文名 235B > 后端已接受最长 100B`，但「原目录不可写（后端熔断）→ 未试写」逐条放弃 |
| 09:11:34 | `动漫图片_pixiv`：11GiB、无子目录 → 文件批次拆分 6932 文件 → 3 批 |
| 09:14:34–10:32:22 | 批次 1/3（3178 文件）1h17m48s：copy 成功 + 巩固 → 修复管线 1037 缺失 **0 修复** → 优雅收摊（剩余 311 交下轮） |
| 10:32:24 | **批次 2/3（3388 文件）照常开跑**——距预算到站仅 9m22s |
| 10:51:58 | `timeout-minutes: 330` 强杀（当时 1.32GiB/4.31GiB、31%，2.5MiB/s，ETA 20m） |
| 10:52–10:53 | 收尾/Persist/自续触发全部 success；修复成效 `成功 183 · 缺失 2126 · 未修复 1943`（修复成功率 8.6%） |

## 4. 根因一：批次循环没有预算闸（每轮 failure 的直接原因）

`sync_budget_stop`（`task_engine.sh` L131–134，锚点 = step 启动 + 320min）目前的检查点：

- L171 同步对之间 ✓
- L461 并行子目录分发前 ✓
- L769 串行子目录之间 ✓
- L859 最终完整同步前 ✓
- **L1459–1662 文件批次循环：无任何预算检查 ✗**

批次循环内已有的闸只有「后端预检熔断」（L1506）和「巩固后端全拒熔断」（L1602），都是健康闸不是时间闸。并且修复管线在批次 1 内部优雅收摊时置位的 `SYNC_TIME_EXHAUSTED=1`（`file_fix_pipeline.sh` L762–767，本轮 10:32:22 确实触发）**不会传播到批次循环**——下一批照开。

雪上加霜的是单批耗时的不可预估性：批次 1 的 1h17m 里，copy 本身只占一部分，巩固（重启容器取真值）+ 修复管线（1037 文件逐条空转）占了尾部。「最小工作片 600s」的假设（L130）对「一批 = copy + 巩固 + 修复」的真实粒度完全失效。

结果：近 4 轮全部 330min 硬杀 → run conclusion=failure → 收尾统计里「本轮修复成效」能打出来（always() 生效）但 trend 的轮内字节计数器丢失（见第 6 节）。

## 5. 根因二：后端熔断过泛化 + 写探针假阳性（修复管线空转的真 bug）

本轮日志里的矛盾实证：

- 05:57:44 写探针 `✅ 后端可写`；
- 05:58:55 起 actual sync 对同一后端持续 405（长密文名路径）；
- 后端熔断状态被置位后，09:08–10:32 修复管线对 1037 个缺失文件全部「未试写」放弃；
- **同一时间段**批次 copy 对同一后端 `wopan176Crypt` 正常落盘（`Copied (new)` 持续到被杀）。

即：405 是**路径特异性**的（密文名超长，名长诊断自己也给出了 `235B > 100B` 的证据），却被提升成了**后端级**熔断（`OPENLIST_BACKEND_DEAD_THRESHOLD=3`，`file_fix.sh _BACKEND_DEAD`），进而短路了本可落盘的短哈希名/zip 分卷修复方法。日志里甚至出现了双重死锁：`⏭ 名长注定失败 → 直接试短哈希名方法` 紧接 `🔀 原目录不可写（后端熔断）→ 跳过原目录的 4 种方法` → `⏭ 文件位于目标端根目录，无目录可换` → `❌ 无法修复`——名长命中本该走替代形态，却被熔断和「根目录无目录可换」两个各自合理的规则合谋归零。

写探针（`openlist_driver.sh _backend_write_probe`）假阳性同样明确：探针通过 1 分钟后实际同步即大量 405，说明探针写入的路径形态（短名/浅层）与实际工作负载（深层长密文名）不等价。

## 6. 能完成吗：速率数学（trend.jsonl，2026-09-13 实读）

`onedrive:/logs/sync_state/trend.jsonl` 最近 8 轮净传：

| run | 净传 | 备注 |
|---|---|---|
| 34550419201 | 18,282,971 B | |
| 34622300065 | 18,282,971 B | 与上轮完全同值 |
| 34660149180 | 18,282,971 B | 与上上轮完全同值 |
| 34674196629 | 0 | |
| 34694126684 | 0 | |
| 34709806073 | 2,271,397,020 B | 最好一轮 |
| 34723502766 | 171,257,627 B | |
| 34728107625 | 171,257,627 B | 与上轮完全同值 |

- 均值 ≈ **334MB/轮**，轮节奏 ≈ 5.75h（时间戳间隔）→ ≈ 1.4GB/天；
- `remaining_bytes` 在 **7.40–10.09TB** 区间波动；
- 外推：均值口径 ≈ **14.5 年**，最速轮口径 ≈ **2.9 年**。

三个必须声明的数据质量 caveat：

1. `transferred_bytes` 在强杀轮次失真：批次字节计数在内存里，330min 硬杀后丢失（本轮批次 2 实际已传 ≥1.32GiB，trend 只记了 171MB）。修复此 bug 后均值会上修，但仍在 GB/轮 量级，不改变结论。
2. `remaining_bytes` 口径是「本轮预览注册的未传量」，受 24h-skip 和预览探测失败影响（9.897TB→7.405TB 的单轮 2.49TB 跳变不是真实同步产出）。真实总积压量级 TB 级成立，精确值测不到。
3. 三轮完全同值（18,282,971 ×3）与两轮完全同值（171,257,627 ×2）强烈暗示同一批内容轮轮重传（疑似反复校验失败→重传的 churn），即**部分净传是无效功**。

**回答「最终能够完成文件同步任务吗」**：机制上能——接力 + 增量持久化 + 轮转自愈的设计闭环是完备的，只要不停就持续推进；速率上不能——按实测吞吐对 TB 级积压是年级别收敛时间，等价于完不成。且当前每轮 failure 的形态还会持续污染统计与告警信号。

## 7. 改进建议（按优先级）

1. **批次循环加预算闸（修 failure 态，改动最小收益最大）**：每批开始前检查 `sync_budget_stop`，并尊重修复管线已置位的 `SYNC_TIME_EXHAUSTED`；闸的粒度不能再用固定 600s 最小片，应以「批内字节 ÷ 本轮实测速率 + 巩固/修复的历史耗时」估单批成本，剩余预算不足一批就不开批。`timeout-minutes: 330` 退回纯兜底。
2. **405 熔断与「密文名超长」解耦（修 8.6% 修复率）**：名长诊断命中超长的文件不应受后端熔断短路，直接走短哈希/zip 分卷替代形态；后端死判定只留给登录失效/全路径拒写类错误。同时补「根目录文件无目录可换」死角——根目录文件也应有短哈希兜底目录。
3. **写探针分层**：探针结论按（后端 × 路径深度/名长桶）记录，或至少在 405 时按错误类别分流（名长 vs 登录 vs 5xx），避免「探针通过 + 实际 405」的假阳性和「路径 405 + 后端级熔断」的假阴性，本轮两者同时发生了。
4. **计数器落盘**：批次/巩固的字节计数即时追加到 /tmp 文件而非内存累加，强杀后收尾仍可读；顺带排查第 6 节的同值重传 churn。
5. **预览降耗**：30m41s 占 320min 预算的 9.6%，其中第 14 对 listing 失败拖满 15 分钟（`OPENLIST_RCLONE_LISTING_TIMEOUT=900`）。源端 size 探测加短超时/失败快弃/跨轮缓存，或常态 `skip_preview`。
6. **排队治理**：本轮排队 4h49m。cron 已从 */5 退到 1h 仍会堆队列（接力正常时 cron 纯兜底），可降到 2h，代价是兜底延迟变大、收益是 run 列表干净。
7. **吞吐本身（决定「能不能完成」的根本项）**：2.5MiB/s 是 transfers=1 钉死（run 32749862280 假成功事故后端）下的实测值。真值校验/巩固已能兜假成功，可以考虑在调试模式对 wopan176 试 `transfers=2` 观察假成功率（workflow 已留输入）；或者把 TB 级大头改派给更快后端。baidupan/aliyundrive 的吞吐本次未测到，无法替你选边。

值得肯定的（本轮日志验证过、不要动）：接力链路、批次巩固的重启取真值（正确暴露 1037 缺失）、修复管线的优雅收摊与增量持久化、收尾对 conclusion 的诚实分流、always() 收尾/Persist 在硬杀后仍完整执行。

## 8. 测不到的部分

- 除 34728107625 外各轮的真实日志（保留期 25h 已过，410）：第 2 节的 330min 模式由 job/step 元数据（耗时精确 330m12s±1s）推断，未逐轮核对日志。
- 多轮 cancelled 的取消者与原因（34695325006、34716617007、34740440666 等）：34660149180 可判定为 6h 上限，其余手动/系统取消无法区分。
- baidupanCrypt/aliyundriveCrypt/wopan175 的实际吞吐与错误率：本轮日志只覆盖 wopan176Crypt 路径，trend.jsonl 无后端分维度。
- 9.9TB remaining 的内容构成（多少低优先级/重复）：trend.jsonl 只有总量。
- Telegram 趋势通知的「预计清零」文案：step 日志不含消息体。
- 同值重传 churn 的具体文件：需要目标端逐文件比对，本次未做。
